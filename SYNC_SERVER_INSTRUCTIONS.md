# Инструкция по синхронизации кода на сервере

## Проблема
На сервере есть локальные изменения, которые мешают `git pull`. Скрипт использует старый метод генерации сертификатов вместо нового метода предпросмотра.

## Решение

### Вариант 1: Сохранить локальные изменения (если они важны)

```bash
# На сервере
cd /root/project/gornostyle

# Сохранить локальные изменения
git stash

# Получить последние изменения
git pull origin develop

# Применить сохраненные изменения обратно (если нужно)
git stash pop

# Если есть конфликты, разрешить их вручную
```

### Вариант 2: Отбросить локальные изменения (если они не важны)

```bash
# На сервере
cd /root/project/gornostyle

# Отбросить локальные изменения в отслеживаемых файлах
git checkout -- src/services/certificateJpgGenerator.js
git checkout -- views/certificate.ejs

# Удалить неотслеживаемый файл (если он конфликтует)
rm -f scripts/regenerate-certificate.js

# Получить последние изменения
git pull origin develop

# Перезапустить приложение
npm run pm2:restart
```

### Вариант 3: Принудительный сброс (если локальные изменения точно не нужны)

```bash
# На сервере
cd /root/project/gornostyle

# Сохранить текущее состояние (на всякий случай)
git stash

# Принудительно сбросить на последний коммит из develop
git fetch origin develop
git reset --hard origin/develop

# Перезапустить приложение
npm run pm2:restart
```

## Проверка после синхронизации

После синхронизации проверьте, что используется новый метод:

```bash
# Запустить скрипт пересоздания
node scripts/regenerate-certificate.js 689547
```

**Ожидаемый вывод (новый метод):**
```
🖼️  Генерация JPG файла (используется метод предпросмотра)...
✅ JPG сертификат создан (как в предпросмотре): ...
```

**Старый метод (неправильно):**
```
📸 Генерируем JPG для сертификата 689547 с URL: https://gornostyle72.ru/certificate/689547
```

## Рекомендация

Используйте **Вариант 2** или **Вариант 3**, так как все изменения уже закоммичены в репозиторий.

