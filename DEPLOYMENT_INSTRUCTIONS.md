# 🚀 Инструкция по внедрению исправления системы отзывов

## ✅ Что было исправлено

1. **Добавлена автоматическая задача обновления статусов тренировок**
   - Запускается каждые 30 минут
   - Обновляет статус с `scheduled` на `completed` для прошедших тренировок

## 📋 Шаги для развертывания на сервере

### Шаг 1: Обновить код на сервере

```bash
# Перейти в директорию проекта
cd /path/to/gornostyle

# Получить последние изменения
git pull origin develop

# Или скопировать файл scheduler.js вручную
```

### Шаг 2: Обновить статусы всех прошедших тренировок

```bash
# Запустить скрипт обновления статусов
node src/scripts/complete-past-group-sessions.js
```

**Ожидаемый результат:**
```
Групповых тренировок переведено в completed: X
```

### Шаг 3: Перезапустить приложение

```bash
# Если используется PM2
pm2 restart gornostyle

# Или перезапустить все процессы
pm2 restart all

# Проверить статус
pm2 status
pm2 logs gornostyle --lines 50
```

### Шаг 4: Проверить работу планировщика

```bash
# Проверить логи (должны появиться записи о задачах)
pm2 logs gornostyle | grep -i "планировщик\|scheduler\|задача"
```

**Ожидаемый вывод:**
```
Инициализация планировщика задач...
✓ Задача "Напоминания о тренировках" настроена на 21:00 (Екатеринбург)
✓ Задача "Запросы на отзывы" настроена на 21:00 (Екатеринбург)
✓ Задача "Обновление статусов" настроена на каждые 30 минут
Планировщик запущен. Активных задач: 3
```

### Шаг 5: Отправить запросы на отзывы за пропущенные дни

```bash
# Для 19 октября
node src/scripts/send-review-requests.js 2025-10-19

# Для 17 октября
node src/scripts/send-review-requests.js 2025-10-17
```

## ✅ Проверка работоспособности

### 1. Проверка обновления статусов

```sql
-- Подключиться к БД
psql -h 90.156.210.24 -p 5432 -U batl-zlat -d skisimulator

-- Проверить статусы тренировок
SELECT 
    session_date,
    COUNT(*) as total,
    COUNT(*) FILTER (WHERE status = 'completed') as completed,
    COUNT(*) FILTER (WHERE status = 'scheduled') as scheduled
FROM training_sessions
WHERE session_date < CURRENT_DATE
GROUP BY session_date
ORDER BY session_date DESC
LIMIT 5;
```

**Ожидаемый результат:** Прошедшие тренировки должны иметь статус `completed`.

### 2. Проверка отправки отзывов

```sql
-- Проверить логи отправки
SELECT 
    client_id,
    telegram_id,
    training_count,
    participant_type,
    sent_at
FROM review_notification_logs
ORDER BY sent_at DESC
LIMIT 10;
```

### 3. Мониторинг в реальном времени

```bash
# Следить за логами приложения
pm2 logs gornostyle --lines 100 --raw

# Особенно в 21:00 для отправки отзывов
# И каждые 30 минут для обновления статусов
```

## 📊 График работы задач

| Задача | Расписание | Что делает |
|--------|-----------|-----------|
| Напоминания о тренировках | 21:00 ежедневно | Отправляет клиентам напоминания о завтрашних тренировках |
| Запросы на отзывы | 21:00 ежедневно | Отправляет клиентам запросы оставить отзыв |
| Обновление статусов | Каждые 30 минут | Обновляет статус прошедших тренировок на `completed` |

## 🔧 Устранение проблем

### Проблема: Задачи не запускаются

**Решение:**
```bash
# Проверить, что приложение запущено
pm2 status

# Перезапустить
pm2 restart gornostyle

# Проверить логи
pm2 logs gornostyle --lines 50
```

### Проблема: Статусы не обновляются

**Решение:**
```bash
# Запустить скрипт вручную
node src/scripts/complete-past-group-sessions.js

# Проверить результат в БД
```

### Проблема: Отзывы не отправляются

**Решение:**
1. Проверить статусы тренировок (должны быть `completed`)
2. Проверить telegram_id клиентов (не должны быть NULL)
3. Запустить скрипт вручную для отладки:
```bash
node src/scripts/send-review-requests.js 2025-10-20
```

## 📝 Контрольный список

- [ ] Код обновлен на сервере
- [ ] Статусы прошедших тренировок обновлены вручную
- [ ] Приложение перезапущено
- [ ] В логах видна инициализация 3 задач планировщика
- [ ] Отправлены запросы на отзывы за пропущенные дни
- [ ] Проверены логи отправки в таблице `review_notification_logs`
- [ ] Настроен мониторинг работы задач

## 🎯 Ожидаемые результаты через 24 часа

✅ Статусы всех завершенных тренировок обновлены  
✅ Клиенты получают запросы на отзывы после тренировок  
✅ Логи показывают регулярное выполнение задач  
✅ Система работает автоматически без ручного вмешательства  

## 📞 Поддержка

Если возникают проблемы:
1. Проверить логи: `pm2 logs gornostyle`
2. Проверить статус: `pm2 status`
3. Проверить БД: запустить диагностический скрипт `./diagnose-review-system.sh`
4. Связаться с разработчиком

---

**Дата создания:** 20 октября 2025  
**Версия:** v2.0.5.3  
**Статус:** ✅ Готово к развертыванию
