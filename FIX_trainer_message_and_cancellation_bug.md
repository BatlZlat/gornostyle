# 🔧 ИСПРАВЛЕНИЯ: Сообщение о смене тренера и баг с отменой тренировки

**Дата:** 22 октября 2025  
**Статус:** ✅ Исправлено

---

## 📋 Исправленные проблемы

### 1. **Изменение сообщения при смене тренера** ✅

**Проблема:** Сообщение клиенту при смене тренера было недостаточно информативным.

**Было:**
```
💡 Приносим извинения за неудобства. Новый тренер обязательно с вами свяжется!
```

**Стало:**
```
💡 Приносим извинения за вынужденную подмену. При необходимости причину можете узнать у администратора:
📞 Номер администратора: +7 (3452) 123-45-67
```

**Файл:** `src/services/trainer-notification-service.js`

---

### 2. **Баг с отменой индивидуальных тренировок** ✅

**Проблема:** При попытке отменить индивидуальную тренировку возникала ошибка "Произошла ошибка при отмене тренировки. Пожалуйста, попробуйте позже."

**Причина:** В SQL запросах для загрузки индивидуальных тренировок отсутствовали необходимые поля:
- `simulator_id` - нужен для освобождения слотов
- `end_time` - нужен для расчета времени окончания тренировки

**Исправления:**

#### **A. Исправлен SQL запрос в функции `showMyBookings()`**

**Файл:** `src/bot/client-bot.js` (строки 5400-5428)

**Добавлены поля:**
```sql
its.simulator_id,
(its.preferred_time + (its.duration || ' minutes')::interval)::time as end_time,
```

#### **B. Исправлен SQL запрос в функции `handleTextMessage()`**

**Файл:** `src/bot/client-bot.js` (строки 3920-3948)

**Добавлены поля:**
```sql
its.simulator_id,
(its.preferred_time + (its.duration || ' minutes')::interval)::time as end_time,
```

#### **C. Исправлена логика освобождения слотов**

**Файл:** `src/bot/client-bot.js` (строки 4237-4254)

**Было:**
```javascript
await pool.query(
    `UPDATE schedule 
     SET is_booked = false 
     WHERE simulator_id = $1 
     AND date = $2 
     AND start_time >= $3 
     AND start_time < $4`,
    [
        selectedSession.simulator_id,
        selectedSession.session_date,
        selectedSession.start_time,
        selectedSession.end_time  // ❌ Могло быть undefined
    ]
);
```

**Стало:**
```javascript
const endTime = selectedSession.end_time || 
    (selectedSession.start_time + (selectedSession.duration || ' minutes')::interval)::time;

await pool.query(
    `UPDATE schedule 
     SET is_booked = false 
     WHERE simulator_id = $1 
     AND date = $2 
     AND start_time >= $3 
     AND start_time < $4`,
    [
        selectedSession.simulator_id,
        selectedSession.session_date,
        selectedSession.start_time,
        endTime  // ✅ Всегда определено
    ]
);
```

---

## 🧪 Тестирование

### **Тест 1: Отмена индивидуальной тренировки**

**Шаги:**
1. Открыть бота → "📋 Мои записи"
2. Найти индивидуальную тренировку
3. Нажать "❌ Отменить тренировку"
4. Выбрать номер тренировки
5. Проверить успешную отмену

**Ожидаемый результат:**
```
✅ Тренировка успешно отменена!

👤 Участник: Тестик1
📅 Дата: 23.10.2025 (ЧТ)
⏰ Время: 10:00
💰 Возвращено: 3000.00 руб.

Средства возвращены на ваш баланс.
```

### **Тест 2: Смена тренера**

**Шаги:**
1. Назначить тренера на индивидуальную тренировку
2. Изменить тренера через админ-панель
3. Проверить уведомление клиенту

**Ожидаемый результат:**
```
🔄 Изменение тренера

📅 Дата: 23.10.2025
⏰ Время: 10:00
🎿 Тип: Горнолыжная тренировка

👤 Предыдущий тренер: Тебякин Данил

👨‍🏫 Новый тренер: Токмаков Сергей
📱 Телефон: +79129233659

💡 Приносим извинения за вынужденную подмену. 
    При необходимости причину можете узнать у администратора:
📞 Номер администратора: +7 (3452) 123-45-67
```

---

## 📊 Технические детали

### **Измененные файлы:**

1. ✅ **`src/services/trainer-notification-service.js`**
   - Обновлен текст уведомления при смене тренера
   - Добавлен номер администратора

2. ✅ **`src/bot/client-bot.js`**
   - Исправлены SQL запросы для загрузки индивидуальных тренировок
   - Добавлены поля `simulator_id` и `end_time`
   - Исправлена логика освобождения слотов при отмене

### **Ключевые изменения:**

- **Добавлено поле `simulator_id`** в запросы индивидуальных тренировок
- **Добавлено вычисление `end_time`** через SQL: `(its.preferred_time + (its.duration || ' minutes')::interval)::time`
- **Добавлена защита от undefined** в логике освобождения слотов
- **Обновлен текст уведомления** с номером администратора

---

## 🎯 Результат

✅ **Отмена индивидуальных тренировок работает корректно**  
✅ **Сообщение при смене тренера стало более информативным**  
✅ **Добавлен контакт администратора для уточнения причин**  
✅ **Исправлены все SQL запросы для корректной работы**

---

**Статус:** Все исправления применены и готовы к тестированию! 🚀

