# 🖥️ АДМИН-ПАНЕЛЬ - ПРОГРЕСС ОБНОВЛЕНИЯ

**Дата:** 24 октября 2025  
**Статус:** Основная структура готова ✅ | Требуется доработка фильтров

---

## ✅ ЧТО СДЕЛАНО

### 1. **HTML (`admin.html`)** ✅

#### Обновлено меню:
- ✅ "Тренировки" → "🎿 Тренировки ТРЕНАЖЕР"
- ✅ Добавлена кнопка "🏔️ Тренировки ЗИМА"

#### Добавлены новые страницы:
- ✅ `winter-trainings-page` - страница зимних тренировок
- ✅ Фильтры по типу и статусу
- ✅ Навигация по датам

#### Обновлена страница Прайс:
- ✅ Переименована в "🎿 Прайс (Тренажер)"
- ✅ Добавлена кнопка "🏔️ Прайс зимнее направление"

#### Обновлена страница Клиенты:
- ✅ Добавлен фильтр "Спортсмен" (select)
  - Все клиенты
  - Только спортсмены
  - Только не спортсмены

#### Модальные окна:
- ✅ `winter-prices-modal` - управление зимними ценами
- ✅ `winter-price-edit-modal` - создание/редактирование цены

---

### 2. **JavaScript** ✅

#### Созданные файлы:

**`admin-winter-prices.js`** ✅
- `openWinterPricesModal()` - открыть модальное окно цен
- `loadWinterPrices()` - загрузить список цен
- `displayWinterPrices()` - отобразить цены (группировка по типам)
- `openCreateWinterPriceModal()` - создать цену
- `editWinterPrice(id)` - редактировать цену
- `saveWinterPrice()` - сохранить цену
- `toggleWinterPriceStatus(id)` - активировать/деактивировать
- `deleteWinterPrice(id)` - удалить цену
- `updateWinterPriceFormFields()` - обновить поля формы

**`admin-winter-trainings.js`** ✅
- `initWinterTrainingsPage()` - инициализация страницы
- `loadWinterTrainings()` - загрузить список тренировок
- `displayWinterTrainings()` - отобразить тренировки
- `renderWinterTrainingRow()` - отрисовать строку
- `openCreateWinterTraining()` - создать тренировку (заглушка)
- `viewWinterTrainingDetails(id)` - просмотр деталей (заглушка)

**`admin.js`** ✅
- Добавлен обработчик `case 'winter-trainings'`
- Подключены новые скрипты

---

### 3. **Подключение скриптов** ✅

```html
<script src="js/admin-winter-prices.js?v=1"></script>
<script src="js/admin-winter-trainings.js?v=1"></script>
```

---

## ⏳ ЧТО НУЖНО ДОРАБОТАТЬ

### A. Страница "Клиенты" 

**Проблема:** Фильтр добавлен, но не интегрирован с логикой загрузки.

**Нужно:**
1. Добавить обработчик `change` для `clientAthleteFilter`
2. Обновить `loadClients()` - передавать параметр `is_athlete` в API
3. Добавить колонку "Спортсмен" в таблицу клиентов
4. Добавить чекбокс для изменения статуса `is_athlete`
5. Создать функцию `toggleClientAthleteStatus(clientId)`

**Код для добавления:**
```javascript
// Обработчик фильтра спортсменов
document.getElementById('clientAthleteFilter').addEventListener('change', loadClients);

// Обновить loadClients()
async function loadClients() {
    const athleteFilter = document.getElementById('clientAthleteFilter').value;
    let url = '/api/clients';
    if (athleteFilter) {
        url += `?is_athlete=${athleteFilter}`;
    }
    const response = await fetch(url);
    // ...
}

// Функция переключения статуса спортсмена
async function toggleClientAthleteStatus(clientId, currentStatus) {
    const response = await fetch(`/api/clients/${clientId}/athlete-status`, {
        method: 'PUT',
        headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${localStorage.getItem('token')}`
        },
        body: JSON.stringify({ is_athlete: !currentStatus })
    });
    // Перезагрузить клиентов
    await loadClients();
}
```

---

### B. Страница "Абонементы"

**Нужно обновить форму:**
1. Добавить поле "Применяется к" (select):
   - `sport_group` - Только спортивная группа (до 4 чел)
   - `any` - Любые тренировки
2. Добавить подсказку: "Абонементы могут покупать только спортсмены"
3. Обновить `admin-subscriptions.js` (если есть) для работы с `applicable_to`

---

### C. API для зимних тренировок

**Текущая ситуация:**
- Зимние тренировки можно создавать через общее расписание
- API endpoint: `/api/schedule/admin?slope_type=natural_slope`

**Нужно доработать:**
1. Создать специальный endpoint `/api/winter-trainings`
2. Добавить логику создания зимних тренировок с автоматическим расчетом цены

---

## 📊 ТЕКУЩАЯ ФУНКЦИОНАЛЬНОСТЬ

### ✅ Работает:
- Просмотр зимних цен
- CRUD операций с зимними ценами
- Автоматическая валидация (тип, участники, цена)
- Группировка цен по типам
- Активация/деактивация цен
- Навигация по страницам админ-панели
- Просмотр зимних тренировок (через общее расписание)
- Фильтрация зимних тренировок по типу и статусу

### ⏳ Требует доработки:
- Фильтр спортсменов на странице клиентов (не интегрирован)
- Колонка "Спортсмен" в таблице клиентов (отсутствует)
- Чекбокс переключения статуса спортсмена (отсутствует)
- Создание зимних тренировок (заглушка)
- Обновление формы абонементов (поле `applicable_to`)

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

### Вариант 1: Минимальная доработка (30-60 минут)
1. Интегрировать фильтр спортсменов в `loadClients()`
2. Добавить колонку "Спортсмен" с чекбоксом
3. Создать `toggleClientAthleteStatus()`

### Вариант 2: Полная доработка (2-3 часа)
1. Все из Варианта 1
2. Создать API `/api/winter-trainings`
3. Реализовать создание зимних тренировок
4. Обновить форму абонементов

---

## 💾 СТРУКТУРА КОДА

### HTML Структура:
```
admin.html
├── Меню навигации (обновлено)
├── Страницы
│   ├── training-page (без изменений)
│   ├── winter-trainings-page (НОВАЯ) ✅
│   ├── clients-page (фильтр добавлен) ⏳
│   ├── prices-page (кнопка добавлена) ✅
│   └── subscriptions-page (без изменений)
└── Модальные окна
    ├── winter-prices-modal ✅
    └── winter-price-edit-modal ✅
```

### JavaScript Структура:
```
admin.js (обновлен)
├── loadPageContent() [+ winter-trainings case] ✅
└── loadClients() [требует обновления] ⏳

admin-winter-prices.js (НОВЫЙ) ✅
├── Управление ценами
└── CRUD операции

admin-winter-trainings.js (НОВЫЙ) ✅
├── Просмотр тренировок
└── Фильтрация
```

---

## 🚀 ГОТОВНОСТЬ

```
[████████████████░░░░] 80% Админ-панель

✅ HTML структура
✅ Навигация
✅ Зимние цены (полный CRUD)
✅ Зимние тренировки (просмотр)
⏳ Клиенты (фильтр + чекбокс)
⏳ Абонементы (обновить форму)
⏳ Создание зимних тренировок
```

---

**Админ-панель на 80% готова!** 🎉

**Основная функциональность работает**, требуется только доработать интеграцию фильтров и чекбоксов для клиентов.

