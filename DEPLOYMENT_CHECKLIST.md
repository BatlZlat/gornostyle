# Чеклист для деплоя системы уведомлений на продакшн

## ✅ Что уже сделано

- [x] Система уведомлений разработана
- [x] Миграция БД применена
- [x] Система протестирована локально
- [x] Подтверждена работоспособность на реальных данных
- [x] Логирование работает
- [x] Документация написана
- [x] schema.sql обновлен

## 🚀 Деплой на продакшн

### Шаг 1: Загрузка файлов на хост

Убедитесь, что следующие файлы загружены на хост:

```
✅ src/services/notification-service.js
✅ src/services/scheduler.js
✅ src/scripts/send-training-reminders.js
✅ src/scripts/test-notifications.js
✅ src/scripts/test-single-notification.js
✅ src/app.js (обновлен - добавлен scheduler)
✅ migrations/012_add_notification_logs.sql (уже применена)
✅ src/models/schema.sql (обновлен)
```

### Шаг 2: Проверка .env на хосте

Убедитесь, что в `.env` на хосте есть:

```bash
TELEGRAM_BOT_TOKEN=ваш_реальный_токен_бота
ADMIN_TELEGRAM_ID=ваш_telegram_id  # опционально, для отчетов
```

### Шаг 3: Перезапуск приложения

```bash
# Подключитесь к хосту
ssh user@your-host

# Перейдите в директорию проекта
cd /path/to/gornostyle

# Перезапустите PM2
pm2 restart gornostyle

# Проверьте логи
pm2 logs gornostyle --lines 20
```

### Шаг 4: Проверка запуска планировщика

В логах должно появиться:
```
Инициализация планировщика задач...
✓ Задача "Напоминания о тренировках" настроена на 21:00 (Екатеринбург)
Планировщик запущен. Активных задач: 1
```

### Шаг 5: Тестирование на хосте (опционально)

Если хотите сразу протестировать:

```bash
# Тестовый запуск без отправки
node src/scripts/test-notifications.js --dry-run

# Или отправить себе тестовое уведомление
node src/scripts/test-single-notification.js <ваш_client_id>
```

### Шаг 6: Ожидание первого автоматического запуска

Система автоматически запустится **сегодня в 21:00** и отправит уведомления всем клиентам, у которых завтра есть тренировки.

## 📊 Мониторинг

### Проверка логов приложения
```bash
pm2 logs gornostyle
```

### Проверка логов уведомлений в БД
```sql
-- На хосте
psql -d skisimulator -c "
SELECT 
    DATE(sent_at) as date,
    COUNT(*) as total,
    SUM(CASE WHEN status = 'sent' THEN 1 ELSE 0 END) as sent,
    SUM(CASE WHEN status = 'failed' THEN 1 ELSE 0 END) as failed
FROM notification_logs
WHERE notification_type = 'training_reminder'
GROUP BY DATE(sent_at)
ORDER BY date DESC
LIMIT 7;"
```

## 🔍 Проверка работоспособности

После первого автоматического запуска (в 21:00) проверьте:

1. **PM2 логи:**
   ```bash
   pm2 logs gornostyle | grep "Напоминания"
   ```

2. **Логи в БД:**
   ```sql
   SELECT * FROM notification_logs 
   ORDER BY sent_at DESC 
   LIMIT 10;
   ```

3. **Спросите клиентов:** Получили ли они уведомления в Telegram?

## ⚠️ Возможные проблемы

### Проблема: "chat not found"

**Причина:** Клиент не начал диалог с ботом

**Решение:** 
- Клиент должен написать боту /start
- После этого уведомления будут приходить

### Проблема: Планировщик не запустился

**Решение:**
```bash
# Проверьте логи
pm2 logs gornostyle

# Убедитесь, что нет ошибок
pm2 restart gornostyle
```

### Проблема: Уведомления не отправляются

**Проверьте:**
1. Токен бота в .env
2. База данных доступна
3. Есть ли тренировки на завтра
4. Есть ли у клиентов telegram_id

## 📞 Ручная отправка

Если нужно отправить уведомления вручную:

```bash
# На хосте
cd /path/to/gornostyle

# Отправить уведомления на завтра
node src/scripts/send-training-reminders.js

# Отправить уведомления на конкретную дату
node src/scripts/send-training-reminders.js 2025-10-15

# Отправить одному клиенту
node src/scripts/test-single-notification.js <client_id> <date>
```

## ✅ Критерии успешного деплоя

- [x] Файлы загружены на хост
- [ ] PM2 перезапущен
- [ ] В логах видно: "Планировщик запущен"
- [ ] Первая автоматическая отправка прошла успешно (после 21:00)
- [ ] Клиенты получают уведомления
- [ ] Логи записываются в БД

## 📖 Документация

- **TESTING_RESULTS.md** - результаты тестирования
- **NOTIFICATION_SYSTEM_SUMMARY.md** - краткая сводка
- **docs/notification-setup-guide.md** - инструкция
- **docs/notification-system.md** - полная документация

---

**Готово к деплою!** 🚀

Просто загрузите файлы на хост и перезапустите PM2.
Система начнет работать автоматически!

