# Быстрый старт: Установка электронной подписи

## Текущий статус
- ✅ Инструкция создана: `INSTALL_ELECTRONIC_SIGNATURE.md`
- ✅ Скрипт установки создан: `install-crypto-packages.sh`
- ✅ Системные пакеты установлены: libccid, pcscd, pcsc-tools
- ✅ Сервис pcscd запущен и работает
- ✅ КриптоПро CSP скачан: linux-amd64_deb.tgz
- ✅ Установщик запущен, компоненты выбраны
- ✅ КриптоПро CSP установлен и работает
- ✅ cptools открывается успешно
- ✅ Браузерный плагин установлен
- ✅ Токен Рутокен lite определен системой
- ✅ Chromium GOST установлен (версия 143.0.7499.109)
- ⏳ Осталось: установить расширение КриптоПро в браузере

---

## Команды для выполнения (скопируйте и выполните в терминале)

### Шаг 1: Установка системных пакетов

```bash
sudo apt update
sudo apt install -y libccid pcscd pcsc-tools
```

**Примечание:** Пакет `libgost-astra` доступен только для Astra Linux, в Ubuntu он не нужен.

### Шаг 2: Настройка сервиса pcscd

```bash
sudo systemctl enable pcscd
sudo systemctl start pcscd
sudo systemctl status pcscd
```

**Ожидаемый результат:** Сервис должен быть `active (running)`

### Шаг 3: Проверка установки

```bash
# Проверка статуса сервиса
systemctl is-active pcscd

# Проверка наличия утилит
which pcsc_scan
pcsc_scan --version 2>/dev/null || echo "pcsc_scan установлен"
```

---

## После установки системных пакетов

### Следующие шаги:

1. **Скачайте КриптоПро CSP:**
   - Сайт: https://www.cryptopro.ru/products/csp/downloads
   - Файл: КриптоПро CSP 5.0 для Linux (x64, deb)
   - Сохраните в: `~/Загрузки` или `~/Downloads`

2. **Установите КриптоПро CSP:**
   ```bash
   cd ~/Загрузки  # или ~/Downloads
   tar zxvf linux-amd64_deb.tgz
   cd linux-amd64_deb/
   sudo ./install_gui.sh
   ```

3. **В установщике отметьте:**
   - ✅ Криптопровайдер КС1
   - ✅ Графические диалоги
   - ✅ Поддержка токенов и смарт-карт
   - ✅ cptools
   - ✅ Библиотека PKCS #11

---

## Альтернативный способ: Запуск скрипта

Если хотите использовать интерактивный скрипт:

```bash
chmod +x install-crypto-packages.sh
./install-crypto-packages.sh
```

---

## Полезные ссылки

- 📄 Полная инструкция: `INSTALL_ELECTRONIC_SIGNATURE.md`
- 🔧 Скрипт установки: `install-crypto-packages.sh`
- 🌐 Статья Контур: https://ca.kontur.ru/articles/54155-rabota_s_elektronnoj_podpisyu_na_linux

