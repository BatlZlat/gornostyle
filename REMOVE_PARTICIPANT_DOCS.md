# Удаление участника из тренировки

## 🎯 Описание функции

Новая функция позволяет администратору удалять участников из групповой тренировки через админ-панель с автоматическим возвратом средств и отправкой уведомлений.

## ✨ Возможности

### 1. Удаление участника
- Удаление участника из групповой тренировки через модальное окно
- Изменение статуса участника с `confirmed` на `cancelled`
- Сохранение истории участия в базе данных

### 2. Возврат средств
- Автоматический возврат полной стоимости тренировки на счет клиента
- Создание записи о возврате в таблице транзакций
- Обновление баланса кошелька клиента

### 3. Уведомления
- **Клиенту:** уведомление об удалении с информацией о возврате средств
- **Администратору:** уведомление с деталями удаления и оставшимся количеством мест

## 📋 Техническая реализация

### API Endpoint

**DELETE** `/api/trainings/:id/participants/:participantId`

**Параметры:**
- `id` - ID тренировки
- `participantId` - ID записи участника в `session_participants`

**Ответ (успех):**
```json
{
    "message": "Участник успешно удален из тренировки",
    "refund": 1500.00,
    "remaining_participants": 3
}
```

**Ответ (ошибка):**
```json
{
    "error": "Участник не найден"
}
```

### Процесс удаления

1. **Проверка существования:**
   - Проверяется существование тренировки
   - Проверяется существование участника
   - Проверяется статус участника (должен быть `confirmed`)

2. **Изменение статуса:**
   - Статус участника меняется на `cancelled`
   - Запись остается в базе данных для истории

3. **Возврат средств:**
   - Получается текущий баланс кошелька клиента
   - Баланс увеличивается на сумму стоимости тренировки
   - Создается запись в `transactions` с типом `refund`

4. **Подсчет участников:**
   - Подсчитывается количество оставшихся участников со статусом `confirmed`
   - Формируется строка вида "3/4" (осталось/максимум)

5. **Отправка уведомлений:**
   - **Клиенту:** информация о удалении, сумма возврата, контакт администратора
   - **Администратору:** ФИО клиента, участника, телефон, дата/время тренировки, группа, тренер, оставшиеся места, сумма возврата

### База данных

#### Таблица `session_participants`
- Статус участника меняется с `confirmed` на `cancelled`
- Запись не удаляется, сохраняется история

#### Таблица `wallets`
- Обновляется баланс: `balance = balance + price`
- Обновляется поле `last_updated`

#### Таблица `transactions`
- Создается новая запись с типом `refund`
- Описание: "Возврат: Группа, [ФИО участника], Дата: [дата], Время: [время], Длительность: [мин] мин."

### Уведомления

#### Уведомление клиенту (Telegram)
```
❗️ Вы были удалены из тренировки администратором:

📅 Дата: 19.10.2025 (ВС)
⏰ Время: 15:00 - 16:00
👥 Группа: Дети 8-12 лет
👨‍🏫 Тренер: Иванов Иван
🎿 Тренажер: Тренажер 1
💰 Возврат: 1500.00 руб.

Деньги в размере 1500.00 руб. возвращены на ваш счет.
По всем вопросам обращайтесь к администратору: +7 XXX XXX-XX-XX
```

#### Уведомление администратору (Telegram)
```
👥 *Удаление участника из тренировки!*

👤 *Клиент:* Иванов Петр
👶 *Участник:* Иванов Сергей (10 лет)
📞 *Телефон:* +7 XXX XXX-XX-XX
📅 *Дата:* 19.10.2025
⏰ *Время:* 15:00:00
👥 *Группа:* Дети 8-12 лет
👨‍🏫 *Тренер:* Иванов Иван
🎿 *Тренажер:* Тренажер 1
🪑 *Мест осталось:* 3/4
💰 *Возврат:* 1500.00 руб.
```

## 🖥️ Интерфейс администратора

### Модальное окно "Детали тренировки"

При открытии модального окна с деталями тренировки (кнопка "Подробнее") в таблице участников появляется новая колонка "Действия" с кнопкой удаления.

**Таблица участников:**
| ФИО | Возраст | Уровень | Контактный телефон | Действия |
|-----|---------|---------|-------------------|----------|
| Иванов Сергей | 10 лет | 2 | +7 XXX XXX-XX-XX | ❌ Удалить |
| Петров Алексей | 11 лет | 3 | +7 XXX XXX-XX-XX | ❌ Удалить |

**Кнопка "❌ Удалить":**
- Красная кнопка с иконкой
- При наведении показывается подсказка: "Удалить участника с возвратом средств"
- При клике открывается диалог подтверждения

**Диалог подтверждения:**
```
Вы уверены, что хотите удалить участника "Иванов Сергей" из тренировки?

Действия:
✅ Статус участника будет изменен на "отменено"
💰 Средства будут возвращены на счет клиента
📨 Клиент получит уведомление об удалении
📱 Администратор получит уведомление

[Отмена] [OK]
```

**После успешного удаления:**
```
✅ Участник "Иванов Сергей" успешно удален из тренировки
Возврат: 1500 руб.
Осталось участников: 3
```

## 📝 Код

### Backend

**Файл:** `src/routes/trainings.js`

```javascript
// Удаление участника из тренировки с возвратом средств
router.delete('/:id/participants/:participantId', async (req, res) => {
    // ... полная реализация в файле
});
```

**Файл:** `src/bot/admin-notify.js`

```javascript
// Функция для отправки уведомления об удалении участника из тренировки
async function notifyAdminParticipantRemoved(trainingData) {
    // ... полная реализация в файле
}

module.exports = {
    // ... другие экспорты
    notifyAdminParticipantRemoved,
};
```

### Frontend

**Файл:** `public/js/admin.js`

```javascript
// Функция для удаления участника из тренировки
async function removeParticipantFromTraining(trainingId, participantId, participantName) {
    // ... полная реализация в файле
}
```

**Модальное окно (обновлено):**
```javascript
<table class="participants-table">
    <thead>
        <tr>
            <th>ФИО</th>
            <th>Возраст</th>
            <th>Уровень</th>
            <th>Контактный телефон</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        ${training.participants.map(participant => `
            <tr>
                <td>${participant.full_name}</td>
                <td>${age} лет</td>
                <td>${participant.skill_level || '-'}</td>
                <td>${participant.phone || '-'}</td>
                <td>
                    <button 
                        class="btn-danger btn-small" 
                        onclick="removeParticipantFromTraining(${training.id}, ${participant.id}, '${participant.full_name}')"
                        title="Удалить участника с возвратом средств">
                        ❌ Удалить
                    </button>
                </td>
            </tr>
        `).join('')}
    </tbody>
</table>
```

## 🧪 Тестирование

### Автоматический тест

Запустите тестовый скрипт:
```bash
./test-remove-participant.sh
```

Скрипт выполнит:
1. Создание тестовой групповой тренировки
2. Добавление двух участников (Тестик1 и Тестик2)
3. Списание средств за участие
4. Проверку созданной тренировки и участников
5. Инструкции для ручного тестирования через админ-панель
6. Проверку результатов после удаления
7. Очистку тестовых данных

### Ручное тестирование

1. **Создайте тестовую тренировку:**
   - Откройте админ-панель
   - Создайте групповую тренировку на будущую дату
   - Добавьте несколько участников

2. **Откройте детали тренировки:**
   - Найдите созданную тренировку в списке
   - Нажмите кнопку "Подробнее"

3. **Удалите участника:**
   - В таблице участников нажмите "❌ Удалить" рядом с одним из участников
   - Подтвердите удаление

4. **Проверьте результат:**
   - Убедитесь, что участник исчез из списка
   - Проверьте баланс клиента (должен увеличиться)
   - Проверьте уведомления в Telegram (клиенту и администратору)

### Проверка базы данных

```sql
-- Проверка статуса участника
SELECT sp.*, c.full_name, ch.full_name as child_name
FROM session_participants sp
LEFT JOIN clients c ON sp.client_id = c.id
LEFT JOIN children ch ON sp.child_id = ch.id
WHERE sp.id = [PARTICIPANT_ID];

-- Проверка баланса и транзакций
SELECT w.balance, t.*
FROM wallets w
LEFT JOIN transactions t ON w.id = t.wallet_id
WHERE w.client_id = [CLIENT_ID]
ORDER BY t.created_at DESC
LIMIT 5;

-- Проверка количества участников
SELECT 
    COUNT(*) FILTER (WHERE status = 'confirmed') as confirmed,
    COUNT(*) FILTER (WHERE status = 'cancelled') as cancelled
FROM session_participants
WHERE session_id = [TRAINING_ID];
```

## 🔒 Безопасность

1. **Проверка существования:**
   - Тренировка должна существовать
   - Участник должен существовать
   - Участник должен быть в этой тренировке

2. **Проверка статуса:**
   - Можно удалять только участников со статусом `confirmed`
   - Повторное удаление невозможно

3. **Транзакции:**
   - Все операции выполняются в рамках транзакции БД
   - При ошибке происходит откат всех изменений
   - Гарантируется целостность данных

4. **Ошибки:**
   - Обработка всех возможных ошибок
   - Логирование ошибок в консоль
   - Информативные сообщения для пользователя

## 📊 Статистика использования

После внедрения функции можно отслеживать:
- Количество удаленных участников
- Сумму возвращенных средств
- Причины удаления (если добавить поле в форму)
- Время между созданием и удалением записи

```sql
-- Статистика удалений за последний месяц
SELECT 
    COUNT(*) as total_cancellations,
    SUM(ts.price) as total_refund
FROM session_participants sp
JOIN training_sessions ts ON sp.session_id = ts.id
WHERE sp.status = 'cancelled'
AND sp.updated_at >= CURRENT_DATE - INTERVAL '1 month';
```

## 🚀 Готово к использованию!

Функция полностью реализована и протестирована. Администраторы могут начать использовать её через админ-панель.

---

**Дата создания:** 18 октября 2025  
**Статус:** ✅ Готово к продакшену  
**Версия:** 1.0.0
