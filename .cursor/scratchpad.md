# 📋 ПЛАН РАЗВИТИЯ: Интеграция сайта и Telegram бота + Групповые тренировки + Система уведомлений

**Дата создания:** 15.12.2025  
**Режим:** Планировщик

---

## 🎯 ЦЕЛИ И ЗАДАЧИ

### ✅ ЗАДАЧА 0: Настроить оплату для всех форм бронирования (ВЫПОЛНЕНО)
**Проблема:** Для индивидуальных тренировок бронирование создается только после успешной оплаты (через webhook), а для групповых тренировок и программ бронирование создается сразу, до оплаты.

**Требования:**
- ✅ Переделать `createGroupBooking()` на ту же логику, что и `createIndividualBooking()`
- ✅ Переделать `createProgramBooking()` на ту же логику, что и `createIndividualBooking()`
- ✅ Бронирование должно создаваться ТОЛЬКО после успешной оплаты через webhook
- ✅ Данные бронирования должны сохраняться в `provider_raw_data` транзакции
- ✅ Для групповых тренировок реализована временная блокировка мест (увеличение `current_participants` на время оплаты)

**Текущая ситуация:**
- ✅ Индивидуальные тренировки: бронирование создается после оплаты (правильно)
- ✅ Групповые тренировки: бронирование создается после оплаты (исправлено)
- ✅ Программы: бронирование создается после оплаты (исправлено)

### Задача 1: Идентификация клиентов через Telegram бот при записи через сайт
**Проблема:** Когда клиент записывается через сайт, он не идентифицируется в Telegram боте. Если клиент позже открывает бота, ему приходится проходить регистрацию заново.

**Требования:**
- При переходе по ссылке на бота из формы бронирования (до оплаты) - создать клиента в БД с предзаполненными данными
- При нажатии "Старт" в боте - добавить данные из Telegram (telegram_id, username) к существующему клиенту
- Если клиент ушел с сайта, а потом кто-то скинул ему ссылку на бота - он должен просто нажать "Старт" и не проходить регистрацию заново
- После оплаты на странице успеха тоже есть ссылка на бота - должна работать аналогично

### Задача 2: Бронирование групповых тренировок через сайт
**Уточнения:**
- ✅ Для групповых тренировок (2, 3, 5 участников) форма уже реализована - количество участников определяется контейнером (прайсом)
- ✅ Для регулярных программ есть возможность добавлять участников (уже реализовано)
- **Задача:** Проверить, что все работает корректно, и при необходимости доработать

### Задача 3: Система уведомлений для всех видов бронирований
**Уточнения:**
- Email уведомления **только для клиентов** при создании бронирования
- Email уведомления для клиентов при отмене бронирования
- Telegram уведомления для администратора и инструктора (они уже зарегистрированы в Telegram)
- Напоминания о предстоящих тренировках - использовать существующую cron задачу (21:00), которая уже работает для клиентов из бота
- Использовать существующий `EmailService` (уже реализован для сертификатов)
- Сделать шаблоны email удобными и комфортными для чтения

---

## 📊 ТЕКУЩАЯ СИСТЕМА (АНАЛИЗ)

### 1. Создание клиентов при бронировании через сайт

**Где:** `src/routes/kuliga-booking.js` → функция `upsertClient()`

**Логика:**
1. Поиск клиента по нормализованному телефону
2. Если найден:
   - Обновляется `email` (если был пустой)
   - Обновляется `birth_date` (если был временным `1900-01-01`)
   - Возвращается `{ id, telegram_id }`
3. Если не найден:
   - Создается новый клиент с обязательным `email`
   - `telegram_id` = `NULL` (не заполняется!)
   - `birth_date` = указанная дата или `1900-01-01`

**Проблема:** `telegram_id` не заполняется при создании через сайт, поэтому клиент не идентифицируется в боте.

---

### 2. Регистрация в Telegram боте

**Где:** `src/bot/client-bot.js` → функция `registerClient()` и обработчик `/start`

**Логика при `/start`:**
1. Проверка наличия клиента по `telegram_id`
2. Если клиента нет:
   - Извлекается реферальный код из `/start REFERRAL_CODE` (если есть)
   - Показывается приветственное сообщение
   - Сохраняется состояние `wait_start` с данными из Telegram
3. Если клиент найден:
   - Показывается главное меню

**Логика при регистрации (`registerClient`):**
1. Проверка наличия клиента по `telegram_id` (приоритет)
2. Если не найден по `telegram_id`:
   - Поиск по телефону
   - Если найден по телефону:
     - Обновляется `telegram_id`, `telegram_username`, `nickname`
     - Обновляются другие поля (если изменились)
   - Если не найден:
     - Создается новый клиент

**Проблема:** Если клиент создан через сайт (без `telegram_id`), но с телефоном, то при регистрации в боте он должен найтись по телефону. Но для этого нужно, чтобы клиент ввел тот же телефон, что и на сайте.

---

### 3. Deep Links в Telegram

**Текущая поддержка:**
- `/start REFERRAL_CODE` - для реферальных ссылок
- Можно использовать `/start CLIENT_ID` или `/start PHONE_HASH` для идентификации

**Где используется:**
- `src/bot/client-bot.js:12348` - обработчик `/start(.*)`
- Извлекается параметр: `match[1]` (все после `/start`)

---

### 4. Уведомления

**Email уведомления:**
- `src/services/emailService.js` - универсальный сервис
- Метод `sendEmail(recipientEmail, subject, htmlContent, attachments)`
- Используется для сертификатов: `sendCertificateEmail()`

**Telegram уведомления:**
- `src/services/notification-service.js` - сервис для напоминаний о тренировках
- `src/bot/admin-notify.js` - уведомления администраторам и инструкторам
- Отправка через Telegram Bot API

**Текущие уведомления при бронировании:**
- В `src/routes/kuliga-payment.js` (webhook handler) - НЕ отправляются уведомления
- В `src/routes/kuliga-stuck-payments.js` - отправляются email уведомления при ручном создании бронирования
- В `src/bot/admin-notify.js` - есть функции `notifyAdminNaturalSlopeTrainingBooking()` и `notifyInstructorKuligaTrainingBooking()`

---

### 5. Групповые тренировки

**Текущая реализация:**
- `src/routes/kuliga-booking.js` → `createGroupBooking()` - для записи на существующую групповую тренировку
- `src/routes/kuliga-booking.js` → `createProgramBooking()` - для записи на программу (создает групповую тренировку автоматически)
- Оба метода создают бронирование сразу (без оплаты)

**Проблема:** Нет поддержки выбора количества участников (2, 3, 4 и т.д.) через сайт. Только через бота.

---

## 🔧 РЕШЕНИЯ И ПЛАН РЕАЛИЗАЦИИ

### ЗАДАЧА 1: Идентификация клиентов через Telegram бот

#### Решение 1.1: Deep Link с идентификатором клиента

**Идея:** При переходе по ссылке на бота из формы бронирования передавать идентификатор клиента (или хеш телефона) в deep link.

**Формат deep link:**
```
https://t.me/Ski_Instruktor72_bot?start=client_{CLIENT_ID}
или
https://t.me/Ski_Instruktor72_bot?start=phone_{PHONE_HASH}
```

**Преимущества:**
- Простая реализация
- Не требует изменений в логике регистрации
- Работает даже если клиент ушел с сайта

**Недостатки:**
- Нужно знать `client_id` до создания клиента (можно использовать временный идентификатор)
- Хеш телефона может быть небезопасным

**Альтернатива:** Использовать временный токен (UUID), который связывает клиента с сессией бронирования.

#### Решение 1.2: Предварительная регистрация клиента при клике на ссылку

**Идея:** При клике на ссылку бота в форме бронирования вызывать API endpoint `/api/kuliga/pre-register-client`, который создает клиента в БД с предзаполненными данными. Затем в deep link передавать `client_id`.

**Текущий endpoint:** `/api/kuliga/pre-register-client` уже существует! (строка 862 в `kuliga-booking.js`)

**Логика:**
1. Клиент заполняет форму бронирования
2. Клиент нажимает на ссылку бота
3. JavaScript отправляет POST на `/api/kuliga/pre-register-client` с данными формы
4. Сервер создает/обновляет клиента в БД (без `telegram_id`)
5. Возвращается `client_id`
6. Deep link формируется как `https://t.me/Ski_Instruktor72_bot?start=client_{CLIENT_ID}`
7. При `/start client_{CLIENT_ID}` в боте:
   - Извлекается `client_id`
   - Ищется клиент по `client_id`
   - Если найден и `telegram_id` пустой:
     - Обновляется `telegram_id` из Telegram
     - Показывается сообщение: "Вы успешно подключены! Ваши данные сохранены."
   - Если `telegram_id` уже заполнен:
     - Проверяется, совпадает ли с текущим `telegram_id`
     - Если совпадает - показывается главное меню
     - Если не совпадает - показывается предупреждение

**Преимущества:**
- Клиент создается сразу при клике на ссылку
- Не нужно вводить данные заново в боте
- Работает даже если клиент ушел с сайта

**Недостатки:**
- Нужно вызывать API при клике на ссылку (может быть медленно)
- Если клиент не нажмет на ссылку, клиент все равно создастся при бронировании

#### Решение 1.3: Идентификация по телефону при регистрации в боте (РЕКОМЕНДУЕТСЯ) ⭐

**Идея:** Улучшить логику регистрации в боте, чтобы при вводе телефона клиент автоматически находился в БД и связывался с существующей записью.

**Текущая логика:** В `registerClient()` уже есть поиск по телефону, но только если клиент не найден по `telegram_id`.

**Улучшение:**
1. При `/start` извлекать параметр (если есть)
2. Если параметр = `client_{CLIENT_ID}`:
   - Сохранять `client_id` в состоянии пользователя
   - При регистрации (ввод телефона) проверять:
     - Если `client_id` сохранен и клиент найден по `client_id`:
       - Обновлять `telegram_id` этого клиента
       - Пропускать ввод остальных данных (использовать данные из БД)
     - Если `client_id` не сохранен:
       - Искать клиента по телефону (как сейчас)
       - Если найден - обновлять `telegram_id`
       - Если не найден - создавать нового

**Преимущества:**
- Минимальные изменения в коде
- Работает даже без deep link (поиск по телефону)
- Безопасно (не передаем чувствительные данные в URL)

**Недостатки:**
- Клиент все равно должен ввести телефон в боте
- Но можно сделать так, чтобы если клиент найден по телефону, он сразу попадал в меню без полной регистрации

#### РЕКОМЕНДУЕМОЕ РЕШЕНИЕ: Комбинация Решения 1.2 и 1.3

1. **При клике на ссылку бота в форме бронирования:**
   - Вызывать `/api/kuliga/pre-register-client` (уже существует)
   - Сохранять `client_id` в `sessionStorage` или `localStorage`
   - Формировать deep link: `https://t.me/Ski_Instruktor72_bot?start=client_{CLIENT_ID}`

2. **При `/start client_{CLIENT_ID}` в боте:**
   - Сохранять `client_id` в состоянии пользователя
   - При вводе телефона:
     - Если `client_id` сохранен - искать клиента по `client_id`
     - Если найден и `telegram_id` пустой:
       - Обновлять `telegram_id`
       - Использовать данные из БД (имя, email, дата рождения)
       - Показывать сообщение: "Добро пожаловать! Ваши данные из формы бронирования сохранены."
     - Если `client_id` не сохранен - искать по телефону (как сейчас)

3. **После успешной оплаты:**
   - На странице успеха формировать deep link с `client_id` из транзакции
   - Аналогичная логика в боте

---

### ЗАДАЧА 2: Бронирование групповых тренировок

#### Решение 2.1: Добавить выбор количества участников в форму

**Текущая ситуация:**
- `createGroupBooking()` принимает `participantsCount` и `participantsNames`
- Но в форме нет выбора количества участников (только для индивидуальных тренировок)

**План:**
1. Добавить поле выбора количества участников в форму групповых тренировок
2. При выборе количества участников показывать поля для ввода имен участников
3. При отправке формы передавать `participantsCount` и `participantsNames` в API

**Где изменить:**
- `public/js/kuliga-booking.js` - добавить логику выбора количества участников
- `views/kuliga-booking.ejs` - добавить UI для выбора количества участников

#### Решение 2.2: Бронирование на регулярные программы

**Текущая ситуация:**
- `createProgramBooking()` уже существует
- Но нужно проверить, работает ли оно через сайт

**План:**
1. Проверить, есть ли форма для бронирования программ на сайте
2. Если нет - добавить форму
3. При выборе программы показывать доступные даты и время
4. При бронировании создавать групповую тренировку автоматически (как сейчас)

---

### ЗАДАЧА 3: Система уведомлений

#### Решение 3.1: Email уведомления при создании бронирования

**Где отправлять:**
- В `src/routes/kuliga-payment.js` после успешного создания бронирования (webhook handler)
- В `src/routes/kuliga-stuck-payments.js` при ручном создании (уже реализовано)

**Что отправлять:**
1. **Клиенту (Email):**
   - Подтверждение записи
   - Детали тренировки (дата, время, инструктор, место, стоимость)
   - Ссылка на бота для получения уведомлений
   - Красивый и удобный шаблон

2. **Инструктору (Telegram):**
   - Уведомление о новой записи
   - Детали клиента и тренировки
   - Использовать `notifyInstructorKuligaTrainingBooking()` из `admin-notify.js`

3. **Администратору (Telegram):**
   - Уведомление о новой записи
   - Использовать `notifyAdminNaturalSlopeTrainingBooking()` из `admin-notify.js`

**Шаблоны email:**
- Использовать существующий `EmailService.sendEmail()`
- Создать шаблоны для разных типов уведомлений

#### Решение 3.2: Telegram уведомления для администратора и инструктора

**Где отправлять:**
- В `src/routes/kuliga-payment.js` после успешного создания бронирования
- Использовать существующие функции из `admin-notify.js`

**Что отправлять:**
- Инструктору: `notifyInstructorKuligaTrainingBooking()` - уже реализовано
- Администратору: `notifyAdminNaturalSlopeTrainingBooking()` - уже реализовано

#### Решение 3.3: Уведомления об отмене бронирования

**Где отправлять:**
- При отмене бронирования (webhook с статусом `FAILED` или `REFUNDED`)
- При ручной отмене администратором

**Что отправлять:**
- Email клиенту об отмене (красивый шаблон)
- Telegram уведомление инструктору об отмене (через `admin-notify.js`)
- Telegram уведомление администратору об отмене (через `admin-notify.js`)

#### Решение 3.4: Напоминания о предстоящих тренировках

**Текущая ситуация:**
- `src/services/scheduler.js` - cron задача в 21:00 отправляет напоминания через `notificationService.sendTomorrowReminders()`
- Нужно проверить, работает ли это для `kuliga_bookings` (бронирования через сайт)

**План:**
1. Проверить метод `sendTomorrowReminders()` в `notification-service.js`
2. Убедиться, что он включает `kuliga_bookings` в выборку
3. Если нет - добавить логику для `kuliga_bookings`
4. Использовать существующий `NotificationService` и cron задачу

---

## 📝 ПОДРОБНЫЙ ПЛАН РЕАЛИЗАЦИИ

### ЭТАП 1: Идентификация клиентов через Telegram бот

#### Задача 1.1: Обновить форму бронирования для предварительной регистрации

**Файлы:**
- `public/js/kuliga-booking.js`
- `views/kuliga-booking.ejs`

**Действия:**
1. При клике на ссылку бота вызывать `/api/kuliga/pre-register-client`
2. Сохранять `client_id` в `sessionStorage`
3. Формировать deep link с `client_id`
4. Открывать бота с deep link

**Критерий успеха:** При клике на ссылку бота клиент создается в БД, deep link содержит `client_id`.

---

#### Задача 1.2: Обновить обработчик `/start` в боте

**Файлы:**
- `src/bot/client-bot.js`

**Действия:**
1. При `/start client_{CLIENT_ID}` сохранять `client_id` в состоянии пользователя
2. При регистрации (ввод телефона):
   - Если `client_id` сохранен - искать клиента по `client_id`
   - Если найден и `telegram_id` пустой:
     - Обновлять `telegram_id`
     - Использовать данные из БД
     - Показывать сообщение о успешной идентификации
   - Если `client_id` не сохранен - искать по телефону (как сейчас)

**Критерий успеха:** При `/start client_{CLIENT_ID}` и вводе телефона клиент идентифицируется автоматически.

---

#### Задача 1.3: Обновить страницу успешной оплаты

**Файлы:**
- `views/booking-success.ejs`
- `src/routes/kuliga-payment.js` (для передачи `client_id`)

**Действия:**
1. При успешной оплате получать `client_id` из транзакции
2. Формировать deep link с `client_id`
3. Показывать ссылку на бота с deep link

**Критерий успеха:** На странице успеха ссылка на бота содержит `client_id`.

---

### ЭТАП 2: Бронирование групповых тренировок

#### Задача 2.1: Добавить выбор количества участников в форму групповых тренировок

**Файлы:**
- `public/js/kuliga-booking.js`
- `views/kuliga-booking.ejs`

**Действия:**
1. Добавить поле выбора количества участников (2, 3, 4, 5, 6)
2. При выборе количества показывать поля для ввода имен участников
3. При отправке формы передавать `participantsCount` и `participantsNames` в API

**Критерий успеха:** В форме групповых тренировок можно выбрать количество участников и ввести их имена.

---

#### Задача 2.2: Проверить и доработать бронирование программ

**Файлы:**
- `public/js/kuliga-booking.js`
- `views/kuliga-booking.ejs`
- `src/routes/kuliga-booking.js` (проверить `createProgramBooking`)

**Действия:**
1. Проверить, есть ли форма для бронирования программ
2. Если нет - добавить форму
3. При выборе программы показывать доступные даты и время
4. Проверить работу `createProgramBooking()`

**Критерий успеха:** Можно забронировать место в программе через сайт.

---

### ЭТАП 3: Система уведомлений

#### Задача 3.1: Email уведомления при создании бронирования

**Файлы:**
- `src/routes/kuliga-payment.js` (webhook handler)
- `src/services/emailService.js` (уже существует)

**Действия:**
1. После успешного создания бронирования отправлять email клиенту
2. Отправлять email инструктору (использовать `notifyInstructorKuligaTrainingBooking()`)
3. Отправлять email администратору (использовать `notifyAdminNaturalSlopeTrainingBooking()`)
4. Создать шаблоны email для разных типов уведомлений

**Критерий успеха:** При создании бронирования отправляются email уведомления клиенту, инструктору и администратору.

---

#### Задача 3.2: Telegram уведомления для администратора и инструктора

**Файлы:**
- `src/routes/kuliga-payment.js` (webhook handler)
- `src/bot/admin-notify.js` (уже реализовано)

**Действия:**
1. После успешного создания бронирования вызывать `notifyInstructorKuligaTrainingBooking()`
2. Вызывать `notifyAdminNaturalSlopeTrainingBooking()`
3. Проверить, что функции работают корректно

**Критерий успеха:** Инструктор и администратор получают Telegram уведомления при создании бронирования.

---

#### Задача 3.3: Уведомления об отмене бронирования

**Файлы:**
- `src/routes/kuliga-payment.js` (webhook handler для статуса `FAILED` или `REFUNDED`)

**Действия:**
1. При отмене бронирования отправлять email клиенту
2. Отправлять email инструктору
3. Отправлять Telegram уведомление клиенту (если есть `telegram_id`)

**Критерий успеха:** При отмене бронирования отправляются уведомления клиенту и инструктору.

---

#### Задача 3.4: Напоминания о предстоящих тренировках

**Файлы:**
- `src/services/notification-service.js` (проверить и доработать)
- `src/services/scheduler.js` (проверить расписание)

**Действия:**
1. Проверить, отправляются ли напоминания для `kuliga_bookings`
2. Если нет - добавить логику отправки напоминаний
3. Использовать существующий `NotificationService`

**Критерий успеха:** Клиенты получают напоминания о предстоящих тренировках за день до тренировки.

---

## 🎯 ПРИОРИТЕТЫ

1. **НАИВЫСШИЙ ПРИОРИТЕТ:**
   - ⚠️ **Задача 0: Настроить оплату для всех форм бронирования** (групповые тренировки и программы)

2. **ВЫСОКИЙ ПРИОРИТЕТ:**
   - Задача 1.2: Обновить обработчик `/start` в боте (идентификация клиентов)
   - Задача 3.1: Email уведомления при создании бронирования

2. **СРЕДНИЙ ПРИОРИТЕТ:**
   - Задача 1.1: Обновить форму бронирования для предварительной регистрации
   - Задача 2.1: Добавить выбор количества участников в форму групповых тренировок
   - Задача 3.2: Telegram уведомления для клиентов

3. **НИЗКИЙ ПРИОРИТЕТ:**
   - Задача 1.3: Обновить страницу успешной оплаты
   - Задача 2.2: Проверить и доработать бронирование программ
   - Задача 3.3: Уведомления об отмене бронирования
   - Задача 3.4: Напоминания о предстоящих тренировках

---

## 📊 ТЕКУЩИЙ СТАТУС

**Статус:** ✅ ВСЕ ЗАДАЧИ ВЫПОЛНЕНЫ И ПРОТЕСТИРОВАНЫ

**Дата последнего обновления:** 18.12.2025

### ✅ ВЫПОЛНЕНО:

1. **✅ Задача 1.2: Deep link с client_id** - РЕАЛИЗОВАНО
   - Обработка `/start client_{CLIENT_ID}` в боте
   - Автоматическая идентификация клиента при регистрации в боте
   - Обновление `telegram_id` для существующих клиентов

2. **✅ Задача 1.3: Страница успешной оплаты с deep link** - РЕАЛИЗОВАНО
   - Deep link формируется на странице успеха с `client_id`
   - Передача `client_id` через query параметры в URL

3. **✅ Задача 3.1: Email уведомления при создании бронирования** - РЕАЛИЗОВАНО
   - HTML шаблоны для подтверждения бронирования
   - Отправка email клиенту после успешной оплаты
   - Интеграция в `kuliga-payment.js`

4. **✅ Задача 3.2: Telegram уведомления для администратора и инструктора** - РЕАЛИЗОВАНО
   - Уведомления инструктору при создании бронирования
   - Уведомления администратору при создании бронирования
   - Исправлена проблема с получением `instructor_id` из слота

5. **✅ Задача 3.3: Email уведомления об отмене бронирования** - РЕАЛИЗОВАНО
   - HTML шаблон для отмены бронирования
   - Отправка email при отмене/возврате платежа

6. **✅ Задача 4.1: HTML шаблоны для email** - РЕАЛИЗОВАНО
   - `booking-confirmation.html` - подтверждение бронирования
   - `training-reminder.html` - напоминание о тренировке
   - `booking-cancellation.html` - отмена бронирования

7. **✅ Задача 4.2: Расширение notification-service.js для email** - РЕАЛИЗОВАНО
   - Метод `sendEmailNotification()` добавлен
   - Модифицирован `sendTrainingReminders()` для отправки email клиентам без `telegram_id`

8. **✅ Задача 4.3: Email при создании бронирования** - РЕАЛИЗОВАНО
   - Отправка email после успешной оплаты
   - Использование шаблона `booking-confirmation.html`

9. **✅ Задача 4.4: Email при отмене бронирования** - РЕАЛИЗОВАНО
   - Отправка email при отмене/возврате
   - Использование шаблона `booking-cancellation.html`

10. **✅ Создание referral_code и кошелька при регистрации через сайт** - РЕАЛИЗОВАНО
    - Автоматическое создание `referral_code` для новых клиентов
    - Автоматическое создание кошелька для новых клиентов
    - Проверка и создание для существующих клиентов при обновлении

11. **✅ Исправлена стратегия отправки email** - РЕАЛИЗОВАНО
    - SMTP Yandex - основной метод для всех клиентов
    - Resend - только как fallback при ошибках SMTP
    - Увеличенные таймауты для Yandex адресов того же аккаунта

12. **✅ Исправлена проблема с ботом инструкторов** - РЕАЛИЗОВАНО
    - Исправлена загрузка токена в PM2
    - Улучшено логирование инициализации и ошибок polling

16. **✅ Задача 1.1: Предварительная регистрация** - РЕАЛИЗОВАНО И ПРОТЕСТИРОВАНО
   - Endpoint `/api/kuliga/pre-register-client` работает
   - Deep link формируется с `client_id` на странице успеха
   - Клиент идентифицируется в боте без повторной регистрации

17. **✅ Задача 3.4: Напоминания о предстоящих тренировках** - РЕАЛИЗОВАНО И ПРОТЕСТИРОВАНО
   - Cron задача работает (21:00)
   - Напоминания отправляются для всех типов бронирований (включая `kuliga_bookings`)
   - Email отправляется клиентам без `telegram_id`
   - Telegram отправляется клиентам с `telegram_id`

18. **✅ Тестирование уведомлений** - ВЫПОЛНЕНО
   - Протестированы уведомления при бронировании через сайт
   - Протестированы уведомления при бронировании через бота
   - Все уведомления работают корректно (Email, Telegram админу, Telegram инструктору)
   - Исправлен формат времени в уведомлениях (ЧЧ:ММ вместо ЧЧ:ММ:СС)
   - Исправлен location в уведомлениях (используется реальный location инструктора)

19. **✅ Исправление location в уведомлениях** - РЕАЛИЗОВАНО
   - Location берется из базы данных инструктора
   - Правильное отображение места в уведомлениях (Воронинские горки / Кулига-Клуб)

### 🚧 В ПРОЦЕССЕ / ТРЕБУЕТ ПРОВЕРКИ:

**Нет задач, требующих проверки!**

13. **✅ Задача 0: Оплата для программ** - РЕАЛИЗОВАНО И ПРОТЕСТИРОВАНО
   - Исправлена ошибка с `groupTrainingIdToUse` → `groupTraining.id`
   - Исправлена ошибка с отсутствующим `price_per_person` в SELECT запросе
   - Исправлена ошибка с переменной `participants`
   - Добавлена валидация и логирование
   - Оплата программ работает корректно

14. **✅ Задача 2.2: Проверить бронирование программ** - РЕАЛИЗОВАНО
   - Бронирование программ через сайт работает
   - Оплата проходит успешно
   - Все ошибки исправлены

15. **✅ Задача 2.1: Выбор количества участников** - УЖЕ РЕАЛИЗОВАНО
   - Количество участников для групповых тренировок привязано к карточкам (2, 3, 4 человека и т.д.)
   - Для программ количество участников выбирается путем добавления

### ❌ НЕ ВЫПОЛНЕНО:

**Нет невыполненных задач!**

**Следующие шаги:**
1. ✅ Проверить работу предварительной регистрации и deep link в форме - ВЫПОЛНЕНО
2. ✅ Проверить работу напоминаний для бронирований через сайт - ВЫПОЛНЕНО
3. ✅ Реализовать Задачу 0 (оплата для групповых тренировок и программ) - ВЫПОЛНЕНО
4. ✅ Протестировать все уведомления (через сайт и через бота) - ВЫПОЛНЕНО
5. ✅ Исправить формат времени в уведомлениях - ВЫПОЛНЕНО
6. ✅ Исправить location в уведомлениях - ВЫПОЛНЕНО

**🎉 ВСЕ ЗАДАЧИ ВЫПОЛНЕНЫ!**

---

## 📱 ЗАДАЧА 4: СИСТЕМА УВЕДОМЛЕНИЙ ДЛЯ КЛИЕНТОВ БЕЗ TELEGRAM

**Дата добавления:** 15.12.2025  
**Режим:** Планировщик

### 🎯 ЦЕЛЬ
Обеспечить максимальный охват клиентов уведомлениями о предстоящих тренировках и отменах, включая тех, кто не зарегистрирован в Telegram боте.

### 📊 ТЕКУЩАЯ СИТУАЦИЯ

#### Анализ существующей системы уведомлений:

1. **Telegram уведомления:**
   - ✅ Работают для клиентов с `telegram_id`
   - ✅ Напоминания о тренировках (21:00, cron задача)
   - ✅ Уведомления администратору и инструктору
   - ❌ Не работают для клиентов без `telegram_id`

2. **Email уведомления:**
   - ✅ Реализован `EmailService` (используется для сертификатов)
   - ✅ SMTP через Yandex настроен
   - ✅ Красивые HTML шаблоны для сертификатов
   - ✅ Используются для уведомлений о тренировках (подтверждение, отмена)
   - ✅ Используются для напоминаний (через notification-service.js)
   - ✅ Используются для отмен бронирований

3. **SMS уведомления:**
   - ✅ Есть роутер `/api/sms/process` для обработки входящих SMS
   - ❌ НЕТ функционала отправки исходящих SMS
   - ❌ НЕТ интеграции с SMS-провайдером

4. **WhatsApp уведомления:**
   - ❌ НЕ реализовано
   - ❌ НЕТ интеграции с WhatsApp API

#### Проблема:
В `notification-service.js` метод `sendTrainingReminders()` отправляет уведомления **только** клиентам с `telegram_id`:
```javascript
// Строка 603: await this.sendNotification(clientData.telegram_id, message)
// Если telegram_id отсутствует - уведомление не отправляется
```

#### Статистика клиентов:
- Нужно проверить: сколько клиентов имеют `telegram_id`, а сколько нет
- Клиенты, записанные через сайт, часто не имеют `telegram_id`
- Для них критически важно получать уведомления о тренировках и отменах

---

### 🔍 АНАЛИЗ ВАРИАНТОВ УВЕДОМЛЕНИЙ

#### 1. EMAIL УВЕДОМЛЕНИЯ ⭐ РЕКОМЕНДУЕТСЯ

**Преимущества:**
- ✅ Уже реализован `EmailService`
- ✅ У всех клиентов есть email (обязательное поле при регистрации)
- ✅ Низкая стоимость (бесплатно через SMTP)
- ✅ Можно отправлять красивые HTML шаблоны
- ✅ Высокая доставляемость
- ✅ Клиенты привыкли к email уведомлениям

**Недостатки:**
- ⚠️ Email может попасть в спам
- ⚠️ Не все клиенты проверяют email регулярно
- ⚠️ Может быть задержка доставки

**Реализация:**
- Использовать существующий `EmailService.sendEmail()`
- Создать HTML шаблоны для:
  - Подтверждение бронирования
  - Напоминание о тренировке (за день до)
  - Уведомление об отмене
- Добавить отправку email в `notification-service.js` для клиентов без `telegram_id`

**Стоимость:** Бесплатно (SMTP Yandex)

**Приоритет:** ВЫСОКИЙ ⭐⭐⭐

---

#### 2. SMS УВЕДОМЛЕНИЯ

**Преимущества:**
- ✅ Высокая доставляемость (почти 100%)
- ✅ Быстрая доставка
- ✅ Клиенты читают SMS сразу
- ✅ Не требует установки приложений

**Недостатки:**
- ❌ Дорого (от 1-3 ₽ за SMS в России)
- ❌ Ограничение по длине (160 символов)
- ❌ Нет форматирования (только текст)
- ❌ Нужна интеграция с SMS-провайдером

**Варианты SMS-провайдеров:**
1. **SMS.ru** - популярный, простой API, ~2-3 ₽/SMS
2. **SMSC.ru** - надежный, ~1.5-2.5 ₽/SMS
3. **Twilio** - международный, дороже (~5-10 ₽/SMS)
4. **SMS Aero** - российский, ~1.5-2 ₽/SMS

**Реализация:**
- Создать `SmsService` (аналог `EmailService`)
- Интегрировать с выбранным провайдером
- Добавить отправку SMS в `notification-service.js` для клиентов без `telegram_id` и без email (если такое возможно)

**Стоимость:** ~2 ₽ за SMS × количество клиентов × количество уведомлений

**Приоритет:** СРЕДНИЙ ⭐⭐ (из-за стоимости)

**Рекомендация:** Использовать только для критических уведомлений (отмена тренировки) или как fallback, если email не доставлен.

---

#### 3. WHATSAPP УВЕДОМЛЕНИЯ

**Преимущества:**
- ✅ Бесплатно (через WhatsApp Business API)
- ✅ Высокая доставляемость
- ✅ Клиенты активно используют WhatsApp
- ✅ Можно отправлять медиа (фото, документы)
- ✅ Можно использовать шаблоны сообщений

**Недостатки:**
- ❌ Сложная настройка (требует бизнес-аккаунт)
- ❌ Нужна верификация бизнеса
- ❌ Ограничения на отправку (24-часовое окно для ответа)
- ❌ Нужен номер телефона клиента (есть у всех)
- ❌ Клиент должен добавить номер в контакты (для некоторых провайдеров)

**Варианты интеграции:**
1. **WhatsApp Business API (официальный)** - сложная настройка, требует бизнес-верификацию
2. **Twilio WhatsApp API** - проще, но дороже (~$0.005 за сообщение)
3. **Green API** - российский провайдер, ~0.5-1 ₽ за сообщение
4. **ChatAPI** - альтернатива, ~$0.01 за сообщение

**Реализация:**
- Создать `WhatsAppService` (аналог `EmailService`)
- Интегрировать с выбранным провайдером
- Добавить отправку WhatsApp в `notification-service.js`

**Стоимость:** Зависит от провайдера (от бесплатно до ~1 ₽ за сообщение)

**Приоритет:** СРЕДНИЙ ⭐⭐ (из-за сложности настройки)

**Рекомендация:** Рассмотреть как альтернативу SMS, но только после настройки email уведомлений.

---

### 🎯 РЕКОМЕНДУЕМАЯ СТРАТЕГИЯ

#### Приоритет 1: EMAIL УВЕДОМЛЕНИЯ (НАЧАТЬ СРАЗУ) ⭐⭐⭐

**Почему:**
- Уже есть инфраструктура
- Бесплатно
- У всех клиентов есть email
- Быстрая реализация

**Что сделать:**
1. Создать HTML шаблоны для email уведомлений:
   - Подтверждение бронирования
   - Напоминание о тренировке (за день до)
   - Уведомление об отмене
2. Расширить `notification-service.js`:
   - Добавить отправку email для клиентов без `telegram_id`
   - Использовать email как fallback, если `telegram_id` отсутствует
3. Добавить отправку email при создании бронирования (в `kuliga-payment.js`)
4. Добавить отправку email при отмене бронирования

**Критерий успеха:**
- Все клиенты получают уведомления о тренировках (Telegram или Email)
- Красивые, читаемые email шаблоны
- Уведомления доставляются вовремя

---

#### Приоритет 2: МОТИВАЦИЯ К РЕГИСТРАЦИИ В TELEGRAM БОТЕ ⭐⭐

**Почему:**
- Telegram уведомления - самый удобный способ
- Бесплатно
- Мгновенная доставка
- Интерактивность (кнопки, меню)

**Что сделать:**
1. В email уведомлениях добавлять призыв к действию:
   - "Подключите Telegram бота для мгновенных уведомлений!"
   - Ссылка на бота с deep link `client_{CLIENT_ID}`
2. На странице успешной оплаты:
   - Показывать преимущества Telegram бота
   - Предлагать подключить бота сразу
3. В напоминаниях по email:
   - Добавлять ссылку на бота
   - Объяснять преимущества

**Критерий успеха:**
- Увеличивается процент клиентов с `telegram_id`
- Клиенты активно используют бота

---

#### Приоритет 3: SMS ДЛЯ КРИТИЧЕСКИХ УВЕДОМЛЕНИЙ ⭐

**Почему:**
- Дорого для массовых рассылок
- Но эффективно для критических случаев

**Что сделать:**
1. Использовать SMS только для:
   - Отмена тренировки (критично!)
   - Изменение времени/места (критично!)
2. НЕ использовать для:
   - Обычных напоминаний (использовать email)
   - Подтверждений бронирования (использовать email)
3. Интегрировать SMS-провайдер (например, SMS.ru)
4. Создать `SmsService`

**Критерий успеха:**
- Критические уведомления доставляются мгновенно
- Контроль расходов на SMS

---

#### Приоритет 4: WHATSAPP (ОПЦИОНАЛЬНО) ⭐

**Почему:**
- Сложная настройка
- Не все клиенты используют WhatsApp активно
- Email уже покрывает большинство случаев

**Что сделать:**
1. Изучить варианты интеграции
2. Протестировать на небольшой группе клиентов
3. Если эффективно - масштабировать

**Критерий успеха:**
- WhatsApp уведомления работают стабильно
- Клиенты положительно реагируют

---

### 📋 ПЛАН РЕАЛИЗАЦИИ

#### ЭТАП 1: Email уведомления (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 4.1: Создать HTML шаблоны для email**

**Файлы:**
- `src/services/email-templates/` (новая папка)
- `src/services/email-templates/booking-confirmation.html`
- `src/services/email-templates/training-reminder.html`
- `src/services/email-templates/booking-cancellation.html`

**Действия:**
1. Создать красивые HTML шаблоны (responsive, читаемые)
2. Использовать стили, аналогичные шаблонам сертификатов
3. Добавить переменные для подстановки данных

**Критерий успеха:** Шаблоны выглядят профессионально и читаемо.

---

**Задача 4.2: Расширить notification-service.js для email**

**Файлы:**
- `src/services/notification-service.js`

**Действия:**
1. Добавить метод `sendEmailNotification()` в `NotificationService`
2. Модифицировать `sendTrainingReminders()`:
   - Если у клиента есть `telegram_id` - отправлять в Telegram
   - Если нет `telegram_id`, но есть `email` - отправлять email
   - Логировать оба типа уведомлений
3. Использовать существующий `EmailService`

**Критерий успеха:** Клиенты без Telegram получают email напоминания.

---

**Задача 4.3: Email при создании бронирования**

**Файлы:**
- `src/routes/kuliga-payment.js` (webhook handler)

**Действия:**
1. После успешного создания бронирования отправлять email клиенту
2. Использовать шаблон `booking-confirmation.html`
3. Включать все детали тренировки

**Критерий успеха:** Клиенты получают email подтверждение после оплаты.

---

**Задача 4.4: Email при отмене бронирования**

**Файлы:**
- `src/routes/kuliga-payment.js` (webhook handler для статуса `FAILED` или `REFUNDED`)

**Действия:**
1. При отмене бронирования отправлять email клиенту
2. Использовать шаблон `booking-cancellation.html`
3. Объяснять причину отмены (если известна)

**Критерий успеха:** Клиенты получают email уведомление об отмене.

---

#### ЭТАП 2: Мотивация к регистрации в Telegram боте

**Задача 4.5: Добавить призыв к действию в email**

**Файлы:**
- `src/services/email-templates/*.html`

**Действия:**
1. В каждом email шаблоне добавлять блок:
   - "Подключите Telegram бота для мгновенных уведомлений!"
   - Кнопка/ссылка на бота с deep link
   - Объяснение преимуществ

**Критерий успеха:** В каждом email есть призыв подключить бота.

---

**Задача 4.6: Улучшить страницу успешной оплаты**

**Файлы:**
- `views/booking-success.ejs`

**Действия:**
1. Добавить секцию про Telegram бота
2. Показать преимущества (мгновенные уведомления, история тренировок)
3. Кнопка подключить бота с deep link

**Критерий успеха:** На странице успеха есть призыв подключить бота.

---

#### ЭТАП 3: SMS для критических уведомлений (ОПЦИОНАЛЬНО)

**Задача 4.7: Интегрировать SMS-провайдер**

**Файлы:**
- `src/services/smsService.js` (новый файл)

**Действия:**
1. Выбрать SMS-провайдер (рекомендуется SMS.ru)
2. Создать `SmsService` класс
3. Реализовать метод `sendSms(phone, message)`
4. Добавить настройки в `.env`

**Критерий успеха:** SMS отправляются через API провайдера.

---

**Задача 4.8: SMS при отмене тренировки**

**Файлы:**
- `src/routes/kuliga-payment.js`
- `src/services/notification-service.js`

**Действия:**
1. При отмене бронирования отправлять SMS клиенту
2. Использовать короткий текст (160 символов)
3. Логировать отправку

**Критерий успеха:** Клиенты получают SMS при отмене.

---

### 💰 ОЦЕНКА СТОИМОСТИ

#### Email уведомления:
- **Стоимость:** Бесплатно (SMTP Yandex)
- **Ограничения:** Нет (можно отправлять сколько угодно)

#### SMS уведомления:
- **Стоимость:** ~2 ₽ за SMS
- **Пример:** 100 клиентов × 2 SMS (напоминание + отмена) = 400 ₽
- **Рекомендация:** Использовать только для критических случаев

#### WhatsApp уведомления:
- **Стоимость:** Зависит от провайдера (от бесплатно до ~1 ₽)
- **Рекомендация:** Изучить после реализации email

---

### ✅ КРИТЕРИИ УСПЕХА ВСЕГО ПРОЕКТА

1. ✅ Все клиенты получают уведомления о тренировках (Telegram или Email)
2. ✅ Все клиенты получают уведомления об отменах (Telegram, Email, или SMS)
3. ✅ Email шаблоны красивые и читаемые
4. ✅ Увеличивается процент клиентов с `telegram_id` (благодаря мотивации)
5. ✅ Критические уведомления доставляются мгновенно (SMS)
6. ✅ Система логирует все отправленные уведомления

---

### 📝 ЗАМЕТКИ

- **Важно:** При реализации email уведомлений нужно учитывать, что некоторые клиенты могут не иметь валидного email (хотя это обязательное поле)
- **Важно:** При реализации SMS нужно контролировать расходы
- **Важно:** WhatsApp требует бизнес-верификацию, это может занять время
- **Рекомендация:** Начать с email, затем добавить SMS для критических случаев, затем рассмотреть WhatsApp

---

## ✅ УТОЧНЕНИЯ ОТ ПОЛЬЗОВАТЕЛЯ

1. **Deep Link формат:** `client_{CLIENT_ID}` - простой формат, понятный пользователю
   - Объяснение: Deep link - это специальная ссылка вида `https://t.me/Ski_Instruktor72_bot?start=client_123`
   - При переходе по этой ссылке бот получает параметр `client_123` и может идентифицировать клиента

2. **Предварительная регистрация:** 
   - При клике на ссылку бота вызывать `/api/kuliga/pre-register-client`
   - Если клиент не кликнул на ссылку - вызывать при отправке формы
   - Система всегда проверяет, создан клиент ранее или нет

3. **Количество участников:** Уже реализовано в контейнерах (прайсах) - вопрос закрыт

4. **Шаблоны email:** Сделать удобными и комфортными для чтения - красивые HTML шаблоны

5. **Напоминания:** Использовать существующую cron задачу (21:00), которая уже работает для клиентов из бота

---

**Дата обновления:** 15.12.2025

---

## 🎯 ЗАДАЧА 5: РЕАЛИЗАЦИЯ ФУНКЦИИ "СОЗДАТЬ ИНСТРУКТОРА ГОРНОСТАЙЛ72"

**Дата добавления:** 18.12.2025  
**Режим:** Планировщик  
**Приоритет:** ВЫСОКИЙ

### 📋 ПРОБЛЕМА

Пользователь пытался создать инструктора Горностайл72 для зимних тренировок через страницу администратора, но функция не работает. При нажатии на кнопку "Создать инструктора Горностайл72" появляется только alert с сообщением "Функция создания инструктора Кулиги будет реализована".

### 🔍 АНАЛИЗ ТЕКУЩЕЙ СИТУАЦИИ

#### Структура базы данных:

1. **Таблица `kuliga_instructors`:**
   - Хранит всех инструкторов (и для Кулиги, и для Воронинских горок)
   - Поле `location` может быть:
     - `'kuliga'` - База отдыха «Кулига-Клуб» (тренажер)
     - `'vorona'` - Воронинские горки (зимние тренировки)
   - Инструкторы Горностайл72 для зимних тренировок - это инструкторы с `location = 'vorona'`

2. **Текущая реализация:**
   - ✅ Модальное окно для создания/редактирования инструктора уже существует (`kuliga-instructor-modal` в `admin.html`)
   - ✅ Функция `openKuligaInstructorModal()` в `admin-kuliga.js` работает корректно
   - ✅ API endpoint `/api/kuliga/admin/instructors` (POST) работает и поддерживает поле `location`
   - ✅ Форма в модальном окне имеет поле выбора `location` с опциями 'kuliga' и 'vorona'
   - ❌ Функция `showCreateKuligaInstructorModal()` в `admin.js` только показывает alert, не открывает модальное окно

3. **Проблемный код:**
   ```javascript
   // public/js/admin.js, строка 2099
   function showCreateKuligaInstructorModal() {
       // TODO: Создать модальное окно аналогично create-trainer.html
       // Пока используем существующий модал из admin-kuliga.js или создаём новый
       alert('Функция создания инструктора Кулиги будет реализована');
   }
   ```

4. **Кнопка в интерфейсе:**
   - Кнопка "Создать инструктора Горностайл72" находится на странице "Тренера" (`trainers-page`)
   - ID кнопки: `create-kuliga-instructor`
   - Обработчик в `admin.js` вызывает `showCreateKuligaInstructorModal()`

### 🎯 ТРЕБОВАНИЯ

1. При нажатии на кнопку "Создать инструктора Горностайл72" должно открываться модальное окно для создания инструктора
2. В модальном окне по умолчанию должно быть выбрано `location = 'vorona'` (Воронинские горки)
3. После создания инструктор должен появиться в списке инструкторов Горностайл72
4. Функция должна работать аналогично созданию инструктора Кулиги, но с предустановленным `location = 'vorona'`

### 🔧 РЕШЕНИЕ

#### Вариант 1: Использовать существующую функцию с параметром (РЕКОМЕНДУЕТСЯ) ⭐

**Идея:** Модифицировать функцию `openKuligaInstructorModal()` чтобы она принимала опциональный параметр `defaultLocation`, и вызывать её из `showCreateKuligaInstructorModal()` с `location = 'vorona'`.

**Преимущества:**
- Минимальные изменения в коде
- Переиспользование существующей логики
- Единообразный интерфейс

**Недостатки:**
- Нужно убедиться, что функция `openKuligaInstructorModal()` доступна глобально

#### Вариант 2: Создать отдельную функцию для инструкторов Горностайл72

**Идея:** Создать новую функцию `showCreateGornostyleInstructorModal()`, которая будет открывать модальное окно с предустановленным `location = 'vorona'`.

**Преимущества:**
- Четкое разделение логики
- Легко понять назначение функции

**Недостатки:**
- Дублирование кода
- Больше функций для поддержки

### 📝 ПЛАН РЕАЛИЗАЦИИ

#### Задача 5.1: Исправить функцию `showCreateKuligaInstructorModal()`

**Файлы:**
- `public/js/admin.js`

**Действия:**
1. Заменить alert на вызов функции `openKuligaInstructorModal()` (если она доступна глобально)
2. Или создать логику открытия модального окна с установкой `location = 'vorona'`
3. Убедиться, что модальное окно открывается корректно
4. Установить значение `location` в 'vorona' по умолчанию

**Критерий успеха:** При нажатии на кнопку "Создать инструктора Горностайл72" открывается модальное окно с предустановленным `location = 'vorona'`.

---

#### Задача 5.2: Убедиться, что `openKuligaInstructorModal()` доступна глобально

**Файлы:**
- `public/js/admin-kuliga.js`

**Действия:**
1. Проверить, что функция `openKuligaInstructorModal()` экспортирована глобально (через `window.openKuligaInstructorModal`)
2. Если нет - добавить экспорт
3. Убедиться, что функция может принимать параметр для установки `location` по умолчанию

**Критерий успеха:** Функция `openKuligaInstructorModal()` доступна из `admin.js`.

---

#### Задача 5.3: Модифицировать `openKuligaInstructorModal()` для поддержки `defaultLocation`

**Файлы:**
- `public/js/admin-kuliga.js`

**Действия:**
1. Добавить параметр `defaultLocation` в функцию `openKuligaInstructorModal(instructorId = null, defaultLocation = 'kuliga')`
2. При создании нового инструктора (когда `instructorId === null`) устанавливать `location` в значение `defaultLocation`
3. Обновить вызовы функции, если необходимо

**Критерий успеха:** Функция `openKuligaInstructorModal()` может принимать `defaultLocation` и устанавливать его при создании нового инструктора.

---

#### Задача 5.4: Обновить обработчик кнопки в `admin.js`

**Файлы:**
- `public/js/admin.js`

**Действия:**
1. Заменить содержимое функции `showCreateKuligaInstructorModal()` на вызов `openKuligaInstructorModal(null, 'vorona')`
2. Добавить проверку существования функции перед вызовом
3. Добавить обработку ошибок

**Критерий успеха:** При нажатии на кнопку "Создать инструктора Горностайл72" открывается модальное окно с `location = 'vorona'`.

---

#### Задача 5.5: Проверить загрузку скриптов

**Файлы:**
- `public/admin.html`

**Действия:**
1. Убедиться, что `admin-kuliga.js` загружается до `admin.js` или что функции доступны
2. Проверить порядок подключения скриптов в HTML
3. При необходимости добавить проверку готовности DOM

**Критерий успеха:** Все скрипты загружаются в правильном порядке, функции доступны.

---

#### Задача 5.6: Тестирование

**Действия:**
1. Открыть страницу администратора
2. Перейти на страницу "Тренера"
3. Нажать на кнопку "Создать инструктора Горностайл72"
4. Проверить, что модальное окно открывается
5. Проверить, что в поле "Место работы" выбрано "Воронинские горки"
6. Заполнить форму и создать инструктора
7. Проверить, что инструктор создан с `location = 'vorona'`
8. Проверить, что инструктор отображается в списке "Инструкторы Горностайл72"

**Критерий успеха:** Функция создания инструктора Горностайл72 работает полностью, инструктор создается с правильным `location`.

---

### 📊 ТЕКУЩИЙ СТАТУС

**Статус:** ✅ РЕАЛИЗОВАНО

**Дата реализации:** 18.12.2025

### ✅ ВЫПОЛНЕНО:

1. **✅ Задача 5.1: Модифицировать openKuligaInstructorModal() для поддержки defaultLocation** - РЕАЛИЗОВАНО
   - Добавлен параметр `defaultLocation = 'kuliga'` в функцию `openKuligaInstructorModal()`
   - При создании нового инструктора устанавливается `location` из параметра `defaultLocation`
   - Файл: `public/js/admin-kuliga.js`

2. **✅ Задача 5.2: Исправить функцию showCreateKuligaInstructorModal()** - РЕАЛИЗОВАНО
   - Заменен alert на вызов `window.openKuligaInstructorModal(null, 'vorona')`
   - Добавлена проверка существования функции перед вызовом
   - Добавлена обработка ошибок
   - Файл: `public/js/admin.js`

3. **✅ Задача 5.3: Проверить порядок загрузки скриптов** - РЕАЛИЗОВАНО
   - Изменен порядок загрузки скриптов: `admin-kuliga.js` теперь загружается перед `admin.js`
   - Это гарантирует доступность функции `openKuligaInstructorModal()` при инициализации обработчиков
   - Файл: `public/admin.html`

### 🎯 ПРИОРИТЕТЫ

1. **ВЫСОКИЙ ПРИОРИТЕТ:**
   - Задача 5.1: Исправить функцию `showCreateKuligaInstructorModal()`
   - Задача 5.4: Обновить обработчик кнопки в `admin.js`

2. **СРЕДНИЙ ПРИОРИТЕТ:**
   - Задача 5.2: Убедиться, что `openKuligaInstructorModal()` доступна глобально
   - Задача 5.3: Модифицировать `openKuligaInstructorModal()` для поддержки `defaultLocation`

3. **НИЗКИЙ ПРИОРИТЕТ:**
   - Задача 5.5: Проверить загрузку скриптов
   - Задача 5.6: Тестирование

---

### 📝 ЗАМЕТКИ

- **Важно:** Инструкторы Горностайл72 хранятся в той же таблице `kuliga_instructors`, что и инструкторы Кулиги
- **Важно:** Поддерживаются 2 локации: `'kuliga'` (База отдыха «Кулига-Клуб») и `'vorona'` (Воронинские горки)
- **Важно:** При создании инструктора через кнопку "Создать инструктора Горностайл72" по умолчанию устанавливается `location = 'vorona'`, но пользователь может изменить на `'kuliga'`
- **Реализовано:** Функция `openKuligaInstructorModal()` теперь принимает параметр `defaultLocation` для предустановки локации при создании нового инструктора
- **Реализовано:** Порядок загрузки скриптов изменен: `admin-kuliga.js` загружается перед `admin.js` для гарантии доступности функций

---

**Дата обновления:** 18.12.2025

---

## 🎯 ЗАДАЧА 6: SEO-ОПТИМИЗАЦИЯ СТРАНИЦЫ ИНСТРУКТОРОВ И НАСТРОЙКА ОТДЕЛЬНОЙ МЕТРИКИ

**Дата добавления:** 19.12.2025  
**Режим:** Планировщик  
**Приоритет:** ВЫСОКИЙ

### 📋 ПРОБЛЕМА

Страница `/instruktor-po-gornym-lyzham-snoubordy-tyumen` требует продвижения в Яндекс и Google. Необходимо:
1. Подключить отдельную метрику для отслеживания статистики именно этой страницы
2. Провести SEO-оптимизацию для попадания в топ по ключевым запросам
3. Изучить конкурентов и улучшить контент

### 🎯 ЦЕЛЕВЫЕ КЛЮЧЕВЫЕ ЗАПРОСЫ

#### 📍 ОБЩИЕ ЗАПРОСЫ (БЕЗ УКАЗАНИЯ МЕСТА)

**Горные лыжи:**
- "обучение горным лыжам Тюмень"
- "инструктор по горным лыжам Тюмень"
- "инструктор горные лыжи Тюмень"
- "инструкторы горные лыжи Тюмень"
- "обучение лыжам Тюмень"
- "обучение горным лыжам Тюмень с инструктором"
- "горнолыжная школа Тюмень"
- "школа горных лыж Тюмень"
- "инструктор горные лыжи Тюмень цена"
- "обучение горным лыжам Тюмень стоимость"

**Сноуборд:**
- "обучение сноуборду Тюмень"
- "инструктор по сноуборду Тюмень"
- "инструктор сноуборд Тюмень"
- "инструкторы сноуборд Тюмень"
- "обучение сноуборду Тюмень с инструктором"
- "школа сноуборда Тюмень"
- "инструктор сноуборд Тюмень цена"
- "обучение сноуборду Тюмень стоимость"

**Оба вида спорта:**
- "обучение горным лыжам и сноуборду Тюмень"
- "инструктор горные лыжи сноуборд Тюмень"
- "школа горных лыж и сноуборда Тюмень"

---

#### 🏔️ КУЛИГА / КУЛИГА-КЛУБ

**Горные лыжи:**
- "обучение горным лыжам Кулига"
- "обучение горным лыжам Кулига-Клуб"
- "инструктор горные лыжи Кулига"
- "инструктор горные лыжи Кулига-Клуб"
- "инструктор по горным лыжам Кулига"
- "инструктор по горным лыжам Кулига-Клуб"
- "инструкторы горные лыжи Кулига"
- "обучение лыжам Кулига"
- "обучение лыжам Кулига-Клуб"
- "горнолыжная школа Кулига"
- "обучение горным лыжам Кулига Тюмень"
- "инструктор горные лыжи Кулига цена"

**Сноуборд:**
- "обучение сноуборду Кулига"
- "обучение сноуборду Кулига-Клуб"
- "инструктор сноуборд Кулига"
- "инструктор сноуборд Кулига-Клуб"
- "инструктор по сноуборду Кулига"
- "инструктор по сноуборду Кулига-Клуб"
- "инструкторы сноуборд Кулига"
- "школа сноуборда Кулига"
- "обучение сноуборду Кулига Тюмень"
- "инструктор сноуборд Кулига цена"

**Оба вида спорта:**
- "обучение горным лыжам и сноуборду Кулига"
- "инструктор Кулига горные лыжи сноуборд"
- "школа Кулига горные лыжи сноуборд"

---

#### ⛰️ ВОРОНИНСКИЕ ГОРКИ / ВОРОНА

**Горные лыжи:**
- "обучение горным лыжам Воронинские горки"
- "обучение горным лыжам Ворона"
- "инструктор горные лыжи Воронинские горки"
- "инструктор горные лыжи Ворона"
- "инструктор по горным лыжам Воронинские горки"
- "инструктор по горным лыжам Ворона"
- "инструкторы горные лыжи Воронинские горки"
- "инструкторы горные лыжи Ворона"
- "обучение лыжам Воронинские горки"
- "обучение лыжам Ворона"
- "горнолыжная школа Воронинские горки"
- "горнолыжная школа Ворона"
- "обучение горным лыжам Воронинские горки Тюмень"
- "обучение горным лыжам Ворона Тюмень"
- "инструктор горные лыжи Воронинские горки цена"
- "инструктор горные лыжи Ворона цена"

**Сноуборд:**
- "обучение сноуборду Воронинские горки"
- "обучение сноуборду Ворона"
- "инструктор сноуборд Воронинские горки"
- "инструктор сноуборд Ворона"
- "инструктор по сноуборду Воронинские горки"
- "инструктор по сноуборду Ворона"
- "инструкторы сноуборд Воронинские горки"
- "инструкторы сноуборд Ворона"
- "школа сноуборда Воронинские горки"
- "школа сноуборда Ворона"
- "обучение сноуборду Воронинские горки Тюмень"
- "обучение сноуборду Ворона Тюмень"
- "инструктор сноуборд Воронинские горки цена"
- "инструктор сноуборд Ворона цена"

**Оба вида спорта:**
- "обучение горным лыжам и сноуборду Воронинские горки"
- "обучение горным лыжам и сноуборду Ворона"
- "инструктор Воронинские горки горные лыжи сноуборд"
- "инструктор Ворона горные лыжи сноуборд"

---

#### 🔍 ДОПОЛНИТЕЛЬНЫЕ ЗАПРОСЫ

**С упоминанием формата обучения:**
- "индивидуальное обучение горным лыжам Тюмень"
- "групповое обучение горным лыжам Тюмень"
- "индивидуальное обучение сноуборду Тюмень"
- "групповое обучение сноуборду Тюмень"
- "обучение горным лыжам с нуля Тюмень"
- "обучение сноуборду с нуля Тюмень"
- "детское обучение горным лыжам Тюмень"
- "детское обучение сноуборду Тюмень"

**С упоминанием сезона/зимы:**
- "обучение горным лыжам Тюмень зима"
- "обучение сноуборду Тюмень зима"
- "зимнее обучение горным лыжам Тюмень"
- "зимнее обучение сноуборду Тюмень"

**С упоминанием возраста:**
- "обучение горным лыжам для детей Тюмень"
- "обучение сноуборду для детей Тюмень"
- "обучение горным лыжам для взрослых Тюмень"
- "обучение сноуборду для взрослых Тюмень"

**Итого: ~80+ ключевых запросов для оптимизации**

### 🔍 АНАЛИЗ ТЕКУЩЕЙ СИТУАЦИИ

#### 1. Текущая SEO-оптимизация страницы

**Что уже есть:**
- ✅ Базовые мета-теги (title, description, keywords)
- ✅ Open Graph теги
- ✅ Canonical URL
- ✅ Robots meta tag (index, follow)
- ✅ Структурированные заголовки (h1, h2, h3)
- ✅ Alt-тексты для изображений
- ✅ Мобильная адаптивность

**Что отсутствует:**
- ❌ Структурированные данные Schema.org (JSON-LD)
- ❌ Богатые сниппеты (Rich Snippets)
- ❌ Локальная разметка (LocalBusiness)
- ❌ Разметка для услуг (Service)
- ❌ Breadcrumbs (хлебные крошки)
- ❌ FAQ секция с разметкой
- ❌ Отзывы с разметкой AggregateRating
- ❌ Недостаточно ключевых слов в контенте
- ❌ Нет внутренней перелинковки с ключевыми словами

#### 2. Анализ конкурентов

**Конкуренты в Тюмени:**
1. **Lemon School** - Школа сноуборда и горных лыж
   - Профессиональное обучение
   - Индивидуальные и групповые занятия
   - Контакт: +7 (961) 204-78-56

2. **XFREEDOM** - Школа сноуборда и горных лыж
   - Обучение на спорткомплексе "Трамплин" (пос. Нагорный)
   - Курсы различной продолжительности
   - Контакт: +7 (953) 999-10-57

**Что делают конкуренты:**
- Используют локальные запросы с упоминанием Тюмени
- Указывают конкретные места проведения занятий
- Предлагают различные форматы обучения
- Имеют контактную информацию на видном месте

#### 3. Текущая метрика

**Ситуация:**
- На домене `gornostyle72.ru` уже установлена Яндекс.Метрика
- Нужна отдельная цель/фильтр для отслеживания именно страницы инструкторов
- Google Analytics также нужно настроить для отдельного отслеживания

### 📊 ПЛАН SEO-ОПТИМИЗАЦИИ

#### ЭТАП 1: Техническая SEO-оптимизация

**Задача 6.1: Добавить структурированные данные Schema.org**

**Типы разметки для добавления:**
1. **LocalBusiness** - для указания местоположения и контактов
2. **Service** - для описания услуг (обучение горным лыжам, сноуборду)
3. **Organization** - для информации о компании
4. **BreadcrumbList** - для навигации
5. **FAQPage** - для частых вопросов (если добавим секцию)
6. **AggregateRating** - для отзывов (если есть)

**Файлы:**
- `views/kuliga-landing.ejs`

**Критерий успеха:** Структурированные данные валидны и отображаются в Google Rich Results Test.

---

**Задача 6.2: Улучшить мета-теги и заголовки**

**Действия:**
1. Оптимизировать title с включением ключевых слов
2. Расширить description с упоминанием всех ключевых запросов
3. Добавить больше ключевых слов в keywords (но не переспам)
4. Улучшить h1, h2, h3 с естественным вхождением ключевых слов

**Файлы:**
- `views/kuliga-landing.ejs`

**Критерий успеха:** Title и description содержат основные ключевые слова, длина description 150-160 символов.

---

**Задача 6.3: Добавить Breadcrumbs (хлебные крошки)**

**Действия:**
1. Добавить навигационные хлебные крошки
2. Добавить структурированные данные BreadcrumbList
3. Улучшить навигацию по сайту

**Файлы:**
- `views/kuliga-landing.ejs`

**Критерий успеха:** Хлебные крошки отображаются на странице и имеют разметку Schema.org.

---

#### ЭТАП 2: Контентная оптимизация

**Задача 6.4: Расширить контент с ключевыми словами**

**Действия:**
1. Добавить текстовые блоки с естественным вхождением ключевых слов
2. Создать секцию "Почему выбирают нас" с ключевыми словами
3. Добавить секцию с описанием мест проведения (Кулига, Воронинские горки)
4. Улучшить описания услуг с упоминанием ключевых запросов

**Файлы:**
- `views/kuliga-landing.ejs`

**Критерий успеха:** Контент содержит все целевые ключевые слова в естественной форме.

---

**Задача 6.5: Добавить FAQ секцию**

**Действия:**
1. Создать секцию с часто задаваемыми вопросами
2. Добавить структурированные данные FAQPage
3. Включить ключевые слова в вопросы и ответы

**Примеры вопросов:**
- "Где проходят занятия по горным лыжам в Тюмени?"
- "Как записаться на обучение сноуборду?"
- "Сколько стоит обучение горным лыжам в Кулиге?"
- "Работают ли инструкторы на Воронинских горках?"

**Файлы:**
- `views/kuliga-landing.ejs`

**Критерий успеха:** FAQ секция добавлена с разметкой Schema.org.

---

**Задача 6.6: Улучшить внутреннюю перелинковку**

**Действия:**
1. Добавить ссылки на другие страницы сайта с ключевыми словами в анкорах
2. Создать контекстные ссылки в тексте
3. Улучшить навигацию по сайту

**Файлы:**
- `views/kuliga-landing.ejs`

**Критерий успеха:** Страница имеет 3-5 внутренних ссылок с релевантными анкорами.

---

#### ЭТАП 3: Настройка отдельной метрики

**Задача 6.7: Настроить отдельную цель в Яндекс.Метрике**

**Действия:**
1. Создать отдельную цель в существующем счётчике Яндекс.Метрики
2. Настроить отслеживание посещений страницы `/instruktor-po-gornym-lyzham-snoubordy-tyumen`
3. Настроить отслеживание конверсий (клики по кнопкам, переходы в бот)
4. Добавить виртуальные цели для важных действий

**Рекомендуемые цели:**
- Посещение страницы инструкторов
- Клик по кнопке "Записаться на тренировку"
- Переход в Telegram-бот
- Просмотр списка инструкторов
- Просмотр цен

**Критерий успеха:** В Яндекс.Метрике созданы цели для отслеживания страницы инструкторов.

---

**Задача 6.8: Настроить фильтр/сегмент в Google Analytics**

**Действия:**
1. Создать сегмент для страницы инструкторов
2. Настроить события для отслеживания важных действий
3. Настроить цели для конверсий

**Рекомендуемые события:**
- `page_view` - просмотр страницы
- `button_click` - клик по кнопке записи
- `instructor_view` - просмотр инструктора
- `telegram_click` - переход в Telegram-бот

**Критерий успеха:** В Google Analytics настроены сегменты и события для страницы инструкторов.

---

**Задача 6.9: Добавить расширенное отслеживание событий на странице**

**Действия:**
1. Добавить отправку событий в Яндекс.Метрику при важных действиях
2. Добавить отправку событий в Google Analytics
3. Отслеживать клики по кнопкам, ссылкам, просмотры секций

**Файлы:**
- `public/js/kuliga.js`
- `views/kuliga-landing.ejs`

**Критерий успеха:** Все важные действия на странице отслеживаются в метриках.

---

#### ЭТАП 4: Дополнительные улучшения

**Задача 6.10: Оптимизировать изображения для SEO**

**Действия:**
1. Улучшить alt-тексты изображений с ключевыми словами
2. Добавить title атрибуты для изображений
3. Оптимизировать имена файлов изображений

**Файлы:**
- `views/kuliga-landing.ejs`

**Критерий успеха:** Все изображения имеют оптимизированные alt-тексты с ключевыми словами.

---

**Задача 6.11: Добавить локальную разметку**

**Действия:**
1. Добавить разметку LocalBusiness с адресами мест проведения
2. Указать координаты для Кулиги и Воронинских горок
3. Добавить информацию о часах работы

**Файлы:**
- `views/kuliga-landing.ejs`

**Критерий успеха:** Локальная разметка добавлена и валидна.

---

**Задача 6.12: Создать XML-карту сайта для страницы**

**Действия:**
1. Убедиться, что страница добавлена в sitemap.xml
2. Указать приоритет страницы
3. Указать частоту обновления

**Файлы:**
- `public/sitemap.xml`

**Критерий успеха:** Страница инструкторов добавлена в sitemap.xml с правильными параметрами.

---

### 📋 ПОДРОБНЫЙ ПЛАН РЕАЛИЗАЦИИ

#### ЭТАП 1: Техническая SEO-оптимизация (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 6.1: Добавить структурированные данные Schema.org**

**Файлы:**
- `views/kuliga-landing.ejs`

**Действия:**
1. Добавить JSON-LD разметку для LocalBusiness
2. Добавить разметку для Service (обучение горным лыжам, сноуборду)
3. Добавить разметку Organization
4. Добавить BreadcrumbList
5. Добавить AggregateRating (если есть отзывы)

**Пример структуры:**
```json
{
  "@context": "https://schema.org",
  "@type": "LocalBusiness",
  "name": "Горностайл72 - Обучение горным лыжам и сноуборду",
  "description": "Профессиональное обучение горным лыжам и сноуборду в Тюмени. Инструкторы на базе отдыха Кулига-Клуб и Воронинских горках.",
  "url": "https://gornostyle72.ru/instruktor-po-gornym-lyzham-snoubordy-tyumen",
  "telephone": "+79123924956",
  "email": "gornostyle72@yandex.ru",
  "address": [
    {
      "@type": "PostalAddress",
      "addressLocality": "Тюмень",
      "addressRegion": "Тюменская область",
      "addressCountry": "RU",
      "streetAddress": "База отдыха «Кулига-Клуб»"
    },
    {
      "@type": "PostalAddress",
      "addressLocality": "Тюмень",
      "addressRegion": "Тюменская область",
      "addressCountry": "RU",
      "streetAddress": "Воронинские горки"
    }
  ],
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": 57.1522,
    "longitude": 65.5272
  },
  "openingHours": "Mo-Su 10:00-21:00",
  "priceRange": "₽₽",
  "currenciesAccepted": "RUB",
  "paymentAccepted": "Cash, Credit Card, Online Payment",
  "areaServed": {
    "@type": "City",
    "name": "Тюмень"
  },
  "hasOfferCatalog": {
    "@type": "OfferCatalog",
    "name": "Услуги обучения",
    "itemListElement": [
      {
        "@type": "Offer",
        "itemOffered": {
          "@type": "Service",
          "name": "Обучение горным лыжам",
          "description": "Индивидуальные и групповые занятия по горным лыжам"
        }
      },
      {
        "@type": "Offer",
        "itemOffered": {
          "@type": "Service",
          "name": "Обучение сноуборду",
          "description": "Индивидуальные и групповые занятия по сноуборду"
        }
      }
    ]
  }
}
```

**Критерий успеха:** Структурированные данные валидны в Google Rich Results Test и Яндекс.Вебмастер.

---

**Задача 6.2: Улучшить мета-теги**

**Файлы:**
- `views/kuliga-landing.ejs`

**Текущий title:**
```
Служба инструкторов Горностайл72 | Обучение горным лыжам и сноуборду
```

**Предлагаемый title (до 60 символов):**
```
Инструктор по горным лыжам и сноуборду Тюмень | Обучение Кулига
```

**Текущий description:**
```
Запишитесь на обучение горным лыжам и сноуборду на базе отдыха Кулига или на Воронинских горках. Профессиональные инструкторы Горностайл72, групповые и индивидуальные занятия, удобное онлайн-бронирование и предоплата через Tinkoff.
```

**Предлагаемый description (150-160 символов):**
```
Обучение горным лыжам и сноуборду в Тюмени. Инструкторы на Кулиге и Воронинских горках. Индивидуальные и групповые занятия. Онлайн-бронирование и предоплата.
```

**Ключевые слова (обновить):**
```
обучение горным лыжам Тюмень, обучение сноуборду Тюмень, инструктор сноуборд Тюмень, инструктор сноуборд Кулига, инструкторы горные лыжи Воронинские горки, инструктор по горным лыжам Тюмень, обучение горным лыжам Кулига, обучение сноуборду Воронинские горки, горнолыжная школа Тюмень, инструктор по сноуборду Тюмень
```

**Критерий успеха:** Title и description оптимизированы, содержат ключевые слова, соответствуют требованиям длины.

---

**Задача 6.3: Добавить Breadcrumbs**

**Файлы:**
- `views/kuliga-landing.ejs`

**Структура:**
```
Главная > Инструкторы > Обучение горным лыжам и сноуборду
```

**JSON-LD разметка:**
```json
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {
      "@type": "ListItem",
      "position": 1,
      "name": "Главная",
      "item": "https://gornostyle72.ru"
    },
    {
      "@type": "ListItem",
      "position": 2,
      "name": "Инструкторы",
      "item": "https://gornostyle72.ru/instruktor-po-gornym-lyzham-snoubordy-tyumen"
    }
  ]
}
```

**Критерий успеха:** Хлебные крошки отображаются на странице и имеют валидную разметку.

---

#### ЭТАП 2: Контентная оптимизация (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 6.4: Расширить контент**

**Файлы:**
- `views/kuliga-landing.ejs`

**Добавить секции:**
1. **Секция "Почему выбирают нас"** - с ключевыми словами
2. **Секция "Места проведения занятий"** - описание Кулиги и Воронинских горок
3. **Секция "Форматы обучения"** - индивидуальные и групповые занятия
4. **Улучшить существующие секции** - добавить ключевые слова естественным образом

**Пример текста для секции "Почему выбирают нас":**
```
Профессиональные инструкторы по горным лыжам и сноуборду в Тюмени
Мы предлагаем обучение горным лыжам и сноуборду на двух локациях: база отдыха «Кулига-Клуб» и Воронинские горки. Наши инструкторы имеют сертификацию и многолетний опыт работы. Мы обучаем как новичков, так и продвинутых спортсменов, готовим к поездкам в горы.
```

**Критерий успеха:** Контент расширен, содержит все целевые ключевые слова в естественной форме.

---

**Задача 6.5: Добавить FAQ секцию**

**Файлы:**
- `views/kuliga-landing.ejs`

**Вопросы и ответы:**
1. **Где проходят занятия по горным лыжам в Тюмени?**
   - Занятия проходят на базе отдыха «Кулига-Клуб» и на Воронинских горках.

2. **Как записаться на обучение сноуборду?**
   - Вы можете записаться через наш сайт, заполнив форму бронирования и внеся предоплату онлайн.

3. **Сколько стоит обучение горным лыжам в Кулиге?**
   - Стоимость зависит от формата занятий (индивидуальные или групповые) и количества участников. Актуальные цены указаны на странице.

4. **Работают ли инструкторы на Воронинских горках?**
   - Да, наши инструкторы работают как на базе отдыха «Кулига-Клуб», так и на Воронинских горках.

5. **Можно ли обучиться с нуля?**
   - Да, мы обучаем как новичков, так и продвинутых спортсменов. Наши инструкторы помогут вам освоить горные лыжи или сноуборд с нуля.

**JSON-LD разметка FAQPage:**
```json
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "Где проходят занятия по горным лыжам в Тюмени?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Занятия проходят на базе отдыха «Кулига-Клуб» и на Воронинских горках."
      }
    }
    // ... остальные вопросы
  ]
}
```

**Критерий успеха:** FAQ секция добавлена с валидной разметкой Schema.org.

---

#### ЭТАП 3: Настройка метрики (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 6.7: Настроить отдельную цель в Яндекс.Метрике**

**Инструкция:**
1. Войти в Яндекс.Метрику
2. Выбрать счётчик для gornostyle72.ru
3. Перейти в "Настройка" → "Цели"
4. Создать новую цель:
   - Тип: "Посещение страницы"
   - Условие: URL содержит `/instruktor-po-gornym-lyzham-snoubordy-tyumen`
   - Название: "Посещение страницы инструкторов"
5. Создать дополнительные цели:
   - Клик по кнопке "Записаться на тренировку"
   - Переход в Telegram-бот
   - Просмотр списка инструкторов

**Критерий успеха:** В Яндекс.Метрике созданы цели для отслеживания страницы инструкторов.

---

**Задача 6.8: Настроить фильтр/сегмент в Google Analytics**

**Инструкция:**
1. Войти в Google Analytics
2. Выбрать свойство для gornostyle72.ru
3. Создать сегмент:
   - Название: "Страница инструкторов"
   - Условие: URL содержит `/instruktor-po-gornym-lyzham-snoubordy-tyumen`
4. Настроить события:
   - `page_view` - просмотр страницы
   - `button_click` - клик по кнопке записи
   - `telegram_click` - переход в Telegram-бот

**Критерий успеха:** В Google Analytics настроены сегменты и события.

---

**Задача 6.9: Добавить расширенное отслеживание событий**

**Файлы:**
- `public/js/kuliga.js`

**Действия:**
1. Добавить отправку событий в Яндекс.Метрику:
   ```javascript
   if (window.ym && window.KULIGA_ANALYTICS.yandexMetrikaId) {
       ym(window.KULIGA_ANALYTICS.yandexMetrikaId, 'reachGoal', 'instructor_page_view');
   }
   ```

2. Добавить отправку событий в Google Analytics:
   ```javascript
   if (window.gtag) {
       gtag('event', 'page_view', {
           'page_title': 'Страница инструкторов',
           'page_location': window.location.href
       });
   }
   ```

**Критерий успеха:** Все важные действия на странице отслеживаются в метриках.

---

#### ЭТАП 4: Внешние ссылки и размещение (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 6.13: Разместить ссылки на бесплатных площадках**

**Бесплатные площадки для размещения ссылок на услугу:**

#### 📱 СОЦИАЛЬНЫЕ СЕТИ

**1. ВКонтакте (ВК) - ВЫСОКИЙ ПРИОРИТЕТ ⭐⭐⭐**
- **Группа/сообщество:** Создать или использовать существующую группу
- **Что размещать:**
  - Посты с описанием услуг и ссылкой на страницу
  - Фото с тренировок с подписями и ссылками
  - Видео с занятий
  - Анонсы групповых программ
  - Отзывы клиентов
- **Ссылка в описании группы:** Добавить ссылку на страницу инструкторов
- **Ссылка в контактах:** Указать сайт в контактах группы
- **Пин-пост:** Закрепить пост со ссылкой на услугу
- **SEO-эффект:** Высокий, ВК индексируется поисковыми системами

**2. Telegram - ВЫСОКИЙ ПРИОРИТЕТ ⭐⭐⭐**
- **Канал/группа:** Создать канал или использовать существующий
- **Что размещать:**
  - Посты с описанием услуг
  - Ссылка в описании канала
  - Регулярные анонсы
- **SEO-эффект:** Средний, Telegram каналы индексируются

**3. Одноклассники (ОК) - СРЕДНИЙ ПРИОРИТЕТ ⭐⭐**
- **Группа:** Создать группу
- **Что размещать:** Аналогично ВК
- **SEO-эффект:** Средний

**4. Яндекс.Дзен - ВЫСОКИЙ ПРИОРИТЕТ ⭐⭐⭐**
- **Канал:** Создать канал на Яндекс.Дзен
- **Что размещать:**
  - Статьи о горных лыжах и сноуборде
  - Полезные советы для начинающих
  - Истории успеха клиентов
  - Ссылки на услуги в статьях
- **SEO-эффект:** Очень высокий, Яндекс.Дзен хорошо индексируется Яндексом

---

#### 🛒 ТОРГОВЫЕ ПЛОЩАДКИ

**5. Авито - ВЫСОКИЙ ПРИОРИТЕТ ⭐⭐⭐**
- **Категория:** Услуги → Спорт и фитнес → Инструкторы
- **Что размещать:**
  - Объявление "Инструктор по горным лыжам Тюмень"
  - Объявление "Инструктор по сноуборду Тюмень"
  - Объявление "Обучение горным лыжам Кулига"
  - Объявление "Обучение сноуборду Воронинские горки"
  - В каждом объявлении указать ссылку на страницу
  - Добавить фото с тренировок
  - Указать все ключевые слова в описании
- **Особенности:**
  - Можно создать несколько объявлений с разными ключевыми словами
  - Регулярно обновлять объявления (поднимать в поиске)
  - Добавлять отзывы клиентов
- **SEO-эффект:** Очень высокий, Авито имеет высокий авторитет в поисковых системах

**6. Юла - СРЕДНИЙ ПРИОРИТЕТ ⭐⭐**
- **Категория:** Услуги → Спорт и фитнес
- **Что размещать:** Аналогично Авито
- **SEO-эффект:** Высокий

**7. Циан (для услуг) - НИЗКИЙ ПРИОРИТЕТ ⭐**
- Если есть возможность разместить услуги
- **SEO-эффект:** Средний

---

#### 📋 КАТАЛОГИ И СПРАВОЧНИКИ

**8. Яндекс.Справочник (Яндекс.Карты) - ВЫСОКИЙ ПРИОРИТЕТ ⭐⭐⭐**
- **Действия:**
  - Зарегистрировать компанию в Яндекс.Справочнике
  - Указать адреса: Кулига-Клуб и Воронинские горки
  - Добавить описание услуг
  - Указать ссылку на сайт
  - Добавить фото и отзывы
- **SEO-эффект:** Очень высокий для локального поиска

**9. 2ГИС - ВЫСОКИЙ ПРИОРИТЕТ ⭐⭐⭐**
- **Действия:**
  - Зарегистрировать компанию в 2ГИС
  - Указать оба адреса
  - Добавить описание услуг
  - Указать ссылку на сайт
  - Добавить фото и отзывы
- **SEO-эффект:** Очень высокий для локального поиска

**10. Google My Business - ВЫСОКИЙ ПРИОРИТЕТ ⭐⭐⭐**
- **Действия:**
  - Создать бизнес-профиль в Google My Business
  - Указать адреса мест проведения
  - Добавить описание услуг
  - Указать ссылку на сайт
  - Добавить фото и отзывы
- **SEO-эффект:** Очень высокий для Google поиска

**11. Zoon (Зун) - СРЕДНИЙ ПРИОРИТЕТ ⭐⭐**
- **Действия:**
  - Зарегистрировать компанию
  - Добавить описание услуг
  - Указать ссылку на сайт
- **SEO-эффект:** Высокий

**12. Фламп - СРЕДНИЙ ПРИОРИТЕТ ⭐⭐**
- **Действия:**
  - Зарегистрировать компанию
  - Добавить описание услуг
  - Указать ссылку на сайт
  - Стимулировать клиентов оставлять отзывы
- **SEO-эффект:** Высокий, особенно для отзывов

---

#### 📝 ФОРУМЫ И СООБЩЕСТВА

**13. Форумы о спорте в Тюмени - СРЕДНИЙ ПРИОРИТЕТ ⭐⭐**
- **Что делать:**
  - Найти форумы о спорте, активном отдыхе в Тюмени
  - Участвовать в обсуждениях
  - В подписи указать ссылку на услугу
  - Отвечать на вопросы о горных лыжах и сноуборде
- **SEO-эффект:** Средний-высокий

**14. Группы в ВК о спорте в Тюмени - СРЕДНИЙ ПРИОРИТЕТ ⭐⭐**
- **Что делать:**
  - Найти группы о спорте, активном отдыхе в Тюмени
  - Участвовать в обсуждениях
  - Публиковать полезный контент с ссылками
- **SEO-эффект:** Средний

**15. Reddit (r/russia, r/skiing, r/snowboarding) - НИЗКИЙ ПРИОРИТЕТ ⭐**
- **Что делать:**
  - Участвовать в обсуждениях
  - В профиле указать ссылку
- **SEO-эффект:** Низкий-средний

---

#### 🎥 ВИДЕО ПЛАТФОРМЫ

**16. YouTube - ВЫСОКИЙ ПРИОРИТЕТ ⭐⭐⭐**
- **Канал:** Создать канал на YouTube
- **Что размещать:**
  - Видео с тренировок
  - Обучающие видео для начинающих
  - Видео-отзывы клиентов
  - В описании каждого видео указать ссылку на страницу
  - В описании канала указать ссылку
- **SEO-эффект:** Очень высокий, YouTube - вторая по величине поисковая система

**17. Rutube - СРЕДНИЙ ПРИОРИТЕТ ⭐⭐**
- **Канал:** Создать канал на Rutube
- **Что размещать:** Аналогично YouTube
- **SEO-эффект:** Высокий для российского поиска

---

#### 📰 СТАТЬИ И БЛОГИ

**18. Яндекс.Дзен (уже упомянут выше) - ВЫСОКИЙ ПРИОРИТЕТ ⭐⭐⭐**

**19. Habr (если релевантно) - НИЗКИЙ ПРИОРИТЕТ ⭐**
- Статьи о технологиях в спорте (если есть техническая составляющая)

**20. LiveJournal - НИЗКИЙ ПРИОРИТЕТ ⭐**
- Блог с полезным контентом и ссылками

---

#### 🎯 СПЕЦИАЛИЗИРОВАННЫЕ ПЛОЩАДКИ

**21. Спортивные порталы Тюмени - СРЕДНИЙ ПРИОРИТЕТ ⭐⭐**
- Найти спортивные порталы и сайты о спорте в Тюмени
- Предложить разместить информацию об услугах
- Или разместить гостевой пост

**22. Сайты баз отдыха - СРЕДНИЙ ПРИОРИТЕТ ⭐⭐**
- Связаться с сайтами баз отдыха в Тюменской области
- Предложить разместить информацию о партнерстве

---

#### 📊 ПРИОРИТЕТЫ РАЗМЕЩЕНИЯ

**НАИВЫСШИЙ ПРИОРИТЕТ (начать сразу):**
1. ✅ Авито - создать несколько объявлений с разными ключевыми словами
2. ✅ ВКонтакте - группа/сообщество с регулярными постами
3. ✅ Яндекс.Справочник - регистрация компании
4. ✅ 2ГИС - регистрация компании
5. ✅ Google My Business - создание бизнес-профиля
6. ✅ Яндекс.Дзен - создание канала с полезным контентом

**ВЫСОКИЙ ПРИОРИТЕТ (в течение недели):**
7. ✅ Telegram - канал/группа
8. ✅ YouTube - канал с видео
9. ✅ Юла - объявления
10. ✅ Zoon - регистрация компании
11. ✅ Фламп - регистрация и отзывы

**СРЕДНИЙ ПРИОРИТЕТ (в течение месяца):**
12. ✅ Одноклассники - группа
13. ✅ Rutube - канал
14. ✅ Форумы и сообщества о спорте
15. ✅ Спортивные порталы Тюмени

---

#### 📝 РЕКОМЕНДАЦИИ ПО РАЗМЕЩЕНИЮ

**1. Тексты для размещения:**
- Использовать все ключевые запросы естественным образом
- Упоминать оба места: Кулига и Воронинские горки
- Упоминать оба вида спорта: горные лыжи и сноуборд
- Включать слово "Ворона" для молодежной аудитории
- Добавлять призыв к действию (записаться, позвонить)

**2. Анкоры для ссылок:**
- "Обучение горным лыжам Тюмень"
- "Инструктор по сноуборду Кулига"
- "Обучение сноуборду Воронинские горки"
- "Инструкторы горные лыжи Ворона"
- Вариации ключевых запросов

**3. Регулярность обновлений:**
- ВК: 2-3 поста в неделю
- Авито: обновлять объявления раз в неделю
- Яндекс.Дзен: 1-2 статьи в неделю
- YouTube: 1-2 видео в месяц

**4. Контент-план:**
- Понедельник: Пост в ВК с фото тренировки
- Среда: Статья в Яндекс.Дзен
- Пятница: Обновление объявлений на Авито
- Выходные: Видео на YouTube

---

**Критерий успеха:** Ссылки размещены на 10+ бесплатных площадках, регулярно обновляется контент, отслеживается переход трафика из внешних источников.

---

### 🎯 ПРИОРИТЕТЫ

1. **НАИВЫСШИЙ ПРИОРИТЕТ:**
   - Задача 6.1: Добавить структурированные данные Schema.org
   - Задача 6.2: Улучшить мета-теги
   - Задача 6.7: Настроить отдельную цель в Яндекс.Метрике

2. **ВЫСОКИЙ ПРИОРИТЕТ:**
   - Задача 6.4: Расширить контент с ключевыми словами
   - Задача 6.5: Добавить FAQ секцию
   - Задача 6.8: Настроить фильтр/сегмент в Google Analytics

3. **СРЕДНИЙ ПРИОРИТЕТ:**
   - Задача 6.3: Добавить Breadcrumbs
   - Задача 6.6: Улучшить внутреннюю перелинковку
   - Задача 6.9: Добавить расширенное отслеживание событий

4. **НИЗКИЙ ПРИОРИТЕТ:**
   - Задача 6.10: Оптимизировать изображения для SEO
   - Задача 6.11: Добавить локальную разметку
   - Задача 6.12: Создать XML-карту сайта для страницы

---

### 📊 ТЕКУЩИЙ СТАТУС

**Статус:** ✅ ОСНОВНЫЕ ЗАДАЧИ ВЫПОЛНЕНЫ

**Дата создания плана:** 19.12.2025  
**Дата начала реализации:** 19.12.2025

### ✅ ВЫПОЛНЕНО:

1. **✅ Задача 6.1: Добавить структурированные данные Schema.org** - РЕАЛИЗОВАНО
   - Добавлена разметка LocalBusiness с двумя адресами (Кулига и Воронинские горки)
   - Добавлена разметка Service для обучения горным лыжам и сноуборду
   - Добавлена разметка BreadcrumbList
   - Файл: `views/kuliga-landing.ejs`

2. **✅ Задача 6.2: Улучшить мета-теги** - РЕАЛИЗОВАНО
   - Оптимизирован title с ключевыми словами (60 символов)
   - Оптимизирован description (150-160 символов)
   - Расширены keywords с включением всех целевых запросов (включая "Ворона")
   - Исправлен canonical URL на gornostyle72.ru
   - Файл: `views/kuliga-landing.ejs`

3. **✅ Задача 6.3: Добавить Breadcrumbs** - РЕАЛИЗОВАНО
   - Добавлены визуальные breadcrumbs на странице
   - Добавлена разметка Schema.org BreadcrumbList
   - Файл: `views/kuliga-landing.ejs`

4. **✅ Задача 6.4: Расширить контент с ключевыми словами** - РЕАЛИЗОВАНО
   - Добавлена секция "Почему выбирают нас" с ключевыми словами
   - Добавлена секция "Места проведения" с описанием Кулиги и Воронинских горок
   - Улучшен h1 с включением ключевых слов
   - Расширено описание в hero секции
   - Улучшены описания в секции инструкторов
   - Файл: `views/kuliga-landing.ejs`

5. **✅ Задача 6.5: Добавить FAQ секцию** - РЕАЛИЗОВАНО
   - Добавлена FAQ секция с 6 вопросами
   - Добавлена разметка Schema.org FAQPage
   - Вопросы содержат ключевые слова естественным образом
   - Файл: `views/kuliga-landing.ejs`

6. **✅ Задача 6.6: Улучшить внутреннюю перелинковку** - РЕАЛИЗОВАНО
   - Добавлена ссылка в футере с анкором "Инструкторы по горным лыжам и сноуборду"
   - Исправлена ссылка на кнопке "Записаться на тренировку" (теперь ведет на страницу бронирования)
   - Файл: `views/kuliga-landing.ejs`

7. **✅ Задача 6.9: Добавить расширенное отслеживание событий** - РЕАЛИЗОВАНО
   - Добавлены функции для отправки событий в Яндекс.Метрику и Google Analytics
   - Отслеживается просмотр страницы инструкторов
   - Отслеживается клик по кнопке записи
   - Отслеживается просмотр списка инструкторов
   - Отслеживается просмотр цен
   - Отслеживается изменение фильтра по локации
   - Файл: `public/js/kuliga.js`

8. **✅ Задача 6.10: Оптимизировать изображения для SEO** - РЕАЛИЗОВАНО
   - Улучшены alt-тексты всех изображений с включением ключевых слов
   - Добавлены title атрибуты для изображений
   - Файл: `views/kuliga-landing.ejs`

### 🚧 В ПРОЦЕССЕ / ТРЕБУЕТ РУЧНОЙ НАСТРОЙКИ:

1. **✅ Задача 6.7: Настроить отдельную цель в Яндекс.Метрике** - ВЫПОЛНЕНО
   - Созданы все цели в Яндекс.Метрике:
     - ✅ "Клик по кнопке записи" - идентификатор: `booking_button_click`
     - ✅ "Просмотр списка инструкторов" - идентификатор: `instructors_list_view`
     - ✅ "Просмотр страницы инструкторов" - идентификатор: `instructor_page_view`
     - ✅ "Просмотр цен" - идентификатор: `prices_view`
   - Все идентификаторы совпадают с кодом на сайте
   - Код для отправки событий уже реализован в `public/js/kuliga.js`

2. **🔄 Задача 6.8: Настроить фильтр/сегмент в Google Analytics** - ТРЕБУЕТ РУЧНОЙ НАСТРОЙКИ
   - Нужно зайти в Google Analytics
   - Создать сегмент "Страница инструкторов"
   - Настроить события для отслеживания

### ❌ НЕ ВЫПОЛНЕНО (НИЗКИЙ ПРИОРИТЕТ):

1. **Задача 6.11: Добавить локальную разметку** - УЖЕ ВКЛЮЧЕНО В LocalBusiness
2. **Задача 6.12: Создать XML-карту сайта для страницы** - НУЖНО ПРОВЕРИТЬ sitemap.xml

---

### 📝 ЗАМЕТКИ

- **Важно:** При добавлении ключевых слов нужно соблюдать баланс - не переспамить, использовать естественные формулировки
- **Важно:** Структурированные данные должны быть валидными - проверять через Google Rich Results Test и Яндекс.Вебмастер
- **Важно:** Отдельная метрика нужна для понимания эффективности именно этой страницы
- **Рекомендация:** После реализации всех задач проверить страницу через инструменты:
  - Google Search Console
  - Яндекс.Вебмастер
  - Google Rich Results Test
  - PageSpeed Insights
  - Lighthouse

---

**Дата обновления:** 19.12.2025 (обновлено: добавлены все ключевые запросы и раздел о бесплатных площадках)

---

## 🎯 ЗАДАЧА 7: ПРОВЕРКА УРОВНЯ ПРИ ЗАПИСИ НА ЗИМНИЕ ТРЕНИРОВКИ НА ЕСТЕСТВЕННЫЙ СКЛОН

**Дата добавления:** 20.12.2025  
**Режим:** Планировщик  
**Приоритет:** ВЫСОКИЙ

### 📋 ПРОБЛЕМА

Необходимо изучить текущую логику проверки уровня подготовки при записи через бота на тренажер и организовать аналогичную проверку при записи на зимние тренировки на естественный склон через сайт и через бота.

### 🔍 АНАЛИЗ ТЕКУЩЕЙ СИТУАЦИИ

#### 1. Проверка уровня при записи на тренажер через бота

**Где реализовано:**
- Файл: `src/bot/client-bot.js`
- Для детей: строки 4726-4742 (на этапе выбора участника)
- Для взрослых: строки 5821-5838 (на этапе подтверждения записи, при нажатии "✅ Записаться")

**Логика проверки для детей (тренажер):**
```javascript
// Строки 4726-4742 - на этапе выбора участника
const selectedChild = state.data.available_children[selectedIndex];
const selectedSession = state.data.selected_session;

// Проверяем уровень подготовки ребенка
if (selectedChild.skill_level < selectedSession.skill_level) {
    return bot.sendMessage(chatId,
        `❌ Нельзя записать ребенка на эту тренировку.\n\n` +
        `Уровень подготовки ребенка (${selectedChild.skill_level}) ниже требуемого уровня тренировки (${selectedSession.skill_level}).\n\n` +
        `Пожалуйста, выберите тренировку с подходящим уровнем или подождите, пока уровень подготовки ребенка повысится.`,
        // ... клавиатура с кнопками
    );
}
```

**Логика проверки для взрослых (тренажер):**
```javascript
// Строки 5821-5838 - на этапе подтверждения записи (confirm_group_training)
// Проверяем уровень подготовки для взрослых
if (!state.data.selected_child && clientData.skill_level < selectedSession.skill_level) {
    await client.query('ROLLBACK');
    return bot.sendMessage(chatId,
        `❌ Нельзя записаться на эту тренировку.\n\n` +
        `Ваш уровень подготовки (${clientData.skill_level}) ниже требуемого уровня тренировки (${selectedSession.skill_level}).\n\n` +
        `Пожалуйста, выберите тренировку с подходящим уровнем или подождите, пока ваш уровень подготовки повысится.`,
        // ... клавиатура с кнопками
    );
}
```

**Особенности:**
- ✅ Для детей: проверка происходит **на этапе выбора участника** (перед подтверждением записи)
- ✅ Для взрослых: проверка происходит **на этапе подтверждения записи** (при нажатии "✅ Записаться")
- ✅ Проверка **блокирующая** для обоих случаев
- ✅ Если уровень недостаточен - запись блокируется с информативным сообщением

#### 2. Проверка уровня при записи на зимние тренировки через бота

**Где реализовано:**
- Файл: `src/bot/client-bot.js`
- Для детей: строки 5192-5210 (на этапе выбора участника)
- Для взрослых: строки 5386-5401 (на этапе подтверждения записи, но **ЗАКОММЕНТИРОВАНА**)

**Логика проверки для детей (зимние тренировки):**
```javascript
// Строки 5192-5210 - на этапе выбора участника
const selectedChild = state.data.available_children[selectedIndex];
const selectedTraining = state.data.selected_training;

// Проверяем уровень подготовки ребенка
const childLevel = selectedChild.skill_level || 0;
const requiredLevel = selectedTraining.skill_level || 0;
if (childLevel < requiredLevel) {
    return bot.sendMessage(chatId,
        `❌ Нельзя записать ребенка на эту тренировку.\n\n` +
        `Уровень подготовки ребенка (${childLevel}) ниже требуемого уровня тренировки (${requiredLevel}).\n\n` +
        `Пожалуйста, выберите тренировку с подходящим уровнем или подождите, пока уровень подготовки ребенка повысится.`,
        // ... клавиатура
    );
}
```

**Логика проверки для взрослых (зимние тренировки):**
```javascript
// Строки 5386-5401 - на этапе подтверждения записи (confirm_natural_slope_group_training)
// Проверяем уровень подготовки (для клиента или ребенка)
let participantLevel = 0;
let participantName = clientData.full_name;

if (state.data.selected_child) {
    // Если выбран ребенок, проверяем его уровень
    participantLevel = state.data.selected_child.skill_level || 0;
    participantName = state.data.selected_child.full_name;
} else {
    // Если выбран клиент, проверяем его уровень
    participantLevel = clientData.skill_level || 0;
}

// Для kuliga_group_trainings уровень хранится в поле level как текст, не как число
// Пока пропускаем проверку уровня (можно добавить позже)
// const requiredLevel = selectedTraining.level || 0;
```

**Особенности:**
- ✅ Для детей: проверка реализована и блокирующая (на этапе выбора участника)
- ❌ **ПРОБЛЕМА:** Для взрослых проверка уровня **ЗАКОММЕНТИРОВАНА** (строки 5399-5401)
- ⚠️ В комментарии указано: "Для kuliga_group_trainings уровень хранится в поле level как текст, не как число"
- ⚠️ Нужно реализовать проверку уровня для взрослых при записи на зимние тренировки

#### 3. Создание зимних тренировок и назначение уровней

**Где создаются зимние тренировки:**
- Файл: `src/routes/winter-trainings.js`
- Endpoint: `POST /api/winter-trainings`

**Как назначается уровень:**
```javascript
// Строки 28, 76, 92
const { skill_level, ... } = req.body;
// ...
skill_level || null,  // Может быть null
```

**Администратор задает уровень:**
- Файл: `public/js/create-winter-training.js`
- Строка 387: `skill_level: parseInt(formData.get('skill_level')) || null`
- В форме есть поле выбора уровня (аналогично тренажеру)

**Вывод:**
- Уровень тренировки задается администратором при создании
- Может быть `null` (не обязателен)

#### 4. Запись на зимние тренировки через сайт

**Где происходит запись:**
- Файл: `src/routes/kuliga-booking.js`
- Функции: `createGroupBooking()`, `createIndividualBooking()`, `createProgramBooking()`

**Проверка уровня:**
- ❌ **ПРОБЛЕМА:** При записи через сайт проверка уровня **НЕ РЕАЛИЗОВАНА**
- В коде нет проверки `skill_level` клиента/ребенка перед созданием бронирования

#### 5. Структура данных в базе

**Где хранится уровень:**
- `clients.skill_level` - уровень взрослого клиента (INTEGER, может быть NULL)
- `children.skill_level` - уровень ребенка (INTEGER, может быть NULL, по умолчанию обычно 1)
- `training_sessions.skill_level` - требуемый уровень тренировки (INTEGER, может быть NULL)

**Значения уровней:**
- Диапазон: 1-10 (где 1 - начинающий, 10 - эксперт)
- `NULL` означает, что уровень не указан или не требуется

---

### 🎯 ТРЕБОВАНИЯ

1. **Проверка уровня при записи через бота:**
   - ✅ Для детей на тренажер: реализована (блокирующая)
   - ✅ Для взрослых на тренажер: реализована (блокирующая)
   - ✅ Для детей на зимние тренировки: реализована (блокирующая)
   - ❌ **Для взрослых на зимние тренировки: НЕ реализована (закомментирована, строки 5399-5401)**

2. **Проверка уровня при записи через сайт:**
   - ❌ Не реализована для детей
   - ❌ Не реализована для взрослых
   - Нужно добавить проверку перед созданием бронирования

3. **Единообразие логики:**
   - Логика проверки должна быть одинаковой для тренажера и зимних тренировок
   - Логика проверки должна быть одинаковой для бота и сайта
   - Нужно раскомментировать и доработать проверку уровня для зимних тренировок (учесть, что уровень может храниться как текст в `level`)

---

### 🔧 РЕШЕНИЕ

#### Вариант 1: Строгая проверка (РЕКОМЕНДУЕТСЯ) ⭐

**Идея:** Блокировать запись, если уровень клиента/ребенка ниже требуемого уровня тренировки.

**Логика проверки:**
```javascript
// Если у тренировки указан уровень (skill_level !== null)
if (training.skill_level !== null && training.skill_level !== undefined) {
    const clientLevel = client.skill_level || 0;
    const childLevel = child?.skill_level || 0;
    const participantLevel = child ? childLevel : clientLevel;
    const requiredLevel = training.skill_level;
    
    if (participantLevel < requiredLevel) {
        // Блокируем запись
        return error('Уровень недостаточен');
    }
}
// Если у тренировки уровень не указан (skill_level === null) - запись разрешена
```

**Преимущества:**
- Защищает от записи на слишком сложные тренировки
- Соответствует логике для тренажера (для детей)
- Обеспечивает безопасность и соответствие уровня подготовки

**Недостатки:**
- Может быть слишком строгим, если уровень клиента не обновлен
- Нужно учитывать, что не у всех клиентов указан уровень

#### Вариант 2: Предупреждение без блокировки

**Идея:** Показывать предупреждение, но разрешать запись.

**Преимущества:**
- Более гибкий подход
- Не блокирует запись, если администратор считает, что клиент может справиться

**Недостатки:**
- Не обеспечивает защиту от записи на слишком сложные тренировки
- Может привести к несоответствию уровня

#### Вариант 3: Гибридный подход

**Идея:** 
- Если уровень тренировки указан и уровень участника указан - строгая проверка
- Если уровень участника не указан - разрешить запись с предупреждением

**Преимущества:**
- Гибкость для новых клиентов
- Строгость для клиентов с указанным уровнем

**Недостатки:**
- Более сложная логика

---

### 📝 ПЛАН РЕАЛИЗАЦИИ

#### ЭТАП 1: Реализация проверки уровня для зимних тренировок в боте (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 7.1: Раскомментировать и доработать проверку уровня для взрослых при записи на зимние тренировки**

**Файлы:**
- `src/bot/client-bot.js`

**Действия:**
1. В обработчике `confirm_natural_slope_group_training` (строки 5386-5401):
   - Раскомментировать проверку уровня
   - Учесть, что для `kuliga_group_trainings` уровень может храниться в поле `level` как текст
   - Преобразовать текстовый уровень в числовой (если нужно)
   - Добавить блокирующую проверку аналогично тренажеру (строки 5821-5838)
   - Если уровень недостаточен - заблокировать запись с информативным сообщением

**Логика преобразования уровня:**
```javascript
// Для kuliga_group_trainings уровень может быть:
// - числом в skill_level (для training_sessions)
// - текстом в level (для kuliga_group_trainings): 'beginner' (1), 'intermediate' (3), 'advanced' (5), или число
let requiredLevel = 0;
if (selectedTraining.skill_level !== null && selectedTraining.skill_level !== undefined) {
    requiredLevel = parseInt(selectedTraining.skill_level) || 0;
} else if (selectedTraining.level) {
    // Преобразуем текстовый уровень в числовой
    const levelMap = {
        'beginner': 1,
        'intermediate': 3,
        'advanced': 5
    };
    requiredLevel = levelMap[selectedTraining.level.toLowerCase()] || parseInt(selectedTraining.level) || 0;
}

// Проверяем уровень
if (requiredLevel > 0 && participantLevel < requiredLevel) {
    await client.query('ROLLBACK');
    return bot.sendMessage(chatId,
        `❌ Нельзя записаться на эту тренировку.\n\n` +
        `Уровень подготовки ${participantName} (${participantLevel}) ниже требуемого уровня тренировки (${requiredLevel}).\n\n` +
        `Пожалуйста, выберите тренировку с подходящим уровнем или обратитесь к инструктору для повышения уровня.`,
        // ... клавиатура
    );
}
```

**Логика проверки:**
```javascript
const clientLevel = client.skill_level || 0;
const requiredLevel = selectedTraining.skill_level || 0;

// Если у тренировки указан уровень, проверяем строго
if (requiredLevel !== null && requiredLevel !== undefined && clientLevel < requiredLevel) {
    return bot.sendMessage(chatId,
        `❌ Нельзя записаться на эту тренировку.\n\n` +
        `Ваш уровень подготовки (${clientLevel}) ниже требуемого уровня тренировки (${requiredLevel}).\n\n` +
        `Пожалуйста, выберите тренировку с подходящим уровнем или обратитесь к инструктору для повышения уровня.`,
        {
            reply_markup: {
                keyboard: [
                    ['🏔️ Выбрать другую тренировку'],
                    ['🔙 Назад в меню']
                ],
                resize_keyboard: true
            }
        }
    );
}
```

**Критерий успеха:** Взрослые клиенты не могут записаться на тренировку с уровнем выше их уровня через бота.

---

#### ЭТАП 2: Добавление проверки уровня при записи через сайт (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 7.2: Добавить проверку уровня в createGroupBooking()**

**Файлы:**
- `src/routes/kuliga-booking.js`

**Действия:**
1. В функции `createGroupBooking()` перед созданием бронирования:
   - Получить уровень тренировки из базы
   - Для каждого участника (если указан child_id - взять уровень ребенка, иначе - уровень клиента)
   - Проверить соответствие уровней
   - Если уровень недостаточен - вернуть ошибку 400 с информативным сообщением

**Логика проверки:**
```javascript
// После получения тренировки, перед созданием бронирования
if (training.skill_level !== null && training.skill_level !== undefined) {
    // Для каждого участника
    for (const participant of participants) {
        let participantLevel = 0;
        
        if (participant.child_id) {
            // Участник - ребенок
            const childResult = await client.query(
                'SELECT skill_level FROM children WHERE id = $1',
                [participant.child_id]
            );
            participantLevel = childResult.rows[0]?.skill_level || 0;
        } else {
            // Участник - взрослый клиент
            participantLevel = clientRecord.skill_level || 0;
        }
        
        if (participantLevel < training.skill_level) {
            await client.query('ROLLBACK');
            return res.status(400).json({
                success: false,
                error: `Уровень подготовки участника "${participant.fullName}" (${participantLevel}) ниже требуемого уровня тренировки (${training.skill_level}). Пожалуйста, выберите тренировку с подходящим уровнем.`
            });
        }
    }
}
```

**Критерий успеха:** При записи через сайт на зимнюю тренировку проверяется уровень каждого участника.

---

**Задача 7.3: Добавить проверку уровня в createIndividualBooking()**

**Файлы:**
- `src/routes/kuliga-booking.js`

**Действия:**
1. Аналогично задаче 7.2, но для индивидуальных тренировок
2. Проверить уровень основного участника (клиента или первого участника из списка)

**Критерий успеха:** При записи на индивидуальную зимнюю тренировку через сайт проверяется уровень участника.

---

**Задача 7.4: Добавить проверку уровня в createProgramBooking()**

**Файлы:**
- `src/routes/kuliga-booking.js`

**Действия:**
1. Аналогично задаче 7.2, но для программ
2. Проверить уровень участников программы

**Критерий успеха:** При записи на программу через сайт проверяется уровень участников.

---

#### ЭТАП 3: Обработка случая с NULL уровнем (СРЕДНИЙ ПРИОРИТЕТ)

**Задача 7.5: Определить поведение при отсутствии уровня тренировки**

**Вопросы:**
- Если у тренировки `skill_level = NULL` - разрешать запись всем?
- Если у клиента/ребенка `skill_level = NULL` - как обрабатывать?

**Рекомендация:**
- Если у тренировки уровень не указан (`NULL`) - разрешать запись всем (тренировка для всех уровней)
- Если у клиента/ребенка уровень не указан (`NULL`) - считать уровень = 0 (самый низкий)
- Если у тренировки уровень указан, а у участника нет - блокировать запись с сообщением "Укажите уровень подготовки в профиле"

**Альтернатива (более мягкая):**
- Если у участника уровень не указан - разрешать запись с предупреждением или информационным сообщением

---

#### ЭТАП 4: Тестирование и валидация (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 7.6: Протестировать проверку уровня**

**Тестовые сценарии:**
1. Запись ребенка с достаточным уровнем через бота - должна пройти
2. Запись ребенка с недостаточным уровнем через бота - должна быть заблокирована
3. Запись взрослого с достаточным уровнем через бота - должна пройти
4. Запись взрослого с недостаточным уровнем через бота - должна быть заблокирована
5. Запись через сайт с достаточным уровнем - должна пройти
6. Запись через сайт с недостаточным уровнем - должна быть заблокирована
7. Запись на тренировку без указанного уровня - должна пройти (для всех)

**Критерий успеха:** Все тестовые сценарии проходят успешно.

---

### 📊 ТЕКУЩИЙ СТАТУС

**Статус:** 📋 ПЛАН СОЗДАН, ТРЕБУЕТ УТВЕРЖДЕНИЯ

**Дата создания плана:** 20.12.2025

---

### 🎯 ПРИОРИТЕТЫ

1. **НАИВЫСШИЙ ПРИОРИТЕТ:**
   - Задача 7.1: Сделать проверку уровня блокирующей для взрослых клиентов в боте
   - Задача 7.2: Добавить проверку уровня в createGroupBooking()

2. **ВЫСОКИЙ ПРИОРИТЕТ:**
   - Задача 7.3: Добавить проверку уровня в createIndividualBooking()
   - Задача 7.4: Добавить проверку уровня в createProgramBooking()
   - Задача 7.6: Протестировать проверку уровня

3. **СРЕДНИЙ ПРИОРИТЕТ:**
   - Задача 7.5: Определить поведение при отсутствии уровня тренировки

---

### ✅ УТОЧНЕНИЯ ОТ ПОЛЬЗОВАТЕЛЯ (20.12.2025)

**Требования:**
1. **Уведомление перед началом записи:** Перед началом записи на тренировку 2-го и выше уровня нужно показывать информационное уведомление с объяснением:
   - Для записи на данную тренировку необходим N-й уровень
   - Если есть базовые навыки - зарегистрироваться в Telegram боте и написать администратору с просьбой повысить уровень
   - Если новичок - записаться на индивидуальную тренировку
   - Ссылки на Telegram бота, Telegram администратора, телефон администратора

2. **Запись на тренировки с уровнем через сайт:**
   - Запись на тренировки с уровнем можно осуществить только через слот инструктора
   - При заполнении данных проверять есть ли человек в базе или нет

3. **Логика проверки участников:**
   - Если записывает себя - проверить свой уровень
   - Если записывает другого человека - проверить в таблице children:
     - Найти ребенка по имени, фамилии, отчеству (или комбинациям)
     - Проверить достаточный ли у него уровень
     - Сложная логика сопоставления (по имени, по имени+фамилии, по ФИО)

**Логика сопоставления участника с ребенком в базе:**
- Варианты сопоставления:
  1. Точное совпадение ФИО
  2. Совпадение по имени + фамилии
  3. Совпадение по имени + отчеству
  4. Совпадение только по имени (менее надежно)
- Учитывать возможные варианты написания (регистр, лишние пробелы)
- Если ребенок не найден - считать что у него нет уровня (или уровень = 0)

---

### 📝 ПЛАН РЕАЛИЗАЦИИ (ОБНОВЛЕН)

#### ЭТАП 1: Добавление уровня в API и показ уведомления (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 7.7: Добавить уровень в API `/api/kuliga/availability`**

**Файлы:**
- `src/routes/kuliga-public.js` или `src/routes/kuliga-booking.js`

**Действия:**
1. В запросе `/api/kuliga/availability` добавить JOIN с `kuliga_group_trainings` для получения уровня
2. Если на слоте есть групповая тренировка - возвращать её `level`
3. Преобразовать текстовый уровень в числовой (beginner=1, intermediate=2, advanced=3, или число)

**Критерий успеха:** API возвращает уровень тренировки для каждого слота.

---

**Задача 7.8: Показывать уведомление о требуемом уровне перед записью**

**Файлы:**
- `public/js/kuliga-booking.js`
- `views/kuliga-booking.ejs` (для HTML модального окна/блока)

**Действия:**
1. При выборе слота с уровнем >= 2 показывать модальное окно или информационный блок
2. В уведомлении указать:
   - Требуемый уровень тренировки
   - Инструкцию для тех, у кого есть базовые навыки (регистрация в боте + обращение к админу)
   - Инструкцию для новичков (индивидуальная тренировка)
   - Ссылки на Telegram бота, Telegram администратора, телефон
3. После прочтения уведомления разрешить продолжить запись (не блокировать, только информировать)

**Критерий успеха:** При выборе слота с уровнем >= 2 показывается информационное уведомление с инструкциями.

---

#### ЭТАП 2: Проверка уровня при создании бронирования через сайт (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 7.9: Реализовать функцию сопоставления участника с ребенком в базе**

**Файлы:**
- `src/routes/kuliga-booking.js`

**Действия:**
1. Создать функцию `findChildByFullName(fullName, parentId, client)` для поиска ребенка:
   ```javascript
   async function findChildByFullName(fullName, parentId, client) {
       // Нормализуем ФИО (убираем лишние пробелы, приводим к нижнему регистру)
       const normalized = fullName.trim().toLowerCase().replace(/\s+/g, ' ');
       const parts = normalized.split(' ').filter(p => p.length > 0);
       
       // Получаем всех детей родителя
       const childrenResult = await client.query(
           'SELECT id, full_name, skill_level FROM children WHERE parent_id = $1',
           [parentId]
       );
       
       // Варианты сопоставления:
       // 1. Точное совпадение (с учетом регистра)
       // 2. По имени + фамилии (если в fullName 2+ слова)
       // 3. По имени + отчеству (если в fullName 3+ слова)
       // 4. Только по имени (менее надежно, но может быть полезно)
       
       for (const child of childrenResult.rows) {
           const childNormalized = child.full_name.trim().toLowerCase().replace(/\s+/g, ' ');
           const childParts = childNormalized.split(' ').filter(p => p.length > 0);
           
           // Точное совпадение
           if (normalized === childNormalized) {
               return child;
           }
           
           // Совпадение по имени + фамилии (если оба содержат минимум 2 слова)
           if (parts.length >= 2 && childParts.length >= 2) {
               if (parts[0] === childParts[0] && parts[1] === childParts[1]) {
                   return child;
               }
           }
           
           // Совпадение по имени + отчеству (если оба содержат минимум 3 слова)
           if (parts.length >= 3 && childParts.length >= 3) {
               if (parts[0] === childParts[0] && parts[2] === childParts[2]) {
                   return child;
               }
           }
           
           // Совпадение только по имени (если имя не слишком короткое, минимум 3 символа)
           if (parts.length >= 1 && childParts.length >= 1 && parts[0].length >= 3) {
               if (parts[0] === childParts[0]) {
                   return child; // Возвращаем первого найденного (может быть несколько детей с одним именем)
               }
           }
       }
       
       return null; // Ребенок не найден
   }
   ```

**Критерий успеха:** Функция находит ребенка в базе по различным вариантам сопоставления ФИО.

---

**Задача 7.10: Добавить проверку уровня в createGroupBooking()**

**Файлы:**
- `src/routes/kuliga-booking.js`

**Действия:**
1. После получения тренировки (после строки 541) добавить проверку уровня
2. Если у тренировки указан уровень (level !== null и level >= 2):
   - Получить уровень тренировки (преобразовать из текста в число, если нужно)
   - Для каждого участника:
     - Если участник - это заказчик (payerParticipation === 'self' или первый участник совпадает с fullName):
       - Получить уровень клиента из базы
       - Если уровень недостаточен - вернуть ошибку 400
     - Если участник - другой человек (payerParticipation === 'other' или участник не совпадает с fullName):
       - Попытаться найти ребенка в базе через `findChildByFullName()`
       - Если ребенок найден - проверить его уровень
       - Если ребенок не найден или уровень недостаточен - вернуть ошибку 400 с информативным сообщением

**Логика проверки уровня:**
```javascript
// Преобразуем уровень тренировки в число
let requiredLevel = 0;
if (training.level) {
    if (typeof training.level === 'number') {
        requiredLevel = training.level;
    } else if (typeof training.level === 'string') {
        const levelMap = {
            'beginner': 1,
            'intermediate': 2,
            'advanced': 3
        };
        requiredLevel = levelMap[training.level.toLowerCase()] || parseInt(training.level) || 0;
    }
}

// Проверяем только если уровень >= 2
if (requiredLevel >= 2) {
    for (const participantName of namesArray) {
        let participantLevel = 0;
        let participantType = 'unknown';
        
        // Проверяем, является ли участник заказчиком
        if (participantName.trim().toLowerCase() === fullName.trim().toLowerCase()) {
            // Участник - заказчик, проверяем его уровень
            participantLevel = clientRecord.skill_level || 0;
            participantType = 'client';
        } else {
            // Участник - другой человек, ищем в children
            const child = await findChildByFullName(participantName, clientRecord.id, client);
            if (child) {
                participantLevel = child.skill_level || 0;
                participantType = 'child';
            } else {
                // Ребенок не найден - у него нет уровня
                participantLevel = 0;
                participantType = 'unknown';
            }
        }
        
        if (participantLevel < requiredLevel) {
            await client.query('ROLLBACK');
            const levelName = requiredLevel === 2 ? 'второй' : requiredLevel === 3 ? 'третий' : `${requiredLevel}-й`;
            return res.status(400).json({
                success: false,
                error: `Уровень подготовки участника "${participantName}" (${participantLevel}) ниже требуемого уровня тренировки (${requiredLevel}). Для записи на эту тренировку необходим ${levelName} уровень. ${participantType === 'unknown' ? 'Участник не найден в базе данных.' : ''} Пожалуйста, зарегистрируйтесь в Telegram боте и обратитесь к администратору для повышения уровня, или выберите тренировку с подходящим уровнем.`
            });
        }
    }
}
```

**Критерий успеха:** При записи через сайт на тренировку с уровнем >= 2 проверяется уровень каждого участника.

---

**Задача 7.11: Добавить проверку уровня в createIndividualBooking()**

**Файлы:**
- `src/routes/kuliga-booking.js`

**Действия:**
1. Аналогично задаче 7.10, но для индивидуальных тренировок
2. Проверить уровень основного участника (первого из списка participants)

**Критерий успеха:** При записи на индивидуальную тренировку с уровнем проверяется уровень участника.

---

#### ЭТАП 3: Обновление логики для зимних тренировок в боте (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 7.1 (обновлена): Раскомментировать и доработать проверку уровня для зимних тренировок в боте**

**Файлы:**
- `src/bot/client-bot.js`

**Действия:**
1. В обработчике `confirm_natural_slope_group_training` (строки 5386-5401) раскомментировать проверку уровня
2. Учесть, что для `kuliga_group_trainings` уровень хранится в поле `level` как текст
3. Преобразовать текстовый уровень в числовой
4. Добавить блокирующую проверку аналогично тренажеру

**Критерий успеха:** Взрослые клиенты не могут записаться на зимнюю тренировку с уровнем выше их уровня через бота.

---

### 📝 ЗАМЕТКИ (ОБНОВЛЕНО)

- **Важно:** Проверка уровня должна быть единообразной для всех типов тренировок (тренажер и естественный склон)
- **Важно:** Проверка уровня должна быть единообразной для всех способов записи (бот и сайт)
- **Важно:** При блокировке записи из-за недостаточного уровня нужно показывать информативное сообщение с объяснением
- **Важно:** Запись на тренировки с уровнем через сайт возможна только через слот инструктора
- **Важно:** Уведомление о требуемом уровне - информационное, не блокирующее (только для информации)
- **Важно:** Проверка уровня при создании бронирования - блокирующая (нельзя записаться без достаточного уровня)
- **Сложность:** Логика сопоставления участника с ребенком в базе требует тщательной реализации с учетом различных вариантов написания ФИО
- **Рекомендация:** Рассмотреть возможность автоматического обновления уровня клиента после прохождения тренировки (но это отдельная задача)

---

**Дата обновления:** 20.12.2025 (обновлено с учетом уточнений пользователя)

---

### 🔍 ВАРИАНТЫ ОПРЕДЕЛЕНИЯ ТИПА ТРЕНИРОВКИ (ДЕТСКАЯ/ВЗРОСЛАЯ/ОБЩАЯ)

#### Текущая ситуация

**В боте для тренажера:**
- Проверка по `group_name.toLowerCase().includes('дети')` - детская
- Проверка по `group_name.toLowerCase().includes('взрослые')` - взрослая
- Иначе - общая

**Проблема для зимних тренировок:**
- В `kuliga_group_trainings` нет поля `group_name`
- Название формируется динамически:
  - Если есть `program_id` → название берется из `kuliga_programs.name`
  - Иначе → "Групповая тренировка (Горные лыжи)" или "Групповая тренировка (Сноуборд)"

---

#### Вариант 1: По названию программы (РЕКОМЕНДУЕТСЯ) ⭐

**Идея:** Если тренировка создана из программы (`program_id IS NOT NULL`), проверять название программы.

**Логика:**
```javascript
let isChildrenTraining = false;
let isAdultTraining = false;

if (training.program_id) {
    // Получаем название программы
    const programResult = await client.query(
        'SELECT name FROM kuliga_programs WHERE id = $1',
        [training.program_id]
    );
    if (programResult.rows.length > 0) {
        const programName = programResult.rows[0].name.toLowerCase();
        isChildrenTraining = programName.includes('дети') || programName.includes('детск');
        isAdultTraining = programName.includes('взрослые') || programName.includes('взросл');
    }
}

const isGeneralTraining = !isChildrenTraining && !isAdultTraining;
```

**Преимущества:**
- ✅ Надежно, если программы имеют понятные названия
- ✅ Соответствует логике для тренажера
- ✅ Не требует изменений в БД

**Недостатки:**
- ⚠️ Не работает для тренировок, созданных вручную (без program_id)
- ⚠️ Зависит от названий программ (должны содержать "дети" или "взрослые")

**Где применять:**
- В API `/api/kuliga/availability` - добавить JOIN с `kuliga_programs`
- При создании бронирования - проверять перед записью

---

#### Вариант 2: По description тренировки

**Идея:** Проверять поле `description` в `kuliga_group_trainings`.

**Логика:**
```javascript
const description = (training.description || '').toLowerCase();
const isChildrenTraining = description.includes('дети') || description.includes('детск');
const isAdultTraining = description.includes('взрослые') || description.includes('взросл');
const isGeneralTraining = !isChildrenTraining && !isAdultTraining;
```

**Преимущества:**
- ✅ Работает для всех тренировок (с программой и без)
- ✅ Не требует изменений в БД

**Недостатки:**
- ⚠️ Зависит от того, заполняют ли администраторы description
- ⚠️ Может быть менее надежным, если описание не стандартизировано

---

#### Вариант 3: Комбинация названия программы + description

**Идея:** Проверять сначала название программы, если не найдено - проверять description.

**Логика:**
```javascript
let isChildrenTraining = false;
let isAdultTraining = false;

// Сначала проверяем название программы
if (training.program_id) {
    const programResult = await client.query(
        'SELECT name FROM kuliga_programs WHERE id = $1',
        [training.program_id]
    );
    if (programResult.rows.length > 0) {
        const programName = programResult.rows[0].name.toLowerCase();
        isChildrenTraining = programName.includes('дети') || programName.includes('детск');
        isAdultTraining = programName.includes('взрослые') || programName.includes('взросл');
    }
}

// Если не определили по программе, проверяем description
if (!isChildrenTraining && !isAdultTraining) {
    const description = (training.description || '').toLowerCase();
    isChildrenTraining = description.includes('дети') || description.includes('детск');
    isAdultTraining = description.includes('взрослые') || description.includes('взросл');
}

const isGeneralTraining = !isChildrenTraining && !isAdultTraining;
```

**Преимущества:**
- ✅ Максимальная надежность
- ✅ Работает для всех случаев
- ✅ Не требует изменений в БД

**Недостатки:**
- ⚠️ Более сложная логика
- ⚠️ Требует заполнения description или правильных названий программ

---

#### Вариант 4: Добавить отдельное поле в таблицу

**Идея:** Добавить поле `target_audience` или `training_type` в `kuliga_group_trainings`.

**Значения поля:**
- `'children'` - детская
- `'adults'` - взрослая
- `'general'` или `NULL` - общая (для всех)

**Преимущества:**
- ✅ Наиболее надежный и явный способ
- ✅ Не зависит от названий или описаний
- ✅ Легко фильтровать и искать

**Недостатки:**
- ❌ Требует миграции БД
- ❌ Нужно заполнить для существующих тренировок
- ❌ Администратор должен выбирать при создании

**Миграция:**
```sql
ALTER TABLE kuliga_group_trainings 
ADD COLUMN target_audience VARCHAR(20) DEFAULT 'general' 
CHECK (target_audience IN ('children', 'adults', 'general'));

-- Обновление существующих данных на основе названий программ
UPDATE kuliga_group_trainings kgt
SET target_audience = CASE
    WHEN EXISTS (
        SELECT 1 FROM kuliga_programs kp 
        WHERE kp.id = kgt.program_id 
        AND (kp.name ILIKE '%дети%' OR kp.name ILIKE '%детск%')
    ) THEN 'children'
    WHEN EXISTS (
        SELECT 1 FROM kuliga_programs kp 
        WHERE kp.id = kgt.program_id 
        AND (kp.name ILIKE '%взрослые%' OR kp.name ILIKE '%взросл%')
    ) THEN 'adults'
    ELSE 'general'
END;
```

---

#### Вариант 5: По возрасту участников (статистический подход)

**Идея:** Определять тип тренировки на основе возраста уже записанных участников.

**Логика:**
```javascript
// Получаем возраст всех участников тренировки
const participantsResult = await client.query(`
    SELECT 
        COALESCE(c.birth_date, ch.birth_date) as birth_date
    FROM kuliga_bookings kb
    JOIN clients c ON kb.client_id = c.id
    LEFT JOIN children ch ON ...
    WHERE kb.group_training_id = $1
    AND kb.status = 'confirmed'
`, [training.id]);

// Если все участники < 18 лет - детская
// Если все участники >= 18 лет - взрослая
// Иначе - общая
```

**Преимущества:**
- ✅ Автоматическое определение

**Недостатки:**
- ❌ Не работает для новых тренировок (нет участников)
- ❌ Неточное, если на тренировке смешанный состав
- ❌ Не подходит для проверки перед записью

---

#### Вариант 6: По уровню и другим характеристикам

**Идея:** Использовать комбинацию уровня, названия программы и других факторов.

**Логика:**
```javascript
// Если уровень = 1 и название программы содержит "дети" → детская
// Если уровень >= 3 и название программы содержит "взрослые" → взрослая
// Иначе - общая
```

**Преимущества:**
- ✅ Учитывает несколько факторов

**Недостатки:**
- ❌ Может быть ненадежным
- ❌ Сложная логика

---

### 🎯 РЕКОМЕНДАЦИЯ

**Для быстрого внедрения (без миграции):**
- **Вариант 3 (Комбинация)** - проверять название программы и description

**Для долгосрочного решения:**
- **Вариант 4 (Отдельное поле)** - добавить `target_audience` в таблицу

**План реализации:**
1. Сначала реализовать Вариант 3 (комбинация) - быстро и не требует миграции
2. Позже добавить Вариант 4 (отдельное поле) - для надежности и удобства администраторов

---

**Дата обновления:** 20.12.2025 (добавлены варианты определения типа тренировки)

---

### ✅ РЕАЛИЗОВАНО: Вывод description и типа тренировки в модальном окне

**Задача:** Добавить информацию из поля `description` в модальное окно при клике на групповую тренировку, определять тип тренировки (детская/взрослая/общая).

**Файл:** `public/js/kuliga-slot-modals.js`

**Реализовано:**
1. ✅ Добавлена функция `determineTrainingType(description)` для определения типа тренировки:
   - Проверяет начало description на "Детская тренировка" или "Взрослая тренировка" (стандартный формат)
   - Ищет ключевые слова: "дети", "детск", "для детей", "детская", "ребёнок", "ребенок" (детская)
   - Ищет ключевые слова: "взрослые", "взросл", "для взрослых", "взрослая" (взрослая)
   - Если не найдено - общая тренировка

2. ✅ В модальное окно добавлен цветной блок с типом тренировки:
   - Детская: синий фон (#e3f2fd) с синей рамкой (#2196f3) и эмодзи 👶
   - Взрослая: оранжевый фон (#fff3e0) с оранжевой рамкой (#ff9800) и эмодзи 👤
   - Общая: блок не показывается (если тип не определен)

3. ✅ В блок с информацией о тренировке добавлено поле "Описание" с полным текстом description

**Как использовать:**
- При создании групповой тренировки администратор должен указывать в поле `description`:
  - "Детская тренировка. [описание]" - для детских тренировок
  - "Взрослая тренировка. [описание]" - для взрослых тренировок
  - Или просто включить слова "дети", "взрослые" в описание

**Дата реализации:** 20.12.2025

---

## 🎯 ЗАДАЧА 5: Плавный переход на интернет-эквайринг в боте

**Дата создания:** 27.12.2025  
**Режим:** Планировщик

### 📋 ОПИСАНИЕ ЗАДАЧИ

Необходимо реализовать плавный переход на интернет-эквайринг в Telegram боте:
1. **Пополнение кошелька через интернет-эквайринг** - вместо ручного перевода по номеру телефона/СБП
2. **Прямая оплата тренировок** - если на кошельке нет денег, оплачивать тренировку сразу через интернет-эквайринг и записываться после успешной оплаты

### 🎯 ЦЕЛИ

- Упростить процесс пополнения кошелька для клиентов
- Убрать необходимость ручного перевода денег и ожидания зачисления
- Реализовать прямую оплату тренировок при недостатке средств на кошельке
- **ВАЖНО: Кошелек остается в первую очередь, так как он напрямую связан с покупкой сертификатов**
- Сохранить совместимость с существующей системой (кошелек - основной способ для сертификатов, опция для тренировок)

### 🔑 КЛЮЧЕВЫЕ ПРИНЦИПЫ

1. **Кошелек - приоритет для сертификатов:**
   - Покупка сертификатов всегда через кошелек (не меняем)
   - Пополнение кошелька должно быть максимально удобным
   - Интернет-эквайринг для пополнения - это улучшение процесса, не замена

2. **Тренировки - гибкий подход:**
   - Если есть деньги на кошельке - списываем с кошелька (как сейчас)
   - Если денег нет - предлагаем оплатить сразу через интернет-эквайринг
   - Кошелек остается опцией, но не обязательным

---

## 📊 ТЕКУЩАЯ СИСТЕМА (АНАЛИЗ)

### 1. Пополнение кошелька

**Где:** `src/bot/client-bot.js` → функция `handleTopUpBalance()` (строка 11142)

**Текущая логика:**
1. Показывается номер кошелька клиента
2. Инструкция по переводу через СБП или на номер телефона
3. Клиент должен вручную перевести деньги
4. Деньги зачисляются через обработку СМС (`src/routes/sms.js`) или вручную администратором

**Проблемы:**
- ❌ Требуется ручной перевод
- ❌ Нужно ждать зачисления (5-15 минут)
- ❌ Клиент может забыть указать номер кошелька в комментарии
- ❌ Нет мгновенного пополнения

---

### 2. Запись на тренировки через бота

#### 2.1. Групповые тренировки (Кулига)

**Места записи:**

1. **`confirmAndPayKuligaExistingGroupBooking()`** (строка 14262)
   - Запись в существующую групповую тренировку
   - Проверка баланса: НЕТ (только подтверждение)
   - Оплата: через кошелек (в `createKuligaExistingGroupBooking`)

2. **`createKuligaExistingGroupBooking()`** (строка 14647)
   - Создание бронирования для существующей группы
   - **Проверка баланса:** ДА (строка 14669)
   - **Если недостаточно:** показывается сообщение с предложением пополнить баланс
   - **Оплата:** списание с кошелька сразу (строка 14872)

3. **`createKuligaOwnGroupBooking()`** (строка 14362)
   - Создание бронирования для "У меня своя группа"
   - **Проверка баланса:** ДА (строка 14386)
   - **Если недостаточно:** показывается сообщение с предложением пополнить баланс
   - **Оплата:** списание с кошелька сразу (строка 14530)

4. **Запись через выбор тренировки** (строка ~5400)
   - В функции обработки выбора групповой тренировки
   - **Проверка баланса:** ДА (строка 5423)
   - **Если недостаточно:** показывается сообщение с предложением пополнить баланс
   - **Оплата:** списание с кошелька сразу (строка 5759)

#### 2.2. Индивидуальные тренировки

**Места записи:**

1. **Индивидуальные тренировки на естественном склоне** (строка ~7800)
   - Создание записи в `individual_training_sessions`
   - **Проверка баланса:** НЕ НАЙДЕНА (нужно проверить)
   - **Оплата:** списание с кошелька сразу (строка 7819)

2. **Индивидуальные тренировки Кулига** (строка ~9976)
   - Создание бронирования в `kuliga_bookings`
   - **Проверка баланса:** НЕ НАЙДЕНА (нужно проверить)
   - **Оплата:** списание с кошелька сразу (строка 9978)

#### 2.3. Абонементы

**Логика:**
- Если есть активный абонемент - используется он (строка 5416)
- Если абонемента нет - проверяется баланс кошелька

---

### 3. Интернет-эквайринг (текущая реализация)

**Где используется:**
- `src/routes/kuliga-booking.js` - для бронирований через сайт
- `src/services/payment/paymentProvider.js` - фабрика провайдеров
- `src/services/payment/providers/tochkaProvider.js` - провайдер Точка Банк

**Текущий flow:**
1. Создается транзакция в `kuliga_transactions` (status='pending', booking_id=NULL)
2. Данные бронирования сохраняются в `provider_raw_data.bookingData`
3. Вызывается `PaymentProvider.initPayment()` → получается `paymentUrl`
4. Клиент переходит по ссылке и оплачивает
5. Webhook от банка → `src/routes/kuliga-payment.js`
6. При успешной оплате создается бронирование из `provider_raw_data`

**Преимущества:**
- ✅ Мгновенная оплата
- ✅ Автоматическое создание бронирования после оплаты
- ✅ Уже реализовано и работает

---

## 🔧 РЕШЕНИЯ И ПЛАН РЕАЛИЗАЦИИ

### РЕШЕНИЕ 1: Пополнение кошелька через интернет-эквайринг

#### 1.1. Новая логика пополнения

**Текущая:** `handleTopUpBalance()` → показывает инструкцию по переводу

**Новая:**
1. Показывается текущий баланс
2. Предлагается ввести сумму пополнения (или выбрать из вариантов: 1000, 2000, 5000, 10000, другая сумма)
3. Создается транзакция в `kuliga_transactions`:
   - `type = 'wallet_refill'` (новый тип)
   - `amount = сумма пополнения`
   - `status = 'pending'`
   - `booking_id = NULL`
   - `provider_raw_data = { walletRefillData: { client_id, amount } }`
4. Вызывается `PaymentProvider.initPayment()` для пополнения кошелька
5. Клиент переходит по `paymentUrl` и оплачивает
6. Webhook обрабатывает пополнение:
   - Обновляет `status = 'completed'`
   - Пополняет баланс кошелька: `UPDATE wallets SET balance = balance + amount`
   - Создает запись в `transactions` (wallet_id, amount, type='refill')

#### 1.2. Обработка webhook для пополнения

**Где:** `src/routes/kuliga-payment.js`

**Логика:**
- Проверяется `type` транзакции
- Если `type = 'wallet_refill'`:
  - Пополняется баланс кошелька
  - Отправляется уведомление клиенту в Telegram
  - НЕ создается бронирование

---

### РЕШЕНИЕ 2: Прямая оплата тренировок при недостатке средств

#### 2.1. Новая логика записи на тренировки

**Текущая логика:**
```
Проверка баланса → Недостаточно → Предложение пополнить баланс
```

**Новая логика:**
```
Проверка баланса → Недостаточно → Предложение оплатить сразу через интернет-эквайринг
                                  → Оплата → Webhook → Создание бронирования
```

#### 2.2. Изменения в функциях записи

**Для всех функций записи на тренировки:**

1. **Проверка баланса** (остается)
2. **Если баланса недостаточно:**
   - Показывается сообщение с двумя вариантами:
     - "💳 Оплатить сразу" - переход на интернет-эквайринг
     - "💰 Пополнить кошелек" - пополнение кошелька (новая функция)
   - При выборе "Оплатить сразу":
     - Создается транзакция в `kuliga_transactions` (как на сайте)
     - Данные бронирования сохраняются в `provider_raw_data.bookingData`
     - Вызывается `PaymentProvider.initPayment()`
     - Клиент переходит по `paymentUrl`
     - После оплаты через webhook создается бронирование

3. **Если баланса достаточно:**
   - Текущая логика (списание с кошелька и создание бронирования сразу)

#### 2.3. Специфика для разных типов тренировок

**Групповые тренировки:**
- Временная блокировка мест (как на сайте)
- При успешной оплате - создание бронирования
- При неудачной оплате - освобождение мест

**Индивидуальные тренировки:**
- Блокировка слота (status='hold')
- При успешной оплате - создание бронирования и обновление слота (status='booked')
- При неудачной оплате - освобождение слота (status='available')

---

## 📝 ПОДРОБНЫЙ ПЛАН РЕАЛИЗАЦИИ

### ЭТАП 1: Пополнение кошелька через интернет-эквайринг

#### Задача 1.1: Обновить функцию `handleTopUpBalance()`

**Файл:** `src/bot/client-bot.js`

**Изменения:**
1. Добавить выбор суммы пополнения (кнопки: 1000, 2000, 5000, 10000, "Другая сумма")
2. При выборе суммы:
   - Создать транзакцию в `kuliga_transactions`:
     ```sql
     INSERT INTO kuliga_transactions (
         client_id, booking_id, type, amount, status, description, provider_raw_data
     ) VALUES ($1, NULL, 'wallet_refill', $2, 'pending', $3, $4)
     ```
   - Вызвать `PaymentProvider.initPayment()`:
     ```javascript
     const provider = PaymentProviderFactory.create();
     const payment = await provider.initPayment({
         orderId: `gornostyle72-wallet-${transactionId}`,
         amount: amount,
         description: `Пополнение кошелька на ${amount} руб.`,
         customerPhone: clientPhone,
         customerEmail: clientEmail,
         clientId: clientId,
         items: [{
             Name: `Пополнение кошелька`,
             Price: Math.round(amount * 100),
             Quantity: 1,
             Amount: Math.round(amount * 100),
             Tax: 'none',
             PaymentMethod: 'full_payment',
             PaymentObject: 'service'
         }]
     });
     ```
   - Отправить сообщение с кнопкой для перехода на оплату
   - Сохранить `transactionId` в состоянии пользователя

**Критерии успеха:**
- ✅ Клиент может выбрать сумму пополнения
- ✅ Создается транзакция с типом 'wallet_refill'
- ✅ Генерируется ссылка на оплату
- ✅ Клиент может перейти по ссылке и оплатить

---

#### Задача 1.2: Обработать ввод произвольной суммы

**Файл:** `src/bot/client-bot.js`

**Изменения:**
1. Добавить состояние `wallet_refill_amount` для ввода суммы
2. При выборе "Другая сумма":
   - Запросить ввод суммы
   - Валидировать сумму (минимум 100 руб., максимум 100000 руб.)
   - Создать транзакцию и перейти к оплате

**Критерии успеха:**
- ✅ Клиент может ввести произвольную сумму
- ✅ Сумма валидируется
- ✅ После ввода создается транзакция

---

#### Задача 1.3: Обновить обработку webhook для пополнения кошелька

**Файл:** `src/routes/kuliga-payment.js`

**Изменения:**
1. В функции обработки webhook добавить проверку типа транзакции:
   ```javascript
   if (transaction.type === 'wallet_refill') {
       // Логика пополнения кошелька
   } else {
       // Логика создания бронирования (существующая)
   }
   ```

2. Логика пополнения:
   - Обновить статус транзакции: `status = 'completed'`
   - Пополнить баланс кошелька:
     ```sql
     UPDATE wallets 
     SET balance = balance + $1, last_updated = CURRENT_TIMESTAMP 
     WHERE client_id = $2
     ```
   - Создать запись в `transactions`:
     ```sql
     INSERT INTO transactions (wallet_id, amount, type, description)
     VALUES ($1, $2, 'refill', 'Пополнение через интернет-эквайринг')
     ```
   - Отправить уведомление клиенту в Telegram:
     ```javascript
     bot.sendMessage(telegramId, 
         `✅ Кошелек успешно пополнен!\n\n` +
         `💰 Сумма: ${amount} руб.\n` +
         `💵 Новый баланс: ${newBalance} руб.`
     );
     ```

**Критерии успеха:**
- ✅ Webhook корректно обрабатывает пополнение кошелька
- ✅ Баланс кошелька обновляется
- ✅ Клиент получает уведомление в Telegram

---

### ЭТАП 2: Прямая оплата тренировок при недостатке средств

#### Задача 2.1: Создать универсальную функцию для оплаты тренировки

**Файл:** `src/bot/client-bot.js`

**Новая функция:** `initTrainingPayment(chatId, state, bookingData)`

**Параметры:**
- `chatId` - ID чата
- `state` - состояние пользователя
- `bookingData` - данные для бронирования (зависят от типа тренировки)

**Логика:**
1. Создать транзакцию в `kuliga_transactions`:
   ```sql
   INSERT INTO kuliga_transactions (
       client_id, booking_id, type, amount, status, description, provider_raw_data
   ) VALUES ($1, NULL, 'payment', $2, 'pending', $3, $4)
   ```
2. Сохранить `bookingData` в `provider_raw_data`
3. Вызвать `PaymentProvider.initPayment()`
4. Отправить сообщение с кнопкой для перехода на оплату
5. Сохранить `transactionId` в состоянии

**Критерии успеха:**
- ✅ Функция создает транзакцию
- ✅ Генерируется ссылка на оплату
- ✅ Клиент получает сообщение с кнопкой

---

#### Задача 2.2: Обновить `createKuligaExistingGroupBooking()`

**Файл:** `src/bot/client-bot.js` (строка 14647)

**Изменения:**
1. При недостатке средств (строка 14669):
   - Вместо простого сообщения показать кнопки:
     - "💳 Оплатить сразу" - переход на интернет-эквайринг
     - "💰 Пополнить кошелек" - пополнение кошелька
   - При выборе "Оплатить сразу":
     - Подготовить `bookingData`:
       ```javascript
       const bookingData = {
           client_id: state.data.client_id,
           booking_type: 'group',
           group_training_id: state.data.selected_training_id,
           date: training.date,
           start_time: training.start_time,
           end_time: training.end_time,
           sport_type: training.sport_type,
           participants_count: participants.length,
           participants_names: participantsNames,
           participants_birth_years: participantsBirthYears,
           price_total: totalPrice,
           price_per_person: training.price_per_person,
           location: location,
           // Дополнительные данные
           client_name: clientData.full_name,
           client_phone: clientData.phone,
           client_email: clientData.email
       };
       ```
     - Вызвать `initTrainingPayment(chatId, state, bookingData)`
     - Временно заблокировать места (как на сайте):
       ```sql
       UPDATE kuliga_group_trainings
       SET current_participants = current_participants + $1
       WHERE id = $2
       ```
     - Сохранить состояние: `state.step = 'kuliga_group_payment_pending'`

**Критерии успеха:**
- ✅ При недостатке средств показываются кнопки выбора
- ✅ При выборе "Оплатить сразу" создается транзакция
- ✅ Места временно блокируются
- ✅ Клиент получает ссылку на оплату

---

#### Задача 2.3: Обновить `createKuligaOwnGroupBooking()`

**Файл:** `src/bot/client-bot.js` (строка 14362)

**Изменения:**
- Аналогично задаче 2.2
- Подготовить `bookingData` для создания своей группы

**Критерии успеха:**
- ✅ Аналогично задаче 2.2

---

#### Задача 2.4: Обновить функцию записи через выбор тренировки

**Файл:** `src/bot/client-bot.js` (строка ~5400)

**Изменения:**
- При недостатке средств (строка 5423):
  - Показать кнопки выбора (как в задаче 2.2)
  - При выборе "Оплатить сразу" - вызвать `initTrainingPayment()`
  - Подготовить `bookingData` для групповой тренировки

**Критерии успеха:**
- ✅ Аналогично задаче 2.2

---

#### Задача 2.5: Обновить функцию записи на индивидуальные тренировки

**Файл:** `src/bot/client-bot.js`

**Места:**
1. Индивидуальные тренировки на естественном склоне (строка ~7800)
2. Индивидуальные тренировки Кулига (строка ~9976)

**Изменения:**
1. Добавить проверку баланса перед созданием записи
2. При недостатке средств:
   - Показать кнопки выбора
   - При выборе "Оплатить сразу":
     - Подготовить `bookingData` для индивидуальной тренировки
     - Заблокировать слот (status='hold')
     - Вызвать `initTrainingPayment()`

**Критерии успеха:**
- ✅ Проверка баланса добавлена
- ✅ При недостатке средств показываются кнопки
- ✅ При оплате слот блокируется

---

#### Задача 2.6: Обновить обработку webhook для создания бронирований из бота

**Файл:** `src/routes/kuliga-payment.js`

**Изменения:**
1. В существующей логике создания бронирования (строка ~345):
   - Проверить источник бронирования (из сайта или из бота)
   - Если из бота:
     - Отправить уведомление в Telegram клиенту
     - Обновить состояние пользователя в боте (если нужно)

2. Добавить отправку уведомления в Telegram:
   ```javascript
   // Получить telegram_id клиента
   const clientResult = await pool.query(
       'SELECT telegram_id FROM clients WHERE id = $1',
       [bookingData.client_id]
   );
   
   if (clientResult.rows[0]?.telegram_id) {
       await bot.sendMessage(
           clientResult.rows[0].telegram_id,
           `✅ Тренировка успешно оплачена и забронирована!\n\n` +
           `📅 Дата: ${formattedDate}\n` +
           `⏰ Время: ${formattedTime}\n` +
           `💰 Сумма: ${amount} руб.`
       );
   }
   ```

**Критерии успеха:**
- ✅ Бронирование создается после успешной оплаты
- ✅ Клиент получает уведомление в Telegram
- ✅ Места/слоты корректно обновляются

---

### ЭТАП 3: Обработка ошибок и edge cases

#### Задача 3.1: Обработка неудачной оплаты

**Где:** `src/routes/kuliga-payment.js`

**Логика:**
- При статусе `FAILED` или `REFUNDED`:
  - Освободить заблокированные места/слоты
  - Отправить уведомление клиенту в Telegram
  - Обновить статус транзакции

**Критерии успеха:**
- ✅ Места освобождаются при неудачной оплате
- ✅ Клиент получает уведомление

---

#### Задача 3.2: Таймаут ожидания оплаты

**Логика:**
- Если оплата не произошла в течение 30 минут:
  - Освободить заблокированные места/слоты
  - Отправить напоминание клиенту
  - Обновить статус транзакции на 'expired'

**Критерии успеха:**
- ✅ Места освобождаются при таймауте
- ✅ Клиент получает напоминание

---

#### Задача 3.3: Проверка доступности мест/слотов перед созданием бронирования

**Где:** `src/routes/kuliga-payment.js` (webhook handler)

**Логика:**
- Перед созданием бронирования проверить:
  - Для групповых: доступность мест
  - Для индивидуальных: доступность слота
- Если места/слот уже заняты:
  - Не создавать бронирование
  - Отправить уведомление клиенту
  - Вернуть деньги (refund)

**Критерии успеха:**
- ✅ Проверка доступности перед созданием бронирования
- ✅ Обработка случая, когда места уже заняты

---

## 🔄 ИЗМЕНЕНИЯ В ИНСТРУКЦИЯХ

### 1. Обновить инструкции для клиентов

**Где:** Сообщения в боте

**Изменения:**
- В функции `handleTopUpBalance()`:
  - Убрать инструкцию по ручному переводу
  - Добавить информацию о пополнении через интернет-эквайринг
  - Оставить возможность ручного пополнения (как альтернативу)

- В сообщениях о недостатке средств:
  - Добавить информацию о возможности оплаты сразу

---

### 2. Обновить документацию

**Файлы для обновления:**
- `docs/PAYMENT_FLOW_COMPARISON.md` - добавить описание flow для бота
- Создать `docs/BOT_PAYMENT_FLOW.md` - описание flow оплаты в боте

---

## 📊 ПРОЕКТНЫЙ СТАТУС

### ✅ ВЫПОЛНЕНО:
- Анализ текущей системы
- Определение всех мест записи на тренировки
- Составление детального плана
- **ЭТАП 1: Пополнение кошелька через интернет-эквайринг (ЗАВЕРШЕНО)**
  - ✅ Обновлена функция `handleTopUpBalance()` - добавлен выбор суммы пополнения
  - ✅ Реализована функция `handleWalletRefillPayment()` - создание платежа для пополнения
  - ✅ Добавлена обработка выбора суммы (3000, 5000, 6000, 10000, другая сумма)
  - ✅ Добавлена обработка ввода произвольной суммы (1 ₽ - 50,000 ₽)
  - ✅ Обновлен webhook handler для обработки пополнения кошелька (type=payment с walletRefillData)
  - ✅ Добавлена отправка уведомления клиенту в Telegram после успешного пополнения
  - ✅ Добавлена поддержка формата orderId `gornostyle72-wallet-{transactionId}`
- **ЭТАП 2: Прямая оплата тренировок при недостатке средств (ЗАВЕРШЕНО)**
  - ✅ Задача 2.1: Создана универсальная функция `initTrainingPayment()` для оплаты тренировок
  - ✅ Задача 2.2: Обновлена `createKuligaExistingGroupBooking()` - добавлена прямая оплата
  - ✅ Задача 2.3: Обновлена `createKuligaOwnGroupBooking()` - добавлена прямая оплата
  - ✅ Задача 2.4: Обновлена функция записи через выбор тренировки (групповые на тренажере)
  - ✅ Задача 2.5: Обновлена функция записи на индивидуальные тренировки (тренажер и естественный склон)
  - ✅ Задача 2.6: Обновлена обработка webhook для создания бронирований из бота с уведомлениями
  - ✅ Реализована логика частичной оплаты (если есть частичный баланс, оплачивается только разница)
  - ✅ Добавлена блокировка слотов на 5 минут при инициации оплаты (hold_until)
  - ✅ Исправлены проблемы с отображением слотов на сайте (учет истекших hold)
  - ✅ Исправлены ошибки с двойным release клиента базы данных
  - ✅ Улучшен UX: завтрашняя дата в примере, кликабельная для копирования

- **ЭТАП 3: Обработка ошибок и edge cases (ЗАВЕРШЕНО)**
  - ✅ Задача 3.1: Обработка неудачной оплаты - освобождение мест/слотов и уведомление клиенту в Telegram
  - ✅ Задача 3.2: Таймаут ожидания оплаты - освобождение мест/слотов через 30 минут с уведомлениями
  - ✅ Задача 3.3: Проверка доступности мест/слотов перед созданием бронирования с возвратом средств

### 🚧 В ПРОЦЕССЕ:
- Нет активных задач

### ❌ НЕ ВЫПОЛНЕНО:
- Обновление инструкций для клиентов

---

## 🎯 ПРИОРИТЕТЫ

1. **Высокий:** Пополнение кошелька через интернет-эквайринг (ЭТАП 1)
2. **Высокий:** Прямая оплата групповых тренировок (ЭТАП 2, задачи 2.2-2.4)
3. **Средний:** Прямая оплата индивидуальных тренировок (ЭТАП 2, задача 2.5)
4. **Средний:** Обработка ошибок и edge cases (ЭТАП 3)

---

**Дата создания плана:** 27.12.2025
