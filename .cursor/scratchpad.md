# 📋 ПЛАН РАЗВИТИЯ: Интеграция сайта и Telegram бота + Групповые тренировки + Система уведомлений

**Дата создания:** 15.12.2025  
**Режим:** Планировщик

---

## 🎯 ЦЕЛИ И ЗАДАЧИ

### ✅ ЗАДАЧА 0: Настроить оплату для всех форм бронирования (ВЫПОЛНЕНО)
**Проблема:** Для индивидуальных тренировок бронирование создается только после успешной оплаты (через webhook), а для групповых тренировок и программ бронирование создается сразу, до оплаты.

**Требования:**
- ✅ Переделать `createGroupBooking()` на ту же логику, что и `createIndividualBooking()`
- ✅ Переделать `createProgramBooking()` на ту же логику, что и `createIndividualBooking()`
- ✅ Бронирование должно создаваться ТОЛЬКО после успешной оплаты через webhook
- ✅ Данные бронирования должны сохраняться в `provider_raw_data` транзакции
- ✅ Для групповых тренировок реализована временная блокировка мест (увеличение `current_participants` на время оплаты)

**Текущая ситуация:**
- ✅ Индивидуальные тренировки: бронирование создается после оплаты (правильно)
- ✅ Групповые тренировки: бронирование создается после оплаты (исправлено)
- ✅ Программы: бронирование создается после оплаты (исправлено)

### Задача 1: Идентификация клиентов через Telegram бот при записи через сайт
**Проблема:** Когда клиент записывается через сайт, он не идентифицируется в Telegram боте. Если клиент позже открывает бота, ему приходится проходить регистрацию заново.

**Требования:**
- При переходе по ссылке на бота из формы бронирования (до оплаты) - создать клиента в БД с предзаполненными данными
- При нажатии "Старт" в боте - добавить данные из Telegram (telegram_id, username) к существующему клиенту
- Если клиент ушел с сайта, а потом кто-то скинул ему ссылку на бота - он должен просто нажать "Старт" и не проходить регистрацию заново
- После оплаты на странице успеха тоже есть ссылка на бота - должна работать аналогично

### Задача 2: Бронирование групповых тренировок через сайт
**Уточнения:**
- ✅ Для групповых тренировок (2, 3, 5 участников) форма уже реализована - количество участников определяется контейнером (прайсом)
- ✅ Для регулярных программ есть возможность добавлять участников (уже реализовано)
- **Задача:** Проверить, что все работает корректно, и при необходимости доработать

### Задача 3: Система уведомлений для всех видов бронирований
**Уточнения:**
- Email уведомления **только для клиентов** при создании бронирования
- Email уведомления для клиентов при отмене бронирования
- Telegram уведомления для администратора и инструктора (они уже зарегистрированы в Telegram)
- Напоминания о предстоящих тренировках - использовать существующую cron задачу (21:00), которая уже работает для клиентов из бота
- Использовать существующий `EmailService` (уже реализован для сертификатов)
- Сделать шаблоны email удобными и комфортными для чтения

---

## 📊 ТЕКУЩАЯ СИСТЕМА (АНАЛИЗ)

### 1. Создание клиентов при бронировании через сайт

**Где:** `src/routes/kuliga-booking.js` → функция `upsertClient()`

**Логика:**
1. Поиск клиента по нормализованному телефону
2. Если найден:
   - Обновляется `email` (если был пустой)
   - Обновляется `birth_date` (если был временным `1900-01-01`)
   - Возвращается `{ id, telegram_id }`
3. Если не найден:
   - Создается новый клиент с обязательным `email`
   - `telegram_id` = `NULL` (не заполняется!)
   - `birth_date` = указанная дата или `1900-01-01`

**Проблема:** `telegram_id` не заполняется при создании через сайт, поэтому клиент не идентифицируется в боте.

---

### 2. Регистрация в Telegram боте

**Где:** `src/bot/client-bot.js` → функция `registerClient()` и обработчик `/start`

**Логика при `/start`:**
1. Проверка наличия клиента по `telegram_id`
2. Если клиента нет:
   - Извлекается реферальный код из `/start REFERRAL_CODE` (если есть)
   - Показывается приветственное сообщение
   - Сохраняется состояние `wait_start` с данными из Telegram
3. Если клиент найден:
   - Показывается главное меню

**Логика при регистрации (`registerClient`):**
1. Проверка наличия клиента по `telegram_id` (приоритет)
2. Если не найден по `telegram_id`:
   - Поиск по телефону
   - Если найден по телефону:
     - Обновляется `telegram_id`, `telegram_username`, `nickname`
     - Обновляются другие поля (если изменились)
   - Если не найден:
     - Создается новый клиент

**Проблема:** Если клиент создан через сайт (без `telegram_id`), но с телефоном, то при регистрации в боте он должен найтись по телефону. Но для этого нужно, чтобы клиент ввел тот же телефон, что и на сайте.

---

### 3. Deep Links в Telegram

**Текущая поддержка:**
- `/start REFERRAL_CODE` - для реферальных ссылок
- Можно использовать `/start CLIENT_ID` или `/start PHONE_HASH` для идентификации

**Где используется:**
- `src/bot/client-bot.js:12348` - обработчик `/start(.*)`
- Извлекается параметр: `match[1]` (все после `/start`)

---

### 4. Уведомления

**Email уведомления:**
- `src/services/emailService.js` - универсальный сервис
- Метод `sendEmail(recipientEmail, subject, htmlContent, attachments)`
- Используется для сертификатов: `sendCertificateEmail()`

**Telegram уведомления:**
- `src/services/notification-service.js` - сервис для напоминаний о тренировках
- `src/bot/admin-notify.js` - уведомления администраторам и инструкторам
- Отправка через Telegram Bot API

**Текущие уведомления при бронировании:**
- В `src/routes/kuliga-payment.js` (webhook handler) - НЕ отправляются уведомления
- В `src/routes/kuliga-stuck-payments.js` - отправляются email уведомления при ручном создании бронирования
- В `src/bot/admin-notify.js` - есть функции `notifyAdminNaturalSlopeTrainingBooking()` и `notifyInstructorKuligaTrainingBooking()`

---

### 5. Групповые тренировки

**Текущая реализация:**
- `src/routes/kuliga-booking.js` → `createGroupBooking()` - для записи на существующую групповую тренировку
- `src/routes/kuliga-booking.js` → `createProgramBooking()` - для записи на программу (создает групповую тренировку автоматически)
- Оба метода создают бронирование сразу (без оплаты)

**Проблема:** Нет поддержки выбора количества участников (2, 3, 4 и т.д.) через сайт. Только через бота.

---

## 🔧 РЕШЕНИЯ И ПЛАН РЕАЛИЗАЦИИ

### ЗАДАЧА 1: Идентификация клиентов через Telegram бот

#### Решение 1.1: Deep Link с идентификатором клиента

**Идея:** При переходе по ссылке на бота из формы бронирования передавать идентификатор клиента (или хеш телефона) в deep link.

**Формат deep link:**
```
https://t.me/Ski_Instruktor72_bot?start=client_{CLIENT_ID}
или
https://t.me/Ski_Instruktor72_bot?start=phone_{PHONE_HASH}
```

**Преимущества:**
- Простая реализация
- Не требует изменений в логике регистрации
- Работает даже если клиент ушел с сайта

**Недостатки:**
- Нужно знать `client_id` до создания клиента (можно использовать временный идентификатор)
- Хеш телефона может быть небезопасным

**Альтернатива:** Использовать временный токен (UUID), который связывает клиента с сессией бронирования.

#### Решение 1.2: Предварительная регистрация клиента при клике на ссылку

**Идея:** При клике на ссылку бота в форме бронирования вызывать API endpoint `/api/kuliga/pre-register-client`, который создает клиента в БД с предзаполненными данными. Затем в deep link передавать `client_id`.

**Текущий endpoint:** `/api/kuliga/pre-register-client` уже существует! (строка 862 в `kuliga-booking.js`)

**Логика:**
1. Клиент заполняет форму бронирования
2. Клиент нажимает на ссылку бота
3. JavaScript отправляет POST на `/api/kuliga/pre-register-client` с данными формы
4. Сервер создает/обновляет клиента в БД (без `telegram_id`)
5. Возвращается `client_id`
6. Deep link формируется как `https://t.me/Ski_Instruktor72_bot?start=client_{CLIENT_ID}`
7. При `/start client_{CLIENT_ID}` в боте:
   - Извлекается `client_id`
   - Ищется клиент по `client_id`
   - Если найден и `telegram_id` пустой:
     - Обновляется `telegram_id` из Telegram
     - Показывается сообщение: "Вы успешно подключены! Ваши данные сохранены."
   - Если `telegram_id` уже заполнен:
     - Проверяется, совпадает ли с текущим `telegram_id`
     - Если совпадает - показывается главное меню
     - Если не совпадает - показывается предупреждение

**Преимущества:**
- Клиент создается сразу при клике на ссылку
- Не нужно вводить данные заново в боте
- Работает даже если клиент ушел с сайта

**Недостатки:**
- Нужно вызывать API при клике на ссылку (может быть медленно)
- Если клиент не нажмет на ссылку, клиент все равно создастся при бронировании

#### Решение 1.3: Идентификация по телефону при регистрации в боте (РЕКОМЕНДУЕТСЯ) ⭐

**Идея:** Улучшить логику регистрации в боте, чтобы при вводе телефона клиент автоматически находился в БД и связывался с существующей записью.

**Текущая логика:** В `registerClient()` уже есть поиск по телефону, но только если клиент не найден по `telegram_id`.

**Улучшение:**
1. При `/start` извлекать параметр (если есть)
2. Если параметр = `client_{CLIENT_ID}`:
   - Сохранять `client_id` в состоянии пользователя
   - При регистрации (ввод телефона) проверять:
     - Если `client_id` сохранен и клиент найден по `client_id`:
       - Обновлять `telegram_id` этого клиента
       - Пропускать ввод остальных данных (использовать данные из БД)
     - Если `client_id` не сохранен:
       - Искать клиента по телефону (как сейчас)
       - Если найден - обновлять `telegram_id`
       - Если не найден - создавать нового

**Преимущества:**
- Минимальные изменения в коде
- Работает даже без deep link (поиск по телефону)
- Безопасно (не передаем чувствительные данные в URL)

**Недостатки:**
- Клиент все равно должен ввести телефон в боте
- Но можно сделать так, чтобы если клиент найден по телефону, он сразу попадал в меню без полной регистрации

#### РЕКОМЕНДУЕМОЕ РЕШЕНИЕ: Комбинация Решения 1.2 и 1.3

1. **При клике на ссылку бота в форме бронирования:**
   - Вызывать `/api/kuliga/pre-register-client` (уже существует)
   - Сохранять `client_id` в `sessionStorage` или `localStorage`
   - Формировать deep link: `https://t.me/Ski_Instruktor72_bot?start=client_{CLIENT_ID}`

2. **При `/start client_{CLIENT_ID}` в боте:**
   - Сохранять `client_id` в состоянии пользователя
   - При вводе телефона:
     - Если `client_id` сохранен - искать клиента по `client_id`
     - Если найден и `telegram_id` пустой:
       - Обновлять `telegram_id`
       - Использовать данные из БД (имя, email, дата рождения)
       - Показывать сообщение: "Добро пожаловать! Ваши данные из формы бронирования сохранены."
     - Если `client_id` не сохранен - искать по телефону (как сейчас)

3. **После успешной оплаты:**
   - На странице успеха формировать deep link с `client_id` из транзакции
   - Аналогичная логика в боте

---

### ЗАДАЧА 2: Бронирование групповых тренировок

#### Решение 2.1: Добавить выбор количества участников в форму

**Текущая ситуация:**
- `createGroupBooking()` принимает `participantsCount` и `participantsNames`
- Но в форме нет выбора количества участников (только для индивидуальных тренировок)

**План:**
1. Добавить поле выбора количества участников в форму групповых тренировок
2. При выборе количества участников показывать поля для ввода имен участников
3. При отправке формы передавать `participantsCount` и `participantsNames` в API

**Где изменить:**
- `public/js/kuliga-booking.js` - добавить логику выбора количества участников
- `views/kuliga-booking.ejs` - добавить UI для выбора количества участников

#### Решение 2.2: Бронирование на регулярные программы

**Текущая ситуация:**
- `createProgramBooking()` уже существует
- Но нужно проверить, работает ли оно через сайт

**План:**
1. Проверить, есть ли форма для бронирования программ на сайте
2. Если нет - добавить форму
3. При выборе программы показывать доступные даты и время
4. При бронировании создавать групповую тренировку автоматически (как сейчас)

---

### ЗАДАЧА 3: Система уведомлений

#### Решение 3.1: Email уведомления при создании бронирования

**Где отправлять:**
- В `src/routes/kuliga-payment.js` после успешного создания бронирования (webhook handler)
- В `src/routes/kuliga-stuck-payments.js` при ручном создании (уже реализовано)

**Что отправлять:**
1. **Клиенту (Email):**
   - Подтверждение записи
   - Детали тренировки (дата, время, инструктор, место, стоимость)
   - Ссылка на бота для получения уведомлений
   - Красивый и удобный шаблон

2. **Инструктору (Telegram):**
   - Уведомление о новой записи
   - Детали клиента и тренировки
   - Использовать `notifyInstructorKuligaTrainingBooking()` из `admin-notify.js`

3. **Администратору (Telegram):**
   - Уведомление о новой записи
   - Использовать `notifyAdminNaturalSlopeTrainingBooking()` из `admin-notify.js`

**Шаблоны email:**
- Использовать существующий `EmailService.sendEmail()`
- Создать шаблоны для разных типов уведомлений

#### Решение 3.2: Telegram уведомления для администратора и инструктора

**Где отправлять:**
- В `src/routes/kuliga-payment.js` после успешного создания бронирования
- Использовать существующие функции из `admin-notify.js`

**Что отправлять:**
- Инструктору: `notifyInstructorKuligaTrainingBooking()` - уже реализовано
- Администратору: `notifyAdminNaturalSlopeTrainingBooking()` - уже реализовано

#### Решение 3.3: Уведомления об отмене бронирования

**Где отправлять:**
- При отмене бронирования (webhook с статусом `FAILED` или `REFUNDED`)
- При ручной отмене администратором

**Что отправлять:**
- Email клиенту об отмене (красивый шаблон)
- Telegram уведомление инструктору об отмене (через `admin-notify.js`)
- Telegram уведомление администратору об отмене (через `admin-notify.js`)

#### Решение 3.4: Напоминания о предстоящих тренировках

**Текущая ситуация:**
- `src/services/scheduler.js` - cron задача в 21:00 отправляет напоминания через `notificationService.sendTomorrowReminders()`
- Нужно проверить, работает ли это для `kuliga_bookings` (бронирования через сайт)

**План:**
1. Проверить метод `sendTomorrowReminders()` в `notification-service.js`
2. Убедиться, что он включает `kuliga_bookings` в выборку
3. Если нет - добавить логику для `kuliga_bookings`
4. Использовать существующий `NotificationService` и cron задачу

---

## 📝 ПОДРОБНЫЙ ПЛАН РЕАЛИЗАЦИИ

### ЭТАП 1: Идентификация клиентов через Telegram бот

#### Задача 1.1: Обновить форму бронирования для предварительной регистрации

**Файлы:**
- `public/js/kuliga-booking.js`
- `views/kuliga-booking.ejs`

**Действия:**
1. При клике на ссылку бота вызывать `/api/kuliga/pre-register-client`
2. Сохранять `client_id` в `sessionStorage`
3. Формировать deep link с `client_id`
4. Открывать бота с deep link

**Критерий успеха:** При клике на ссылку бота клиент создается в БД, deep link содержит `client_id`.

---

#### Задача 1.2: Обновить обработчик `/start` в боте

**Файлы:**
- `src/bot/client-bot.js`

**Действия:**
1. При `/start client_{CLIENT_ID}` сохранять `client_id` в состоянии пользователя
2. При регистрации (ввод телефона):
   - Если `client_id` сохранен - искать клиента по `client_id`
   - Если найден и `telegram_id` пустой:
     - Обновлять `telegram_id`
     - Использовать данные из БД
     - Показывать сообщение о успешной идентификации
   - Если `client_id` не сохранен - искать по телефону (как сейчас)

**Критерий успеха:** При `/start client_{CLIENT_ID}` и вводе телефона клиент идентифицируется автоматически.

---

#### Задача 1.3: Обновить страницу успешной оплаты

**Файлы:**
- `views/booking-success.ejs`
- `src/routes/kuliga-payment.js` (для передачи `client_id`)

**Действия:**
1. При успешной оплате получать `client_id` из транзакции
2. Формировать deep link с `client_id`
3. Показывать ссылку на бота с deep link

**Критерий успеха:** На странице успеха ссылка на бота содержит `client_id`.

---

### ЭТАП 2: Бронирование групповых тренировок

#### Задача 2.1: Добавить выбор количества участников в форму групповых тренировок

**Файлы:**
- `public/js/kuliga-booking.js`
- `views/kuliga-booking.ejs`

**Действия:**
1. Добавить поле выбора количества участников (2, 3, 4, 5, 6)
2. При выборе количества показывать поля для ввода имен участников
3. При отправке формы передавать `participantsCount` и `participantsNames` в API

**Критерий успеха:** В форме групповых тренировок можно выбрать количество участников и ввести их имена.

---

#### Задача 2.2: Проверить и доработать бронирование программ

**Файлы:**
- `public/js/kuliga-booking.js`
- `views/kuliga-booking.ejs`
- `src/routes/kuliga-booking.js` (проверить `createProgramBooking`)

**Действия:**
1. Проверить, есть ли форма для бронирования программ
2. Если нет - добавить форму
3. При выборе программы показывать доступные даты и время
4. Проверить работу `createProgramBooking()`

**Критерий успеха:** Можно забронировать место в программе через сайт.

---

### ЭТАП 3: Система уведомлений

#### Задача 3.1: Email уведомления при создании бронирования

**Файлы:**
- `src/routes/kuliga-payment.js` (webhook handler)
- `src/services/emailService.js` (уже существует)

**Действия:**
1. После успешного создания бронирования отправлять email клиенту
2. Отправлять email инструктору (использовать `notifyInstructorKuligaTrainingBooking()`)
3. Отправлять email администратору (использовать `notifyAdminNaturalSlopeTrainingBooking()`)
4. Создать шаблоны email для разных типов уведомлений

**Критерий успеха:** При создании бронирования отправляются email уведомления клиенту, инструктору и администратору.

---

#### Задача 3.2: Telegram уведомления для администратора и инструктора

**Файлы:**
- `src/routes/kuliga-payment.js` (webhook handler)
- `src/bot/admin-notify.js` (уже реализовано)

**Действия:**
1. После успешного создания бронирования вызывать `notifyInstructorKuligaTrainingBooking()`
2. Вызывать `notifyAdminNaturalSlopeTrainingBooking()`
3. Проверить, что функции работают корректно

**Критерий успеха:** Инструктор и администратор получают Telegram уведомления при создании бронирования.

---

#### Задача 3.3: Уведомления об отмене бронирования

**Файлы:**
- `src/routes/kuliga-payment.js` (webhook handler для статуса `FAILED` или `REFUNDED`)

**Действия:**
1. При отмене бронирования отправлять email клиенту
2. Отправлять email инструктору
3. Отправлять Telegram уведомление клиенту (если есть `telegram_id`)

**Критерий успеха:** При отмене бронирования отправляются уведомления клиенту и инструктору.

---

#### Задача 3.4: Напоминания о предстоящих тренировках

**Файлы:**
- `src/services/notification-service.js` (проверить и доработать)
- `src/services/scheduler.js` (проверить расписание)

**Действия:**
1. Проверить, отправляются ли напоминания для `kuliga_bookings`
2. Если нет - добавить логику отправки напоминаний
3. Использовать существующий `NotificationService`

**Критерий успеха:** Клиенты получают напоминания о предстоящих тренировках за день до тренировки.

---

## 🎯 ПРИОРИТЕТЫ

1. **НАИВЫСШИЙ ПРИОРИТЕТ:**
   - ⚠️ **Задача 0: Настроить оплату для всех форм бронирования** (групповые тренировки и программы)

2. **ВЫСОКИЙ ПРИОРИТЕТ:**
   - Задача 1.2: Обновить обработчик `/start` в боте (идентификация клиентов)
   - Задача 3.1: Email уведомления при создании бронирования

2. **СРЕДНИЙ ПРИОРИТЕТ:**
   - Задача 1.1: Обновить форму бронирования для предварительной регистрации
   - Задача 2.1: Добавить выбор количества участников в форму групповых тренировок
   - Задача 3.2: Telegram уведомления для клиентов

3. **НИЗКИЙ ПРИОРИТЕТ:**
   - Задача 1.3: Обновить страницу успешной оплаты
   - Задача 2.2: Проверить и доработать бронирование программ
   - Задача 3.3: Уведомления об отмене бронирования
   - Задача 3.4: Напоминания о предстоящих тренировках

---

## 📊 ТЕКУЩИЙ СТАТУС

**Статус:** ✅ ВСЕ ЗАДАЧИ ВЫПОЛНЕНЫ И ПРОТЕСТИРОВАНЫ

**Дата последнего обновления:** 18.12.2025

### ✅ ВЫПОЛНЕНО:

1. **✅ Задача 1.2: Deep link с client_id** - РЕАЛИЗОВАНО
   - Обработка `/start client_{CLIENT_ID}` в боте
   - Автоматическая идентификация клиента при регистрации в боте
   - Обновление `telegram_id` для существующих клиентов

2. **✅ Задача 1.3: Страница успешной оплаты с deep link** - РЕАЛИЗОВАНО
   - Deep link формируется на странице успеха с `client_id`
   - Передача `client_id` через query параметры в URL

3. **✅ Задача 3.1: Email уведомления при создании бронирования** - РЕАЛИЗОВАНО
   - HTML шаблоны для подтверждения бронирования
   - Отправка email клиенту после успешной оплаты
   - Интеграция в `kuliga-payment.js`

4. **✅ Задача 3.2: Telegram уведомления для администратора и инструктора** - РЕАЛИЗОВАНО
   - Уведомления инструктору при создании бронирования
   - Уведомления администратору при создании бронирования
   - Исправлена проблема с получением `instructor_id` из слота

5. **✅ Задача 3.3: Email уведомления об отмене бронирования** - РЕАЛИЗОВАНО
   - HTML шаблон для отмены бронирования
   - Отправка email при отмене/возврате платежа

6. **✅ Задача 4.1: HTML шаблоны для email** - РЕАЛИЗОВАНО
   - `booking-confirmation.html` - подтверждение бронирования
   - `training-reminder.html` - напоминание о тренировке
   - `booking-cancellation.html` - отмена бронирования

7. **✅ Задача 4.2: Расширение notification-service.js для email** - РЕАЛИЗОВАНО
   - Метод `sendEmailNotification()` добавлен
   - Модифицирован `sendTrainingReminders()` для отправки email клиентам без `telegram_id`

8. **✅ Задача 4.3: Email при создании бронирования** - РЕАЛИЗОВАНО
   - Отправка email после успешной оплаты
   - Использование шаблона `booking-confirmation.html`

9. **✅ Задача 4.4: Email при отмене бронирования** - РЕАЛИЗОВАНО
   - Отправка email при отмене/возврате
   - Использование шаблона `booking-cancellation.html`

10. **✅ Создание referral_code и кошелька при регистрации через сайт** - РЕАЛИЗОВАНО
    - Автоматическое создание `referral_code` для новых клиентов
    - Автоматическое создание кошелька для новых клиентов
    - Проверка и создание для существующих клиентов при обновлении

11. **✅ Исправлена стратегия отправки email** - РЕАЛИЗОВАНО
    - SMTP Yandex - основной метод для всех клиентов
    - Resend - только как fallback при ошибках SMTP
    - Увеличенные таймауты для Yandex адресов того же аккаунта

12. **✅ Исправлена проблема с ботом инструкторов** - РЕАЛИЗОВАНО
    - Исправлена загрузка токена в PM2
    - Улучшено логирование инициализации и ошибок polling

16. **✅ Задача 1.1: Предварительная регистрация** - РЕАЛИЗОВАНО И ПРОТЕСТИРОВАНО
   - Endpoint `/api/kuliga/pre-register-client` работает
   - Deep link формируется с `client_id` на странице успеха
   - Клиент идентифицируется в боте без повторной регистрации

17. **✅ Задача 3.4: Напоминания о предстоящих тренировках** - РЕАЛИЗОВАНО И ПРОТЕСТИРОВАНО
   - Cron задача работает (21:00)
   - Напоминания отправляются для всех типов бронирований (включая `kuliga_bookings`)
   - Email отправляется клиентам без `telegram_id`
   - Telegram отправляется клиентам с `telegram_id`

18. **✅ Тестирование уведомлений** - ВЫПОЛНЕНО
   - Протестированы уведомления при бронировании через сайт
   - Протестированы уведомления при бронировании через бота
   - Все уведомления работают корректно (Email, Telegram админу, Telegram инструктору)
   - Исправлен формат времени в уведомлениях (ЧЧ:ММ вместо ЧЧ:ММ:СС)
   - Исправлен location в уведомлениях (используется реальный location инструктора)

19. **✅ Исправление location в уведомлениях** - РЕАЛИЗОВАНО
   - Location берется из базы данных инструктора
   - Правильное отображение места в уведомлениях (Воронинские горки / Кулига-Клуб)

### 🚧 В ПРОЦЕССЕ / ТРЕБУЕТ ПРОВЕРКИ:

**Нет задач, требующих проверки!**

13. **✅ Задача 0: Оплата для программ** - РЕАЛИЗОВАНО И ПРОТЕСТИРОВАНО
   - Исправлена ошибка с `groupTrainingIdToUse` → `groupTraining.id`
   - Исправлена ошибка с отсутствующим `price_per_person` в SELECT запросе
   - Исправлена ошибка с переменной `participants`
   - Добавлена валидация и логирование
   - Оплата программ работает корректно

14. **✅ Задача 2.2: Проверить бронирование программ** - РЕАЛИЗОВАНО
   - Бронирование программ через сайт работает
   - Оплата проходит успешно
   - Все ошибки исправлены

15. **✅ Задача 2.1: Выбор количества участников** - УЖЕ РЕАЛИЗОВАНО
   - Количество участников для групповых тренировок привязано к карточкам (2, 3, 4 человека и т.д.)
   - Для программ количество участников выбирается путем добавления

### ❌ НЕ ВЫПОЛНЕНО:

**Нет невыполненных задач!**

**Следующие шаги:**
1. ✅ Проверить работу предварительной регистрации и deep link в форме - ВЫПОЛНЕНО
2. ✅ Проверить работу напоминаний для бронирований через сайт - ВЫПОЛНЕНО
3. ✅ Реализовать Задачу 0 (оплата для групповых тренировок и программ) - ВЫПОЛНЕНО
4. ✅ Протестировать все уведомления (через сайт и через бота) - ВЫПОЛНЕНО
5. ✅ Исправить формат времени в уведомлениях - ВЫПОЛНЕНО
6. ✅ Исправить location в уведомлениях - ВЫПОЛНЕНО

**🎉 ВСЕ ЗАДАЧИ ВЫПОЛНЕНЫ!**

---

## 📱 ЗАДАЧА 4: СИСТЕМА УВЕДОМЛЕНИЙ ДЛЯ КЛИЕНТОВ БЕЗ TELEGRAM

**Дата добавления:** 15.12.2025  
**Режим:** Планировщик

### 🎯 ЦЕЛЬ
Обеспечить максимальный охват клиентов уведомлениями о предстоящих тренировках и отменах, включая тех, кто не зарегистрирован в Telegram боте.

### 📊 ТЕКУЩАЯ СИТУАЦИЯ

#### Анализ существующей системы уведомлений:

1. **Telegram уведомления:**
   - ✅ Работают для клиентов с `telegram_id`
   - ✅ Напоминания о тренировках (21:00, cron задача)
   - ✅ Уведомления администратору и инструктору
   - ❌ Не работают для клиентов без `telegram_id`

2. **Email уведомления:**
   - ✅ Реализован `EmailService` (используется для сертификатов)
   - ✅ SMTP через Yandex настроен
   - ✅ Красивые HTML шаблоны для сертификатов
   - ✅ Используются для уведомлений о тренировках (подтверждение, отмена)
   - ✅ Используются для напоминаний (через notification-service.js)
   - ✅ Используются для отмен бронирований

3. **SMS уведомления:**
   - ✅ Есть роутер `/api/sms/process` для обработки входящих SMS
   - ❌ НЕТ функционала отправки исходящих SMS
   - ❌ НЕТ интеграции с SMS-провайдером

4. **WhatsApp уведомления:**
   - ❌ НЕ реализовано
   - ❌ НЕТ интеграции с WhatsApp API

#### Проблема:
В `notification-service.js` метод `sendTrainingReminders()` отправляет уведомления **только** клиентам с `telegram_id`:
```javascript
// Строка 603: await this.sendNotification(clientData.telegram_id, message)
// Если telegram_id отсутствует - уведомление не отправляется
```

#### Статистика клиентов:
- Нужно проверить: сколько клиентов имеют `telegram_id`, а сколько нет
- Клиенты, записанные через сайт, часто не имеют `telegram_id`
- Для них критически важно получать уведомления о тренировках и отменах

---

### 🔍 АНАЛИЗ ВАРИАНТОВ УВЕДОМЛЕНИЙ

#### 1. EMAIL УВЕДОМЛЕНИЯ ⭐ РЕКОМЕНДУЕТСЯ

**Преимущества:**
- ✅ Уже реализован `EmailService`
- ✅ У всех клиентов есть email (обязательное поле при регистрации)
- ✅ Низкая стоимость (бесплатно через SMTP)
- ✅ Можно отправлять красивые HTML шаблоны
- ✅ Высокая доставляемость
- ✅ Клиенты привыкли к email уведомлениям

**Недостатки:**
- ⚠️ Email может попасть в спам
- ⚠️ Не все клиенты проверяют email регулярно
- ⚠️ Может быть задержка доставки

**Реализация:**
- Использовать существующий `EmailService.sendEmail()`
- Создать HTML шаблоны для:
  - Подтверждение бронирования
  - Напоминание о тренировке (за день до)
  - Уведомление об отмене
- Добавить отправку email в `notification-service.js` для клиентов без `telegram_id`

**Стоимость:** Бесплатно (SMTP Yandex)

**Приоритет:** ВЫСОКИЙ ⭐⭐⭐

---

#### 2. SMS УВЕДОМЛЕНИЯ

**Преимущества:**
- ✅ Высокая доставляемость (почти 100%)
- ✅ Быстрая доставка
- ✅ Клиенты читают SMS сразу
- ✅ Не требует установки приложений

**Недостатки:**
- ❌ Дорого (от 1-3 ₽ за SMS в России)
- ❌ Ограничение по длине (160 символов)
- ❌ Нет форматирования (только текст)
- ❌ Нужна интеграция с SMS-провайдером

**Варианты SMS-провайдеров:**
1. **SMS.ru** - популярный, простой API, ~2-3 ₽/SMS
2. **SMSC.ru** - надежный, ~1.5-2.5 ₽/SMS
3. **Twilio** - международный, дороже (~5-10 ₽/SMS)
4. **SMS Aero** - российский, ~1.5-2 ₽/SMS

**Реализация:**
- Создать `SmsService` (аналог `EmailService`)
- Интегрировать с выбранным провайдером
- Добавить отправку SMS в `notification-service.js` для клиентов без `telegram_id` и без email (если такое возможно)

**Стоимость:** ~2 ₽ за SMS × количество клиентов × количество уведомлений

**Приоритет:** СРЕДНИЙ ⭐⭐ (из-за стоимости)

**Рекомендация:** Использовать только для критических уведомлений (отмена тренировки) или как fallback, если email не доставлен.

---

#### 3. WHATSAPP УВЕДОМЛЕНИЯ

**Преимущества:**
- ✅ Бесплатно (через WhatsApp Business API)
- ✅ Высокая доставляемость
- ✅ Клиенты активно используют WhatsApp
- ✅ Можно отправлять медиа (фото, документы)
- ✅ Можно использовать шаблоны сообщений

**Недостатки:**
- ❌ Сложная настройка (требует бизнес-аккаунт)
- ❌ Нужна верификация бизнеса
- ❌ Ограничения на отправку (24-часовое окно для ответа)
- ❌ Нужен номер телефона клиента (есть у всех)
- ❌ Клиент должен добавить номер в контакты (для некоторых провайдеров)

**Варианты интеграции:**
1. **WhatsApp Business API (официальный)** - сложная настройка, требует бизнес-верификацию
2. **Twilio WhatsApp API** - проще, но дороже (~$0.005 за сообщение)
3. **Green API** - российский провайдер, ~0.5-1 ₽ за сообщение
4. **ChatAPI** - альтернатива, ~$0.01 за сообщение

**Реализация:**
- Создать `WhatsAppService` (аналог `EmailService`)
- Интегрировать с выбранным провайдером
- Добавить отправку WhatsApp в `notification-service.js`

**Стоимость:** Зависит от провайдера (от бесплатно до ~1 ₽ за сообщение)

**Приоритет:** СРЕДНИЙ ⭐⭐ (из-за сложности настройки)

**Рекомендация:** Рассмотреть как альтернативу SMS, но только после настройки email уведомлений.

---

### 🎯 РЕКОМЕНДУЕМАЯ СТРАТЕГИЯ

#### Приоритет 1: EMAIL УВЕДОМЛЕНИЯ (НАЧАТЬ СРАЗУ) ⭐⭐⭐

**Почему:**
- Уже есть инфраструктура
- Бесплатно
- У всех клиентов есть email
- Быстрая реализация

**Что сделать:**
1. Создать HTML шаблоны для email уведомлений:
   - Подтверждение бронирования
   - Напоминание о тренировке (за день до)
   - Уведомление об отмене
2. Расширить `notification-service.js`:
   - Добавить отправку email для клиентов без `telegram_id`
   - Использовать email как fallback, если `telegram_id` отсутствует
3. Добавить отправку email при создании бронирования (в `kuliga-payment.js`)
4. Добавить отправку email при отмене бронирования

**Критерий успеха:**
- Все клиенты получают уведомления о тренировках (Telegram или Email)
- Красивые, читаемые email шаблоны
- Уведомления доставляются вовремя

---

#### Приоритет 2: МОТИВАЦИЯ К РЕГИСТРАЦИИ В TELEGRAM БОТЕ ⭐⭐

**Почему:**
- Telegram уведомления - самый удобный способ
- Бесплатно
- Мгновенная доставка
- Интерактивность (кнопки, меню)

**Что сделать:**
1. В email уведомлениях добавлять призыв к действию:
   - "Подключите Telegram бота для мгновенных уведомлений!"
   - Ссылка на бота с deep link `client_{CLIENT_ID}`
2. На странице успешной оплаты:
   - Показывать преимущества Telegram бота
   - Предлагать подключить бота сразу
3. В напоминаниях по email:
   - Добавлять ссылку на бота
   - Объяснять преимущества

**Критерий успеха:**
- Увеличивается процент клиентов с `telegram_id`
- Клиенты активно используют бота

---

#### Приоритет 3: SMS ДЛЯ КРИТИЧЕСКИХ УВЕДОМЛЕНИЙ ⭐

**Почему:**
- Дорого для массовых рассылок
- Но эффективно для критических случаев

**Что сделать:**
1. Использовать SMS только для:
   - Отмена тренировки (критично!)
   - Изменение времени/места (критично!)
2. НЕ использовать для:
   - Обычных напоминаний (использовать email)
   - Подтверждений бронирования (использовать email)
3. Интегрировать SMS-провайдер (например, SMS.ru)
4. Создать `SmsService`

**Критерий успеха:**
- Критические уведомления доставляются мгновенно
- Контроль расходов на SMS

---

#### Приоритет 4: WHATSAPP (ОПЦИОНАЛЬНО) ⭐

**Почему:**
- Сложная настройка
- Не все клиенты используют WhatsApp активно
- Email уже покрывает большинство случаев

**Что сделать:**
1. Изучить варианты интеграции
2. Протестировать на небольшой группе клиентов
3. Если эффективно - масштабировать

**Критерий успеха:**
- WhatsApp уведомления работают стабильно
- Клиенты положительно реагируют

---

### 📋 ПЛАН РЕАЛИЗАЦИИ

#### ЭТАП 1: Email уведомления (ВЫСОКИЙ ПРИОРИТЕТ)

**Задача 4.1: Создать HTML шаблоны для email**

**Файлы:**
- `src/services/email-templates/` (новая папка)
- `src/services/email-templates/booking-confirmation.html`
- `src/services/email-templates/training-reminder.html`
- `src/services/email-templates/booking-cancellation.html`

**Действия:**
1. Создать красивые HTML шаблоны (responsive, читаемые)
2. Использовать стили, аналогичные шаблонам сертификатов
3. Добавить переменные для подстановки данных

**Критерий успеха:** Шаблоны выглядят профессионально и читаемо.

---

**Задача 4.2: Расширить notification-service.js для email**

**Файлы:**
- `src/services/notification-service.js`

**Действия:**
1. Добавить метод `sendEmailNotification()` в `NotificationService`
2. Модифицировать `sendTrainingReminders()`:
   - Если у клиента есть `telegram_id` - отправлять в Telegram
   - Если нет `telegram_id`, но есть `email` - отправлять email
   - Логировать оба типа уведомлений
3. Использовать существующий `EmailService`

**Критерий успеха:** Клиенты без Telegram получают email напоминания.

---

**Задача 4.3: Email при создании бронирования**

**Файлы:**
- `src/routes/kuliga-payment.js` (webhook handler)

**Действия:**
1. После успешного создания бронирования отправлять email клиенту
2. Использовать шаблон `booking-confirmation.html`
3. Включать все детали тренировки

**Критерий успеха:** Клиенты получают email подтверждение после оплаты.

---

**Задача 4.4: Email при отмене бронирования**

**Файлы:**
- `src/routes/kuliga-payment.js` (webhook handler для статуса `FAILED` или `REFUNDED`)

**Действия:**
1. При отмене бронирования отправлять email клиенту
2. Использовать шаблон `booking-cancellation.html`
3. Объяснять причину отмены (если известна)

**Критерий успеха:** Клиенты получают email уведомление об отмене.

---

#### ЭТАП 2: Мотивация к регистрации в Telegram боте

**Задача 4.5: Добавить призыв к действию в email**

**Файлы:**
- `src/services/email-templates/*.html`

**Действия:**
1. В каждом email шаблоне добавлять блок:
   - "Подключите Telegram бота для мгновенных уведомлений!"
   - Кнопка/ссылка на бота с deep link
   - Объяснение преимуществ

**Критерий успеха:** В каждом email есть призыв подключить бота.

---

**Задача 4.6: Улучшить страницу успешной оплаты**

**Файлы:**
- `views/booking-success.ejs`

**Действия:**
1. Добавить секцию про Telegram бота
2. Показать преимущества (мгновенные уведомления, история тренировок)
3. Кнопка подключить бота с deep link

**Критерий успеха:** На странице успеха есть призыв подключить бота.

---

#### ЭТАП 3: SMS для критических уведомлений (ОПЦИОНАЛЬНО)

**Задача 4.7: Интегрировать SMS-провайдер**

**Файлы:**
- `src/services/smsService.js` (новый файл)

**Действия:**
1. Выбрать SMS-провайдер (рекомендуется SMS.ru)
2. Создать `SmsService` класс
3. Реализовать метод `sendSms(phone, message)`
4. Добавить настройки в `.env`

**Критерий успеха:** SMS отправляются через API провайдера.

---

**Задача 4.8: SMS при отмене тренировки**

**Файлы:**
- `src/routes/kuliga-payment.js`
- `src/services/notification-service.js`

**Действия:**
1. При отмене бронирования отправлять SMS клиенту
2. Использовать короткий текст (160 символов)
3. Логировать отправку

**Критерий успеха:** Клиенты получают SMS при отмене.

---

### 💰 ОЦЕНКА СТОИМОСТИ

#### Email уведомления:
- **Стоимость:** Бесплатно (SMTP Yandex)
- **Ограничения:** Нет (можно отправлять сколько угодно)

#### SMS уведомления:
- **Стоимость:** ~2 ₽ за SMS
- **Пример:** 100 клиентов × 2 SMS (напоминание + отмена) = 400 ₽
- **Рекомендация:** Использовать только для критических случаев

#### WhatsApp уведомления:
- **Стоимость:** Зависит от провайдера (от бесплатно до ~1 ₽)
- **Рекомендация:** Изучить после реализации email

---

### ✅ КРИТЕРИИ УСПЕХА ВСЕГО ПРОЕКТА

1. ✅ Все клиенты получают уведомления о тренировках (Telegram или Email)
2. ✅ Все клиенты получают уведомления об отменах (Telegram, Email, или SMS)
3. ✅ Email шаблоны красивые и читаемые
4. ✅ Увеличивается процент клиентов с `telegram_id` (благодаря мотивации)
5. ✅ Критические уведомления доставляются мгновенно (SMS)
6. ✅ Система логирует все отправленные уведомления

---

### 📝 ЗАМЕТКИ

- **Важно:** При реализации email уведомлений нужно учитывать, что некоторые клиенты могут не иметь валидного email (хотя это обязательное поле)
- **Важно:** При реализации SMS нужно контролировать расходы
- **Важно:** WhatsApp требует бизнес-верификацию, это может занять время
- **Рекомендация:** Начать с email, затем добавить SMS для критических случаев, затем рассмотреть WhatsApp

---

## ✅ УТОЧНЕНИЯ ОТ ПОЛЬЗОВАТЕЛЯ

1. **Deep Link формат:** `client_{CLIENT_ID}` - простой формат, понятный пользователю
   - Объяснение: Deep link - это специальная ссылка вида `https://t.me/Ski_Instruktor72_bot?start=client_123`
   - При переходе по этой ссылке бот получает параметр `client_123` и может идентифицировать клиента

2. **Предварительная регистрация:** 
   - При клике на ссылку бота вызывать `/api/kuliga/pre-register-client`
   - Если клиент не кликнул на ссылку - вызывать при отправке формы
   - Система всегда проверяет, создан клиент ранее или нет

3. **Количество участников:** Уже реализовано в контейнерах (прайсах) - вопрос закрыт

4. **Шаблоны email:** Сделать удобными и комфортными для чтения - красивые HTML шаблоны

5. **Напоминания:** Использовать существующую cron задачу (21:00), которая уже работает для клиентов из бота

---

**Дата обновления:** 15.12.2025

---

## 🎯 ЗАДАЧА 5: РЕАЛИЗАЦИЯ ФУНКЦИИ "СОЗДАТЬ ИНСТРУКТОРА ГОРНОСТАЙЛ72"

**Дата добавления:** 18.12.2025  
**Режим:** Планировщик  
**Приоритет:** ВЫСОКИЙ

### 📋 ПРОБЛЕМА

Пользователь пытался создать инструктора Горностайл72 для зимних тренировок через страницу администратора, но функция не работает. При нажатии на кнопку "Создать инструктора Горностайл72" появляется только alert с сообщением "Функция создания инструктора Кулиги будет реализована".

### 🔍 АНАЛИЗ ТЕКУЩЕЙ СИТУАЦИИ

#### Структура базы данных:

1. **Таблица `kuliga_instructors`:**
   - Хранит всех инструкторов (и для Кулиги, и для Воронинских горок)
   - Поле `location` может быть:
     - `'kuliga'` - База отдыха «Кулига-Клуб» (тренажер)
     - `'vorona'` - Воронинские горки (зимние тренировки)
   - Инструкторы Горностайл72 для зимних тренировок - это инструкторы с `location = 'vorona'`

2. **Текущая реализация:**
   - ✅ Модальное окно для создания/редактирования инструктора уже существует (`kuliga-instructor-modal` в `admin.html`)
   - ✅ Функция `openKuligaInstructorModal()` в `admin-kuliga.js` работает корректно
   - ✅ API endpoint `/api/kuliga/admin/instructors` (POST) работает и поддерживает поле `location`
   - ✅ Форма в модальном окне имеет поле выбора `location` с опциями 'kuliga' и 'vorona'
   - ❌ Функция `showCreateKuligaInstructorModal()` в `admin.js` только показывает alert, не открывает модальное окно

3. **Проблемный код:**
   ```javascript
   // public/js/admin.js, строка 2099
   function showCreateKuligaInstructorModal() {
       // TODO: Создать модальное окно аналогично create-trainer.html
       // Пока используем существующий модал из admin-kuliga.js или создаём новый
       alert('Функция создания инструктора Кулиги будет реализована');
   }
   ```

4. **Кнопка в интерфейсе:**
   - Кнопка "Создать инструктора Горностайл72" находится на странице "Тренера" (`trainers-page`)
   - ID кнопки: `create-kuliga-instructor`
   - Обработчик в `admin.js` вызывает `showCreateKuligaInstructorModal()`

### 🎯 ТРЕБОВАНИЯ

1. При нажатии на кнопку "Создать инструктора Горностайл72" должно открываться модальное окно для создания инструктора
2. В модальном окне по умолчанию должно быть выбрано `location = 'vorona'` (Воронинские горки)
3. После создания инструктор должен появиться в списке инструкторов Горностайл72
4. Функция должна работать аналогично созданию инструктора Кулиги, но с предустановленным `location = 'vorona'`

### 🔧 РЕШЕНИЕ

#### Вариант 1: Использовать существующую функцию с параметром (РЕКОМЕНДУЕТСЯ) ⭐

**Идея:** Модифицировать функцию `openKuligaInstructorModal()` чтобы она принимала опциональный параметр `defaultLocation`, и вызывать её из `showCreateKuligaInstructorModal()` с `location = 'vorona'`.

**Преимущества:**
- Минимальные изменения в коде
- Переиспользование существующей логики
- Единообразный интерфейс

**Недостатки:**
- Нужно убедиться, что функция `openKuligaInstructorModal()` доступна глобально

#### Вариант 2: Создать отдельную функцию для инструкторов Горностайл72

**Идея:** Создать новую функцию `showCreateGornostyleInstructorModal()`, которая будет открывать модальное окно с предустановленным `location = 'vorona'`.

**Преимущества:**
- Четкое разделение логики
- Легко понять назначение функции

**Недостатки:**
- Дублирование кода
- Больше функций для поддержки

### 📝 ПЛАН РЕАЛИЗАЦИИ

#### Задача 5.1: Исправить функцию `showCreateKuligaInstructorModal()`

**Файлы:**
- `public/js/admin.js`

**Действия:**
1. Заменить alert на вызов функции `openKuligaInstructorModal()` (если она доступна глобально)
2. Или создать логику открытия модального окна с установкой `location = 'vorona'`
3. Убедиться, что модальное окно открывается корректно
4. Установить значение `location` в 'vorona' по умолчанию

**Критерий успеха:** При нажатии на кнопку "Создать инструктора Горностайл72" открывается модальное окно с предустановленным `location = 'vorona'`.

---

#### Задача 5.2: Убедиться, что `openKuligaInstructorModal()` доступна глобально

**Файлы:**
- `public/js/admin-kuliga.js`

**Действия:**
1. Проверить, что функция `openKuligaInstructorModal()` экспортирована глобально (через `window.openKuligaInstructorModal`)
2. Если нет - добавить экспорт
3. Убедиться, что функция может принимать параметр для установки `location` по умолчанию

**Критерий успеха:** Функция `openKuligaInstructorModal()` доступна из `admin.js`.

---

#### Задача 5.3: Модифицировать `openKuligaInstructorModal()` для поддержки `defaultLocation`

**Файлы:**
- `public/js/admin-kuliga.js`

**Действия:**
1. Добавить параметр `defaultLocation` в функцию `openKuligaInstructorModal(instructorId = null, defaultLocation = 'kuliga')`
2. При создании нового инструктора (когда `instructorId === null`) устанавливать `location` в значение `defaultLocation`
3. Обновить вызовы функции, если необходимо

**Критерий успеха:** Функция `openKuligaInstructorModal()` может принимать `defaultLocation` и устанавливать его при создании нового инструктора.

---

#### Задача 5.4: Обновить обработчик кнопки в `admin.js`

**Файлы:**
- `public/js/admin.js`

**Действия:**
1. Заменить содержимое функции `showCreateKuligaInstructorModal()` на вызов `openKuligaInstructorModal(null, 'vorona')`
2. Добавить проверку существования функции перед вызовом
3. Добавить обработку ошибок

**Критерий успеха:** При нажатии на кнопку "Создать инструктора Горностайл72" открывается модальное окно с `location = 'vorona'`.

---

#### Задача 5.5: Проверить загрузку скриптов

**Файлы:**
- `public/admin.html`

**Действия:**
1. Убедиться, что `admin-kuliga.js` загружается до `admin.js` или что функции доступны
2. Проверить порядок подключения скриптов в HTML
3. При необходимости добавить проверку готовности DOM

**Критерий успеха:** Все скрипты загружаются в правильном порядке, функции доступны.

---

#### Задача 5.6: Тестирование

**Действия:**
1. Открыть страницу администратора
2. Перейти на страницу "Тренера"
3. Нажать на кнопку "Создать инструктора Горностайл72"
4. Проверить, что модальное окно открывается
5. Проверить, что в поле "Место работы" выбрано "Воронинские горки"
6. Заполнить форму и создать инструктора
7. Проверить, что инструктор создан с `location = 'vorona'`
8. Проверить, что инструктор отображается в списке "Инструкторы Горностайл72"

**Критерий успеха:** Функция создания инструктора Горностайл72 работает полностью, инструктор создается с правильным `location`.

---

### 📊 ТЕКУЩИЙ СТАТУС

**Статус:** 🔴 НЕ НАЧАТО

### 🎯 ПРИОРИТЕТЫ

1. **ВЫСОКИЙ ПРИОРИТЕТ:**
   - Задача 5.1: Исправить функцию `showCreateKuligaInstructorModal()`
   - Задача 5.4: Обновить обработчик кнопки в `admin.js`

2. **СРЕДНИЙ ПРИОРИТЕТ:**
   - Задача 5.2: Убедиться, что `openKuligaInstructorModal()` доступна глобально
   - Задача 5.3: Модифицировать `openKuligaInstructorModal()` для поддержки `defaultLocation`

3. **НИЗКИЙ ПРИОРИТЕТ:**
   - Задача 5.5: Проверить загрузку скриптов
   - Задача 5.6: Тестирование

---

### 📝 ЗАМЕТКИ

- **Важно:** Инструкторы Горностайл72 для зимних тренировок хранятся в той же таблице `kuliga_instructors`, что и инструкторы Кулиги, но с `location = 'vorona'`
- **Важно:** Модальное окно и API уже поддерживают создание инструкторов с разными `location`, нужно только правильно вызвать функцию
- **Рекомендация:** Использовать существующую функцию `openKuligaInstructorModal()` с параметром `defaultLocation` для минимизации изменений в коде

---

**Дата обновления:** 18.12.2025
