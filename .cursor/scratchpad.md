# 🏔️ ПЛАН: РАСШИРЕНИЕ СИСТЕМЫ ДЛЯ ПОДДЕРЖКИ ВОРОНИНСКИХ ГОРОК

## Background and Motivation

Проект расширяется для поддержки двух мест проведения зимних тренировок:
1. **Кулига** (текущее место)
2. **Воронинские горки** (новое место)

**Цель:** Создать единый сервис для записи на занятия по горным лыжам и сноуборду как на Кулигу, так и на Воронинские горки.

**Важные решения:**
- Название таблиц БД остается с префиксом `kuliga_` (для обратной совместимости)
- Добавляется поле `location` или `training_location` для идентификации места проведения
- Название бренда: **Горностайл72** (вместо упоминаний только "Кулига")

---

## Key Challenges and Analysis

### 1. Структура базы данных

**Таблицы, которые нужно модифицировать:**

#### 1.1. `kuliga_instructors`
- ✅ **Добавить:** `location VARCHAR(20) NOT NULL CHECK (location IN ('kuliga', 'vorona'))`
- **Значение по умолчанию:** Для существующих инструкторов - `'kuliga'`
- **Обязательное поле:** Да, при создании нового инструктора
- **Маппинг отображения:**
  - `'kuliga'` → "База отдыха «Кулига-Клуб»"
  - `'vorona'` → "Воронинские горки"

#### 1.2. `kuliga_schedule_slots`
- ✅ **Добавить:** `location VARCHAR(20)` - наследуется от инструктора, но можно переопределить
- **Логика:** Слот создается с `location` инструктора, можно изменить при необходимости
- **Индекс:** `CREATE INDEX idx_kuliga_slots_location ON kuliga_schedule_slots(location)`

#### 1.3. `kuliga_group_trainings`
- ✅ **Добавить:** `location VARCHAR(20) NOT NULL` - берется из слота при создании
- **Привязка:** Групповая тренировка связана со слотом, который уже имеет `location`

#### 1.4. `kuliga_bookings`
- ✅ **Добавить:** `location VARCHAR(20) NOT NULL`
- **Логика:** 
  - Для индивидуальных: из `slot_id` (через `kuliga_schedule_slots`)
  - Для групповых: из `group_training_id` (через `kuliga_group_trainings`)
- **Важно:** При записи клиент выбирает место, система фильтрует инструкторов по `location`

#### 1.5. Другие таблицы
- `kuliga_transactions` - можно добавить `location` для отчетности
- `kuliga_instructor_payouts` - можно добавить фильтрацию по `location`

---

### 2. API Endpoints (Backend)

**Файлы, требующие модификации:**

#### 2.1. `src/routes/kuliga-admin.js`
- **GET `/api/kuliga/admin/instructors`**: Добавить фильтр по `location`
- **POST `/api/kuliga/admin/instructors`**: Обязательное поле `location`
- **PUT `/api/kuliga/admin/instructors/:id`**: Обновление `location` (с проверкой: нельзя изменить, если есть активные слоты/тренировки)

#### 2.2. `src/routes/kuliga-public.js`
- **GET `/api/kuliga/availability`**: Добавить параметр `location` для фильтрации слотов
- **GET `/api/kuliga/instructors`**: Добавить параметр `location` для фильтрации инструкторов
- **GET `/api/kuliga/group-trainings`**: Добавить параметр `location` для фильтрации групповых тренировок

#### 2.3. `src/routes/kuliga-booking.js`
- **POST `/api/kuliga/bookings` (индивидуальная)**: Проверка, что выбранный слот соответствует выбранному `location`
- **POST `/api/kuliga/bookings` (групповая)**: Проверка, что групповая тренировка соответствует выбранному `location`

#### 2.4. `src/routes/kuliga-instructor-schedule.js`
- **POST `/api/kuliga/instructor/slots/create`**: Слоты создаются с `location` инструктора
- **POST `/api/kuliga/instructor/regular-group-trainings`**: Групповые тренировки наследуют `location` из слотов

---

### 3. Frontend (Веб-интерфейсы)

#### 3.1. Админ-панель (`public/admin.html`, `public/js/admin-kuliga.js`)
- ✅ **Исправлено:** Кнопка меню: "🏔️ СЛУЖБА ИНСТРУКТОРОВ ГОРНОСТАЙЛ72"
- ✅ **Исправлено:** Заголовок страницы: "Служба инструкторов Горностайл72"
- **Добавить:** Поле выбора `location` в форме создания/редактирования инструктора
- **Добавить:** Фильтр по `location` в списке инструкторов
- **Добавить:** Индикатор места работы инструктора в карточке

#### 3.2. Личный кабинет инструктора (`public/trainer_kuliga.html`, `public/js/kuliga-instructor-schedule.js`)
- **Логика:** Инструктор видит только свои слоты и тренировки (уже реализовано через `instructor_id`)
- **Уточнение:** Нужно ли показывать `location` в интерфейсе инструктора? (Скорее всего нет, т.к. инструктор привязан к одному месту)

#### 3.3. Страница бронирования (`views/kuliga-booking.ejs`, `public/js/kuliga-booking.js`)
- **Добавить:** Шаг выбора места проведения тренировки (Кулига / Воронинские горки)
- **Логика:** После выбора места показываются только инструкторы с этим `location`
- **API запросы:** Все запросы availability/instructors/group-trainings должны включать параметр `location`

#### 3.4. Лендинг (`views/kuliga-landing.ejs`, `public/js/kuliga.js`)
- **Обновить:** Текст - упомянуть оба места (Кулига и Воронинские горки)
- **Добавить:** Возможно, секция с описанием мест проведения тренировок
- **Обновить:** SEO мета-теги для включения Воронинских горок

---

### 4. Telegram Bots

#### 4.1. Клиентский бот (`src/bot/client-bot.js`)
- **Функция:** `showAvailableKuligaInstructors` - добавить выбор места перед выбором инструктора
- **Функция:** `showAvailableGroupTrainings` - добавить фильтрацию по `location`
- **Состояния:** Добавить состояние выбора места (`kuliga_location_select`)
- **Логика:** После выбора места показываются только инструкторы/тренировки с этим `location`

#### 4.2. Бот инструкторов (`src/bot/kuliga-instructor-bot.js`)
- **Логика:** Инструктор видит только свои данные (уже реализовано)
- **Уточнение:** Нужно ли показывать `location`? (Скорее всего нет)

---

### 5. Документация и контент

#### 5.1. Лендинг (`views/kuliga-landing.ejs`)
- **Обновить заголовок:** "Служба инструкторов Горностайл72"
- **Обновить описание:** Упомянуть оба места проведения тренировок
- **Обновить секцию локации:** "База отдыха «Кулига-Клуб» и Воронинские горки"
- **Обновить SEO:**
  - Title: "Обучение горным лыжам и сноуборду | Кулига и Воронинские горки | Горностайл72"
  - Description: "Запишитесь на обучение горным лыжам и сноуборду на базе отдыха Кулига или на Воронинских горках..."
  - Keywords: добавить "Воронинские горки", "обучение лыжам Тюмень"

#### 5.2. Главная страница (`views/index.ejs`)
- **Проверить:** Есть ли упоминания о Кулиге? Обновить для упоминания обоих мест

#### 5.3. Другие страницы
- Проверить все страницы на упоминания только "Кулига" и обновить

---

## High-level Task Breakdown

### ЭТАП 1: Переименование и базовые изменения (БЫСТРЫЕ ИСПРАВЛЕНИЯ)

#### 1.1. Админ-панель ✅
- ✅ Переименовать кнопку меню: "🏔️ СЛУЖБА ИНСТРУКТОРОВ ГОРНОСТАЙЛ72"
- ✅ Изменить заголовок страницы: "Служба инструкторов Горностайл72"
- **Критерий успеха:** В админ-панели обновлены названия

#### 1.2. Лендинг (views/kuliga-landing.ejs)
- Обновить заголовки и описания для упоминания обоих мест
- Обновить SEO мета-теги
- **Критерий успеха:** На лендинге упомянуты и Кулига, и Воронинские горки

---

### ЭТАП 2: База данных и миграция

#### 2.1. Создание миграции для добавления поля `location`
- **Файл:** `src/db/migrations/038_add_location_to_kuliga_tables.sql`
- **Таблицы для модификации:**
  1. `kuliga_instructors` - добавить `location VARCHAR(20) NOT NULL DEFAULT 'kuliga'`
  2. `kuliga_schedule_slots` - добавить `location VARCHAR(20)` (наследуется от инструктора)
  3. `kuliga_group_trainings` - добавить `location VARCHAR(20) NOT NULL`
  4. `kuliga_bookings` - добавить `location VARCHAR(20) NOT NULL`
- **Индексы:** Создать индексы для быстрого поиска по `location`
- **Триггеры:** Автоматическое заполнение `location` из инструктора для слотов
- **Критерий успеха:** Миграция применена, все таблицы имеют поле `location`

#### 2.2. Обновление существующих данных
- Для всех существующих инструкторов установить `location = 'kuliga'`
- Для всех существующих слотов установить `location` из инструктора
- Для всех существующих групповых тренировок установить `location` из слота
- Для всех существующих бронирований установить `location` из слота/тренировки
- **Критерий успеха:** Все существующие записи имеют корректное значение `location`

---

### ЭТАП 3: Backend API изменения

#### 3.1. Обновление роутов для инструкторов (`src/routes/kuliga-admin.js`)
- Добавить валидацию `location` при создании/обновлении инструктора
- Добавить фильтр по `location` в GET `/api/kuliga/admin/instructors`
- При изменении `location` инструктора - проверка на активные слоты/тренировки
- **Критерий успеха:** API корректно обрабатывает поле `location`

#### 3.2. Обновление публичных роутов (`src/routes/kuliga-public.js`)
- Добавить параметр `location` в GET `/api/kuliga/availability`
- Добавить параметр `location` в GET `/api/kuliga/instructors`
- Добавить параметр `location` в GET `/api/kuliga/group-trainings`
- Фильтровать результаты по `location`
- **Критерий успеха:** API возвращает только инструкторов/слоты/тренировки с выбранным `location`

#### 3.3. Обновление роутов бронирования (`src/routes/kuliga-booking.js`)
- Добавить параметр `location` в запрос создания бронирования
- Проверка соответствия выбранного слота/тренировки указанному `location`
- Сохранение `location` в бронировании
- **Критерий успеха:** Бронирования создаются с корректным `location`

#### 3.4. Обновление роутов инструктора (`src/routes/kuliga-instructor-schedule.js`)
- При создании слотов автоматически устанавливать `location` инструктора
- При создании групповых тренировок наследовать `location` из слота
- **Критерий успеха:** Слоты и групповые тренировки автоматически получают правильный `location`

---

### ЭТАП 4: Frontend изменения

#### 4.1. Админ-панель (`public/js/admin-kuliga.js`)
- Добавить поле выбора `location` в форму создания/редактирования инструктора
- Добавить индикатор места в карточке инструктора
- Добавить фильтр по `location` в список инструкторов
- **Критерий успеха:** Админ может создавать инструкторов с указанием места работы

#### 4.2. Страница бронирования (`public/js/kuliga-booking.js`, `views/kuliga-booking.ejs`)
- Добавить шаг выбора места (радио-кнопки: Кулига / Воронинские горки)
- После выбора места обновлять список доступных инструкторов
- После выбора места обновлять список доступных групповых тренировок
- Все запросы к API должны включать параметр `location`
- **Критерий успеха:** Клиент может выбрать место и видит только релевантных инструкторов/тренировки

#### 4.3. Лендинг (`views/kuliga-landing.ejs`, `public/js/kuliga.js`)
- Обновить тексты для упоминания обоих мест
- Обновить SEO мета-теги
- Возможно добавить секцию с описанием мест
- **Критерий успеха:** Лендинг упоминает оба места проведения тренировок

---

### ЭТАП 5: Telegram боты

#### 5.1. Клиентский бот (`src/bot/client-bot.js`)
- Добавить состояние выбора места (`kuliga_location_select`)
- Добавить обработчик выбора места
- После выбора места показывать только инструкторов с этим `location`
- После выбора места показывать только групповые тренировки с этим `location`
- Сохранять выбранное место в состоянии пользователя
- При создании бронирования передавать `location`
- **Критерий успеха:** Пользователь бота может выбрать место и видит только релевантные опции

#### 5.2. Бот инструкторов
- **Уточнение:** Нужно ли что-то менять? (Скорее всего нет, т.к. инструктор привязан к одному месту)

---

### ЭТАП 6: Тестирование и проверка

#### 6.1. Функциональное тестирование
- Создание инструктора с разными `location`
- Создание слотов и проверка наследования `location`
- Создание групповых тренировок и проверка наследования `location`
- Бронирование индивидуальной тренировки с выбором места
- Бронирование групповой тренировки с выбором места
- Фильтрация инструкторов по `location` на сайте
- Фильтрация инструкторов по `location` в боте
- **Критерий успеха:** Все сценарии работают корректно

#### 6.2. Проверка данных
- Все существующие записи имеют корректное `location = 'kuliga'`
- Новые записи автоматически получают правильный `location`
- Нет записей с `NULL` в поле `location`
- **Критерий успеха:** База данных в консистентном состоянии

---

## Project Status Board

### ✅ ВЫПОЛНЕНО

#### ЭТАП 1: Переименование ✅ **ЗАВЕРШЕНО**
- [x] Обновить лендинг (`views/kuliga-landing.ejs`) - упомянуть оба места
- [x] Обновить SEO мета-теги на лендинге
- [x] Проверить главную страницу (`views/index.ejs`) на упоминания

#### ЭТАП 2: База данных ✅ **ЗАВЕРШЕНО**
- [x] Создать миграцию `038_add_location_to_kuliga_tables.sql`
- [x] Применить миграцию
- [x] Обновить существующие данные

#### ЭТАП 3: Backend ✅ **ЗАВЕРШЕНО**
- [x] Обновить `kuliga-admin.js` (создание/редактирование инструкторов)
- [x] Обновить `kuliga-public.js` (фильтрация по location)
- [x] Обновить `kuliga-booking.js` (проверка location при бронировании)
- [x] Обновить `kuliga-instructor-schedule.js` (автоматическое наследование location)

#### ЭТАП 4: Frontend ✅ **ЗАВЕРШЕНО**
- [x] Обновить админ-панель (поле location в форме инструктора)
- [x] Обновить страницу бронирования (шаг выбора места)
- [x] Обновить лендинг (тексты)

#### ЭТАП 5: Боты ✅ **ЗАВЕРШЕНО**
- [x] Обновить клиентский бот (выбор места)
- [ ] Протестировать бота (частично)

### ⏳ ОСТАЛОСЬ ВЫПОЛНИТЬ

#### ЭТАП 6: Тестирование ⏳ **В ПРОЦЕССЕ**
- [x] Создан план тестирования (`docs/TESTING_PLAN.md`)
- [x] Создан скрипт проверки уведомлений (`scripts/check-notifications-location.js`)
- [x] Создан скрипт тестирования функций (`scripts/test-location-features.js`)
- [ ] Полное функциональное тестирование всех сценариев с выбором location
- [ ] Проверка данных (все записи имеют корректный location)
- [ ] Тестирование на мобильных устройствах

#### ЭТАП 7: Обновление уведомлений ⏳ **В ПРОЦЕССЕ**
- [x] Добавлена функция `getLocationDisplayName` в `admin-notify.js`
- [x] Обновлено `notifyAdminWinterGroupTrainingCreatedByAdmin`
- [x] Обновлено `notifyAdminWinterGroupTrainingCreated`
- [ ] Обновить оставшиеся уведомления в `admin-notify.js` (17 проблем)
- [ ] Обновить все уведомления в `client-bot.js` (84 проблемы)
- [ ] Обновить уведомления в `notification-service.js` (1 проблема)
- [ ] Проверить все уведомления на соответствие текстов (Кулига и Воронинские горки)

---

## Current Status / Progress Tracking

**Режим:** Planner → Executor

**Статус:** 🔧 **ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С УВЕДОМЛЕНИЯМИ**

1. ✅ Миграция БД выполнена успешно - все записи имеют location = 'kuliga'
2. ✅ Backend API обновлен для поддержки location (kuliga-admin.js, kuliga-public.js, kuliga-booking.js)
3. ✅ Frontend админ-панель обновлен - добавлено поле location в формы создания/редактирования инструктора
4. ✅ Страница бронирования обновлена - добавлен выбор места проведения (Кулига/Воронинские горки)
5. ✅ JavaScript для бронирования обновлен - передача location в API запросы
6. ✅ Telegram бот обновлен - добавлен выбор location в начале процесса бронирования, фильтрация тренировок по location
7. ✅ Schema.sql обновлен - добавлена информация о миграции 038 и поле location во все таблицы

**Выполнено:**
- ✅ Переименована кнопка в админ-панели: "🏔️ СЛУЖБА ИНСТРУКТОРОВ ГОРНОСТАЙЛ72"
- ✅ Изменен заголовок страницы: "Служба инструкторов Горностайл72"
- ✅ Проанализирована структура проекта
- ✅ Составлен детальный план внедрения

---

## 🔔 ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С УВЕДОМЛЕНИЯМИ ИНСТРУКТОРАМ

### Проблема
После миграции 038 (добавление поля `location`) уведомления инструкторам не отправлялись на продакшене.

### Выполненные исправления

#### 1. ✅ Добавлена передача `location` в уведомления
- **Файл:** `src/routes/kuliga-booking.js` - добавлен `location: slotLocation` в уведомление инструктору при индивидуальном бронировании через сайт
- **Файл:** `src/bot/client-bot.js` (функция `createKuligaOwnGroupBooking`) - добавлен `location` из `state.data.location`
- **Файл:** `src/bot/client-bot.js` (функция `createKuligaExistingGroupBooking`) - добавлен `location` из переменной `location`

#### 2. ✅ Улучшено логирование
- **Файл:** `src/bot/admin-notify.js` - добавлено детальное логирование:
  - Логирование факта вызова функции `notifyInstructorKuligaTrainingBooking`
  - Логирование передаваемых данных
  - Детальное логирование ошибок с полным стеком

#### 3. ✅ Создан скрипт диагностики
- **Файл:** `scripts/check-notifications-db.js` - скрипт для проверки состояния базы данных:
  - Проверка наличия `telegram_id` у инструкторов
  - Проверка заполненности поля `location` во всех таблицах
  - Проверка бронирований за последние 2 дня
  - Сводка проблем

#### 4. ✅ Создана документация
- **Файл:** `NOTIFICATION_FIX_INSTRUCTIONS.md` - подробная инструкция по применению исправлений на продакшене
- **Файл:** `NOTIFICATION_FIX_PLAN.md` - план диагностики и исправления

### Что нужно сделать на продакшене

1. **Диагностика БД:**
   - Подключиться к БД через SSH туннель: `ssh -L 6432:127.0.0.1:5432 root@5.129.248.187`
   - Запустить скрипт диагностики: `node scripts/check-notifications-db.js`
   - Проверить результаты и исправить проблемы, если они есть

2. **Применить исправления кода:**
   - Скопировать исправленные файлы на сервер
   - Перезапустить приложение

3. **Тестирование:**
   - Создать тестовое бронирование
   - Проверить логи на наличие записей `[NOTIFY]`
   - Убедиться, что инструктор получил уведомление

### Возможные причины проблемы

1. Отсутствие `telegram_id` у инструктора - инструктор не зарегистрирован в боте
2. NULL значения `location` - старые записи могли не получить location при миграции
3. Отсутствие передачи `location` в функцию уведомления - код не передавал location

Все эти проблемы должны быть решены после применения исправлений.

---

## Executor's Feedback or Assistance Requests

### ✅ Ответы от пользователя:

1. **Название поля в БД:** ✅ `location`

2. **Значения поля `location`:** ✅ `'kuliga'` и `'vorona'` (короткие идентификаторы)

3. **Отображение на фронтенде:**
   - `'kuliga'` → "База отдыха «Кулига-Клуб»"
   - `'vorona'` → "Воронинские горки"

4. **Прайс-лист:** ✅ Одинаковый для обоих мест

5. **Инструкторы:** ✅ Один инструктор работает только на одном месте

6. **Существующие данные:** ✅ Все текущие записи связаны с Кулигой - при миграции установить `location = 'kuliga'` для всех существующих записей

7. **Миграция существующих данных:**
   - ✅ **Подтверждено пользователем:** Все текущие записи в таблицах и все инструкторы связаны с Кулигой
   - **План миграции:**
     - Все существующие инструкторы → установить `location = 'kuliga'`
     - Все существующие слоты → установить `location = 'kuliga'` (или получить из инструктора через JOIN)
     - Все существующие групповые тренировки → установить `location = 'kuliga'` (или получить из слота/инструктора через JOIN)
     - Все существующие бронирования → установить `location = 'kuliga'` (или получить из слота/тренировки через JOIN)

### Дополнительные решения:

- **Изменение `location` инструктора:** Запретить изменение, если есть активные слоты/тренировки в будущем (дата >= сегодня)
- **Отображение в интерфейсе инструктора:** Не показывать (инструктор привязан к одному месту)
- **Функция маппинга:** Создать утилиту для преобразования `location` → читаемое название

---

## Lessons

_Будут добавляться по мере реализации_

---

## Next Steps

1. ✅ **Получены ответы от пользователя на все вопросы**
2. **Начать реализацию с ЭТАП 1 (переименование)**
   - Обновить лендинг (`views/kuliga-landing.ejs`)
   - Обновить SEO мета-теги
3. **Создать утилиту для маппинга location → читаемое название**
   - Файл: `src/utils/location-mapper.js` или `public/js/location-mapper.js`
   - Функция: `getLocationName(location)` → возвращает читаемое название
4. **Создать миграцию БД (ЭТАП 2)**
   - Файл: `src/db/migrations/038_add_location_to_kuliga_tables.sql`
   - Добавить поле `location` во все нужные таблицы
   - Обновить существующие данные (все → `'kuliga'`)
5. **Последовательно обновить все компоненты** (ЭТАП 3-6)

---

## 🔄 ПЛАН: ЛОГИКА ОБРАБОТКИ КОНФЛИКТОВ СЛОТОВ ПРИ ГЕНЕРАЦИИ ТРЕНИРОВОК ИЗ ПРОГРАММ

### Background and Motivation

При генерации тренировок из программ администратором система должна корректно обрабатывать различные сценарии конфликтов с существующими слотами инструктора:
- Точное совпадение времени (10:30 = 10:30)
- Пересечение временных интервалов (слот 10:15-11:15 и программа 10:30-11:30)
- Занятые слоты (booked, blocked, group)
- Свободные слоты (available)

**Цель:** Определить четкую логику обработки всех возможных вариантов конфликтов и согласовать поведение системы.

---

### Key Challenges and Analysis

#### 1. Текущая логика в `generateProgramTrainings`

**Что делает сейчас:**
- Проверяет точное совпадение `start_time` (строка 3453: `AND start_time = $3`)
- Если слот найден с точным совпадением:
  - Если статус `available` или `group` → использует слот
  - Если статус другой (`booked`, `blocked`) → пропускает (skipped++)
- Если слот не найден → создает новый слот

**Проблемы:**
1. ❌ Нет проверки пересечения временных интервалов (10:15-11:15 пересекается с 10:30-11:30)
2. ❌ Не учитывается длительность программы (может быть 60, 75, 90, 105, 120 минут)
3. ❌ Слоты имеют длительность 1 час (start_time + 1 час = end_time), а программы могут быть дольше
4. ❌ Нет проверки, что существующий слот не перекрывается другим занятием

#### 2. Сценарии конфликтов

**Сценарий 1: Точное совпадение времени**
- У инструктора: слот 10:30-11:30, статус `available`
- Программа: 10:30, длительность 90 минут → 10:30-12:00
- **Текущее поведение:** ✅ Использует существующий слот, но программа длиннее слота!
- **Проблема:** Слот заканчивается в 11:30, а тренировка в 12:00

**Сценарий 2: Пересечение интервалов (слот раньше)**
- У инструктора: слот 10:15-11:15, статус `available`
- Программа: 10:30, длительность 90 минут → 10:30-12:00
- **Текущее поведение:** ❌ Не проверяет, создает новый слот 10:30-12:00
- **Проблема:** Два слота перекрываются (10:15-11:15 и 10:30-12:00)

**Сценарий 3: Пересечение интервалов (слот позже)**
- У инструктора: слот 11:00-12:00, статус `available`
- Программа: 10:30, длительность 90 минут → 10:30-12:00
- **Текущее поведение:** ❌ Не проверяет, создает новый слот 10:30-12:00
- **Проблема:** Два слота перекрываются

**Сценарий 4: Слот полностью внутри программы**
- У инструктора: слот 10:45-11:45, статус `available`
- Программа: 10:30, длительность 90 минут → 10:30-12:00
- **Текущее поведение:** ❌ Не проверяет, создает новый слот
- **Проблема:** Перекрытие

**Сценарий 5: Программа полностью внутри слота**
- У инструктора: слот 10:00-12:00, статус `available`
- Программа: 10:30, длительность 60 минут → 10:30-11:30
- **Текущее поведение:** ❌ Не проверяет, создает новый слот
- **Проблема:** Создается дублирующий слот

**Сценарий 6: Занятый слот (booked)**
- У инструктора: слот 10:30-11:30, статус `booked` (индивидуальная тренировка)
- Программа: 10:30, длительность 90 минут → 10:30-12:00
- **Текущее поведение:** ✅ Пропускает (skipped++)
- **Ожидаемое поведение:** ✅ Корректно - слот занят

**Сценарий 7: Заблокированный слот (blocked)**
- У инструктора: слот 10:30-11:30, статус `blocked`
- Программа: 10:30, длительность 90 минут → 10:30-12:00
- **Текущее поведение:** ✅ Пропускает (skipped++)
- **Ожидаемое поведение:** ✅ Корректно - слот заблокирован

**Сценарий 8: Слот со статусом group (групповая тренировка)**
- У инструктора: слот 10:30-11:30, статус `group` (другая групповая тренировка)
- Программа: 10:30, длительность 90 минут → 10:30-12:00
- **Текущее поведение:** ✅ Использует слот
- **Проблема:** ❌ Это другая групповая тренировка! Нельзя использовать тот же слот

**Сценарий 9: Слот со статусом group от той же программы**
- У инструктора: слот 10:30-11:30, статус `group`, связан с той же программой (проверяется выше, строка 3432-3444)
- Программа: 10:30, длительность 90 минут → 10:30-12:00
- **Текущее поведение:** ✅ Пропускает (тренировка уже существует)
- **Ожидаемое поведение:** ✅ Корректно

**Сценарий 10: Пересечение с несколькими слотами**
- У инструктора: 
  - Слот 10:15-11:15, статус `available`
  - Слот 11:00-12:00, статус `available`
- Программа: 10:30, длительность 90 минут → 10:30-12:00
- **Текущее поведение:** ❌ Создает новый слот, не проверяя пересечения
- **Проблема:** Перекрытие с обоими слотами

---

### High-level Task Breakdown

#### ЭТАП 1: Анализ и согласование логики ✅ **ЗАВЕРШЕНО**

#### ЭТАП 2: Реализация ✅ **ЗАВЕРШЕНО**

**Задача 1.1: Определить правила обработки конфликтов**

**Вариант A: Строгая проверка (рекомендуется)**
- Проверять пересечение временных интервалов (`OVERLAPS` в PostgreSQL)
- Если есть любое пересечение → пропускать (skipped++)
- Логировать причины пропуска
- **Плюсы:** Нет конфликтов, чистое расписание
- **Минусы:** Может быть слишком строго (например, слот 10:00-11:00 и программа 11:00-12:00 - это не пересечение)

**Вариант B: Умная проверка с объединением**
- Если есть пересечение со свободным слотом (`available`):
  - Если программа полностью внутри свободного слота → использовать существующий слот, обновить end_time
  - Если программа перекрывается с несколькими свободными слотами → удалить пересекающиеся слоты, создать новый
  - Если программа частично пересекается → пропустить
- **Плюсы:** Максимальное использование существующих слотов
- **Минусы:** Сложная логика, могут быть edge cases

**Вариант C: Только точное совпадение + проверка свободности**
- Проверять только точное совпадение start_time
- Если совпадает и статус `available` или `group` (той же программы) → использовать
- Если не совпадает → создавать новый слот
- Но добавить проверку пересечений перед созданием
- **Плюсы:** Простая логика
- **Минусы:** Могут создаваться перекрывающиеся слоты

**Рекомендация:** **Вариант A (строгая проверка)** с модификацией:
- Проверять пересечение интервалов: `(new_start < existing_end) AND (new_end > existing_start)`
- Если есть пересечение с любым слотом (независимо от статуса) → пропускать
- Логировать детальную информацию о конфликтах
- Возвращать информацию о пропущенных тренировках в ответе API

**Задача 1.2: Определить поведение для разных статусов слотов**

| Статус слота | Поведение при конфликте | Обоснование |
|-------------|------------------------|-------------|
| `available` | Пропускать, если пересекается | Слот свободен, но может быть зарезервирован инструктором |
| `booked` | Пропускать | Слот занят индивидуальной тренировкой |
| `blocked` | Пропускать | Слот заблокирован инструктором |
| `group` | Пропускать (кроме той же программы) | Слот занят другой групповой тренировкой |

**Задача 1.3: Обработка длины программы**

**Проблема:** Слоты имеют фиксированную длительность 1 час, а программы могут быть 60, 75, 90, 105, 120 минут.

**Варианты:**
1. **Создавать слот с правильной длительностью** (рекомендуется)
   - При создании нового слота использовать `end_time = start_time + training_duration`
   - Обновлять существующий слот, если программа длиннее
   
2. **Создавать несколько слотов под длинную программу**
   - Для программы 90 минут создавать слоты 10:30-11:30 и 11:30-12:30
   - **Проблема:** Сложность в управлении

**Рекомендация:** **Вариант 1** - создавать/обновлять слот с правильной длительностью.

**Задача 1.4: Уведомления о конфликтах**

- Администратору: сообщать о количестве пропущенных тренировок и причинах
- Инструктору: не уведомлять о пропущенных (это внутренняя логика)
- В ответе API: возвращать детальную информацию о конфликтах

---

**Задача 2.1: ✅ Создать функцию проверки пересечения временных интервалов** (использован SQL OVERLAPS)

```javascript
/**
 * Проверяет, пересекаются ли два временных интервала
 * @param {string} date1 - Дата первого интервала (YYYY-MM-DD)
 * @param {string} startTime1 - Время начала первого интервала (HH:mm:ss)
 * @param {string} endTime1 - Время окончания первого интервала (HH:mm:ss)
 * @param {string} date2 - Дата второго интервала (YYYY-MM-DD)
 * @param {string} startTime2 - Время начала второго интервала (HH:mm:ss)
 * @param {string} endTime2 - Время окончания второго интервала (HH:mm:ss)
 * @returns {boolean} - true если интервалы пересекаются
 */
function timeIntervalsOverlap(date1, startTime1, endTime1, date2, startTime2, endTime2) {
    // Если разные даты - не пересекаются
    if (date1 !== date2) return false;
    
    // Преобразуем время в минуты для сравнения
    const [h1, m1, s1] = startTime1.split(':').map(Number);
    const [h2, m2, s2] = endTime1.split(':').map(Number);
    const [h3, m3, s3] = startTime2.split(':').map(Number);
    const [h4, m4, s4] = endTime2.split(':').map(Number);
    
    const start1 = h1 * 60 + m1;
    const end1 = h2 * 60 + m2;
    const start2 = h3 * 60 + m3;
    const end2 = h4 * 60 + m4;
    
    // Интервалы пересекаются если: start1 < end2 AND start2 < end1
    return start1 < end2 && start2 < end1;
}
```

**Задача 2.2: ✅ Обновить SQL запрос для проверки пересечений**

Использован PostgreSQL `OVERLAPS`:
```sql
WHERE instructor_id = $1 
  AND date = $2::date
  AND (start_time, end_time) OVERLAPS ($3::time, $4::time)
```

**Задача 2.3: ✅ Обновить логику в `generateProgramTrainings` (РЕАЛИЗОВАНО)**

**Что реализовано:**
- ✅ Проверка пересечений через SQL `OVERLAPS`
- ✅ Удаление свободных пересекающихся слотов (`available`)
- ✅ Пропуск тренировок при конфликте с занятыми слотами (`booked`, `blocked`, `group`)
- ✅ Создание нового слота для программы (длительность всегда 1 час)
- ✅ Сбор информации об удаленных слотах и конфликтах для уведомлений

1. Вычислить `end_time` программы = `start_time + 60 минут` (программа всегда 1 час)
2. Проверить пересечение с существующими слотами:
   ```sql
   SELECT id, status, start_time, end_time 
   FROM kuliga_schedule_slots
   WHERE instructor_id = $1 
     AND date = $2::date
     AND (start_time, end_time) OVERLAPS ($3::time, $4::time)
   ```
3. Обработать найденные пересечения:
   - **Если есть пересекающиеся слоты со статусом `available`:**
     - Удалить все пересекающиеся свободные слоты
     - Записать информацию об удаленных слотах для уведомлений
     - Создать новый слот для программы (10:30-11:30)
   - **Если есть пересекающиеся слоты со статусом `booked`, `blocked` или `group`:**
     - Пропускать тренировку (skipped++)
     - Логировать конфликт с деталями
     - Записать информацию о конфликте для уведомления администратора
4. Если пересечений нет:
   - Если есть точное совпадение start_time:
     - Статус `available` → удалить слот, создать новый (или использовать существующий)
     - Статус `group` + та же программа → пропускать (тренировка уже существует)
     - Статус `group` + другая программа → пропускать (конфликт)
   - Если точного совпадения нет → создать новый слот (10:30-11:30)

---

#### ЭТАП 3: Улучшение уведомлений и отчетности ✅ **ЗАВЕРШЕНО**

**Задача 3.1: ✅ Расширить возвращаемую информацию**

В `generateProgramTrainings` возвращать:
```javascript
{
    created: 5,           // Создано тренировок
    skipped: 3,          // Пропущено тренировок
    slotsCreated: 2,     // Создано новых слотов
    conflicts: [         // Детали конфликтов (НОВОЕ)
        {
            date: '2025-12-07',
            time: '10:30',
            reason: 'slot_conflict',
            conflicting_slot_id: 123,
            conflicting_slot_status: 'booked',
            conflicting_slot_time: '10:15-11:15'
        },
        ...
    ]
}
```

**Задача 3.2: ✅ Уведомление администратору о конфликтах**

**Что реализовано:**
- ✅ Обновлена функция `notifyInstructorSlotsCreatedByAdmin` - добавлена информация об удаленных свободных слотах
- ✅ Обновлена функция `notifyAdminProgramTrainingsGenerated` - добавлена информация об удаленных слотах и пропущенных тренировках
- ✅ Уведомления отправляются инструктору и администратору с детальной информацией

Добавить в уведомление администратору информацию о пропущенных тренировках:
```
✅ Сгенерированы тренировки из программы

📋 Программа: Лыжники новички +14
👨‍🏫 Инструктор: Тебякин Данила
📅 Создано слотов: 2
🏃 Создано тренировок: 2

⚠️ Пропущено тренировок: 3
Причины:
• 07.12.2025 10:30 - пересечение со слотом 10:15-11:15 (статус: booked)
• 14.12.2025 11:00 - пересечение со слотом 11:00-12:00 (статус: blocked)
• 21.12.2025 10:30 - пересечение со слотом 10:30-11:30 (статус: group)
```

---

### ✅ СОГЛАСОВАННАЯ ЛОГИКА ОБРАБОТКИ

#### Правило 1: Проверка пересечений и обработка свободных слотов
- Перед созданием новой тренировки проверять пересечение с ВСЕМИ существующими слотами инструктора на эту дату
- Использовать SQL `OVERLAPS` для проверки пересечения временных интервалов (программа всегда 1 час: 10:30-11:30)
- **Если найдены пересекающиеся слоты со статусом `available`:**
  - Удалить все пересекающиеся свободные слоты
  - Создать новый слот для программы (длительность 1 час)
  - Логировать удаление слотов
  - Уведомлять инструктора и администратора об удалении слотов
- **Если найдены пересекающиеся слоты со статусом `booked`, `blocked` или `group` (другая программа):**
  - Пропускать тренировку (skipped++)
  - Логировать конфликт с указанием статуса занятого слота
  - Уведомлять администратора о пропуске (инструктору не уведомлять)

#### Правило 2: Использование существующих слотов
- Если есть точное совпадение `start_time`:
  - Статус `available` → использовать слот (или удалить и создать новый, если нужно)
  - Статус `group` + та же программа → пропускать (тренировка уже существует)
  - Статус `group` + другая программа → пропускать (конфликт)
  - Статус `booked` → пропускать (слот занят, нельзя трогать)
  - Статус `blocked` → пропускать (слот заблокирован, нельзя трогать)
- Если точного совпадения нет, но есть пересечения только со свободными слотами → удалить пересекающиеся, создать новый

#### Правило 3: Длительность слота
- ✅ Программа всегда длится 1 час практики
- При создании нового слота: `end_time = start_time + 60 минут` (всегда 1 час)
- Минимальный шаг 1:30 между слотами уже заложен при создании слотов инструктором

#### Правило 4: Логирование и уведомления
- Все удаленные свободные слоты логировать с деталями
- Все пропущенные тренировки логировать с причиной (занятый слот, статус)
- Возвращать детальную информацию о конфликтах и удаленных слотах в ответе API
- Уведомлять инструктора о:
  - Удалении его свободных слотов
  - Создании новых слотов для программы
- Уведомлять администратора о:
  - Удалении слотов инструктора (какие слоты удалены)
  - Создании новых слотов
  - Пропуске тренировок из-за занятых слотов (количество и причины)

---

## Current Status / Progress Tracking

**Режим:** ✅ **EXECUTOR** - Реализация завершена

**Статус:** ✅ **РЕАЛИЗОВАНО**

**Ответы пользователя:**

1. **Логика обработки пересечений (СОГЛАСОВАНО):**
   - ✅ Если пересекающиеся слоты свободны (`available`) → удаляем их и создаем новый слот для программы
   - ✅ Если нельзя сместить из-за минимального шага 1:30 → удаляем пересекающиеся слоты
   - ✅ Пример 1: слоты 10:30 и 12:00, программа на 10:15 → удаляем 10:30, создаем 10:15, 12:00 остается
   - ✅ Пример 2: программа на 11:00, слоты 10:30 и 12:00 → удаляем оба, создаем 11:00
   - ✅ Если слоты заняты (`booked`, `blocked`, `group` от другой программы) → нельзя назначать, пропускать тренировку
   - ✅ Уведомлять инструктора и администратора обо всех изменениях (удаление слотов, создание новых)

2. **Длительность программы (СОГЛАСОВАНО):**
   - ✅ Программа всегда длится 1 час практики (это время слота)
   - ✅ Разминка/переодевание (15-20 мин) заложены в шаг 1:30 между слотами
   - ✅ Все программы создают слоты длительностью 1 час (10:30-11:30)

3. **Уведомления (УТОЧНЕНО):**
   - ✅ Уведомлять инструктора и администратора о:
     - Удалении свободных слотов при создании программы
     - Создании новых слотов для программы
     - Пропуске тренировок из-за занятых слотов (конфликты)

4. **Права администратора (СОГЛАСОВАНО):**
   - ✅ Администратор может делать что угодно со свободными слотами (`available`)
   - ✅ Если слот забронирован (`booked`, `blocked`, `group`) → нельзя двигать/смещать

---

## Executor's Feedback or Assistance Requests

_Ожидается ответ от пользователя на вопросы выше для продолжения реализации_
