# 🔍 Анализ проблемы: Почему не приходят запросы на отзывы

## 📋 Краткое резюме

**Проблема:** Клиенты не получают уведомления с просьбой оставить отзыв после тренировок.

**Корневая причина:** Статус тренировок не обновляется с `scheduled` на `completed` после их завершения.

## 🕵️ Детальный анализ

### 1. Проверка базы данных

```sql
SELECT 
    session_date,
    COUNT(*) as trainings_count,
    string_agg(DISTINCT status::text, ', ') as statuses
FROM training_sessions
WHERE session_date >= CURRENT_DATE - INTERVAL '3 days'
GROUP BY session_date;
```

**Результат:**
- 17.10.2025 - 1 тренировка, статус: `scheduled` ❌
- 19.10.2025 - 3 тренировки, статус: `scheduled` ❌  
- 23.10.2025 - 2 тренировки, статус: `scheduled`
- 24.10.2025 - 1 тренировка, статус: `scheduled`
- 26.10.2025 - 1 тренировка, статус: `scheduled`

**Проблема:** Даже прошедшие тренировки (17 и 19 октября) имеют статус `scheduled`.

### 2. Проверка логики отправки отзывов

**Файл:** `src/services/review-notification-service.js`

**Запрос на получение тренировок:**
```javascript
WHERE ts.session_date = $1
    AND ts.status = 'scheduled'  // ⚠️ Здесь проблема!
    AND sp.status = 'confirmed'
```

**Логика:** 
- Система ищет тренировки с датой = СЕГОДНЯ
- И статусом = 'scheduled'
- Но если статус не обновляется на 'completed', запрос не находит завершенные тренировки

### 3. Проверка скрипта обновления статусов

**Файл:** `src/scripts/complete-past-group-sessions.js`

**Существует:** ✅ Да  
**Запускается автоматически:** ❌ Нет  
**Добавлен в планировщик:** ❌ Нет  

```javascript
UPDATE training_sessions
SET status = 'completed', updated_at = NOW()
WHERE training_type = TRUE
  AND status = 'scheduled'
  AND (
    session_date < $1
    OR (session_date = $1 AND end_time < $2)
  )
```

**Что делает:** Обновляет статус завершенных групповых тренировок на `completed`.

### 4. Проверка планировщика

**Файл:** `src/services/scheduler.js`

**Существует задача отправки отзывов:** ✅ Да (21:00 ежедневно)  
**Существует задача обновления статусов:** ❌ Нет

## 🐛 Корневая причина

Цепочка проблем:

1. Тренировка проходит 19.10.2025
2. Статус остается `scheduled` (нет автоматического обновления)
3. В 21:00 запускается задача отправки отзывов
4. Запрос ищет тренировки с датой 19.10 и статусом `scheduled`
5. **НО** логика фильтрует по `ts.status = 'scheduled'`, что означает "запланированные", а не "завершенные"
6. Из-за неоднозначности термина, запрос не находит тренировки
7. Уведомления не отправляются

## ✅ Решения

### Решение 1: Добавить автоматическое обновление статусов (Рекомендуется)

**Добавить в `src/services/scheduler.js`:**

```javascript
/**
 * Настраивает задачу обновления статусов завершенных тренировок
 * Запускается каждые 30 минут
 */
scheduleStatusUpdates() {
    const task = cron.schedule('*/30 * * * *', async () => {
        try {
            console.log(`[${new Date().toISOString()}] Запуск задачи: обновление статусов тренировок`);
            
            const { exec } = require('child_process');
            exec('node src/scripts/complete-past-group-sessions.js', (error, stdout, stderr) => {
                if (error) {
                    console.error(`Ошибка обновления статусов: ${error}`);
                    return;
                }
                console.log(stdout);
            });
            
        } catch (error) {
            console.error(`[${new Date().toISOString()}] Ошибка при обновлении статусов:`, error);
        }
    }, {
        scheduled: true,
        timezone: "Asia/Yekaterinburg"
    });

    this.tasks.push({
        name: 'status_updates',
        description: 'Обновление статусов завершенных тренировок',
        schedule: '*/30 * * * * (Екатеринбург)',
        task: task
    });

    console.log('✓ Задача "Обновление статусов" настроена на каждые 30 минут');
}
```

**Добавить в `init()`:**
```javascript
init() {
    console.log('Инициализация планировщика задач...');
    
    this.scheduleTrainingReminders();
    this.scheduleReviewRequests();
    this.scheduleStatusUpdates();  // ⭐ Добавить эту строку
    
    console.log(`Планировщик запущен. Активных задач: ${this.tasks.length}`);
}
```

### Решение 2: Изменить логику запроса (Альтернатива)

**Изменить в `src/services/review-notification-service.js`:**

Убрать проверку статуса и полагаться только на дату:

```javascript
WHERE ts.session_date = $1
    -- AND ts.status = 'scheduled'  // Убрать эту строку
    AND sp.status = 'confirmed'
    AND c.telegram_id IS NOT NULL
```

**Проблема этого подхода:** Будут отправляться отзывы и для отмененных тренировок.

### Решение 3: Комбинированный подход (Лучший)

1. Добавить автоматическое обновление статусов
2. Изменить логику запроса на более точную:

```javascript
WHERE ts.session_date = $1
    AND ts.status IN ('scheduled', 'completed')  // Оба статуса
    AND ts.end_time < CURRENT_TIME  // Тренировка уже закончилась
    AND sp.status = 'confirmed'
    AND c.telegram_id IS NOT NULL
```

## 🧪 Тестирование решения

### Шаг 1: Обновить статусы вручную

```bash
node src/scripts/complete-past-group-sessions.js
```

### Шаг 2: Проверить обновленные статусы

```sql
SELECT 
    session_date,
    COUNT(*) as total,
    COUNT(*) FILTER (WHERE status = 'completed') as completed,
    COUNT(*) FILTER (WHERE status = 'scheduled') as scheduled
FROM training_sessions
WHERE session_date < CURRENT_DATE
GROUP BY session_date
ORDER BY session_date DESC
LIMIT 5;
```

### Шаг 3: Запустить отправку отзывов для прошедшей даты

```bash
node src/scripts/send-review-requests.js 2025-10-19
```

### Шаг 4: Проверить логи

```sql
SELECT * FROM review_notification_logs
WHERE sent_at::date = '2025-10-19'
ORDER BY sent_at DESC;
```

## 📝 План внедрения

### Приоритет 1: Срочно (Сегодня)

1. ✅ Обновить статусы прошедших тренировок вручную
2. ✅ Добавить задачу обновления статусов в планировщик
3. ✅ Перезапустить сервер
4. ✅ Протестировать отправку за 19.10.2025

### Приоритет 2: Важно (Завтра)

1. ✅ Добавить мониторинг отправки отзывов
2. ✅ Проверить логи за последние дни
3. ✅ Убедиться, что задача запускается в 21:00

### Приоритет 3: Улучшения (На будущее)

1. ⚠️ Добавить статус 'cancelled' для отмененных тренировок
2. ⚠️ Улучшить логирование планировщика
3. ⚠️ Добавить админ-панель для просмотра статусов задач

## 🚀 Немедленные действия

**1. Обновить статусы всех прошедших тренировок:**
```bash
cd /home/dan/Project/gornostyle
node src/scripts/complete-past-group-sessions.js
```

**2. Добавить задачу в планировщик** (см. Решение 1)

**3. Перезапустить приложение:**
```bash
pm2 restart gornostyle
# или
pm2 restart all
```

**4. Отправить отзывы за пропущенные дни:**
```bash
node src/scripts/send-review-requests.js 2025-10-19
node src/scripts/send-review-requests.js 2025-10-17
```

## 📊 Ожидаемый результат

После внедрения решения:

✅ Статусы тренировок будут обновляться каждые 30 минут  
✅ Запросы на отзывы будут отправляться в 21:00  
✅ Клиенты будут получать уведомления после тренировок  
✅ Система будет работать автоматически без ручного вмешательства  

## 📞 Контрольные точки

- **Через 1 час:** Проверить обновление статусов
- **В 21:00 сегодня:** Проверить отправку отзывов
- **Завтра утром:** Проверить логи за прошедший день

---

**Дата анализа:** 20 октября 2025  
**Статус:** 🔴 Критическая проблема найдена  
**Решение:** ✅ Готово к внедрению
