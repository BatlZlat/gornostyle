# Ski-instruktor Bot

Telegram-бот для управления горнолыжной школой, позволяющий клиентам записываться на тренировки, управлять своим профилем и балансом.

## 🚀 Основные функции

- 📝 Регистрация и управление профилем
- 🎯 Запись на тренировки
- 👶 Управление данными детей
- 💰 Управление балансом
- 🎁 Подарочные сертификаты
- 📋 Просмотр расписания и записей

## 🛠 Технологии

- Node.js
- PostgreSQL
- node-telegram-bot-api
- Express.js
- EJS (шаблонизатор)

## 📋 Структура проекта

```
src/
├── bot/                    # Telegram бот
│   ├── index.js           # Основной файл бота
│   ├── handlers.js        # Обработчики команд
│   └── db-utils.js        # Утилиты для работы с БД
├── db/                     # Работа с базой данных
│   ├── index.js           # Конфигурация подключения
│   └── migrations/        # Миграции базы данных
├── views/                  # Шаблоны EJS
│   ├── admin/             # Админ-панель
│   └── public/            # Публичные страницы
└── public/                 # Статические файлы
    ├── css/               # Стили
    └── js/                # Клиентские скрипты
```

## 🚀 Установка и запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/ski-instruktor.git
cd ski-instruktor
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в корневой директории:
```env
TELEGRAM_BOT_TOKEN=your_bot_token
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ski_instruktor
DB_USER=your_username
DB_PASSWORD=your_password
```

4. Запустите миграции базы данных:
```bash
npm run migrate
```

5. Запустите бота:
```bash
npm run start
```

## 📝 Команды бота

- `/start` - Начало работы с ботом
- `/help` - Справка по командам

## 👥 Роли пользователей

1. **Клиент**
   - Регистрация и управление профилем
   - Запись на тренировки
   - Управление данными детей
   - Просмотр баланса и истории операций

2. **Администратор**
   - Управление расписанием
   - Управление клиентами
   - Управление тренерами
   - Просмотр статистики

## 🔄 Процесс регистрации

1. Начало работы с ботом
2. Ввод ФИО
3. Ввод даты рождения
4. Ввод номера телефона
5. Добавление данных ребенка (опционально)
6. Подтверждение регистрации

## 💰 Управление балансом

- Просмотр текущего баланса
- Пополнение счета
- История операций
- Списание средств за тренировки

## 🎯 Запись на тренировки

- Выбор даты и времени
- Выбор тренера
- Выбор типа тренировки
- Подтверждение записи
- Отмена записи

## 🔒 Безопасность

- Валидация всех входящих данных
- Защита от SQL-инъекций
- Безопасное хранение паролей
- Логирование действий

## 👤 Управление учетными данными администратора

Для изменения логина и пароля администратора используйте скрипт:

```bash
node src/scripts/generateAdminCredentials.js <новый_логин> <новый_пароль>
```

Например:
```bash
node src/scripts/generateAdminCredentials.js admin mynewpassword123
```

Скрипт сгенерирует:
1. Хеш для нового логина
2. Хеш для нового пароля
3. Новый секретный ключ для JWT

Добавьте полученные значения в файл `.env`:
```env
ADMIN_LOGIN_HASH=<сгенерированный_хеш_логина>
ADMIN_PASSWORD_HASH=<сгенерированный_хеш_пароля>
JWT_SECRET=<сгенерированный_секретный_ключ>
```

После обновления `.env` перезапустите сервер для применения изменений.

## 📈 Статистика и аналитика

- Количество клиентов
- Популярные тренировки
- Занятость тренеров
- Финансовая статистика

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте pull request

## 📄 Лицензия

MIT License

## Автоматическое обновление статусов групповых тренировок

Для корректного расчёта финансов необходимо, чтобы прошедшие групповые тренировки автоматически переводились в статус `completed`. Для этого используется скрипт:

```
node src/scripts/complete-past-group-sessions.js
```

### Пример строки для crontab (запуск в 01 и 31 минуту каждого часа):

```
1,31 * * * * /usr/bin/node /home/dan/Project/gornostyle/src/scripts/complete-past-group-sessions.js >> /home/dan/Project/gornostyle/schedule-cron.log 2>&1
```

Путь к node может отличаться, проверьте через `which node`.

## 🗄️ Резервное копирование и восстановление базы данных

### Как работает автоматический бэкап

- На сервере ежедневно в 2:00 ночи по cron автоматически создаётся резервная копия базы данных PostgreSQL `skisimulator`.
- Скрипт бэкапа находится на сервере по пути `/root/scripts/backup_database.sh`.
- Все бэкапы хранятся в директории `/root/gornostyle-backups`.
- Каждый бэкап — это сжатый файл вида: `skisimulator_YYYY-MM-DD_HH-MM-SS.sql.gz`.
- Старые бэкапы (старше 30 дней) удаляются автоматически.
- Вся информация о работе скрипта пишется в лог `/root/gornostyle-backups/backup.log`.

### Как посмотреть список бэкапов

1. Подключитесь к серверу по SSH:
   ```bash
   ssh -i ~/.ssh/gornostyle_backup root@90.156.210.24
   ```
2. Перейдите в папку с бэкапами:
   ```bash
   cd /root/gornostyle-backups
   ls -lh
   ```

### Как восстановить базу данных из бэкапа

1. Подключитесь к серверу по SSH:
   ```bash
   ssh -i ~/.ssh/gornostyle_backup root@90.156.210.24
   ```
2. Перейдите в папку с бэкапами:
   ```bash
   cd /root/gornostyle-backups
   ```
3. Выберите нужный файл бэкапа, например `skisimulator_2025-06-16_17-48-18.sql.gz`.
4. Выполните команду восстановления:
   ```bash
   export PGPASSWORD='Nemezida2324%)'
   gunzip -c skisimulator_2025-06-16_17-48-18.sql.gz | psql -h 90.156.210.24 -U batl-zlat -d skisimulator -p 5432
   unset PGPASSWORD
   ```
   > **Внимание!** Перед восстановлением убедитесь, что приложение остановлено, чтобы избежать конфликтов.

### Как изменить расписание или параметры бэкапа

- Скрипт бэкапа: `/root/scripts/backup_database.sh`
- Cron-задача на сервере:
  ```bash
  crontab -e
  # Пример строки:
  0 2 * * * /root/scripts/backup_database.sh >> /root/gornostyle-backups/backup.log 2>&1
  ```
- Для изменения параметров подключения к базе (имя, пользователь, пароль) — отредактируйте переменные в начале скрипта.

### Важно
- Никогда не храните пароли в публичных репозиториях!
- Доступ к серверу и бэкапам должен быть только у доверенных лиц.
- Регулярно проверяйте логи и наличие свежих бэкапов.

## Как работать с репозиторием на GitHub (простыми словами)

### Основные термины
- **Репозиторий** — это хранилище вашего кода и истории изменений.
- **Ветка (branch)** — отдельная линия разработки. Например, `master` — основная (стабильная), `develop` — для работы, `feature/что-то` — для новых функций.
- **Коммит (commit)** — сохранение изменений с комментарием (как "снимок" состояния кода).
- **Push (пушить)** — отправить ваши изменения из локального компьютера в репозиторий на GitHub.
- **Pull Request (PR)** — запрос на добавление ваших изменений из одной ветки в другую (например, из `develop` в `master`). Это как заявка: "Я сделал изменения, посмотрите и добавьте их в основную версию".
- **Merge** — слияние изменений из одной ветки в другую.
- **Feature-ветка** — отдельная ветка для работы над одной задачей или функцией (например, `feature/telegram-bot`).

### Как теперь работать
1. **Вся работа — в ветке `develop` или в отдельных feature-ветках.**
2. **Ветка `master` защищена:** нельзя отправлять изменения напрямую, только через Pull Request.

#### Пример рабочего процесса

1. **Создать новую ветку для задачи:**
   ```bash
   git checkout develop
   git pull
   git checkout -b feature/название-задачи
   # ...работаете, делаете изменения...
   git add .
   git commit -m "Краткое описание изменений"
   git push origin feature/название-задачи
   ```
2. **Создать Pull Request на GitHub:**
   - Перейдите на сайт репозитория
   - Нажмите "Compare & pull request" или "New pull request"
   - Выберите вашу ветку и ветку назначения (`develop` или `master`)
   - Оставьте комментарий и создайте PR
3. **После проверки и слияния PR:**
   - Ваша ветка объединится с основной
   - Можно удалить feature-ветку

#### Как сделать релиз (обновить master)
1. Убедитесь, что все изменения протестированы в `develop`
2. Создайте Pull Request из `develop` в `master`
3. После слияния PR создайте тег (версию):
   ```bash
   git checkout master
   git pull
   git tag -a v1.1.0 -m "Описание релиза"
   git push origin v1.1.0
   ```

### Краткий словарь
- **Пушить** — отправлять изменения на сервер (GitHub)
- **Фича** — новая функция или задача
- **PR (Pull Request)** — запрос на слияние изменений
- **Мержить** — объединять ветки
- **Тег (tag)** — метка для релиза (например, v1.0.0)

---