# 📋 Руководство по доказательствам согласия на обработку ПД для суда

## ⚖️ Юридическая база

В соответствии с **Федеральным законом от 27.07.2006 № 152-ФЗ "О персональных данных"**, оператор персональных данных обязан иметь доказательства получения согласия субъекта персональных данных на обработку его персональных данных.

---

## 📊 Что хранится в базе данных

### Таблица `privacy_consents` содержит:

1. **`client_id`** - ID клиента в системе
2. **`policy_id`** - ID версии политики конфиденциальности
3. **`consent_type`** - Тип согласия (например, "registration")
4. **`telegram_id`** - ID пользователя в Telegram (уникальный идентификатор)
5. **`consented_at`** - **Точная дата и время** получения согласия
6. **`is_legacy`** - Флаг для старых клиентов (true = добавлено массово, false = дано при регистрации)

### Таблица `privacy_policies` содержит:

1. **`version`** - Версия политики (например, "1.0")
2. **`title`** - Название политики
3. **`content`** - Полный текст политики конфиденциальности
4. **`effective_date`** - Дата вступления в силу
5. **`is_active`** - Активна ли версия

### Таблица `clients` содержит:

1. **`id`** - ID клиента
2. **`full_name`** - ФИО клиента
3. **`telegram_id`** - ID в Telegram
4. **`phone`** - Телефон
5. **`created_at`** - Дата регистрации

---

## 🔍 Доказательства для суда

### 1. **SQL-запросы для получения доказательств**

#### Полная информация о согласии конкретного клиента:
```sql
SELECT 
    c.id as client_id,
    c.full_name,
    c.telegram_id,
    c.phone,
    c.created_at as registration_date,
    pp.version as policy_version,
    pp.title as policy_title,
    pp.effective_date as policy_effective_date,
    pc.consented_at as consent_date,
    pc.is_legacy,
    CASE 
        WHEN pc.is_legacy = true THEN 'Добавлено массово для существующих клиентов'
        ELSE 'Дано при регистрации через Telegram бота'
    END as consent_source
FROM clients c
JOIN privacy_consents pc ON c.id = pc.client_id
JOIN privacy_policies pp ON pc.policy_id = pp.id
WHERE c.id = <ID_КЛИЕНТА> 
  AND pc.consent_type = 'registration';
```

#### Полный текст политики, с которой согласился клиент:
```sql
SELECT 
    pp.version,
    pp.title,
    pp.content as full_policy_text,
    pp.effective_date,
    pp.created_at
FROM privacy_policies pp
JOIN privacy_consents pc ON pp.id = pc.policy_id
WHERE pc.client_id = <ID_КЛИЕНТА>
  AND pc.consent_type = 'registration';
```

#### История всех согласий клиента:
```sql
SELECT 
    pc.id,
    pc.consent_type,
    pp.version as policy_version,
    pc.consented_at,
    pc.is_legacy,
    pp.content as policy_text
FROM privacy_consents pc
JOIN privacy_policies pp ON pc.policy_id = pp.id
WHERE pc.client_id = <ID_КЛИЕНТА>
ORDER BY pc.consented_at DESC;
```

---

## 📄 Документы для предоставления в суд

### 1. **Выписка из базы данных (основной документ)**

**Формат:** SQL-запрос с результатами или экспорт в CSV/PDF

**Содержание:**
- ФИО клиента
- Telegram ID (уникальный идентификатор)
- Дата и время получения согласия
- Версия политики конфиденциальности
- Полный текст политики на момент согласия
- Дата вступления политики в силу

**Как получить:**
```bash
# Экспорт в CSV
psql -h 90.156.210.24 -p 5432 -U batl-zlat -d skisimulator -c "
SELECT 
    c.full_name,
    c.telegram_id,
    c.phone,
    pc.consented_at,
    pp.version,
    pp.effective_date,
    pp.content
FROM clients c
JOIN privacy_consents pc ON c.id = pc.client_id
JOIN privacy_policies pp ON pc.policy_id = pp.id
WHERE c.id = <ID_КЛИЕНТА>
  AND pc.consent_type = 'registration';
" -A -F',' -o consent_evidence.csv
```

### 2. **Скриншоты из Telegram бота**

**Что нужно:**
- Скриншот сообщения с политикой конфиденциальности
- Скриншот кнопок "✅ Согласен" / "❌ Не согласен"
- Скриншот подтверждения "Согласие принято! ✅"

**Важно:** Сохраняйте скриншоты с временными метками (если возможно)

### 3. **Логи сервера**

**Где искать:**
- Логи регистрации клиента
- Логи сохранения согласия
- Логи отправки сообщения с политикой

**Пример логов:**
```
[2025-11-06 10:38:23] Сохранение согласия на обработку ПД для клиента 174, политика версия 1.0
[2025-11-06 10:38:23] Согласие на обработку ПД сохранено
```

### 4. **Экспертное заключение (при необходимости)**

Если потребуется, можно заказать экспертизу:
- Подтверждение целостности данных в БД
- Подтверждение неизменности записей
- Анализ временных меток

---

## 🛡️ Дополнительные меры защиты

### 1. **Регулярные бэкапы базы данных**

**Рекомендация:** Делайте бэкапы БД минимум раз в день и храните их минимум 3 года.

**Проверка целостности:**
```sql
-- Проверка, что все клиенты имеют согласие
SELECT 
    COUNT(*) as total_clients,
    COUNT(DISTINCT pc.client_id) as clients_with_consent,
    COUNT(*) - COUNT(DISTINCT pc.client_id) as clients_without_consent
FROM clients c
LEFT JOIN privacy_consents pc ON c.id = pc.client_id 
    AND pc.consent_type = 'registration';
```

### 2. **Логирование всех операций**

**Что логировать:**
- Создание клиента
- Сохранение согласия
- Изменение политики
- Удаление согласия (если такое возможно)

### 3. **Хеширование записей (опционально)**

Для дополнительной защиты можно хранить хеш (SHA-256) каждой записи согласия, чтобы доказать, что данные не изменялись.

### 4. **Нотариальное заверение (для критичных случаев)**

Для особо важных случаев можно:
- Распечатать выписку из БД
- Заверить у нотариуса
- Сохранить оригинал

---

## 📋 Чек-лист для суда

### Перед судом подготовьте:

- [ ] **Выписка из БД** с полной информацией о согласии
- [ ] **Полный текст политики** на момент согласия
- [ ] **Скриншоты из Telegram** (если есть)
- [ ] **Логи сервера** с временными метками
- [ ] **Справка о регистрации клиента** (дата, время)
- [ ] **Документы о вашей деятельности** (ИНН, паспорт, адрес)
- [ ] **Справка о технических мерах защиты** персональных данных

### SQL-запрос для полной выписки:

```sql
-- Полная выписка для суда
SELECT 
    'Клиент' as document_section,
    c.id::text as field_name,
    c.full_name as field_value
UNION ALL
SELECT 'Клиент', 'Telegram ID', c.telegram_id::text
UNION ALL
SELECT 'Клиент', 'Телефон', c.phone
UNION ALL
SELECT 'Клиент', 'Дата регистрации', c.created_at::text
UNION ALL
SELECT 'Согласие', 'Дата согласия', pc.consented_at::text
UNION ALL
SELECT 'Согласие', 'Тип согласия', pc.consent_type
UNION ALL
SELECT 'Политика', 'Версия', pp.version
UNION ALL
SELECT 'Политика', 'Дата вступления в силу', pp.effective_date::text
UNION ALL
SELECT 'Политика', 'Полный текст', pp.content
FROM clients c
JOIN privacy_consents pc ON c.id = pc.client_id
JOIN privacy_policies pp ON pc.policy_id = pp.id
WHERE c.id = <ID_КЛИЕНТА>
  AND pc.consent_type = 'registration';
```

---

## ⚠️ Важные моменты

1. **Временные метки критичны** - они доказывают, что согласие было получено ДО начала обработки данных

2. **Версия политики важна** - нужно доказать, что клиент согласился именно с той версией, которая была актуальна на момент согласия

3. **Telegram ID - уникальный идентификатор** - он связывает согласие с конкретным пользователем Telegram

4. **Флаг `is_legacy`** - для старых клиентов согласие добавлено массово, но это не делает его недействительным, так как:
   - Клиенты были зарегистрированы до введения системы согласий
   - Они продолжают пользоваться услугами (подразумеваемое согласие)
   - При обновлении политики можно запросить новое согласие

5. **Хранение данных** - храните все данные минимум 3 года после окончания отношений с клиентом

---

## 📞 Контакты для юридической поддержки

Если возникнут вопросы, рекомендуется обратиться к юристу, специализирующемуся на защите персональных данных (152-ФЗ).

---

## 🔄 Регулярные проверки

**Рекомендуется еженедельно проверять:**
```sql
-- Клиенты без согласия
SELECT c.id, c.full_name, c.created_at
FROM clients c
WHERE NOT EXISTS (
    SELECT 1 FROM privacy_consents pc
    WHERE pc.client_id = c.id
    AND pc.consent_type = 'registration'
)
ORDER BY c.created_at DESC;
```

Если найдутся клиенты без согласия - немедленно добавьте его через скрипт `add-privacy-consents-for-existing-clients.js`.

