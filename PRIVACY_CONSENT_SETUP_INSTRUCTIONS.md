# 📋 Инструкция по настройке системы согласий на обработку ПД

## ✅ Что было сделано

1. **Созданы таблицы в БД:**
   - `privacy_policies` - для хранения версий политики конфиденциальности
   - `privacy_consents` - для хранения согласий пользователей

2. **Создана миграция:** `src/db/migrations/026_create_privacy_policies_and_consents.sql`

3. **Обновлена схема:** `src/models/schema.sql`

4. **Интегрировано сохранение согласия:**
   - При регистрации нового клиента согласие автоматически сохраняется в БД
   - Используется активная версия политики конфиденциальности

5. **Созданы скрипты:**
   - `src/scripts/init-privacy-policy.js` - для создания начальной записи политики
   - `src/scripts/add-privacy-consents-for-existing-clients.js` - для массового добавления согласий старым клиентам

---

## 🚀 Порядок действий для настройки

### Шаг 1: Применить миграцию

```bash
# Подключитесь к БД и выполните миграцию
psql -h 90.156.210.24 -p 5432 -U batl-zlat -d skisimulator -f src/db/migrations/026_create_privacy_policies_and_consents.sql
```

Или через переменные окружения:
```bash
export PGPASSWORD='Nemezida2324%)'
psql -h 90.156.210.24 -p 5432 -U batl-zlat -d skisimulator -f src/db/migrations/026_create_privacy_policies_and_consents.sql
```

### Шаг 2: Создать начальную запись политики

```bash
# Активируйте виртуальное окружение (если используется)
source venv-landing/bin/activate

# Запустите скрипт инициализации
node src/scripts/init-privacy-policy.js
```

### Шаг 3: Добавить согласия для существующих клиентов

```bash
# Запустите скрипт для массового добавления согласий
node src/scripts/add-privacy-consents-for-existing-clients.js
```

Этот скрипт:
- Найдет всех клиентов без согласия
- Создаст запись согласия для каждого (с флагом `is_legacy = true`)
- Использует дату регистрации клиента как дату согласия

---

## 📊 Проверка результата

После выполнения всех шагов проверьте:

```sql
-- Проверка политик
SELECT id, version, title, is_active, effective_date 
FROM privacy_policies;

-- Проверка согласий
SELECT 
    COUNT(*) as total_consents,
    COUNT(DISTINCT client_id) as unique_clients,
    COUNT(*) FILTER (WHERE is_legacy = true) as legacy_consents,
    COUNT(*) FILTER (WHERE is_legacy = false) as new_consents
FROM privacy_consents;

-- Проверка клиентов без согласия
SELECT COUNT(*) as clients_without_consent
FROM clients c
WHERE NOT EXISTS (
    SELECT 1 FROM privacy_consents pc
    WHERE pc.client_id = c.id
    AND pc.consent_type = 'registration'
);
```

---

## 🔄 Как это работает

### Для новых клиентов:
1. Пользователь проходит регистрацию в Telegram боте
2. На этапе согласия нажимает "✅ Согласен"
3. При создании клиента в БД автоматически сохраняется запись в `privacy_consents`
4. Связывается с активной версией политики (`privacy_policies`)

### Для старых клиентов:
- Согласие добавляется массово через скрипт
- Помечается флагом `is_legacy = true`
- Дата согласия = дата регистрации клиента

---

## 📝 Структура данных

### `privacy_policies`
- `id` - уникальный идентификатор
- `version` - версия политики (например, "1.0")
- `title` - название политики
- `content` - текст политики
- `is_active` - активна ли версия
- `effective_date` - дата вступления в силу

### `privacy_consents`
- `id` - уникальный идентификатор
- `client_id` - ID клиента
- `policy_id` - ID версии политики
- `consent_type` - тип согласия (например, "registration")
- `telegram_id` - ID пользователя в Telegram
- `consented_at` - дата и время согласия
- `is_legacy` - флаг для старых клиентов

---

## ⚠️ Важные замечания

1. **Одна версия политики:** Если политика обновляется, нужно:
   - Создать новую запись в `privacy_policies` с новой версией
   - Установить `is_active = false` для старой версии
   - Установить `is_active = true` для новой версии

2. **Старые клиенты:** Все существующие клиенты получат согласие с флагом `is_legacy = true`. Это позволяет отслеживать, кто дал согласие при регистрации, а кому оно было добавлено массово.

3. **Если не согласен:** Если пользователь не согласен с политикой, регистрация не завершается (как и сейчас).

---

## 🐛 Отладка

Если что-то пошло не так:

1. **Проверьте логи при регистрации:**
   - Должно быть сообщение: "Сохранение согласия на обработку ПД для клиента X, политика версия Y"
   - Если политика не найдена: "⚠️ ВНИМАНИЕ: Не найдена активная политика конфиденциальности"

2. **Проверьте наличие активной политики:**
   ```sql
   SELECT * FROM privacy_policies WHERE is_active = true;
   ```

3. **Проверьте согласия конкретного клиента:**
   ```sql
   SELECT pc.*, pp.version, pp.title
   FROM privacy_consents pc
   JOIN privacy_policies pp ON pc.policy_id = pp.id
   WHERE pc.client_id = <ID_КЛИЕНТА>;
   ```

