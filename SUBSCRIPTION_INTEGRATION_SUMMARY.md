# Резюме интеграции системы абонементов

## ✅ Выполненные изменения

### 1. База данных
- **Миграция 022**: Увеличен размер поля `transactions.type` с `VARCHAR(20)` до `VARCHAR(50)` для поддержки новых типов транзакций
- **Файл**: `src/db/migrations/022_increase_transaction_type_length.sql`
- **Файл**: `src/models/schema.sql` (обновлено определение таблицы)

### 2. Helper-функции для работы с абонементами
- **Файл**: `src/services/subscription-helper.js`
- **Функции**:
  - `checkAndUseSubscription()` - проверяет наличие активного абонемента и списывает занятие
  - `returnSubscriptionSession()` - возвращает занятие в абонемент при отмене
  - `checkTrainingSubscriptionUsage()` - проверяет, использовался ли абонемент для тренировки

### 3. Модификация логики записи на групповую зимнюю тренировку
- **Файл**: `src/bot/client-bot.js` (строки 4400-4650)
- **Изменения**:
  - Добавлена проверка наличия активного абонемента перед проверкой баланса
  - При наличии абонемента:
    - Не списываются деньги с баланса
    - Списывается одно занятие из абонемента
    - Создается запись в `natural_slope_subscription_usage`
    - Создается транзакция типа `subscription_usage` с суммой 0
    - В описании указывается количество оставшихся занятий (например: "Занятий осталось: 9/10")
  - При отсутствии абонемента: работает стандартная логика (списание с баланса)
  - Уведомления клиенту показывают:
    - Название абонемента
    - Количество оставшихся занятий
    - Баланс не изменился

### 4. Модификация логики отмены тренировки
- **Файл**: `src/bot/client-bot.js` (строки 5533-5645)
- **Изменения**:
  - Добавлена проверка, использовался ли абонемент для тренировки
  - При отмене тренировки, оплаченной абонементом:
    - Занятие возвращается в абонемент (`remaining_sessions += 1`)
    - Статус абонемента меняется на `active` (если срок действия не истек)
    - Удаляется запись из `natural_slope_subscription_usage`
    - Деньги НЕ возвращаются на баланс
  - При отмене тренировки, оплаченной деньгами:
    - Работает стандартная логика возврата средств

### 5. Обновление уведомлений
- **Файл**: `src/bot/admin-notify.js` (функция `notifyAdminWinterGroupTrainingCreated`)
- **Изменения**:
  - Добавлена информация об использовании абонемента в уведомлениях админу:
    - Название абонемента
    - Количество оставшихся занятий
  - Если использовался абонемент - не показывается стоимость в рублях

### 6. Обновление админ-панели
- **Файл**: `public/js/admin.js` (функция `getTransactionTypeRu`)
- **Изменения**:
  - Добавлены переводы для новых типов транзакций:
    - `subscription_purchase` → "Покупка абонемента"
    - `subscription_usage` → "Запись по абонементу"
  - В таблице транзакций:
    - Для записи по абонементу показывается сумма 0 ₽
    - В описании указывается количество оставшихся занятий

### 7. Тест системы
- **Файл**: `test-subscription-booking.js`
- **Функционал**: Автоматический тест полного цикла использования абонемента

## 📋 Правила применения абонемента

1. **Автоматическое применение**: При записи на групповую зимнюю тренировку система автоматически проверяет наличие активного абонемента
2. **Приоритет**: Абонемент имеет приоритет над балансом - если есть активный абонемент, он используется в первую очередь
3. **Применимость**: 
   - ✅ Групповые зимние тренировки (`slope_type = 'natural_slope'` AND `winter_training_type = 'group'`)
   - ✅ Применяется к клиенту и всем его детям
   - ❌ НЕ применяется к индивидуальным тренировкам
   - ❌ НЕ применяется к тренировкам на тренажере
4. **Возврат**: При отмене тренировки занятие автоматически возвращается в абонемент
5. **Выбор абонемента**: Если у клиента несколько активных абонементов, используется тот, который истекает раньше

## 🔄 Логика работы

### Запись на тренировку:
```
1. Клиент выбирает групповую зимнюю тренировку
2. Система проверяет наличие активного абонемента
   ├─ Если есть абонемент:
   │   ├─ Создается запись участника
   │   ├─ Списывается 1 занятие из абонемента
   │   ├─ Создается запись в subscription_usage
   │   ├─ Создается транзакция (amount=0, type='subscription_usage')
   │   ├─ Уведомление клиенту с информацией об абонементе
   │   └─ Уведомление админу с информацией об абонементе
   └─ Если нет абонемента:
       ├─ Проверяется баланс
       ├─ Списываются деньги
       ├─ Создается транзакция (type='payment')
       └─ Стандартные уведомления
```

### Отмена тренировки:
```
1. Клиент отменяет тренировку
2. Система проверяет способ оплаты
   ├─ Если использовался абонемент:
   │   ├─ Возвращается 1 занятие в абонемент
   │   ├─ Удаляется запись из subscription_usage
   │   ├─ Уведомление клиенту (занятие возвращено)
   │   └─ Деньги НЕ возвращаются
   └─ Если оплачено деньгами:
       ├─ Возвращаются деньги на баланс
       ├─ Создается транзакция (type='amount')
       └─ Стандартное уведомление
```

## 📊 Пример транзакций в админке

### Покупка абонемента:
```
ID: 1025
Тип: Покупка абонемента
Сумма: 13600 ₽
Дата: 03.11.2025
Описание: Покупка абонемента "10 занятий Кулига Парк"
```

### Запись по абонементу:
```
ID: 1026
Тип: Запись по абонементу
Сумма: 0 ₽
Дата: 03.11.2025
Описание: Запись по абонементу: Групповая тренировка в Кулига Парк, Иван Иванов, Дата: 04.11.2025, Время: 10:30, Длительность: 60 мин. Занятий осталось: 9/10
```

## 🧪 Тестирование

### Запуск теста:
```bash
node test-subscription-booking.js
```

### Что тестируется:
1. Поиск клиента с активным абонементом
2. Поиск доступной групповой зимней тренировки
3. Использование абонемента при записи
4. Проверка записи в subscription_usage
5. Возврат занятия при отмене
6. Очистка тестовых данных

## ⚠️ Важные замечания

1. **Переменная окружения**: `BASE_URL` должна быть:
   - Локально: `http://localhost:8080`
   - На продакшене: `https://gornostyle72.ru`
   - Файл `.env` в `.gitignore`, поэтому на каждом сервере своя конфигурация

2. **Миграция**: Необходимо применить миграцию 022 перед деплоем:
   ```bash
   psql -h HOST -U USER -d DATABASE -f src/db/migrations/022_increase_transaction_type_length.sql
   ```

3. **Совместимость**: Старые записи продолжают работать как раньше, новая логика применяется только при наличии активного абонемента

4. **Приоритет**: Система всегда пытается использовать абонемент в первую очередь, что экономит деньги клиента

## 📝 Файлы для деплоя

Измененные файлы, которые нужно загрузить на продакшн:
1. `src/bot/client-bot.js`
2. `src/bot/admin-notify.js`
3. `src/services/subscription-helper.js` (новый)
4. `src/models/schema.sql`
5. `src/db/migrations/022_increase_transaction_type_length.sql` (новый)
6. `public/js/admin.js`

## ✅ Результат

Система абонементов полностью интегрирована в процесс записи на групповые зимние тренировки. Клиенты могут:
- Покупать абонементы через бота
- Автоматически использовать их при записи
- Получать занятия обратно при отмене
- Видеть актуальную информацию о количестве занятий

Администраторы получают полную информацию об использовании абонементов в уведомлениях и могут отслеживать все транзакции в панели управления.

