# Способы записи на тренировки на странице `/instruktor-po-gornym-lyzham-snoubordy-tyumen`

## 📋 Общая информация

**Важно:** На главной странице инструкторов **НЕТ модальных окон** для записи. Все действия записи ведут на отдельную страницу `/instruktor-po-gornym-lyzham-snoubordy-tyumen/booking`, которая открывается как отдельная страница (не модальное окно).

---

## 🔹 Способ 1: Запись через карточки прайса (цена-контейнеры)

**Где находится:**
- Секция `#prices` на главной странице
- Карточки с ценами отображаются через `renderPriceList()` в `kuliga.js`

**Что происходит:**
1. Клиент кликает кнопку "Записаться" на карточке прайса
2. Обработчик: `initPriceBookingButtons()` в `kuliga.js` (строка 363)
3. Переход на `/instruktor-po-gornym-lyzham-snoubordy-tyumen/booking` с параметрами:
   - `priceId` - ID тарифа
   - `type` - тип (individual/group)
   - `price` - цена из прайса
   - `duration` - длительность (обычно 60)
   - `participants` - количество участников (для групповых)

**Особенности:**
- ✅ Фиксированная цена (из прайса)
- ✅ Фиксированное количество участников (из прайса)
- ❌ Нельзя добавить участников в форме
- ✅ Цена не редактируется

**Файлы:**
- `public/js/kuliga.js` (строки 109-200, 363-380)
- `views/kuliga-landing.ejs` (строка 51)

---

## 🔹 Способ 2: Запись на программу (регулярные групповые программы)

**Где находится:**
- Секция `#groups` на главной странице
- Карточки программ отображаются через `renderProgramsAndSchedule()` в `kuliga.js`

**Что происходит:**
1. Клиент кликает кнопку "Записаться" на карточке программы
2. Обработчик: `initProgramBookingButtons()` в `kuliga.js` (строка 346)
3. Переход на `/instruktor-po-gornym-lyzham-snoubordy-tyumen/booking` с параметрами:
   - `programId` - ID программы
   - `date` - дата ближайшего занятия (если есть)
   - `time` - время ближайшего занятия (если есть)

**Особенности:**
- ✅ Можно добавить участников (до max_participants - current_participants)
- ✅ Цена фиксированная за человека (из программы)
- ✅ Дата и время могут быть предзаполнены
- ⚠️ На странице бронирования показывается модальное окно подтверждения (`showConfirmationModal`) только для программ

**Файлы:**
- `public/js/kuliga.js` (строки 202-344, 346-361)
- `views/kuliga-landing.ejs` (строки 54-78)

---

## 🔹 Способ 3: Запись через клик на слот инструктора (расписание)

**Где находится:**
- Секция `#instructors` на главной странице
- Расписание инструкторов отображается через `renderInstructors()` в `kuliga.js`
- Слоты имеют класс `.kuliga-slot--clickable`

**Что происходит:**
1. Клиент кликает на слот в расписании инструктора
2. Обработчик: `initSlotClicks()` в `kuliga.js` (строка 703)
3. Данные сохраняются в `localStorage` (ключ `kuligaSlotPreset`)
4. Переход на `/instruktor-po-gornym-lyzham-snoubordy-tyumen/booking` с параметрами:
   - `slotId` - ID слота
   - `instructorId` - ID инструктора
   - `date` - дата слота
   - `startTime` - время начала
   - `endTime` - время окончания (если есть)
   - `location` - место проведения
   - `bookingType` - individual/group
   - `priceType` - individual/group

**Особенности:**
- ✅ Дата и время автоматически выбираются в форме
- ✅ Если на слоте есть групповая тренировка:
  - Цена берется из групповой тренировки (price_per_person)
  - Можно добавить участников (до max_participants - current_participants)
- ✅ Если на слоте НЕТ групповой тренировки:
  - Цена рассчитывается динамически из прайса
  - Можно добавить участников (до 8 человек)
  - Цена пересчитывается при изменении количества участников

**Файлы:**
- `public/js/kuliga.js` (строки 618-759, 703-759)
- `views/kuliga-landing.ejs` (строка 118)

---

## 🔹 Способ 4: Главная кнопка "Записаться на тренировку"

**Где находится:**
- Hero-секция в начале страницы (`kuliga-hero`)
- Кнопка с классом `kuliga-button--primary`

**Что происходит:**
1. Клиент кликает кнопку "Записаться на тренировку" в hero-секции
2. Переход на `/instruktor-po-gornym-lyzham-snoubordy-tyumen/booking?priceType=individual`

**Особенности:**
- ✅ Базовая запись на индивидуальную тренировку
- ✅ Клиент сам выбирает все параметры

**Файлы:**
- `views/kuliga-landing.ejs` (строка 34)

---

## 📄 Страница бронирования `/instruktor-po-gornym-lyzham-snoubordy-tyumen/booking`

### Модальные окна на странице бронирования:

1. **`showConfirmationModal(payload)`** (строка 1410)
   - Открывается только для **программ** перед отправкой формы
   - Показывает сводку: участники, заказчик, цена к оплате
   - Кнопки: "Отмена" и "Перейти к оплате"

2. **`showTelegramBookingModal(payload)`** (строка 1314)
   - Открывается для всех типов бронирования
   - Показывает информацию о бронировании и ссылку на Telegram-бота
   - Не используется в текущей версии (код есть, но не вызывается)

**Файлы:**
- `views/kuliga-booking.ejs` - шаблон страницы
- `public/js/kuliga-booking.js` - логика формы бронирования

---

## 📊 Сравнительная таблица способов записи

| Способ | Цена | Участники | Дата/Время | Модальное окно |
|--------|------|-----------|------------|----------------|
| Карточки прайса | Фиксированная | Фиксированное | Не выбрано | ❌ Нет |
| Программа | Фиксированная за чел | Можно добавить | Предзаполнено | ✅ Да (подтверждение) |
| Слот инструктора (с ГТ) | Из ГТ | Можно добавить | Предзаполнено | ❌ Нет |
| Слот инструктора (без ГТ) | Динамическая | Можно добавить (до 8) | Предзаполнено | ❌ Нет |
| Главная кнопка | Не выбрано | Не выбрано | Не выбрано | ❌ Нет |

**ГТ** = Групповая тренировка

---

## 🔍 Где искать код

### Главная страница инструкторов:
- **Шаблон:** `views/kuliga-landing.ejs`
- **JS:** `public/js/kuliga.js`
- **Роут:** `src/routes/kuliga-public.js` (строка 32)

### Страница бронирования:
- **Шаблон:** `views/kuliga-booking.ejs`
- **JS:** `public/js/kuliga-booking.js`
- **Роут:** `src/routes/kuliga-public.js` (строка 50)

### API для получения данных:
- `/api/kuliga/prices` - прайс
- `/api/kuliga/programs` - программы
- `/api/kuliga/instructors` - инструкторы с расписанием
- `/api/kuliga/availability` - доступные слоты (с информацией о групповых тренировках)

