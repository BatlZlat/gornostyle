# Исправление дублирования отправки напоминаний

## 🔍 Проблема

Напоминания отправляются дважды с разными результатами:

1. **Первый раз (21:00:02):** 5 клиентов получили, 1 не получил (bot was blocked)
2. **Второй раз (21:00:03):** Все 6 клиентов получили ошибку "chat not found"

## 🐛 Причина

Возможные причины дублирования:

1. **Два процесса/экземпляра приложения** запущены одновременно (например, через PM2)
2. **Задача запускается дважды** без защиты от повторного запуска
3. **Конфликт между разными системами** (например, `scheduler.js` и `app.js`)

## ✅ Исправление

### Добавлена защита от повторного запуска в `src/services/scheduler.js`:

1. **Добавлен флаг `isRunning`** для отслеживания состояния задач:
   ```javascript
   this.isRunning = {
       trainingReminders: false,
       reviewRequests: false,
       statusUpdates: false,
       scheduledMessages: false
   };
   ```

2. **Проверка перед запуском** каждой задачи:
   ```javascript
   if (this.isRunning.trainingReminders) {
       console.log('Задача уже выполняется, пропускаем');
       return;
   }
   this.isRunning.trainingReminders = true;
   ```

3. **Сброс флага в `finally` блоке** для гарантированного освобождения:
   ```javascript
   finally {
       this.isRunning.trainingReminders = false;
   }
   ```

## 📋 Что нужно проверить на продакшене

1. **Проверить количество запущенных процессов:**
   ```bash
   pm2 list
   # или
   ps aux | grep node
   ```

2. **Убедиться, что только один экземпляр приложения запущен**

3. **Проверить логи на наличие сообщений:**
   ```
   "Задача отправки напоминаний уже выполняется, пропускаем"
   ```

## 🔍 Дополнительные проблемы

### "chat not found" во второй попытке

Если первая попытка отправила сообщения успешно, а вторая получает "chat not found", это может означать:

1. **Разные токены ботов** используются в разных процессах
2. **Клиенты удалили бота** после первой попытке (маловероятно за 1 секунду)
3. **Неправильный `telegram_id`** в базе данных

### Запросы на отзывы - все получили "chat not found"

Это связано с тем, что тренировки Кулиги не были включены в `getCompletedTrainingsToday()` до исправления. После применения исправлений из `REMINDER_NOTIFICATIONS_FIX.md` эта проблема должна быть решена.

## ⚠️ Рекомендации

1. **Применить исправления** из `REMINDER_NOTIFICATIONS_FIX.md` (включение тренировок Кулиги)
2. **Применить защиту от повторного запуска** из этого файла
3. **Проверить, что только один экземпляр приложения запущен**
4. **Мониторить логи** после применения исправлений

