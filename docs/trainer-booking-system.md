# Система бронирования для тренеров

## 📋 Содержание

1. [Обзор системы](#обзор-системы)
2. [Архитектура](#архитектура)
3. [Инструкция для администраторов](#инструкция-для-администраторов)
4. [Инструкция для тренеров](#инструкция-для-тренеров)
5. [API Endpoints](#api-endpoints)
6. [База данных](#база-данных)
7. [Примеры использования](#примеры-использования)

---

## Обзор системы

Система бронирования позволяет тренерам самостоятельно бронировать временные слоты на тренажерах через веб-интерфейс. Система полностью интегрирована с существующей системой управления расписанием и блокировками.

### Основные возможности

- 🎿 **Для тренеров**:
  - Просмотр календаря на 2 месяца вперед
  - Бронирование свободных слотов на неделю вперед
  - Отмена своих бронирований
  - Просмотр причин блокировок (другие тренеры, администратор, тренировки)

- 👤 **Для администраторов**:
  - Управление учетными данными тренеров (логин/пароль)
  - Просмотр всех блокировок с указанием автора (тренер или администратор)
  - Получение уведомлений о бронированиях и отменах в Telegram

---

## Архитектура

### Компоненты системы

#### Backend (Node.js + Express)

1. **Аутентификация** (`src/middleware/trainerAuth.js`)
   - JWT токены для тренеров
   - Проверка активности тренера
   - Отдельные токены для админов и тренеров

2. **API для бронирования** (`src/routes/trainerBookings.js`)
   - GET `/api/trainer/schedule` - получение расписания
   - GET `/api/trainer/my-bookings` - свои бронирования
   - POST `/api/trainer/bookings` - создание бронирования
   - DELETE `/api/trainer/bookings/:id` - отмена бронирования

3. **Уведомления** (`src/bot/admin-notify.js`)
   - `notifyTrainerBookingCreated()` - уведомление о бронировании
   - `notifyTrainerBookingCancelled()` - уведомление об отмене

#### Frontend

1. **Страница входа** (`/trainer-login.html`)
   - Форма входа с username/password
   - Проверка токена
   - Редирект на календарь после входа

2. **Календарь тренера** (`/trainer.html`)
   - Отображение 2 месяцев вперед
   - Цветовая индикация дней (зеленый/желтый)
   - Клик по дню открывает расписание

3. **Модальное окно** (внутри `trainer.html`)
   - Слоты для обоих тренажеров
   - Цветовая индикация слотов (свободно/занято/мои брони)
   - Бронирование/отмена по клику

#### База данных

**Новые поля в `trainers`:**
- `username` VARCHAR(100) UNIQUE - логин тренера
- `password` VARCHAR(255) - пароль в открытом виде

**Новые поля в `schedule_blocks`:**
- `trainer_id` INTEGER - ID тренера, создавшего блокировку
- `blocked_by_type` VARCHAR(20) - тип: 'admin' или 'trainer'

---

## Инструкция для администраторов

### 1. Создание учетной записи для тренера

#### Способ 1: При создании нового тренера

1. Перейдите в **Админ-панель** → **Тренера**
2. Нажмите **"Создать тренера"**
3. Заполните основные данные (ФИО, телефон, дата рождения и т.д.)
4. В разделе **"🔐 Доступ к системе бронирования"**:
   - Введите **Логин** (например: `ivanov`)
   - Введите **Пароль** (например: `password123`)
5. Нажмите **"Создать тренера"**

#### Способ 2: Для существующего тренера

1. Перейдите в **Админ-панель** → **Тренера**
2. Найдите нужного тренера и нажмите **✏️ Редактировать**
3. Прокрутите вниз до раздела **"🔐 Доступ к системе бронирования"**
4. Введите логин и пароль
5. Нажмите **"Сохранить"**

### 2. Просмотр бронирований тренеров

В разделе **"Управление блокировками слотов"** (/schedule-blocks.html):

- Блокировки с указанием **"Заблокировал: 🎿 ФИО тренера (тренер)"** - созданы тренером
- Блокировки с указанием **"Заблокировал: 👤 Администратор"** - созданы администратором

### 3. Уведомления в Telegram

Администраторы получают уведомления:

**При бронировании:**
```
🎿 Бронирование тренером

👤 Тренер: Иванов Иван Иванович
📅 Дата: 15.10.2025
⏰ Время: 10:00 - 11:00
🎿 Тренажер: Тренажер 1
```

**При отмене:**
```
❌ Отмена бронирования тренером

👤 Тренер: Иванов Иван Иванович
📅 Дата: 15.10.2025
⏰ Время: 10:00 - 11:00
🎿 Тренажер: Тренажер 1
```

### 4. Управление доступом

**Деактивация тренера:**
- При увольнении тренера (is_active = false) он автоматически теряет доступ к системе
- Его существующие бронирования остаются в системе

**Изменение пароля:**
- Можно в любой момент изменить пароль тренера в форме редактирования

**Удаление доступа:**
- Очистите поля username и password для отключения доступа к системе бронирования

---

## Инструкция для тренеров

### 1. Вход в систему

1. Перейдите на **https://gornostyle72.ru/trainer-login.html**
2. Введите **логин** и **пароль**, которые выдал администратор
3. Нажмите **"Войти"**

**Важно:** Если учетные данные не работают, обратитесь к администратору.

### 2. Просмотр календаря

После входа вы увидите календарь на 2 месяца вперед:

- 🟢 **Зеленые дни** - полностью свободны
- 🟡 **Желтые дни** - есть запланированные тренировки

**Навигация:**
- Используйте кнопки **←** и **→** для перехода между месяцами
- Кликните на любой день (начиная с сегодняшнего), чтобы открыть расписание

### 3. Бронирование времени

1. **Кликните на нужный день** в календаре
2. Откроется окно с расписанием для **обоих тренажеров**
3. Вы увидите временные слоты с 10:00 до 21:00 (по 30 минут каждый)

**Типы слотов:**

- ✅ **Белые с зеленой рамкой** - свободно, можно забронировать
- 🔵 **Синие** - ваши бронирования (клик для отмены)
- ⚫ **Серые** - занято (тренировка или другой тренер)

**Как забронировать:**
1. Кликните на свободный слот
2. Подтвердите бронирование в диалоговом окне
3. Слот станет синим

**Как отменить:**
1. Кликните на свой синий слот
2. Подтвердите отмену
3. Слот станет свободным

### 4. Ограничения

- ⚠️ **Бронирование доступно только на неделю вперед**
- ❌ Нельзя отменять чужие бронирования
- ❌ Нельзя бронировать прошедшие даты
- ❌ Нельзя бронировать время, когда тренажер не работает

### 5. Просмотр причин блокировок

Серые (заблокированные) слоты показывают причину:

- **"Занят 📅"** - запланирована тренировка
- **"ФИО тренера"** - забронировано другим тренером
- **"Обед"**, **"Развозка детей"** и т.д. - блокировка администратора

Если видите блокировку администратора, но она вам не мешает, можете попросить администратора разблокировать это время.

### 6. Выход из системы

Нажмите кнопку **"Выход"** в правом верхнем углу.

---

## API Endpoints

### Аутентификация

#### POST `/api/trainer/login`
Вход тренера в систему.

**Request:**
```json
{
  "username": "ivanov",
  "password": "password123"
}
```

**Response (200):**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "trainer": {
    "id": 1,
    "fullName": "Иванов Иван Иванович",
    "username": "ivanov",
    "sportType": "ski",
    "phone": "+7-999-123-45-67"
  }
}
```

**Response (401):**
```json
{
  "error": "invalid_credentials",
  "message": "Неверный логин или пароль, либо аккаунт неактивен"
}
```

#### GET `/api/trainer/verify`
Проверка валидности токена.

**Headers:**
```
Authorization: Bearer <token>
```

**Response (200):**
```json
{
  "valid": true,
  "trainer": { ... }
}
```

### Расписание и бронирование

#### GET `/api/trainer/schedule`
Получение расписания на 2 месяца вперед.

**Headers:**
```
Authorization: Bearer <token>
```

**Query Parameters:**
- `simulator_id` (optional) - фильтр по тренажеру

**Response (200):**
```json
[
  {
    "id": 123,
    "simulator_id": 1,
    "date": "2025-10-15",
    "start_time": "10:00:00",
    "end_time": "10:30:00",
    "is_booked": false,
    "is_blocked": false,
    "block_reason": null,
    "has_training": false,
    "simulator_name": "Тренажер 1",
    "simulator_is_working": true,
    "working_hours_start": "10:00:00",
    "working_hours_end": "21:00:00"
  }
]
```

#### POST `/api/trainer/bookings`
Создание бронирования.

**Headers:**
```
Authorization: Bearer <token>
```

**Request:**
```json
{
  "simulator_id": 1,
  "date": "2025-10-15",
  "start_time": "10:00:00",
  "end_time": "10:30:00"
}
```

**Response (201):**
```json
{
  "message": "Бронирование создано успешно",
  "booking": {
    "id": 456,
    "trainer_id": 1,
    "simulator_id": 1,
    "start_date": "2025-10-15",
    "end_date": "2025-10-15",
    "start_time": "10:00:00",
    "end_time": "10:30:00",
    "reason": "Иванов Иван Иванович",
    "blocked_by_type": "trainer"
  }
}
```

**Response (400):**
```json
{
  "error": "Можно бронировать только на неделю вперед"
}
```

**Response (409):**
```json
{
  "error": "Слот уже забронирован",
  "details": "Выбранное время уже занято"
}
```

#### DELETE `/api/trainer/bookings/:id`
Отмена бронирования.

**Headers:**
```
Authorization: Bearer <token>
```

**Response (200):**
```json
{
  "message": "Бронирование отменено успешно",
  "booking": {
    "id": 456,
    "date": "2025-10-15",
    "startTime": "10:00:00",
    "endTime": "10:30:00"
  }
}
```

**Response (403):**
```json
{
  "error": "Нельзя отменить чужое бронирование"
}
```

---

## База данных

### Таблица `trainers`

Новые поля для системы бронирования:

```sql
ALTER TABLE trainers 
ADD COLUMN username VARCHAR(100) UNIQUE,
ADD COLUMN password VARCHAR(255);
```

| Поле | Тип | Описание |
|------|-----|----------|
| username | VARCHAR(100) | Логин тренера (уникальный) |
| password | VARCHAR(255) | Пароль в открытом виде |

**Индексы:**
```sql
CREATE INDEX idx_trainers_username ON trainers(username);
```

### Таблица `schedule_blocks`

Новые поля для отслеживания бронирований:

```sql
ALTER TABLE schedule_blocks 
ADD COLUMN trainer_id INTEGER REFERENCES trainers(id) ON DELETE SET NULL,
ADD COLUMN blocked_by_type VARCHAR(20) DEFAULT 'admin' 
  CHECK (blocked_by_type IN ('admin', 'trainer'));
```

| Поле | Тип | Описание |
|------|-----|----------|
| trainer_id | INTEGER | ID тренера, создавшего блокировку (NULL = администратор) |
| blocked_by_type | VARCHAR(20) | Тип блокировки: 'admin' или 'trainer' |

**Индексы:**
```sql
CREATE INDEX idx_schedule_blocks_trainer ON schedule_blocks(trainer_id);
CREATE INDEX idx_schedule_blocks_blocked_by_type ON schedule_blocks(blocked_by_type);
```

### Примеры запросов

**Получить все бронирования тренера:**
```sql
SELECT * FROM schedule_blocks
WHERE trainer_id = 1 AND is_active = TRUE
ORDER BY start_date, start_time;
```

**Получить все блокировки с информацией о создателе:**
```sql
SELECT sb.*, 
       t.full_name as trainer_name,
       a.full_name as admin_name
FROM schedule_blocks sb
LEFT JOIN trainers t ON sb.trainer_id = t.id
LEFT JOIN administrators a ON sb.created_by = a.id
WHERE sb.is_active = TRUE
ORDER BY sb.created_at DESC;
```

---

## Примеры использования

### Сценарий 1: Тренер бронирует время

1. Тренер входит в систему с логином `ivanov` и паролем `password123`
2. Видит календарь, выбирает завтрашний день (зеленый)
3. Кликает на день, открывается расписание
4. Видит свободный слот 10:00-10:30 на Тренажере 1
5. Кликает на слот, подтверждает бронирование
6. Слот становится синим
7. Администратор получает уведомление в Telegram

### Сценарий 2: Администратор создает учетные данные

1. Администратор в панели нажимает "Создать тренера"
2. Заполняет ФИО: "Петров Петр Петрович"
3. Заполняет остальные данные
4. В разделе "Доступ к системе бронирования":
   - Логин: `petrov`
   - Пароль: `pass123`
5. Сохраняет
6. Сообщает тренеру учетные данные
7. Тренер может войти и бронировать время

### Сценарий 3: Тренер отменяет бронирование

1. Тренер заходит в календарь
2. Выбирает день со своей бронью
3. Видит синий слот 14:00-14:30
4. Кликает на него, подтверждает отмену
5. Слот становится белым (свободным)
6. Администратор получает уведомление об отмене

### Сценарий 4: Конфликт времени

1. Тренер пытается забронировать слот 12:00-12:30
2. Но администратор уже заблокировал это время для обеда
3. Тренер видит серый слот с причиной "Обед"
4. Обращается к администратору с просьбой разблокировать
5. Администратор снимает блокировку
6. Тренер может забронировать это время

---

## Безопасность

### Аутентификация

- JWT токены с временем жизни 24 часа
- Отдельные типы токенов для админов (`type: 'admin'`) и тренеров (`type: 'trainer'`)
- Проверка активности тренера при каждом запросе
- Токены хранятся в secure cookie

### Авторизация

- Тренер может видеть только расписание на 2 месяца вперед
- Тренер может бронировать только на неделю вперед
- Тренер может отменять только свои бронирования
- Тренер не имеет доступа к админ-панели

### Хранение паролей

**⚠️ ВАЖНО:** Пароли тренеров хранятся в открытом виде!

**Обоснование:**
- Функционал управления минимальный
- Если тренер злоупотребляет системой, его можно просто уволить
- Упрощение для администраторов (могут видеть и сообщать пароли)

**Риски:**
- Низкие, так как функционал ограничен только бронированием
- Максимальный ущерб - тренер забронирует много времени
- Решение: увольнение тренера (is_active = false)

---

## Troubleshooting

### Проблема: Тренер не может войти

**Решение:**
1. Проверьте, что у тренера установлены username и password в админ-панели
2. Проверьте, что тренер активен (is_active = TRUE)
3. Проверьте правильность логина и пароля (регистрозависимые)
4. Очистите cookie браузера

### Проблема: Не приходят уведомления администратору

**Решение:**
1. Проверьте `ADMIN_TELEGRAM_ID` в `.env` файле
2. Проверьте `ADMIN_BOT_TOKEN` в `.env` файле
3. Проверьте логи сервера на ошибки отправки
4. Убедитесь, что бот запущен (`src/bot/admin-bot.js`)

### Проблема: Тренер не видит расписание

**Решение:**
1. Проверьте, что создан месячный график (таблица `schedule`)
2. Проверьте консоль браузера на ошибки API
3. Проверьте, что JWT токен валиден
4. Перелогиньтесь

### Проблема: Слот не бронируется

**Возможные причины:**
- Слот за пределами недели вперед
- Слот уже забронирован
- Тренажер не работает
- Слот в прошлом

**Решение:** Проверьте сообщение об ошибке и устраните причину

---

## Обновления системы

### Миграция базы данных

Файл: `migrations/013_add_trainer_auth_and_bookings.sql`

Применить через:
```bash
node apply_migration_013.js
```

Или вручную через psql:
```bash
psql -U username -d skisimulator -f migrations/013_add_trainer_auth_and_bookings.sql
```

### Проверка установки

После развертывания проверьте:

1. ✅ Поля `username`, `password` в таблице `trainers`
2. ✅ Поля `trainer_id`, `blocked_by_type` в таблице `schedule_blocks`
3. ✅ Роуты `/api/trainer/*` отвечают
4. ✅ Страница `/trainer-login.html` открывается
5. ✅ Страница `/trainer.html` требует авторизацию
6. ✅ Уведомления в Telegram работают

---

## Контакты и поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Проверьте консоль браузера
3. Обратитесь к администратору системы

**Версия документации:** 1.0  
**Дата:** 11 октября 2025  
**Автор:** Система Горностайл72

