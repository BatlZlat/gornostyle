# Автоматическое сгорание просроченных сертификатов

## Обзор

Система автоматически помечает просроченные сертификаты статусом `expired` каждый день в полночь.

## Как работает

### Cron-задача

**Расписание:** Каждый день в 00:00 по времени Екатеринбурга (UTC+5)

**Файл:** `src/services/scheduler.js`

**Процесс:**
1. Находит все сертификаты с `status = 'active'` и `expiry_date < NOW()`
2. Обновляет статус: `active` → `expired`
3. Собирает статистику:
   - Количество истекших сертификатов
   - Общая сумма
   - Список сертификатов
4. Отправляет отчет администратору в Telegram

### SQL запрос

```sql
UPDATE certificates 
SET status = 'expired', updated_at = CURRENT_TIMESTAMP 
WHERE status = 'active' AND expiry_date < NOW()
RETURNING id, certificate_number, purchaser_id, recipient_name, nominal_value;
```

### Уведомление администратору

Если истекли сертификаты, администратор получает уведомление в Telegram:

```
🎟️ Отчет об истекших сертификатах

📊 Истекло сертификатов: 5
💰 Общая сумма: 25 000 ₽

Детали:
1. #123456 → Иванов Иван - 5 000 ₽
2. #654321 → Петров Петр - 10 000 ₽
3. #789012 - 3 000 ₽
4. #345678 → Сидорова Мария - 7 000 ₽
5. #901234 - 5 000 ₽

⏰ 05.12.2025, 00:00
```

### Защита от ошибок

- **Защита от повторного запуска:** Если задача уже выполняется, новый запуск пропускается
- **Обработка ошибок:** При ошибке администратор получает уведомление с деталями
- **Логирование:** Все операции логируются с временными метками

## Тестирование

### Ручной запуск задачи

Создан скрипт для тестирования: `scripts/test-certificate-expiration.js`

**Использование:**

```bash
node scripts/test-certificate-expiration.js
```

**Что делает скрипт:**

1. Показывает текущую статистику сертификатов
2. Находит просроченные сертификаты (status = active, expiry_date < NOW)
3. Показывает детали просроченных сертификатов
4. Запрашивает подтверждение у пользователя
5. Обновляет статус просроченных сертификатов
6. Показывает итоговую статистику

**Пример вывода:**

```
🎟️ Запуск тестирования автоматического сгорания сертификатов...

📊 Текущее состояние сертификатов:
┌─────────┬────────────┬──────────────┐
│ Статус  │ Количество │ Сумма (₽)    │
├─────────┼────────────┼──────────────┤
│ active  │ 80         │ 400 000      │
│ used    │ 60         │ 300 000      │
│ expired │ 10         │ 50 000       │
└─────────┴────────────┴──────────────┘

🔍 Просроченные сертификаты (status = active, expiry_date < NOW):
⚠️ Найдено 5 просроченных сертификатов:

┌────┬────────┬────────────────┬──────────────┬────────┬────────────┬──────────────────┐
│ ID │ Номер  │ Получатель     │ Номинал (₽) │ Статус │ Истек      │ Дней просрочки   │
├────┼────────┼────────────────┼──────────────┼────────┼────────────┼──────────────────┤
│ 45 │ 123456 │ Иванов Иван    │ 5 000        │ active │ 01.11.2024 │ 35               │
│ 46 │ 654321 │ Петров Петр    │ 10 000       │ active │ 15.10.2024 │ 51               │
│ 47 │ 789012 │ —              │ 3 000        │ active │ 01.09.2024 │ 95               │
│ 48 │ 345678 │ Сидорова Мария │ 7 000        │ active │ 20.11.2024 │ 15               │
│ 49 │ 901234 │ —              │ 5 000        │ active │ 10.11.2024 │ 25               │
└────┴────────┴────────────────┴──────────────┴────────┴────────────┴──────────────────┘

💰 Общая сумма просроченных: 30 000 ₽

❓ Пометить эти сертификаты как 'expired'? (да/нет): да

⚙️ Обновление статуса просроченных сертификатов...
✅ Обновлено сертификатов: 5

📝 Обновленные сертификаты:
┌────┬────────┬────────────────┬──────────────┐
│ ID │ Номер  │ Получатель     │ Номинал (₽) │
├────┼────────┼────────────────┼──────────────┤
│ 45 │ 123456 │ Иванов Иван    │ 5 000        │
│ 46 │ 654321 │ Петров Петр    │ 10 000       │
│ 47 │ 789012 │ —              │ 3 000        │
│ 48 │ 345678 │ Сидорова Мария │ 7 000        │
│ 49 │ 901234 │ —              │ 5 000        │
└────┴────────┴────────────────┴──────────────┘

📊 Итоговое состояние сертификатов:
┌─────────┬────────────┬──────────────┐
│ Статус  │ Количество │ Сумма (₽)    │
├─────────┼────────────┼──────────────┤
│ active  │ 75         │ 370 000      │
│ used    │ 60         │ 300 000      │
│ expired │ 15         │ 80 000       │
└─────────┴────────────┴──────────────┘

✅ Тестирование завершено успешно!

👋 Завершение работы скрипта
```

## Настройки

### Переменные окружения

Для отправки уведомлений администратору необходимо установить в `.env`:

```env
ADMIN_TELEGRAM_ID=123456789
ADMIN_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

Если эти переменные не установлены, задача будет работать, но уведомления отправляться не будут.

### Изменение расписания

Для изменения времени запуска задачи отредактируйте строку в `src/services/scheduler.js`:

```javascript
// Текущее расписание: каждый день в 00:00
const task = cron.schedule('0 0 * * *', async () => {
    // ...
});

// Примеры других расписаний:
// Каждый день в 02:00
const task = cron.schedule('0 2 * * *', async () => {

// Каждый час
const task = cron.schedule('0 * * * *', async () => {

// Каждые 30 минут
const task = cron.schedule('*/30 * * * *', async () => {
```

## Мониторинг

### Логи

Все операции логируются в консоль с временными метками:

```
[2025-12-05T00:00:00.000Z] Запуск задачи: автоматическое сгорание просроченных сертификатов
[2025-12-05T00:00:01.234Z] Задача завершена. Истекло сертификатов: 5, общая сумма: 25000₽
✓ Отчет об истекших сертификатах отправлен администратору
```

### Проверка статуса задачи

Можно проверить, запущена ли задача:

```javascript
const scheduler = require('./src/services/scheduler');

// Получить информацию о всех задачах
const tasks = scheduler.getTasksInfo();
console.log(tasks);

// Результат:
// [
//   {
//     name: 'certificate_expiration',
//     description: 'Автоматическое сгорание просроченных сертификатов',
//     schedule: '0 0 * * * (Екатеринбург)',
//     running: true
//   },
//   ...
// ]
```

## Troubleshooting

### Задача не выполняется

1. Проверьте, что приложение запущено
2. Проверьте логи на наличие ошибок
3. Проверьте, что планировщик инициализирован в `src/app.js`:
   ```javascript
   const scheduler = require('./services/scheduler');
   scheduler.init();
   ```

### Уведомления не приходят

1. Проверьте переменные окружения `ADMIN_TELEGRAM_ID` и `ADMIN_BOT_TOKEN`
2. Проверьте логи на ошибки отправки
3. Убедитесь, что бот добавлен администратором и имеет права на отправку сообщений

### Сертификаты не помечаются как expired

1. Запустите тестовый скрипт: `node scripts/test-certificate-expiration.js`
2. Проверьте SQL запрос вручную в базе данных:
   ```sql
   SELECT * FROM certificates 
   WHERE status = 'active' AND expiry_date < NOW();
   ```
3. Проверьте логи на ошибки выполнения запроса

## История изменений

**05.12.2025** - Создана система автоматического сгорания сертификатов
- Добавлена cron-задача в планировщик
- Реализованы уведомления администратора
- Создан тестовый скрипт
- Написана документация

