# 💳 Инструкция по настройке Tinkoff Acquiring для самозанятых

## 📋 Содержание
1. [Регистрация в Тинькофф для самозанятых](#1-регистрация-в-тинькофф-для-самозанятых)
2. [Подключение Tinkoff Acquiring](#2-подключение-tinkoff-acquiring)
3. [Получение API ключей](#3-получение-api-ключей)
4. [Настройка проекта](#4-настройка-проекта)
5. [Интеграция с кодом](#5-интеграция-с-кодом)
6. [Тестирование платежей](#6-тестирование-платежей)
7. [Переход на продакшн](#7-переход-на-продакшн)
8. [Важные особенности для самозанятых](#8-важные-особенности-для-самозанятых)

---

## 1. Регистрация в Тинькофф для самозанятых

### Шаг 1.1: Открытие счета для самозанятых

1. **Перейдите на сайт Тинькофф:**
   - https://www.tinkoff.ru/business/self-employed/

2. **Нажмите "Открыть счет онлайн"**
   - Заполните форму: ФИО, номер телефона, email
   - Укажите, что вы самозанятый (статус НПД)

3. **Дождитесь одобрения:**
   - Обычно занимает 1-3 рабочих дня
   - Вам придет SMS с подтверждением

4. **Установите приложение Тинькофф Бизнес:**
   - [iOS](https://apps.apple.com/ru/app/id1450502837)
   - [Android](https://play.google.com/store/apps/details?id=com.idamob.tinkoff.business)

5. **Активируйте счет:**
   - Войдите в приложение по SMS-коду
   - Подпишите договор электронной подписью

---

## 2. Подключение Tinkoff Acquiring

### Шаг 2.1: Заявка на эквайринг

1. **Войдите в личный кабинет Тинькофф Бизнес:**
   - Веб: https://business.tinkoff.ru/
   - Или через приложение

2. **Найдите раздел "Эквайринг":**
   - Главное меню → "Эквайринг и платежи" → "Интернет-эквайринг"

3. **Нажмите "Подключить интернет-эквайринг":**
   - Заполните заявку:
     - Название бизнеса: **Горностайл72 - Служба инструкторов Кулиги**
     - Сайт: **https://gornostyle.ru/instruktora-kuliga**
     - Описание: **Услуги инструкторов по горным лыжам и сноуборду**
     - Предполагаемый оборот: **до 500 000 ₽/месяц** (укажите реальную оценку)

4. **Укажите данные для возвратов:**
   - Реквизиты вашего счета в Тинькофф (куда будут приходить деньги)

5. **Дождитесь одобрения:**
   - Обычно 1-2 рабочих дня
   - Вам придет уведомление на email и в приложение

### Шаг 2.2: Тарифы для самозанятых

**Тинькофф предлагает выгодные условия для самозанятых:**

| Оборот в месяц | Комиссия |
|----------------|----------|
| До 100 000 ₽   | **0%** (бесплатно) |
| От 100 000 ₽   | **2.49%** |
| От 500 000 ₽   | **2.19%** (по договоренности) |

✅ **Преимущества:**
- Первые 3 месяца - **0% комиссии** для новых клиентов
- Автоматическое формирование чеков (для самозанятых обязательно)
- Нет абонентской платы
- Быстрый вывод средств (1-2 рабочих дня)

---

## 3. Получение API ключей

### Шаг 3.1: Создание терминала

1. **Войдите в личный кабинет Тинькофф:**
   - https://business.tinkoff.ru/

2. **Перейдите в раздел "Настройки эквайринга":**
   - Меню → "Эквайринг" → "Настройки"

3. **Создайте новый терминал:**
   - Нажмите "Добавить терминал"
   - Название: **Горностайл72 Кулига - Сайт**
   - URL успешной оплаты: `https://gornostyle.ru/instruktora-kuliga/payment/success`
   - URL неуспешной оплаты: `https://gornostyle.ru/instruktora-kuliga/payment/fail`

4. **Получите Terminal Key (TerminalKey):**
   - После создания терминала вам выдадут `TerminalKey` (например: `1234567890DEMO`)
   - **Сохраните его** - он понадобится для интеграции

### Шаг 3.2: Получение пароля (Password)

1. **В настройках терминала найдите раздел "Пароль":**
   - Нажмите "Сгенерировать пароль" или "Показать пароль"
   - **Скопируйте пароль** (например: `abcdef1234567890`)

2. **Настройте уведомления (Webhook URL):**
   - URL для уведомлений: `https://gornostyle.ru/api/kuliga/payment/callback`
   - Это нужно для автоматического подтверждения платежей

### Шаг 3.3: Тестовые ключи

**Для тестирования используйте тестовый терминал:**

```
TerminalKey (тестовый): TinkoffBankTest
Password (тестовый): TinkoffBankTest
```

**Тестовые карты:**
- Успешная оплата: `4300 0000 0000 0777`
- Неуспешная оплата: `4300 0000 0000 0002`
- CVV: любой (например, 123)
- Срок: любой будущий (например, 12/25)
- Имя: любое

---

## 4. Настройка проекта

### Шаг 4.1: Добавление ключей в .env

Откройте файл `.env` в корне проекта и добавьте:

```bash
# Tinkoff Acquiring для Кулиги
TINKOFF_TERMINAL_KEY=ваш_terminal_key_здесь
TINKOFF_PASSWORD=ваш_пароль_здесь
TINKOFF_API_URL=https://securepay.tinkoff.ru/v2

# Для тестирования (раскомментируйте при тестировании)
# TINKOFF_TERMINAL_KEY=TinkoffBankTest
# TINKOFF_PASSWORD=TinkoffBankTest

# URL для уведомлений
KULIGA_PAYMENT_SUCCESS_URL=https://gornostyle.ru/instruktora-kuliga/payment/success
KULIGA_PAYMENT_FAIL_URL=https://gornostyle.ru/instruktora-kuliga/payment/fail
KULIGA_PAYMENT_CALLBACK_URL=https://gornostyle.ru/api/kuliga/payment/callback
```

⚠️ **Важно:** Добавьте `.env` в `.gitignore`, чтобы не залить ключи в Git!

### Шаг 4.2: Установка зависимостей

Библиотека для работы с Tinkoff API уже включена в проект через `axios`. Дополнительные пакеты не требуются.

---

## 5. Интеграция с кодом

### Шаг 5.1: Структура API

Мы создадим следующие endpoints:

1. **POST `/api/kuliga/payment/init`** - Инициализация платежа
2. **POST `/api/kuliga/payment/callback`** - Webhook от Тинькофф (подтверждение оплаты)
3. **GET `/api/kuliga/payment/status/:orderId`** - Проверка статуса платежа
4. **POST `/api/kuliga/payment/refund`** - Возврат средств (при отмене тренировки)

### Шаг 5.2: Пример инициализации платежа

```javascript
// src/routes/kuliga-payment.js

const axios = require('axios');
const crypto = require('crypto');

// Функция для генерации токена (подпись запроса)
function generateToken(params, password) {
    // Удаляем Token, если он есть
    delete params.Token;
    
    // Добавляем пароль
    params.Password = password;
    
    // Сортируем ключи по алфавиту
    const sortedKeys = Object.keys(params).sort();
    
    // Создаем строку для хеширования
    const values = sortedKeys.map(key => params[key]);
    const concatenated = values.join('');
    
    // Хешируем SHA-256
    const hash = crypto.createHash('sha256').update(concatenated).digest('hex');
    
    return hash;
}

// Инициализация платежа
async function initPayment(orderId, amount, description, clientEmail, clientPhone) {
    const params = {
        TerminalKey: process.env.TINKOFF_TERMINAL_KEY,
        Amount: Math.round(amount * 100), // Копейки (1000₽ = 100000 копеек)
        OrderId: orderId,
        Description: description,
        
        // Данные клиента (для чека)
        DATA: {
            Email: clientEmail,
            Phone: clientPhone
        },
        
        // Чек для самозанятого (обязательно!)
        Receipt: {
            Email: clientEmail,
            Phone: clientPhone,
            Taxation: 'usn_income', // Для самозанятых
            Items: [
                {
                    Name: description,
                    Price: Math.round(amount * 100),
                    Quantity: 1,
                    Amount: Math.round(amount * 100),
                    Tax: 'none', // Самозанятые не платят НДС
                    PaymentMethod: 'full_payment',
                    PaymentObject: 'service' // Услуга
                }
            ]
        },
        
        // URL для возврата
        SuccessURL: process.env.KULIGA_PAYMENT_SUCCESS_URL,
        FailURL: process.env.KULIGA_PAYMENT_FAIL_URL,
        NotificationURL: process.env.KULIGA_PAYMENT_CALLBACK_URL
    };
    
    // Генерируем токен
    params.Token = generateToken(params, process.env.TINKOFF_PASSWORD);
    
    // Отправляем запрос в Тинькофф
    try {
        const response = await axios.post(
            `${process.env.TINKOFF_API_URL}/Init`,
            params
        );
        
        if (response.data.Success) {
            return {
                success: true,
                paymentId: response.data.PaymentId,
                paymentURL: response.data.PaymentURL // URL для оплаты
            };
        } else {
            return {
                success: false,
                error: response.data.Message
            };
        }
    } catch (error) {
        console.error('Ошибка инициализации платежа Тинькофф:', error);
        return {
            success: false,
            error: error.message
        };
    }
}

module.exports = { initPayment, generateToken };
```

### Шаг 5.3: Пример обработки webhook (подтверждение оплаты)

```javascript
// src/routes/kuliga-payment.js (продолжение)

async function handlePaymentCallback(req, res) {
    try {
        const data = req.body;
        
        // Проверяем подпись (Token)
        const token = data.Token;
        delete data.Token;
        
        const expectedToken = generateToken(data, process.env.TINKOFF_PASSWORD);
        
        if (token !== expectedToken) {
            console.error('Неверная подпись webhook от Тинькофф');
            return res.status(400).send('Invalid signature');
        }
        
        // Обрабатываем статус платежа
        const { OrderId, Status, PaymentId, Amount } = data;
        
        if (Status === 'CONFIRMED') {
            // Платеж успешно подтвержден
            
            // 1. Обновляем статус транзакции в БД
            await db.query(`
                UPDATE kuliga_transactions
                SET status = 'completed', 
                    tinkoff_status = 'CONFIRMED',
                    tinkoff_payment_id = $1
                WHERE tinkoff_order_id = $2
            `, [PaymentId, OrderId]);
            
            // 2. Обновляем статус бронирования
            const booking = await db.query(`
                SELECT * FROM kuliga_bookings WHERE id = $1
            `, [OrderId]);
            
            if (booking.rows.length > 0) {
                await db.query(`
                    UPDATE kuliga_bookings
                    SET status = 'confirmed'
                    WHERE id = $1
                `, [OrderId]);
                
                // 3. Обновляем статус слота (если индивидуальная тренировка)
                if (booking.rows[0].booking_type === 'individual') {
                    await db.query(`
                        UPDATE kuliga_schedule_slots
                        SET status = 'booked'
                        WHERE id = $1
                    `, [booking.rows[0].slot_id]);
                }
                
                // 4. Отправляем уведомления клиенту и инструктору
                // (будет реализовано позже)
            }
            
            console.log(`Платеж ${PaymentId} успешно подтвержден`);
        } else if (Status === 'REJECTED' || Status === 'CANCELED') {
            // Платеж отклонен
            await db.query(`
                UPDATE kuliga_transactions
                SET status = 'failed', tinkoff_status = $1
                WHERE tinkoff_order_id = $2
            `, [Status, OrderId]);
            
            console.log(`Платеж ${PaymentId} отклонен`);
        }
        
        // Отвечаем Тинькофф, что webhook обработан
        res.send('OK');
        
    } catch (error) {
        console.error('Ошибка обработки webhook Тинькофф:', error);
        res.status(500).send('Internal Server Error');
    }
}

module.exports = { initPayment, generateToken, handlePaymentCallback };
```

---

## 6. Тестирование платежей

### Шаг 6.1: Тестирование на локальной машине

1. **Используйте тестовые ключи** (см. Шаг 3.3)

2. **Запустите ngrok для webhook:**
   ```bash
   ngrok http 3000
   ```
   
   Скопируйте публичный URL (например: `https://abc123.ngrok.io`)

3. **Обновите webhook URL в настройках терминала:**
   - Замените `https://gornostyle.ru` на `https://abc123.ngrok.io`
   - Webhook URL: `https://abc123.ngrok.io/api/kuliga/payment/callback`

4. **Создайте тестовый платеж:**
   - Зайдите на страницу бронирования
   - Выберите тренировку
   - Заполните форму
   - Нажмите "Оплатить"

5. **Оплатите тестовой картой:**
   - Номер: `4300 0000 0000 0777`
   - CVV: `123`
   - Срок: `12/25`

6. **Проверьте логи:**
   - В консоли должно появиться: `Платеж XXXXX успешно подтвержден`

### Шаг 6.2: Проверка возвратов

1. **Создайте тестовый возврат:**
   ```bash
   curl -X POST http://localhost:3000/api/kuliga/payment/refund \
     -H "Content-Type: application/json" \
     -d '{
       "paymentId": "XXXXX",
       "amount": 1000
     }'
   ```

2. **Проверьте в личном кабинете Тинькофф:**
   - Раздел "Операции" → должен появиться возврат

---

## 7. Переход на продакшн

### Шаг 7.1: Замена тестовых ключей на боевые

1. **Обновите `.env`:**
   ```bash
   TINKOFF_TERMINAL_KEY=ваш_боевой_terminal_key
   TINKOFF_PASSWORD=ваш_боевой_пароль
   ```

2. **Обновите webhook URL в Тинькофф:**
   - Замените ngrok URL на реальный: `https://gornostyle.ru/api/kuliga/payment/callback`

3. **Перезапустите сервер:**
   ```bash
   pm2 restart gornostyle
   ```

### Шаг 7.2: Проверка на продакшне

1. **Создайте реальный платеж на 1₽:**
   - Используйте свою карту
   - Проверьте, что платеж прошел успешно

2. **Проверьте webhook:**
   - В логах должно появиться подтверждение
   - В БД должен обновиться статус транзакции

3. **Сделайте возврат:**
   - Убедитесь, что деньги вернулись на карту (1-3 рабочих дня)

---

## 8. Важные особенности для самозанятых

### 8.1. Автоматическое формирование чеков

✅ **Тинькофф автоматически формирует чеки для самозанятых!**

Это значит:
- Вам НЕ нужно вручную формировать чеки в приложении "Мой налог"
- Тинькофф отправляет чеки в налоговую автоматически
- Чеки приходят клиентам на email и в SMS

**Важно:** В запросе `Init` обязательно указывайте блок `Receipt` (см. Шаг 5.2)

### 8.2. Возвраты и налоговая

⚠️ **Важно для возвратов:**

При возврате средств клиенту:
- Тинькофф автоматически отменяет чек в налоговой
- Деньги НЕ считаются в обороте
- Налог НЕ взимается с возвращенных сумм

**Как сделать возврат правильно:**

```javascript
async function refundPayment(paymentId, amount) {
    const params = {
        TerminalKey: process.env.TINKOFF_TERMINAL_KEY,
        PaymentId: paymentId,
        Amount: Math.round(amount * 100), // Копейки
        
        // Чек для отмены (обязательно!)
        Receipt: {
            Taxation: 'usn_income',
            Items: [
                {
                    Name: 'Возврат за тренировку',
                    Price: Math.round(amount * 100),
                    Quantity: 1,
                    Amount: Math.round(amount * 100),
                    Tax: 'none',
                    PaymentMethod: 'full_payment',
                    PaymentObject: 'service'
                }
            ]
        }
    };
    
    params.Token = generateToken(params, process.env.TINKOFF_PASSWORD);
    
    const response = await axios.post(
        `${process.env.TINKOFF_API_URL}/Cancel`,
        params
    );
    
    return response.data;
}
```

### 8.3. Вывод средств

💰 **Как выводить деньги:**

1. **Автоматический вывод:**
   - Настройте в личном кабинете автоматический вывод на ваш счет в Тинькофф
   - Деньги будут приходить каждый день/неделю (настраивается)

2. **Ручной вывод:**
   - Зайдите в приложение Тинькофф Бизнес
   - Раздел "Эквайринг" → "Вывод средств"
   - Укажите сумму и подтвердите

3. **Сроки:**
   - Обычно 1-2 рабочих дня
   - Без комиссии (внутри Тинькофф)

### 8.4. Налоги

📊 **Как платить налоги:**

1. **Тинькофф интегрирован с "Мой налог":**
   - Все чеки автоматически отправляются в налоговую
   - Налог рассчитывается автоматически (4% для физлиц, 6% для ИП/ООО)

2. **Оплата налогов:**
   - Через приложение "Мой налог" до 25 числа следующего месяца
   - Или настройте автоплатеж в Тинькофф

3. **Отчетность:**
   - Для самозанятых отчетность НЕ требуется
   - Все данные уже в налоговой через чеки

---

## 9. Полезные ссылки

- **Документация Tinkoff Acquiring API:** https://www.tinkoff.ru/kassa/develop/api/
- **Личный кабинет:** https://business.tinkoff.ru/
- **Поддержка:** 8 800 755-25-50
- **Telegram поддержки:** @tinkoff_acquiring_support

---

## 10. Чеклист перед запуском

- [ ] Зарегистрирован счет для самозанятых в Тинькофф
- [ ] Подключен интернет-эквайринг
- [ ] Получены TerminalKey и Password
- [ ] Добавлены ключи в `.env` (боевые, не тестовые!)
- [ ] Настроен webhook URL (`/api/kuliga/payment/callback`)
- [ ] Протестирована инициализация платежа
- [ ] Протестирован webhook (подтверждение оплаты)
- [ ] Протестирован возврат средств
- [ ] Проверено автоматическое формирование чеков
- [ ] Настроен автоматический вывод средств
- [ ] Проверена интеграция с "Мой налог"

---

**Готово! Теперь вы можете принимать платежи через Tinkoff Acquiring! 🎉**

Если возникнут вопросы - пишите в поддержку Тинькофф или в наш Telegram: @gornostyle72

