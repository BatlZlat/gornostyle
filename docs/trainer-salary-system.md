# Система управления ЗП Инструкторов

## 📋 Описание

Система управления зарплатами тренеров позволяет администратору настраивать и отслеживать выплаты инструкторам за проведенные тренировки.

## 🎯 Основные функции

### 1. **Настройки ЗП для каждого тренера**
- **Тип выплаты:**
  - **Процент от тренировки** - тренер получает определенный процент от стоимости тренировки
  - **Фиксированная сумма** - тренер получает фиксированную сумму за каждую тренировку

### 2. **Статистика**
- Всего инструкторов
- Выплачено в этом месяце
- К выплате (Pending)

### 3. **Список инструкторов**
- ФИО и специализация
- Тип и размер выплаты
- Статистика по каждому:
  - Тренировок проведено
  - Заработано всего
  - Ожидает выплаты

### 4. **История выплат**
- Фильтры по инструктору и статусу
- Детальная информация о каждой выплате
- Управление статусами выплат:
  - **Pending (В ожидании)** → **Approved (Одобрено)** → **Paid (Выплачено)**

## 🏗️ Архитектура

### База данных

**Таблица `trainers`:**
```sql
- default_payment_type VARCHAR(20) DEFAULT 'percentage' -- Тип выплаты
- default_percentage DECIMAL(5,2) DEFAULT 50.00         -- Процент
- default_fixed_amount DECIMAL(10,2)                    -- Фиксированная сумма
```

**Таблица `trainer_payments`:**
```sql
- trainer_id INTEGER                     -- ID тренера
- training_session_id INTEGER            -- ID тренировки
- amount DECIMAL(10,2)                   -- Сумма выплаты
- payment_type VARCHAR(20)               -- Тип: percentage/fixed
- percentage DECIMAL(5,2)                -- Процент (если применимо)
- status VARCHAR(20)                     -- pending/approved/paid
```

### API Endpoints

**GET `/api/trainer-salary/stats`**
- Получить статистику по выплатам

**GET `/api/trainer-salary/trainers`**
- Получить список всех тренеров с настройками ЗП

**PUT `/api/trainer-salary/trainers/:id`**
- Обновить настройки ЗП тренера
```json
{
  "paymentType": "percentage",
  "value": 50
}
```

**GET `/api/trainer-salary/payments`**
- Получить историю выплат (с фильтрами)
- Query params: `trainer_id`, `status`

**POST `/api/trainer-salary/payments/:id/approve`**
- Одобрить выплату (pending → approved)

**POST `/api/trainer-salary/payments/:id/paid`**
- Отметить как выплаченную (approved → paid)

## 🔄 Процесс работы

### 1. **Автоматическое создание выплаты**
При записи на тренировку (через бота или веб-интерфейс):
1. Система автоматически создает запись в `trainer_payments` со статусом **pending**
2. Сумма рассчитывается на основе настроек тренера:
   - Если `percentage` → `amount = training_price * percentage / 100`
   - Если `fixed` → `amount = default_fixed_amount`

### 2. **Одобрение выплаты**
Администратор:
1. Открывает **Финансы** → **💰 ЗП Инструктора**
2. Просматривает список выплат со статусом **pending**
3. Нажимает **"✓ Одобрить"**
4. Статус меняется на **approved**

### 3. **Выплата**
После фактической выплаты денег:
1. Администратор нажимает **"💰 Выплачено"**
2. Статус меняется на **paid**
3. Запись попадает в историю выплаченных

## 📊 Примеры использования

### Пример 1: Процент от тренировки
```
Тренер: Иван Петров
Тип: Процент
Значение: 60%

Тренировка: Индивидуальная, 60 минут, 2000₽
Выплата тренеру: 2000₽ × 60% = 1200₽
```

### Пример 2: Фиксированная сумма
```
Тренер: Мария Сидорова
Тип: Фиксированная сумма
Значение: 800₽

Тренировка: Групповая, 60 минут, 1500₽
Выплата тренеру: 800₽ (фиксированно)
```

## 🎨 Интерфейс

### Страница "ЗП Инструктора"
Доступна из **Финансы** → **💰 ЗП Инструктора**

**Статистические карточки:**
- 📊 Всего инструкторов
- 💰 Выплачено в этом месяце
- ⏳ К выплате (Pending)

**Список инструкторов:**
Каждая карточка содержит:
- ФИО и специализация
- Настройки выплаты (тип + сумма/процент)
- Статистика (тренировки, заработано, pending)
- Кнопка "Сохранить" для изменения настроек

**История выплат:**
Таблица со всеми выплатами, фильтры по:
- Инструктору
- Статусу (pending/approved/paid)

Действия:
- **✓ Одобрить** (для pending)
- **💰 Выплачено** (для approved)

## ✅ Преимущества системы

1. **Автоматизация** - выплаты создаются автоматически
2. **Гибкость** - два типа выплат (процент/фикс.)
3. **Прозрачность** - полная история всех выплат
4. **Контроль** - трехступенчатая система статусов
5. **Статистика** - отслеживание всех показателей

## 🚀 Дальнейшее развитие

- Экспорт выплат в Excel
- Отправка уведомлений тренерам о выплатах
- Интеграция с бухгалтерскими системами
- Автоматические выплаты по расписанию
- Детальная аналитика по каждому тренеру
