# 🔍 Анализ проблемы 403 "Forbidden by consent" в Точка Банке

## Проблема

При попытке создать платеж получаем ошибку:
```
403 Forbidden by consent
```

## Причина

Согласно документации Точка Банка ([developers.tochka.com](https://developers.tochka.com/docs/tochka-api/)), для создания платежей через API необходимо:

1. **Разрешение `MakeAcquiringOperation`** - это обязательное разрешение для работы с интернет-эквайрингом
2. **Разрешение должно быть в статусе `Authorised`** (авторизовано)

## Решение

### Вариант 1: Проверить существующие разрешения

Используйте метод API для получения списка разрешений:

```bash
GET https://enter.tochka.com/uapi/consent/v1.0/consents
Headers:
  Authorization: Bearer {JWT_TOKEN}
  customer-code: {TOCHKA_CUSTOMER_CODE}
```

Проверьте, есть ли разрешение `MakeAcquiringOperation` со статусом `Authorised`.

### Вариант 2: Создать новое разрешение

Если разрешения нет, создайте его через API:

```bash
POST https://enter.tochka.com/uapi/consent/v1.0/consents
Headers:
  Authorization: Bearer {JWT_TOKEN}
  Content-Type: application/json
Body:
{
  "Data": {
    "permissions": ["MakeAcquiringOperation"],
    "expirationDateTime": "2026-12-31T23:59:59.999+00:00"
  }
}
```

### Вариант 3: Через личный кабинет

1. Войдите в личный кабинет Точка Банка
2. Перейдите в раздел "API" или "Разработчикам"
3. Найдите раздел "Разрешения" или "Consents"
4. Создайте новое разрешение с правом `MakeAcquiringOperation`
5. Подтвердите разрешение (статус должен стать `Authorised`)

## Где найти merchantId

Согласно документации, `merchantId` должен быть **15 символов** (например: `200000000001056`).

Где найти:
1. **Личный кабинет** → "Эквайринг" → "Интернет-эквайринг" → "Торговые точки"
2. **API метод** `Get Retailers` - получить информацию о ретейлере
3. **Техподдержка** - если не нашли в кабинете

## Важные моменты

1. **Разрешения имеют срок действия** - проверьте `expirationDateTime`
2. **Статус разрешения** должен быть `Authorised`, а не `AwaitingAuthorisation`
3. **JWT токен** должен содержать информацию о разрешениях
4. **Локальное тестирование** - возможно, API требует запросы с зарегистрированного IP или домена

## Следующие шаги

1. ✅ Проверить существующие разрешения через API
2. ✅ Если нет - создать разрешение `MakeAcquiringOperation`
3. ✅ Убедиться, что статус `Authorised`
4. ✅ Проверить срок действия разрешения
5. ✅ Попробовать с сервера (возможно, есть ограничения по IP)

## Ссылки на документацию

- [Работа с разрешениями](https://developers.tochka.com/docs/tochka-api/api/rabota-s-razresheniyami)
- [Get All Consents List](https://developers.tochka.com/docs/tochka-api/api/get-all-consents-list-consent-v-1-0-consents-get)
- [Create New Consent](https://developers.tochka.com/docs/tochka-api/api/create-new-consent-consent-v-1-0-consents-post)
- [Create Payment Operation](https://developers.tochka.com/docs/tochka-api/api/create-payment-operation-acquiring-v-1-0-payments-post)

