# Система уведомлений о тренировках

## Описание

Автоматическая система отправки напоминаний клиентам о предстоящих тренировках через Telegram бот. Уведомления отправляются каждый день в 21:00 по времени Екатеринбурга (UTC+5) всем клиентам, у которых запланированы тренировки на следующий день.

## Основные особенности

### 1. Умная группировка

Если у клиента несколько записей на тренировки (например, записаны двое детей), система отправляет **одно сводное уведомление** со всеми тренировками, а не отдельное сообщение для каждой.

### 2. Поддержка двух типов тренировок

- **Групповые тренировки** (`training_sessions`)
- **Индивидуальные тренировки** (`individual_training_sessions`)

### 3. Логирование

Все отправленные уведомления сохраняются в таблице `notification_logs` с полной информацией о статусе доставки.

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     Планировщик (Cron)                       │
│                   Запуск каждый день 21:00                   │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│               Сервис уведомлений                             │
│             (notification-service.js)                        │
├─────────────────────────────────────────────────────────────┤
│  1. Получение тренировок на завтра                          │
│  2. Группировка по клиентам                                 │
│  3. Формирование сообщений                                  │
│  4. Отправка через Telegram Bot API                         │
│  5. Логирование результатов                                 │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                   База данных                                │
├─────────────────────────────────────────────────────────────┤
│  • training_sessions (групповые)                            │
│  • individual_training_sessions (индивидуальные)            │
│  • session_participants (участники)                         │
│  • clients (клиенты с telegram_id)                          │
│  • children (дети клиентов)                                 │
│  • notification_logs (логи уведомлений)                     │
└─────────────────────────────────────────────────────────────┘
```

## Компоненты системы

### 1. Сервис уведомлений
**Файл:** `src/services/notification-service.js`

Основной модуль, отвечающий за всю логику уведомлений:

- `getTrainingsByDate(date)` - получение всех тренировок на указанную дату
- `groupTrainingsByClient(trainings)` - группировка тренировок по клиентам
- `formatNotificationMessage(clientData, date)` - формирование текста уведомления
- `sendNotification(telegramId, message)` - отправка сообщения через Telegram
- `logNotification(...)` - сохранение лога в БД
- `sendTrainingReminders(date)` - основной метод отправки напоминаний
- `sendTomorrowReminders()` - отправка напоминаний на завтра

### 2. Планировщик
**Файл:** `src/services/scheduler.js`

Управляет cron-задачами:

- `init()` - инициализация всех задач
- `scheduleTrainingReminders()` - настройка задачи отправки напоминаний
- `testTrainingReminders()` - тестовый запуск (для отладки)
- `getTasksInfo()` - информация о запущенных задачах

### 3. Скрипт отправки
**Файл:** `src/scripts/send-training-reminders.js`

Консольный скрипт для запуска отправки напоминаний:

```bash
# Отправить напоминания на завтра
node src/scripts/send-training-reminders.js

# Отправить напоминания на конкретную дату
node src/scripts/send-training-reminders.js 2025-10-15
```

### 4. Тестовый скрипт
**Файл:** `src/scripts/test-notifications.js`

Скрипт для тестирования системы:

```bash
# Тестовый запуск (без реальной отправки)
node src/scripts/test-notifications.js --dry-run

# Реальная отправка на завтра
node src/scripts/test-notifications.js

# Реальная отправка на конкретную дату
node src/scripts/test-notifications.js 2025-10-15
```

## База данных

### Таблица notification_logs

```sql
CREATE TABLE notification_logs (
    id SERIAL PRIMARY KEY,
    client_id INTEGER REFERENCES clients(id),
    telegram_id VARCHAR(255) NOT NULL,
    notification_type VARCHAR(50) NOT NULL,
    training_date DATE NOT NULL,
    message TEXT NOT NULL,
    status VARCHAR(20) NOT NULL,
    error_message TEXT,
    sent_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Поля:**
- `client_id` - ID клиента
- `telegram_id` - Telegram ID для отправки
- `notification_type` - Тип уведомления ('training_reminder')
- `training_date` - Дата тренировки
- `message` - Текст отправленного сообщения
- `status` - Статус ('sent' или 'failed')
- `error_message` - Текст ошибки (если есть)
- `sent_at` - Дата и время отправки

### Применение миграции

```bash
psql -U your_user -d your_database -f migrations/012_add_notification_logs.sql
```

## Формат уведомлений

### Пример 1: Один ребенок на тренировке

```
🔔 Напоминание о тренировке!

Завтра, 10 октября:

📅 10:00 - 11:00
👤 Участник: Иван Петров (ребенок)
🏂 Тип: Групповая тренировка
⛷ Снаряжение: Лыжи
🏔 Тренажер: Slope Simulator 1
👨‍🏫 Тренер: Алексей Смирнов
📊 Уровень: 3

━━━━━━━━━━━━━━━━━━━━

Ждем вас! Приезжайте на тренировку за 10-15 минут до начала. 
Не забудьте взять с собой воду и одежду, желательно закрывающую 
колени и локти 😊
```

### Пример 2: Несколько детей на разных тренировках

```
🔔 Напоминание о тренировках!

Завтра, 10 октября:

📅 10:00 - 11:00
👤 Участник: Иван Петров (ребенок)
🏂 Тип: Групповая тренировка
⛷ Снаряжение: Лыжи
🏔 Тренажер: Slope Simulator 1
👨‍🏫 Тренер: Алексей Смирнов

━━━━━━━━━━━━━━━━━━━━

📅 14:00 - 15:00
👤 Участница: Мария Петрова (ребенок)
🏂 Тип: Индивидуальная тренировка
⛷ Снаряжение: Сноуборд
🏔 Тренажер: Slope Simulator 2

━━━━━━━━━━━━━━━━━━━━

Ждем вас! Приезжайте на тренировку за 10-15 минут до начала. 
Не забудьте взять с собой воду и одежду, желательно закрывающую 
колени и локти 😊
```

## Настройка и запуск

### 1. Автоматический запуск

Система автоматически запускается при старте приложения благодаря инициализации в `src/app.js`:

```javascript
const scheduler = require('./services/scheduler');
scheduler.init();
```

Планировщик настроен на запуск каждый день в **21:00** по времени Екатеринбурга.

### 2. Ручной запуск

Для тестирования или внеплановой отправки:

```bash
# Отправить напоминания на завтра
node src/scripts/send-training-reminders.js

# Отправить на конкретную дату
node src/scripts/send-training-reminders.js 2025-10-15
```

### 3. Тестирование

Перед использованием протестируйте систему:

```bash
# Тестовый запуск (без отправки)
node src/scripts/test-notifications.js --dry-run
```

## Мониторинг

### Логи в консоли

При каждом запуске система выводит детальные логи:

```
═══════════════════════════════════════════════════════════
  Запуск отправки напоминаний о тренировках
═══════════════════════════════════════════════════════════
Время запуска: 09.10.2025, 21:00:00 (Екатеринбург)

Отправка напоминаний на завтра: 2025-10-10

Найдено 5 записей на тренировки
Уникальных клиентов: 3

✓ Отправлено клиенту Иванов Петр (ID: 123)
✓ Отправлено клиенту Сидорова Мария (ID: 124)
✓ Отправлено клиенту Кузнецов Иван (ID: 125)

═══════════════════════════════════════════════════════════
  Статистика отправки
═══════════════════════════════════════════════════════════
Всего клиентов:     3
Успешно отправлено: 3
Ошибок:             0
═══════════════════════════════════════════════════════════
```

### Уведомления администратору

Если в `.env` указаны `ADMIN_TELEGRAM_ID` и `TELEGRAM_BOT_TOKEN`, администратор получает сводку после каждой отправки:

```
📊 Отчет об отправке напоминаний

📅 Дата тренировок: 2025-10-10
👥 Клиентов: 3
✅ Отправлено: 3
❌ Ошибок: 0

⏰ 09.10.2025, 21:00:15
```

### Проверка логов в БД

```sql
-- Последние отправленные уведомления
SELECT 
    nl.sent_at,
    c.full_name,
    nl.training_date,
    nl.status
FROM notification_logs nl
JOIN clients c ON nl.client_id = c.id
ORDER BY nl.sent_at DESC
LIMIT 10;

-- Статистика по дням
SELECT 
    DATE(sent_at) as date,
    COUNT(*) as total,
    SUM(CASE WHEN status = 'sent' THEN 1 ELSE 0 END) as sent,
    SUM(CASE WHEN status = 'failed' THEN 1 ELSE 0 END) as failed
FROM notification_logs
WHERE notification_type = 'training_reminder'
GROUP BY DATE(sent_at)
ORDER BY date DESC;

-- Клиенты с ошибками отправки
SELECT 
    c.full_name,
    nl.telegram_id,
    nl.error_message,
    nl.sent_at
FROM notification_logs nl
JOIN clients c ON nl.client_id = c.id
WHERE nl.status = 'failed'
ORDER BY nl.sent_at DESC;
```

## Устранение неполадок

### Уведомления не отправляются

1. **Проверьте токен бота:**
   ```bash
   echo $TELEGRAM_BOT_TOKEN
   ```

2. **Проверьте, что у клиентов указан telegram_id:**
   ```sql
   SELECT id, full_name, telegram_id 
   FROM clients 
   WHERE telegram_id IS NULL;
   ```

3. **Проверьте планировщик:**
   ```javascript
   const scheduler = require('./services/scheduler');
   console.log(scheduler.getTasksInfo());
   ```

### Некоторые клиенты не получают уведомления

1. **Проверьте telegram_id клиента:**
   ```sql
   SELECT telegram_id FROM clients WHERE id = 123;
   ```

2. **Проверьте логи ошибок:**
   ```sql
   SELECT * FROM notification_logs 
   WHERE client_id = 123 AND status = 'failed'
   ORDER BY sent_at DESC;
   ```

3. **Убедитесь, что клиент не заблокировал бота**

### Дублирование уведомлений

Система спроектирована так, чтобы избежать дублирования:
- Группировка по `client_id` гарантирует одно сообщение на клиента
- Задача запускается строго один раз в день

Если происходит дублирование, проверьте:
```bash
# Убедитесь, что не запущено несколько экземпляров приложения
pm2 list
```

## Расширение функционала

### Добавление новых типов уведомлений

1. Добавьте новый тип в таблицу `notification_logs`
2. Создайте метод в `notification-service.js`
3. Настройте новую cron-задачу в `scheduler.js`

### Изменение времени отправки

Отредактируйте файл `src/services/scheduler.js`:

```javascript
// Изменить время с 21:00 на 20:00
const task = cron.schedule('0 20 * * *', async () => {
    // ...
}, {
    timezone: "Asia/Yekaterinburg"
});
```

### Настройка уведомлений за N часов до тренировки

Добавьте новую задачу в `scheduler.js` с расчетом времени отправки на основе времени начала тренировки.

## Безопасность

- Токен бота хранится в `.env` и никогда не коммитится в git
- Все SQL-запросы используют параметризованные запросы для защиты от SQL-инъекций
- Telegram ID клиентов не показываются в публичных API

## Производительность

- Оптимизированные SQL-запросы с использованием JOIN
- Задержка 100мс между отправками для предотвращения rate limiting в Telegram API
- Индексы на часто используемых полях в `notification_logs`

## Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Проверьте таблицу `notification_logs`
3. Запустите тестовый скрипт с флагом `--dry-run`
4. Обратитесь к разработчику

---

**Версия:** 1.0.0  
**Дата создания:** 09.10.2025  
**Автор:** Система автоматических уведомлений Горностайл72

