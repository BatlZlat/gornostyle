# Как работает система бонусов - Подробное объяснение

## 🎯 Принцип работы

Система бонусов работает по принципу **"триггеров"** - когда происходит определенное событие, система автоматически проверяет все активные акции и начисляет подходящие бонусы.

---

## 📋 Примеры работы системы

### 1️⃣ **Регистрация нового пользователя**

**Что происходит:**
1. Пользователь завершает регистрацию в боте
2. Система вызывает `checkAndAwardBonus('registration', clientId)`
3. Проверяет все активные акции типа `registration`
4. Если условия выполнены - начисляет бонус

**Пример акции:**
```sql
INSERT INTO bonus_settings (
    name, bonus_type, bonus_amount, is_active
) VALUES (
    'Добро пожаловать!', 'registration', 200.00, true
);
```

**Результат:** Каждый новый пользователь получит 200₽ на баланс при регистрации.

---

### 2️⃣ **Посещение 7 тренировок**

**Что происходит:**
1. Пользователь записывается на тренировку (групповую или индивидуальную)
2. Система вызывает `checkMilestoneBonuses(clientId)`
3. Подсчитывает общее количество тренировок клиента
4. Проверяет milestone акции (5, 7, 10, 15 тренировок)
5. Если достигнут milestone - начисляет бонус

**Пример акции:**
```sql
INSERT INTO bonus_settings (
    name, bonus_type, bonus_amount, is_active
) VALUES (
    'За 7 тренировок', 'attendance_milestone', 500.00, true
);
```

**Результат:** Когда клиент посетит 7-ю тренировку, получит 500₽ бонус.

---

### 3️⃣ **Ранняя запись на тренировку**

**Что происходит:**
1. Пользователь записывается на тренировку
2. Система вычисляет, за сколько дней до тренировки записывается
3. Если записался за 7+ дней - начисляет бонус

**Пример акции:**
```sql
INSERT INTO bonus_settings (
    name, bonus_type, bonus_amount, min_amount, is_active
) VALUES (
    'Планируй заранее', 'early_booking', 300.00, 7, true
);
```

**Результат:** Если записался за неделю или больше - получит 300₽ бонус.

---

### 4️⃣ **Утренние тренировки**

**Что происходит:**
1. Пользователь записывается на тренировку с 6:00 до 12:00
2. Система проверяет время тренировки
3. Если утренняя - начисляет бонус

**Пример акции:**
```sql
INSERT INTO bonus_settings (
    name, bonus_type, bonus_amount, is_active
) VALUES (
    'Ранняя пташка', 'morning_training', 200.00, true
);
```

**Результат:** Каждая утренняя тренировка дает 200₽ бонус.

---

### 5️⃣ **День рождения**

**Что происходит:**
1. Система проверяет дату рождения клиента
2. Если сегодня день рождения - начисляет бонус
3. Один раз в год

**Пример акции:**
```sql
INSERT INTO bonus_settings (
    name, bonus_type, bonus_amount, is_active
) VALUES (
    'С днем рождения!', 'birthday', 1000.00, true
);
```

**Результат:** В день рождения клиент получит 1000₽ бонус.

---

## 🔧 Как добавить новую акцию

### Шаг 1: Создать акцию в админ-панели

1. Открыть админ-панель → "🎁 Акции"
2. Нажать "➕ Создать акцию"
3. Заполнить форму:
   - **Название:** "Новогодняя акция"
   - **Тип бонуса:** "booking" (запись на тренировку)
   - **Сумма:** 500₽
   - **Минимальная сумма:** 2000₽ (только для покупок от 2000₽)
   - **Период действия:** 01.01.2025 - 31.01.2025
4. Сохранить

### Шаг 2: Система автоматически работает

Как только акция создана и активирована, система автоматически:
- ✅ Проверяет все записи на тренировки
- ✅ Если сумма тренировки >= 2000₽ и дата в периоде действия
- ✅ Начисляет 500₽ бонус
- ✅ Отправляет уведомление клиенту

---

## 🎯 Интеграция с существующими событиями

### ✅ Уже интегрировано:

1. **Регистрация** - `finishRegistration()` в client-bot.js
2. **Групповые тренировки** - после записи в `session_participants`
3. **Индивидуальные тренировки** - после записи в `individual_training_sessions`
4. **Реферальная система** - уже работает отдельно

### 🔄 Можно добавить:

1. **Покупка сертификатов** - в `certificates.js`
2. **Покупка абонементов** - когда реализуем абонементы
3. **Отзывы** - когда клиент оставляет отзыв
4. **Пополнение баланса** - за крупные пополнения

---

## 📊 Примеры сложных акций

### 1️⃣ **"Новичок"** - только для новых пользователей
```sql
-- Акция только для тех, кто зарегистрировался в последние 30 дней
INSERT INTO bonus_settings (
    name, bonus_type, bonus_amount, valid_from, valid_until, is_active
) VALUES (
    'Новичок', 'registration', 300.00, 
    CURRENT_TIMESTAMP - INTERVAL '30 days', 
    CURRENT_TIMESTAMP + INTERVAL '30 days', 
    true
);
```

### 2️⃣ **"Постоянный клиент"** - за 10 тренировок
```sql
INSERT INTO bonus_settings (
    name, bonus_type, bonus_amount, is_active
) VALUES (
    'Постоянный клиент', 'attendance_milestone', 1000.00, true
);
```

### 3️⃣ **"Вечерний спортсмен"** - за вечерние тренировки
```sql
INSERT INTO bonus_settings (
    name, bonus_type, bonus_amount, is_active
) VALUES (
    'Вечерний спортсмен', 'evening_training', 250.00, true
);
```

### 4️⃣ **"Семейная скидка"** - за групповые тренировки
```sql
INSERT INTO bonus_settings (
    name, bonus_type, bonus_amount, slope_type, is_active
) VALUES (
    'Семейная скидка', 'group_booking', 200.00, 'both', true
);
```

---

## 🛡️ Защита от злоупотреблений

### ✅ Встроенные защиты:

1. **Максимум на пользователя** - можно ограничить количество бонусов
2. **Минимальная сумма** - бонус только при определенной сумме покупки
3. **Период действия** - акция работает только в указанные даты
4. **Проверка дублирования** - система не даст получить один бонус дважды
5. **Транзакции БД** - все операции атомарные

### 📋 Примеры защиты:

```sql
-- Бонус максимум 1 раз в месяц
INSERT INTO bonus_settings (
    name, bonus_type, bonus_amount, max_bonus_per_user, is_active
) VALUES (
    'Ежемесячный бонус', 'booking', 500.00, 1, true
);

-- Бонус только для покупок от 3000₽
INSERT INTO bonus_settings (
    name, bonus_type, bonus_amount, min_amount, is_active
) VALUES (
    'Крупная покупка', 'booking', 1000.00, 3000.00, true
);
```

---

## 🎉 Преимущества системы

### ✅ Для администратора:
- **Гибкость** - можно создать любую акцию через админ-панель
- **Контроль** - видит статистику по всем акциям
- **Безопасность** - защита от злоупотреблений
- **Автоматизация** - не нужно ничего программировать

### ✅ Для клиентов:
- **Прозрачность** - понятно, за что дается бонус
- **Мотивация** - стимул к активности
- **Уведомления** - получают сообщения о начислении
- **Гибкость** - разные бонусы для разных действий

---

## 🚀 Заключение

**Система бонусов полностью автоматизирована!**

Администратор может:
1. ✅ Создать любую акцию через админ-панель
2. ✅ Система автоматически проверяет условия
3. ✅ Автоматически начисляет бонусы
4. ✅ Отправляет уведомления клиентам
5. ✅ Ведет статистику и историю

**Никакого дополнительного программирования не требуется!** 🎯
