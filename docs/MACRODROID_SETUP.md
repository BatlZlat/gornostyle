# 📱 Инструкция по настройке MacroDroid для приема платежей

## Обзор

MacroDroid используется для автоматической отправки SMS-уведомлений о платежах на сервер. При получении SMS о пополнении кошелька через СБП, MacroDroid отправляет текст SMS на API сервера для автоматической обработки платежа.

---

## 🔧 Настройка HTTP-запроса в MacroDroid

### 1. Основные параметры запроса

**Метод запроса:** `POST`

**URL:** 
```
https://gornostyle72.ru/api/sms/process
```

⚠️ **Важно:** 
- Используйте **HTTPS** (не HTTP)
- Используйте домен `gornostyle72.ru` (не IP-адрес)
- Полный путь: `/api/sms/process` (не `/api/sms/p`)

### 2. Заголовки (Headers)

Добавьте заголовок авторизации:

**Название:** `Authorization`  
**Значение:** `Bearer <MACRODROID_TOKEN>`

Где `<MACRODROID_TOKEN>` — это значение из переменной окружения `MACRODROID_TOKEN` в файле `.env` на сервере.

**Пример:**
```
Authorization: Bearer your_macrodroid_token_here
```

### 3. Тело запроса (Body)

**Тип контента:** `application/json`

**Формат:**
```json
{
  "sms_text": "Текст SMS сообщения"
}
```

**Пример:**
```json
{
  "sms_text": "Пополнение на сумму 1000 р. Номер кошелька: 1234567890123456"
}
```

### 4. Настройки запроса

- ✅ **Блокировать следующие действия до завершения** — включено
- ✅ **Следовать перенаправлениям** — включено
- ❌ **Разрешить любой сертификат** — выключено (используйте валидный SSL)
- **Тайм-аут:** 30 секунд

### 5. Базовая авторизация

❌ **Не используется** — авторизация происходит через заголовок `Authorization` с Bearer токеном.

### 6. Сертификат клиента (mTLS)

❌ **Не используется**

---

## 📋 Полная конфигурация для MacroDroid

### Вкладка "Настройки"

| Параметр | Значение |
|----------|----------|
| Метод запроса | `POST` |
| URL | `https://gornostyle72.ru/api/sms/process` |
| Блокировать следующие действия до завершения | ✅ Включено |
| Разрешить любой сертификат | ❌ Выключено |
| Следовать перенаправлениям | ✅ Включено |
| Тайм-аут (сек) | `30` |

### Вкладка "Параметры запроса" (Headers)

| Название | Значение |
|----------|----------|
| `Authorization` | `Bearer <ваш_MACRODROID_TOKEN>` |
| `Content-Type` | `application/json` |

### Вкладка "Тело сообщения"

**Тип:** JSON

**Структура:**
```json
{
  "sms_text": "{sms_body}"
}
```

Где `{sms_body}` — переменная MacroDroid с текстом полученного SMS.

---

## 🔐 Получение токена MACRODROID_TOKEN

Токен хранится в файле `.env` на сервере в переменной `MACRODROID_TOKEN`.

**Как получить токен:**
1. Подключитесь к серверу по SSH
2. Перейдите в директорию проекта: `cd /root/project/gornostyle`
3. Просмотрите значение токена: `grep MACRODROID_TOKEN .env`

⚠️ **Важно:** Токен должен быть достаточно сложным и уникальным. Не используйте простые значения типа "123" или "test".

---

## 🧪 Тестирование настройки

### 1. Проверка доступности API

Выполните тестовый запрос с сервера:

```bash
curl -X POST https://gornostyle72.ru/api/sms/process \
  -H "Authorization: Bearer <ваш_MACRODROID_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{"sms_text": "Пополнение на сумму 100 р. Номер кошелька: 1234567890123456"}'
```

**Ожидаемый ответ при успехе:**
```json
{
  "message": "Платеж успешно обработан",
  "wallet_number": "1234567890123456",
  "client_name": "Имя Клиента",
  "new_balance": 100
}
```

**Ожидаемый ответ при ошибке (кошелек не найден):**
```json
{
  "message": "Платеж не обработан - кошелек не найден",
  "saved_to_failed": true
}
```

### 2. Проверка логов на сервере

Проверьте логи приложения:

```bash
pm2 logs gornostyle
```

Ищите строки:
- `📱 ПОЛУЧЕНО SMS НА СЕРВЕР`
- `📱 Получено СМС от MacroDroid`
- `💰 Начинаем обработку платежа`

---

## ⚠️ Частые проблемы

### Проблема 1: "Требуется авторизация" (401)

**Причина:** Неправильный или отсутствующий токен в заголовке `Authorization`.

**Решение:**
- Проверьте, что заголовок `Authorization` установлен
- Убедитесь, что значение начинается с `Bearer ` (с пробелом после Bearer)
- Проверьте, что токен совпадает с `MACRODROID_TOKEN` в `.env` на сервере

### Проблема 2: "Текст СМС не указан" (400)

**Причина:** В теле запроса отсутствует поле `sms_text` или оно пустое.

**Решение:**
- Убедитесь, что в теле запроса есть поле `sms_text`
- Проверьте, что переменная MacroDroid с текстом SMS правильно подставлена

### Проблема 3: Тайм-аут запроса

**Причина:** Сервер не отвечает в течение 30 секунд.

**Решение:**
- Проверьте доступность сервера: `curl -I https://gornostyle72.ru`
- Проверьте логи сервера на наличие ошибок
- Увеличьте тайм-аут в настройках MacroDroid до 60 секунд

### Проблема 4: SSL сертификат не доверен

**Причина:** Использование самоподписанного сертификата или проблемы с SSL.

**Решение:**
- Убедитесь, что используется домен `gornostyle72.ru` (не IP)
- Проверьте, что SSL сертификат валиден: `openssl s_client -connect gornostyle72.ru:443`
- Если необходимо, временно включите "Разрешить любой сертификат" для тестирования

---

## 📝 Пример триггера MacroDroid

### Условие (Trigger)
- **Тип:** Получено SMS
- **Отправитель:** (оставьте пустым или укажите номер банка)
- **Содержит текст:** "Пополнение" или "кошелек" (опционально)

### Действие (Action)
- **Тип:** HTTP-запрос
- **Метод:** POST
- **URL:** `https://gornostyle72.ru/api/sms/process`
- **Заголовки:**
  - `Authorization: Bearer <ваш_токен>`
  - `Content-Type: application/json`
- **Тело (JSON):**
  ```json
  {
    "sms_text": "{sms_body}"
  }
  ```

---

## 🔄 Миграция с старого сервера

Если вы переезжали на новый сервер, обновите следующие параметры:

### Старые настройки (старый сервер)
- URL: `http://90.156.210.24:8080/api/sms/process`

### Новые настройки (новый сервер)
- URL: `https://gornostyle72.ru/api/sms/process`

**Изменения:**
1. ✅ Замените IP-адрес на домен
2. ✅ Измените HTTP на HTTPS
3. ✅ Уберите порт `:8080` (nginx проксирует на порт 8080 автоматически)
4. ✅ Убедитесь, что токен `MACRODROID_TOKEN` совпадает на новом сервере

---

## 📊 Мониторинг работы

### Логи на сервере

Все входящие SMS логируются в таблицу `sms_log` в базе данных. Для просмотра последних записей:

```sql
SELECT * FROM sms_log ORDER BY created_at DESC LIMIT 10;
```

### Неудачные платежи

Платежи, которые не удалось обработать (например, кошелек не найден), сохраняются в таблицу `failed_payments`:

```sql
SELECT * FROM failed_payments ORDER BY created_at DESC LIMIT 10;
```

---

## 🔗 Связанные документы

- [Структура сервера](server_infrastructure_notes_2025-11-12.md)
- [API документация SMS обработки](../src/routes/sms.js)

---

**Последнее обновление:** 2025-01-XX  
**Версия:** 1.0




