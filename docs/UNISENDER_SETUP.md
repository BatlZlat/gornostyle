# Настройка Unisender для отправки email

## Статус

✅ **ПРОБЛЕМА РЕШЕНА:** SMTP порты разблокированы хостингом Timeweb. Основная отправка через SMTP Yandex работает корректно.

Unisender интегрирован как **fallback** (резервный) сервис на случай, если SMTP снова станет недоступен.

---

## Проблема (историческая справка)

Ранее на сервере были закрыты SMTP порты (465, 587, 25), что блокировало отправку через SMTP Yandex. Unisender работает через HTTPS API (порт 443), который обычно открыт, поэтому был интегрирован как альтернатива.

## Решение

Unisender интегрирован как fallback сервис для отправки email через API (транзакционные письма). Он автоматически используется, если SMTP не работает.

⚠️ **ВАЖНО:** 
- На бесплатном тарифе Unisender позволяет отправлять письма **только на свои подтвержденные email адреса**
- Для отправки клиентам необходим **платный тариф**
- **Рекомендуется тариф "Оптом"** для транзакционных писем через API (оплата за количество писем, без ограничений по контактам, не требует загрузки базы)

## Настройка

### 1. Регистрация в Unisender

1. Зайдите на https://www.unisender.com/ru/
2. Зарегистрируйтесь (бесплатный тариф: 1500 писем/месяц)
3. Подтвердите email

### 2. Получение API ключа

1. Войдите в личный кабинет Unisender: https://cp.unisender.com/
2. Нажмите на ваш email в **левом нижнем углу** экрана
3. Выберите пункт **"Настройки"**
4. Откройте вкладку **"Интеграция и API"**
5. Если статус "Доступ к API" отображается как "Выключен", нажмите **"Включить"**
6. Нажмите на ссылку **"Показать полностью"** рядом с полем "Ключ доступа к API"
7. Введите пароль от вашего аккаунта для подтверждения
8. Скопируйте ваш API ключ (он будет отображен полностью)

**Важно:** API ключ является конфиденциальной информацией. Не передавайте его третьим лицам и не размещайте в открытом доступе.

### 3. Подтверждение email отправителя

⚠️ **ВАЖНО:** Email отправителя должен быть подтвержден в Unisender!

1. Войдите в личный кабинет Unisender
2. Перейдите в раздел **"Настройки"** → **"Отправители"** (или "Sender Emails")
3. Добавьте и подтвердите email, с которого будете отправлять письма
4. Обычно лучше использовать email, с которым вы регистрировались в Unisender (он уже подтвержден)

### 4. Настройка переменных окружения

Добавьте в `.env` файл на сервере:

```bash
# Unisender API ключ (получить в личном кабинете Unisender)
UNISENDER_API_KEY=ваш_api_ключ_здесь

# Email отправителя (ДОЛЖЕН БЫТЬ ПОДТВЕРЖДЕН в Unisender!)
# Обычно это email, с которым вы регистрировались в Unisender
# Например: batl.zlat87@gmail.com (если это ваш email аккаунта Unisender)
UNISENDER_FROM_EMAIL=ваш_подтвержденный_email@example.com

# Имя отправителя
UNISENDER_FROM_NAME=Горностайл72
```

**Важно:** Если не указать `UNISENDER_FROM_EMAIL`, система попытается использовать `EMAIL_USER` из `.env`, но этот email должен быть подтвержден в Unisender!

### 5. Проверка работы

После настройки переменных окружения, система автоматически будет использовать Unisender как fallback, если SMTP не работает.

**Тестирование:**
```bash
# На сервере, тестируйте на свой подтвержденный email (обычно email аккаунта Unisender)
node scripts/test-unisender.js ваш_подтвержденный_email@example.com
```

## Логика работы (fallback цепочка)

1. **Первая попытка:** SMTP Yandex (порт 465, SSL) ✅ **РАБОТАЕТ**
2. **Вторая попытка:** SMTP Yandex (порт 587, STARTTLS) - если порт 465 недоступен
3. **Третья попытка:** Unisender API (HTTPS, порт 443) - если SMTP полностью не работает (резерв)
4. **Последняя попытка:** Resend API (если настроен) - только для тестовых адресов

**Текущий статус:** Основная отправка через SMTP Yandex работает корректно. Unisender остается как резервный вариант.

## Преимущества Unisender

- ✅ Работает в России
- ✅ Не требует SMTP портов (работает через HTTPS)
- ✅ Бесплатный тариф: 1500 писем/месяц
- ✅ Надежная доставка
- ✅ Поддержка вложений

## Ограничения бесплатного тарифа

⚠️ **ВАЖНО:** На бесплатном тарифе Unisender позволяет отправлять письма **только на свои подтвержденные email адреса** в аккаунте.

- Бесплатный тариф: 1500 писем/месяц
- Можно отправлять только на подтвержденные email в вашем аккаунте Unisender
- Для отправки клиентам нужен **платный тариф**

### Решение для production (транзакционные письма через API)

Если вам нужна отправка транзакционных писем клиентам через API (booking confirmations, certificates и т.д.), **рекомендуется тариф "Оптом"**:

- ✅ **Оплата за количество писем** (без ограничений по контактам)
- ✅ **Не требует загрузки базы контактов** - идеально для транзакционных писем
- ✅ **Отправка на любые адреса** через метод `sendEmail`
- ✅ Можно купить пакет писем на месяц или год

**Альтернатива:** Тариф "Лайт" - если отправляете много писем на одну базу контактов (но для транзакционных писем "Оптом" обычно выгоднее)

**Другие варианты:** Альтернативные email сервисы с API, работающие в России

## Отладка

Если письма не отправляются через Unisender, проверьте:

1. **Правильность API ключа** в `.env`
2. **Email отправителя подтвержден** в Unisender (раздел "Настройки" → "Отправители")
   - ❌ Ошибка `"Unchecked sender email"` означает, что email не подтвержден
   - ✅ Решение: используйте email вашего аккаунта Unisender или подтвердите нужный email
3. **На бесплатном тарифе** можно отправлять только на свои подтвержденные email адреса
4. **Логи сервера** на наличие ошибок Unisender
5. **Баланс писем** в личном кабинете Unisender

### Типичные ошибки

**Ошибка: "Unchecked sender email"**
- **Причина:** Email отправителя не подтвержден в Unisender
- **Решение:** 
  1. Подтвердите email в разделе "Настройки" → "Отправители"
  2. Или используйте email вашего аккаунта Unisender (обычно уже подтвержден)
  3. Обновите `UNISENDER_FROM_EMAIL` в `.env` файле

**Ошибка: "On a free plan you can use API for sending to your own confirmed emails only"**
- **Причина:** Бесплатный тариф позволяет отправлять только на свои подтвержденные email
- **Решение:** Используйте платный тариф для отправки клиентам

Логи будут содержать сообщения вида:
- `✅ Unisender инициализирован` - сервис готов к работе
- `📧 Попытка отправки email через Unisender API...` - идет отправка
- `✅ Email отправлен успешно через Unisender` - успех
- `❌ Unisender вернул ошибку: ...` - ошибка (см. типичные ошибки выше)

