# Настройка Unisender для отправки email

## Проблема

На сервере закрыты SMTP порты (465, 587, 25), поэтому стандартная отправка через SMTP Yandex не работает. Unisender работает через HTTPS API (порт 443), который обычно открыт.

## Решение

Unisender интегрирован как fallback сервис для отправки email. Он автоматически используется, если SMTP не работает.

## Настройка

### 1. Регистрация в Unisender

1. Зайдите на https://www.unisender.com/ru/
2. Зарегистрируйтесь (бесплатный тариф: 1500 писем/месяц)
3. Подтвердите email

### 2. Получение API ключа

1. Войдите в личный кабинет Unisender: https://cp.unisender.com/
2. Нажмите на ваш email в **левом нижнем углу** экрана
3. Выберите пункт **"Настройки"**
4. Откройте вкладку **"Интеграция и API"**
5. Если статус "Доступ к API" отображается как "Выключен", нажмите **"Включить"**
6. Нажмите на ссылку **"Показать полностью"** рядом с полем "Ключ доступа к API"
7. Введите пароль от вашего аккаунта для подтверждения
8. Скопируйте ваш API ключ (он будет отображен полностью)

**Важно:** API ключ является конфиденциальной информацией. Не передавайте его третьим лицам и не размещайте в открытом доступе.

### 3. Настройка переменных окружения

Добавьте в `.env` файл на сервере:

```bash
# Unisender API ключ (получить в личном кабинете Unisender)
UNISENDER_API_KEY=ваш_api_ключ_здесь

# Email отправителя (можно использовать ваш корпоративный email)
UNISENDER_FROM_EMAIL=batl-zlat@yandex.ru

# Имя отправителя
UNISENDER_FROM_NAME=Горностайл72
```

### 4. Проверка работы

После настройки переменных окружения, система автоматически будет использовать Unisender как fallback, если SMTP не работает.

## Логика работы

1. **Первая попытка:** SMTP Yandex (порт 465, SSL)
2. **Вторая попытка:** SMTP Yandex (порт 587, STARTTLS) - если порт 465 недоступен
3. **Третья попытка:** Unisender API (HTTPS, порт 443) - если SMTP полностью не работает
4. **Последняя попытка:** Resend API (если настроен) - только для тестовых адресов

## Преимущества Unisender

- ✅ Работает в России
- ✅ Не требует SMTP портов (работает через HTTPS)
- ✅ Бесплатный тариф: 1500 писем/месяц
- ✅ Надежная доставка
- ✅ Поддержка вложений

## Ограничения

- Бесплатный тариф: 1500 писем/месяц
- Для большего количества писем нужен платный тариф

## Отладка

Если письма не отправляются через Unisender, проверьте:

1. Правильность API ключа в `.env`
2. Логи сервера на наличие ошибок Unisender
3. Баланс писем в личном кабинете Unisender

Логи будут содержать сообщения вида:
- `✅ Unisender инициализирован` - сервис готов к работе
- `📧 Попытка отправки email через Unisender API...` - идет отправка
- `✅ Email отправлен успешно через Unisender` - успех
- `❌ Unisender вернул ошибку: ...` - ошибка (проверьте API ключ)

