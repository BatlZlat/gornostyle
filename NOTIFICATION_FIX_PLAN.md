# План диагностики и исправления проблемы с уведомлениями инструкторам

## 🔍 Проблема
Уведомления инструкторам не отправлялись после миграции 038 (добавление поля `location`).

## 📋 План действий

### Этап 1: Диагностика базы данных
1. Подключиться к продакшн БД
2. Проверить наличие `telegram_id` у инструкторов
3. Проверить наличие поля `location` в таблицах
4. Проверить наличие записей о бронированиях сегодня

### Этап 2: Исправление кода
1. **Исправить передачу `location` в уведомлениях:**
   - `src/routes/kuliga-booking.js:643` - добавить `location: slotLocation`
   - `src/bot/client-bot.js:14071` - добавить `location` из `state.data.location`
   - `src/bot/client-bot.js:14293` - добавить `location` из данных тренировки

2. **Исправить SQL запросы для получения `location`:**
   - Проверить, что все запросы получают `location` из правильных таблиц

3. **Добавить логирование для отладки:**
   - Логировать факт вызова функции уведомления
   - Логировать наличие `instructor_telegram_id`
   - Логировать ошибки отправки

### Этап 3: Тестирование
1. Проверить отправку уведомлений на тестовом бронировании
2. Проверить логи на наличие ошибок
3. Убедиться, что все уведомления доставляются

## 🔧 Обнаруженные проблемы

### Проблема 1: Отсутствие `location` в уведомлениях
**Места:**
- `src/routes/kuliga-booking.js:633` - не передается `location` в `notifyInstructorKuligaTrainingBooking`
- `src/bot/client-bot.js:14059` - не передается `location` в `notifyInstructorKuligaTrainingBooking`
- `src/bot/client-bot.js:14284` - не передается `location` в `notifyInstructorKuligaTrainingBooking`

**Решение:** Добавить `location` во все вызовы функции.

### Проблема 2: Возможные NULL значения
После миграции могут быть случаи, когда:
- `instructor_telegram_id` = NULL (инструктор не зарегистрирован в боте)
- `location` = NULL (не заполнено в старых записях)

**Решение:** Добавить проверки и дефолтные значения.

## ✅ Шаги для продакшна

1. Подключиться к БД через SSH туннель
2. Проверить состояние данных
3. Применить исправления кода
4. Перезапустить приложение
5. Проверить работу уведомлений

